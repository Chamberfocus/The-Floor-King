/**
 * Flooring knowledge engine — families, install systems, show_if, measured vs
 * order quantity, carton rounding only when coverage exists.
 */
import { readFileSync, readdirSync } from "node:fs";
import { join } from "node:path";
import { describe, expect, it } from "vitest";
import { companionQty, defaultWastePct, profileFor } from "@/lib/flooring-profiles";
import { carpetCutList, carpetLineIsModularCoverage, carpetInstallSystemsForBoxedRate, notesCarpetInstallSystemsForBoxedRate, poCarpetInstallSystemsForBoxedRate, catalogCarpetInstallSystemsForBoxedRate, parseCutsFromText, padRollCount, lineSpec } from "@/lib/job-scope";
import { carpetYardageFromCuts, stairsCarpet, subfloorSheets, resolvedSheetSqft } from "@/lib/questionnaire-calc";
import { recoverAreaSqftFromQuantity } from "@/lib/questionnaire-emit";
import { bagsNeeded, selfLevelPourThicknessIn } from "@/lib/floor-prep";
import { cutLabel, cutSqYd } from "@/lib/order-cuts";
import { lineQty, lineOrderQty, lineIsBoxedCartonTbd, lineIsCountNotTapedSqft, lineIsStairWrapTbd, knowledgePickDescription, rollGoodsLineHasCuts, hardSurfaceAreaCartonCount, lineSkipsAreaCartonMath, lineUsesAreaCartonMath } from "@/lib/estimate-calc";
import { catalogCostInLineUnit, catalogRateInLineUnit, catalogToLineMeasure } from "@/lib/catalog-pricing";
import { poCostOfEstimateLine } from "@/lib/po-build";
import {
  accessoryQuantity,
  piecesForLinearFeet,
  resolvedPieceLengthIn,
} from "@/lib/accessories";
import {
  accessoryUnitForType,
  applyHardSurfaceStairTrimFill,
  answersHaveTrimType,
  trimLabelsFromPicks,
  applyTrimTypeSeed,
  HS_TRANSITION_OPTION_TO_TRIM,
  HS_BASE_OPTION_TO_TRIM,
  annotateRemovalDescription,
  billsBySqydFamily,
  cartonTakeoff,
  catalogCategoryForFamily,
  coerceTrimUnit,
  computeMaterialTakeoff,
  padFoamTakeoffLabel,
  takeoffDisplayTitle,
  cutWidthChoicesFt,
  defaultCutWidthFt,
  enteredCutWidthFt,
  equivalentSqyd,
  formatTakeoffStrip,
  formatEquivalentSqyd,
  builderAreaFallbackLabel,
  ROLL_GOODS_CUTS_MISSING_CAPTION,
  ROLL_GOODS_CUTS_HEADER,
  ROLL_GOODS_CUTS_EMPTY_HINT,
  lineMeasurementsRollTotalLabel,
  questionnaireCutGroupOrderLabel,
  questionnaireCutsGrandOrderLabel,
  formatBillingQty,
  takeoffConceptRows,
  takeoffUnitKeyLabel,
  rollGoodsOrderTbdDescription,
  familyFromCatalogCategory,
  familyFromSurfaceLabel,
  hardwoodConstructionFromLabel,
  hardwoodConstructionFromSpecies,
  installContextFromValByKey,
  withProductFamilies,
  flooringFamiliesFromCategories,
  unscopedProductFamilies,
  installMethodOptionsFor,
  permittedInstallSystems,
  installSystemFromLabel,
  matchesShowIf,
  hardSurfaceInstallMethodOptions,
  jobNeedsMixedInstallMethodPicks,
  solePermittedInstallSystem,
  leftoverIllegalSoleInstallLabels,
  coalesceSoleInstallSystem,
  stripIllegalInstallSystems,
  synthesizeSoleInstallMethod,
  questionApplies,
  sqydToSqft,
  visibleKnowledgeKeys,
  resolveQuestionVisibility,
  knowledgeQuestionByKey,
  amountUnitLabelForQuestion,
  knowledgeWarnings,
  mixedJobAssignmentGaps,
  knowledgeHelpFor,
  knowledgeWhenApplies,
  jobNeedsAcclimationClimate,
  installNeedsAcclimationClimate,
  climateControlConfirmed,
  tileJobIsWallOnly,
  TILE_WALL_HIDES_KEYS,
  MERGED_CLIMATE_HIDES_KEYS,
  MERGED_CURB_HIDES_KEYS,
  CARPET_ONLY_HIDES_KEYS,
  DEAD_STAIR_GATE_HIDES_KEYS,
  DEAD_STAIR_FOLLOWUP_HIDES_KEYS,
  TILE_THINSET_HIDES_KEYS,
  CARPET_TILE_VAPOR_HIDES_KEYS,
  CONCRETE_HIDES_KEYS,
  WOOD_DECK_MOISTURE_HIDES_KEYS,
  GLUE_WOOD_VAPOR_HIDES_KEYS,
  WOOD_DECK_AQUA_HIDES_KEYS,
  WOOD_DECK_VAPOR_HIDES_KEYS,
  GLUE_EXISTING_VAPOR_HIDES_KEYS,
  EXISTING_FLOOR_AQUA_HIDES_KEYS,
  EXISTING_FLOOR_VAPOR_HIDES_KEYS,
  LOOSE_LAY_VAPOR_HIDES_KEYS,
  NON_VINYL_DEMO_SKIM_HIDES_KEYS,
  NON_HARDWOOD_FASTENER_HIDES_KEYS,
  NON_CARPET_DEMO_PAD_HIDES_KEYS,
  NONE_DEMO_DISPOSAL_HIDES_KEYS,
  EXCLUSIVE_TILE_METHOD_HIDES_KEYS,
  CARPET_TILE_HIDES_KEYS,
  SOLID_HARDWOOD_HIDES_KEYS,
  jobHasNonTileFloorFamily,
  jobIsExclusiveTile,
  jobIsExclusiveWallTile,
  jobIsExclusiveCarpetTile,
  jobIsExclusiveCarpetTileOnly,
  jobIsExclusiveSolidHardwood,
  jobIsExclusiveConcrete,
  jobHasHardSurfaceInstallScope,
  jobHasCarpetInstallScope,
  jobIsExclusiveCarpetOnly,
  jobHidesDeadStairGate,
  jobHidesDeadStairFollowups,
  jobHidesSlabMoistureOnWoodDeck,
  jobHidesVaporOnGlueWoodDeck,
  jobHidesVaporOnWoodDeck,
  jobHidesAquaBarOnGlueWoodDeck,
  jobHidesVaporOnGlueExistingFloor,
  jobHidesVaporOnExistingFloor,
  jobHidesAquaBarOnGlueExistingFloor,
  jobHidesAquaBarOnExistingFloor,
  jobHidesVaporOnLooseLay,
  jobHidesVinylSkimOnNonVinylDemo,
  jobHidesFastenersOnNonHardwood,
  jobHidesPadOnNonCarpetDemo,
  jobHidesDisposalOnNoDemo,
  labelsAreWoodDeckOnly,
  labelsAreExistingFlooringOnly,
  jobAllowsFloatingVaporUnderlayment,
  choiceOptionApplies,
  tileWallHidesPrepOptionLabel,
  tileWallHidesDemoOptionLabel,
  vaporBarrierHidesUnderlaymentOptionLabel,
  FURNITURE_MOVING_KEYS,
  jobIsVacant,
  jobIsNewConstruction,
  jobIsExclusiveNewConstruction,
  NEW_CONSTRUCTION_HIDES_KEYS,
  REMOVAL_QUESTION_KEYS,
  KNOWLEDGE_QUESTIONS,
  sortEstimateQuestions,
  estimatorPhaseForQuestion,
  estimatorPhaseLabel,
  showIfReferencedKeys,
  prepQuantitiesAreFinal,
  prepQuantitySuffix,
  answerGateValues,
  coerceYesNoChoiceAnswer,
  synthesizeStairGate,
  stairStepCountFromAnswers,
  jobNeedsHardSurfaceStairTrim,
  HARD_SURFACE_STAIR_TRIM_LABELS,
  groupMeasuredSqftByLabel,
  groupMeasuredSqftByFamily,
  measuredSqftForFamilyTakeoff,
  measuredSqftForFamiliesTakeoff,
  measuredSqftForPrepTakeoff,
  measuredSqftForQuestionCover,
  roomsAssignedToFamilies,
  roomsForPrepTakeoff,
  emitAreaSqftForQuestion,
  deliveryAddonCost,
  reviewBucketForQuestion,
  formatMeasuredLabel,
  EXTRA_AREA_MEASURED_LABEL,
  EXTRA_AREA_MEASURED_PLACEHOLDER,
  EXTRA_AREA_MEASURED_HINT,
  EXTRA_AREA_COUNT_TBD_HINT,
  EXTRA_AREA_COUNT_QTY_LABEL,
  EXTRA_AREA_COUNT_QTY_HINT,
  materialWastePctForEmit,
  rollGoodsHaveCuts,
  rollGoodsNeedCuts,
  carpetInstallSystemsFromLabels,
  areaBillsBySquareYard,
  billingUnitForArea,
  billingUnitForCategory,
  areaDerivedMaterialAllowed,
  boxedCartonAreaTakeoffAllowed,
  boxedCartonCoverageTbdDescription,
  areaDerivedMaterialQty,
  extraMeasuredSqftForTakeoff,
  extraAsksCountQty,
  extraCountQtyForEmit,
  extraCountReviewLine,
  prepCountReviewLine,
  measuredInstallLaborAllowed,
  configuredInstallRate,
  rollGoodsSeamWarnings,
  measuredRectsFromRooms,
  seamImplication,
  buildSalespersonReview,
  reviewToJobNotes,
  mergeReviewWarnings,
  emptyInstallContext,
} from "@/lib/flooring-knowledge";
import { billsBySquareYard, billedQtyToSqft, billedRateToCartonCost, defaultUnitForCategory, isCountPricedLine, lineDisplayUnit, lineUnitKey, pickedProductUnit, rollReceiveUnit, SQYD_CATEGORIES } from "@/lib/units";
import { installDaysForJob } from "@/lib/scheduling";
import { SCHEDULING_DEFAULTS } from "@/lib/data/scheduling";
import {
  buildCustomerScope,
  customerFacingJobNotes,
  customerFacingLineNote,
  customerLineLabel,
  isCrewPrepConfidenceLine,
  isCrewReviewSectionHeader,
  isCrewStairStepHowManyLine,
  isCrewLabeledCountHowManyLine,
  isCrewUnlabeledCountHowManyLine,
  isCrewDimensionHowManyLine,
  isGuidedTakeoffMathLine,
  parseProjectDetails,
  stripCrewIdentityFromCustomerLabel,
  stripCrewReviewBucketPrefix,
} from "@/lib/customer-scope";
import { invoiceItemFromSnapshotLine } from "@/lib/invoice-from-approval";
import type { EstimateLineItem, EstimateOption, ShowIfClause } from "@/lib/types";
import {
  buildApprovalSnapshotPayload,
  commercialLineFingerprint,
  CUSTOMER_SAFE_SNAPSHOT_LINE_KEYS,
  sanitizeApprovalPayloadForCustomer,
  type ApprovalSnapshotLine,
} from "@/lib/estimate-approval";
import { snapshotLinesAsEstimateLines } from "@/lib/approval-snapshot-view";

const root = process.cwd();

describe("catalog families map onto existing ProductCategory values", () => {
  it("does not invent categories outside the Floor King catalog", () => {
    for (const cat of ["carpet", "lvp", "hardwood", "laminate", "tile", "vinyl"] as const) {
      expect(familyFromCatalogCategory(cat)).toBe(cat);
      expect(catalogCategoryForFamily(cat)).toBe(cat);
    }
    expect(familyFromCatalogCategory("trim")).toBe("other");
  });

  it("maps questionnaire surface labels, including legacy ones", () => {
    expect(familyFromSurfaceLabel("LVP / LVT")).toBe("lvp");
    expect(familyFromSurfaceLabel("LVP / Vinyl")).toBe("lvp");
    expect(familyFromSurfaceLabel("Sheet vinyl")).toBe("vinyl");
    expect(familyFromSurfaceLabel("Engineered")).toBe("hardwood");
    expect(familyFromSurfaceLabel("Engineered hardwood")).toBe("hardwood");
    expect(hardwoodConstructionFromLabel("Engineered hardwood")).toBe("engineered");
    expect(hardwoodConstructionFromLabel("Hardwood")).toBe("solid");
  });

  it("flags floor-map products that the job type / surface did not scope", () => {
    expect(flooringFamiliesFromCategories(["carpet", "lvp", "trim", "labor"])).toEqual(["carpet", "lvp"]);
    const lvpOnly = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(unscopedProductFamilies(lvpOnly, ["hardwood"])).toEqual(["hardwood"]);
    expect(unscopedProductFamilies(lvpOnly, ["lvp"])).toEqual([]);
    const mixedType = installContextFromValByKey({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(unscopedProductFamilies(mixedType, ["carpet", "lvp"])).toEqual([]);
    const pending = installContextFromValByKey({
      project_type: ["Hard surface"],
    });
    expect(pending.surfacePending).toBe(true);
    expect(unscopedProductFamilies(pending, ["hardwood"])).toEqual([]);

    const merged = withProductFamilies(lvpOnly, ["hardwood", "lvp"]);
    expect(merged.families).toEqual(["lvp", "hardwood"]);
    expect(merged.unscopedProductFamilies).toEqual(["hardwood"]);
    expect(knowledgeWarnings(merged).some((w) => w.id === "unscoped-products")).toBe(true);
    expect(knowledgeWarnings(lvpOnly).some((w) => w.id === "unscoped-products")).toBe(false);
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/withProductFamilies/);
    expect(q).toMatch(/hardSurfaceInstallMethodOptions/);
    expect(q).not.toMatch(/installMethodOptionsForFamilies\(flooringCtx/);

    const lam = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    });
    expect(lam.systems).toEqual(["floating"]);
    expect(lam.installPending).toBe(false);
    const lamPlusVinyl = withProductFamilies(lam, ["vinyl"]);
    expect(lamPlusVinyl.families.sort()).toEqual(["laminate", "vinyl"]);
    expect(lamPlusVinyl.systems).toEqual([]);
    expect(lamPlusVinyl.installPending).toBe(true);
  });
});

describe("install systems differ by family", () => {
  it("laminate is floating only — no adhesive branch", () => {
    expect(permittedInstallSystems("laminate")).toEqual(["floating"]);
    expect(installMethodOptionsFor("laminate").map((o) => o.label)).toEqual(["Floating / click"]);
  });

  it("LVP offers floating, glue, and loose-lay", () => {
    expect(permittedInstallSystems("lvp")).toEqual(["floating", "glue", "loose_lay"]);
  });

  it("engineered hardwood is wider than solid", () => {
    expect(permittedInstallSystems("hardwood", "solid")).toEqual(["nail", "staple", "glue"]);
    expect(permittedInstallSystems("hardwood", "engineered")).toEqual([
      "nail",
      "staple",
      "glue",
      "floating",
    ]);
  });

  it("tile is thinset; sheet vinyl is glue; carpet is stretch/glue/tile", () => {
    expect(permittedInstallSystems("tile")).toEqual(["thinset"]);
    expect(permittedInstallSystems("vinyl")).toEqual(["glue"]);
    expect(permittedInstallSystems("carpet")).toEqual(["stretch_in", "glue", "carpet_tile"]);
  });

  it("parses install labels used by show_if", () => {
    expect(installSystemFromLabel("Glue-down")).toBe("glue");
    expect(installSystemFromLabel("Floating / click")).toBe("floating");
    expect(installSystemFromLabel("Thinset / mortar")).toBe("thinset");
    expect(installSystemFromLabel("Stretch-in")).toBe("stretch_in");
  });

  it("hard-surface install chips never include carpet stretch-in or carpet tile", () => {
    const mixed = hardSurfaceInstallMethodOptions(["carpet", "lvp"]);
    expect(mixed.map((o) => o.label)).toEqual(["Floating / click", "Glue-down", "Loose-lay"]);
    expect(mixed.some((o) => /stretch|carpet tile/i.test(o.label))).toBe(false);

    const pendingHs = hardSurfaceInstallMethodOptions(["carpet"]);
    expect(pendingHs.map((o) => o.system)).toEqual(
      expect.arrayContaining(["floating", "glue", "nail", "thinset", "loose_lay"]),
    );
    expect(pendingHs.some((o) => o.system === "stretch_in" || o.system === "carpet_tile")).toBe(false);
  });

  it("infers the only legal method for laminate, tile, and sheet vinyl — not LVP", () => {
    expect(solePermittedInstallSystem(["laminate"])).toBe("floating");
    expect(solePermittedInstallSystem(["tile"])).toBe("thinset");
    expect(solePermittedInstallSystem(["vinyl"])).toBe("glue");
    expect(solePermittedInstallSystem(["lvp"])).toBeNull();
    expect(solePermittedInstallSystem(["hardwood"], "engineered")).toBeNull();
    expect(solePermittedInstallSystem(["laminate", "vinyl"])).toBeNull();
    expect(
      leftoverIllegalSoleInstallLabels(["laminate"], "unknown", ["Glue-down"]),
    ).toEqual(["Glue-down"]);
    expect(
      leftoverIllegalSoleInstallLabels(["vinyl"], "unknown", ["Floating / click"]),
    ).toEqual(["Floating / click"]);
    expect(
      leftoverIllegalSoleInstallLabels(["laminate"], "unknown", ["Floating / click"]),
    ).toEqual([]);
    expect(
      leftoverIllegalSoleInstallLabels(["lvp"], "unknown", ["Glue-down"]),
    ).toEqual([]);
    expect(coalesceSoleInstallSystem(["laminate"], "unknown", ["glue"]).systems).toEqual([
      "floating",
    ]);
    expect(coalesceSoleInstallSystem(["vinyl"], "unknown", ["floating"]).systems).toEqual(["glue"]);
    expect(coalesceSoleInstallSystem(["tile"], "unknown", ["floating"]).systems).toEqual(["thinset"]);
    expect(coalesceSoleInstallSystem(["lvp"], "unknown", ["glue"]).systems).toEqual(["glue"]);
  });
});

describe("measured area vs order quantity", () => {
  it("1 square yard = 9 square feet, labeled as equivalent area not an order", () => {
    expect(equivalentSqyd(450)).toBe(50);
    expect(sqydToSqft(50)).toBe(450);
  });

  it("carpet sqft ÷ 9 is NOT the order quantity without cuts", () => {
    const t = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 450,
      wastePct: 10,
    });
    expect(t.measured.sqft).toBe(450);
    expect(t.measured.sqydEquivalent).toBe(50);
    expect(t.orderBasis).toBe("none");
    expect(t.billingUnit).toBe("sqyd");
    expect(t.orderSqft).toBe(0);
    expect(t.billingQty).toBe(0);
    expect(t.wastePct).toBe(0);
    expect(t.warnings.some((w) => /not a professional carpet cut plan/i.test(w))).toBe(true);
    expect(materialWastePctForEmit({ family: "carpet", requestedWastePct: 10 })).toBe(0);
    const strip = formatTakeoffStrip(t);
    expect(strip).toMatch(/Measured 450 sq ft \(50 sq yd equivalent area — not an order quantity\)/);
    expect(strip).toMatch(/Waste 0%/);
    expect(strip).toMatch(/Order TBD — enter cuts \(sq ft ÷ 9 is not an order\)/);
    expect(strip).toMatch(/Billing TBD — not 50 sq yd from taped area/);
    expect(strip).toMatch(/Unit sq yd/);
    expect(strip).not.toMatch(/Order 50 sq yd/);
    expect(strip).not.toMatch(/Order 55/);
  });

  it("labels sq ft ÷ 9 as equivalent area, not an order", () => {
    expect(formatEquivalentSqyd(450)).toBe("50 sq yd equivalent area — not an order qty");
    expect(formatEquivalentSqyd(9)).toBe("1 sq yd equivalent area — not an order qty");
  });

  it("carpet cuts ARE the order quantity (no second waste factor)", () => {
    const t = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 450,
      cutsSqft: 540, // 15' × 12' + leftover — more than the room
    });
    expect(t.orderBasis).toBe("cuts");
    expect(t.wastePct).toBe(0);
    expect(t.orderSqft).toBe(540);
    expect(t.billingQty).toBe(60);
    expect(t.warnings).toEqual([]);
    const strip = formatTakeoffStrip(t);
    expect(strip).toMatch(/Measured 450 sq ft \(50 sq yd equivalent area — not an order quantity\)/);
    expect(strip).toMatch(/Waste 0%/);
    expect(strip).toMatch(/Order 540 sq ft · 60 sq yd \(from cuts/);
    expect(strip).toMatch(/Billing 60 sq yd \(this number is yards, not square feet\)/);
    expect(strip).toMatch(/Unit sq yd/);
    expect(strip).not.toMatch(/Order 50 sq yd[^.]/);
  });

  it("sheet vinyl is roll goods billed in sq yd, same measured ≠ order rule", () => {
    expect(billsBySqydFamily("vinyl")).toBe(true);
    expect(billsBySquareYard("vinyl")).toBe(true);
    expect(profileFor("vinyl")?.unit).toBe("sqyd");
    const t = computeMaterialTakeoff({ family: "vinyl", measuredSqft: 180 });
    expect(t.billingUnit).toBe("sqyd");
    expect(t.orderBasis).toBe("none");
    expect(t.billingQty).toBe(0);
    expect(t.measured.sqydEquivalent).toBe(20);
    expect(materialWastePctForEmit({ family: "vinyl", requestedWastePct: 8 })).toBe(0);
    expect(materialWastePctForEmit({ family: "lvp", requestedWastePct: 10 })).toBe(10);
  });

  it("never writes taped roll-goods area onto a Builder material quantity", () => {
    expect(areaDerivedMaterialAllowed("carpet")).toBe(false);
    expect(areaDerivedMaterialAllowed("vinyl")).toBe(false);
    expect(areaDerivedMaterialAllowed("lvp")).toBe(true);
    expect(areaDerivedMaterialAllowed("hardwood")).toBe(true);
    expect(areaDerivedMaterialAllowed("laminate")).toBe(true);
    expect(areaDerivedMaterialAllowed("tile")).toBe(true);
    expect(areaDerivedMaterialAllowed("other")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "gal")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "gallon")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "kit")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "each")).toBe(false);
    expect(areaDerivedMaterialAllowed("lvp", "sqft")).toBe(true);
    expect(
      areaDerivedMaterialQty({
        family: "other",
        measuredSqft: 500,
        billingUnit: "sqft",
      }),
    ).toBeNull();
    expect(
      areaDerivedMaterialQty({
        family: "other",
        measuredSqft: 500,
        billingUnit: "sqft",
        productUnit: "gal",
      }),
    ).toBeNull();
    // 450 sq ft = 50 sq yd equivalent — that conversion is not an order.
    expect(
      areaDerivedMaterialQty({ family: "carpet", measuredSqft: 450, billingUnit: "sqyd" }),
    ).toBeNull();
    expect(
      areaDerivedMaterialQty({ family: "vinyl", measuredSqft: 180, billingUnit: "sqyd" }),
    ).toBeNull();
    expect(
      areaDerivedMaterialQty({ family: "lvp", measuredSqft: 500, billingUnit: "sqft" }),
    ).toBe(500);
    expect(
      areaDerivedMaterialQty({ family: "hardwood", measuredSqft: 500, billingUnit: "sqft" }),
    ).toBe(500);
    // Install labor still follows measured area until cuts own the job.
    expect(measuredInstallLaborAllowed("carpet", 0)).toBe(true);
    expect(measuredInstallLaborAllowed("carpet", undefined)).toBe(true);
    expect(measuredInstallLaborAllowed("carpet", 540)).toBe(false);
    expect(measuredInstallLaborAllowed("vinyl", 90)).toBe(false);
    expect(measuredInstallLaborAllowed("lvp", 0)).toBe(true);
    expect(measuredInstallLaborAllowed("lvp", 999)).toBe(true);
  });

  it("exclusive carpet tile is modular — measured + waste, not a roll cut plan", () => {
    expect(rollGoodsNeedCuts("carpet")).toBe(true);
    expect(rollGoodsNeedCuts("carpet", [])).toBe(true);
    expect(rollGoodsNeedCuts("carpet", ["stretch_in"])).toBe(true);
    expect(rollGoodsNeedCuts("carpet", ["glue"])).toBe(true);
    expect(rollGoodsNeedCuts("carpet", ["carpet_tile"])).toBe(false);
    expect(rollGoodsNeedCuts("carpet", ["stretch_in", "carpet_tile"])).toBe(true);
    expect(rollGoodsNeedCuts("vinyl", ["carpet_tile"])).toBe(true);
    expect(rollGoodsNeedCuts("lvp", ["carpet_tile"])).toBe(false);
    // Hard-surface glue must not leak into the carpet-tile decision.
    expect(carpetInstallSystemsFromLabels(["Glue-down", "Carpet tile"])).toEqual(["glue", "carpet_tile"]);
    expect(carpetInstallSystemsFromLabels(["Floating / click", "Carpet tile"])).toEqual(["carpet_tile"]);
    expect(rollGoodsNeedCuts("carpet", carpetInstallSystemsFromLabels(["Carpet tile"]))).toBe(false);
    expect(
      rollGoodsNeedCuts("carpet", carpetInstallSystemsFromLabels(["Floating / click", "Carpet tile"])),
    ).toBe(false);

    expect(areaDerivedMaterialAllowed("carpet")).toBe(false);
    expect(areaDerivedMaterialAllowed("carpet", "sqyd", ["carpet_tile"])).toBe(true);
    expect(
      areaDerivedMaterialQty({
        family: "carpet",
        measuredSqft: 450,
        billingUnit: "sqyd",
        carpetInstallSystems: ["carpet_tile"],
      }),
    ).toBe(50);
    expect(materialWastePctForEmit({ family: "carpet", requestedWastePct: 10 })).toBe(0);
    expect(
      materialWastePctForEmit({
        family: "carpet",
        requestedWastePct: 10,
        carpetInstallSystems: ["carpet_tile"],
      }),
    ).toBe(10);

    const tile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 450,
      wastePct: 10,
      carpetSystems: ["carpet_tile"],
    });
    expect(tile.orderBasis).toBe("measured_plus_waste");
    expect(tile.measured.sqft).toBe(450);
    expect(tile.measured.sqydEquivalent).toBe(50);
    expect(tile.wastePct).toBe(10);
    expect(tile.wasteSqft).toBe(45);
    expect(tile.orderSqft).toBe(495);
    expect(tile.billingUnit).toBe("sqyd");
    expect(tile.billingQty).toBe(55);
    expect(tile.cartons).toBeNull();
    expect(tile.notes.some((n) => /carton coverage/i.test(n))).toBe(true);
    const strip = formatTakeoffStrip(tile);
    expect(strip).toMatch(/Measured 450 sq ft \(50 sq yd equivalent area — not an order quantity\)/);
    expect(strip).toMatch(/Waste 10%/);
    expect(strip).toMatch(/Order 495 sq ft · 55 sq yd/);
    expect(strip).toMatch(/Billing 55 sq yd \(this number is yards, not square feet\)/);
    expect(strip).toMatch(/Unit sq yd/);
    expect(strip).not.toMatch(/Order TBD/);
    expect(strip).not.toMatch(/from cuts/);

    const boxed = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 450,
      wastePct: 10,
      sqftPerBox: 24,
      carpetSystems: ["carpet_tile"],
    });
    expect(boxed.cartons).toEqual({
      coverageSqft: 24,
      cartonCount: 21,
      orderedCoverageSqft: 504,
    });
    expect(boxed.orderSqft).toBe(504);
    expect(boxed.billingQty).toBe(56);

    const glue = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 450,
      wastePct: 10,
      carpetSystems: ["glue"],
    });
    expect(glue.orderBasis).toBe("none");
    expect(glue.billingQty).toBe(0);
    expect(glue.warnings.some((w) => /not a professional carpet cut plan/i.test(w))).toBe(true);
  });

  it("AI notes estimate does not order roll goods from taped sq ft ÷ 9", () => {
    const src = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(src).toMatch(/areaDerivedMaterialAllowed/);
    expect(src).toMatch(/rollGoodsOrderTbdDescription/);
    expect(src).not.toMatch(/Math\.ceil\(\(sqft \/ 9\) \* \(1 \+ profile\.waste/);
    expect(src).toMatch(/length_in: null/);
    expect(src).toMatch(/areaDerivedMaterialQty/);
    expect(src).toMatch(/waste_pct: waste/);
  });

  it("boxed LVP uses waste and carton rounding only when coverage exists", () => {
    const withBox = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 500,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(withBox.measured.sqft).toBe(500);
    expect(withBox.wasteSqft).toBe(50);
    expect(withBox.cartons).toEqual({
      coverageSqft: 23.64,
      cartonCount: 24,
      orderedCoverageSqft: 567.36,
    });
    expect(withBox.orderSqft).toBe(567.36);

    const noBox = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 500,
      wastePct: 10,
    });
    expect(noBox.cartons).toBeNull();
    expect(noBox.orderSqft).toBe(550);
    expect(noBox.notes.some((n) => /not invented/i.test(n))).toBe(true);
    const strip = formatTakeoffStrip(withBox);
    expect(strip).toMatch(/Measured 500 sq ft/);
    expect(strip).toMatch(/Waste 10% \(50 sq ft\)/);
    expect(strip).toMatch(/Order 567\.36 sq ft \(24 cartons @ 23\.64 sq ft\)/);
    expect(strip).toMatch(/Billing 567\.36 sq ft \(this number is square feet, not yards\)/);
    expect(strip).toMatch(/Unit sq ft/);
    expect(strip).not.toMatch(/sq yd/);
  });

  it("does not invent carton coverage from zero or missing metadata", () => {
    expect(cartonTakeoff(550, null)).toBeNull();
    expect(cartonTakeoff(550, 0)).toBeNull();
    expect(cartonTakeoff(0, 23.64)).toBeNull();
  });
});

describe("roll-goods seam implications are not a cut plan", () => {
  it("does not invent a seam from a missing catalog roll width", () => {
    expect(seamImplication({ name: "Dining", lengthFt: 14, widthFt: 16 }, null)).toBeNull();
    expect(
      rollGoodsSeamWarnings({
        family: "carpet",
        rooms: [{ name: "Dining", lengthFt: 14, widthFt: 16 }],
      }),
    ).toEqual([]);
  });

  it("flags a seam when both room dimensions exceed the catalog roll width", () => {
    expect(seamImplication({ name: "Dining", lengthFt: 14, widthFt: 16 }, 12)).toBe("must_seam");
    const w = rollGoodsSeamWarnings({
      family: "carpet",
      rollWidthFt: 12,
      rooms: [{ name: "Dining", lengthFt: 14, widthFt: 16 }],
    });
    expect(w).toHaveLength(1);
    expect(w[0].id).toBe("carpet-must-seam");
    expect(w[0].text).toMatch(/12' carpet roll/);
    expect(w[0].text).toMatch(/Dining \(14' × 16'\)/);
    expect(w[0].text).toMatch(/Enter cuts — this is not a cut plan/);
    expect(w[0].text).not.toMatch(/order 50/);
  });

  it("flags direction when the room fits one way on the roll", () => {
    expect(seamImplication({ name: "Living", lengthFt: 12, widthFt: 16 }, 12)).toBe("direction_matters");
    const w = rollGoodsSeamWarnings({
      family: "vinyl",
      rollWidthFt: 12,
      rooms: [{ name: "Living", lengthFt: 12, widthFt: 16 }],
    });
    expect(w[0].id).toBe("vinyl-direction");
    expect(w[0].text).toMatch(/confirm direction/);
    expect(w[0].text).toMatch(/Living \(12' × 16'\)/);
  });

  it("is silent when the room fits the roll, and silent once cuts exist", () => {
    expect(seamImplication({ name: "Office", lengthFt: 10, widthFt: 11 }, 12)).toBe("fits");
    expect(
      rollGoodsSeamWarnings({
        family: "carpet",
        rollWidthFt: 12,
        rooms: [{ name: "Office", lengthFt: 10, widthFt: 11 }],
      }),
    ).toEqual([]);
    expect(
      rollGoodsSeamWarnings({
        family: "carpet",
        rollWidthFt: 12,
        hasCuts: true,
        rooms: [{ name: "Dining", lengthFt: 14, widthFt: 16 }],
      }),
    ).toEqual([]);
  });

  it("does not pick a width when catalog products disagree", () => {
    const w = rollGoodsSeamWarnings({
      family: "carpet",
      catalogWidthsFt: [12, 15],
      rooms: [{ name: "Dining", lengthFt: 14, widthFt: 16 }],
    });
    expect(w).toHaveLength(1);
    expect(w[0].id).toBe("carpet-roll-widths");
    expect(w[0].text).toMatch(/12' and 15'/);
    expect(w[0].text).not.toMatch(/seam is required/);
  });

  it("includes closets as their own rectangles and mentions pattern match", () => {
    const rooms = measuredRectsFromRooms([
      {
        name: "Living",
        lengthFt: 14,
        widthFt: 16,
        sections: [{ name: "Closet", lengthFt: 3, widthFt: 5 }],
      },
    ]);
    expect(rooms).toEqual([
      { name: "Living", lengthFt: 14, widthFt: 16 },
      { name: "Living / Closet", lengthFt: 3, widthFt: 5 },
    ]);
    const w = rollGoodsSeamWarnings({
      family: "carpet",
      rollWidthFt: 12,
      patternMatch: true,
      rooms,
    });
    expect(w.some((x) => x.id === "carpet-must-seam" && /Pattern matching/.test(x.text))).toBe(true);
    expect(w.some((x) => x.id === "carpet-must-seam" && /Closet/.test(x.text))).toBe(false);
  });

  it("skips irregular sq ft overrides that have no L×W", () => {
    expect(
      measuredRectsFromRooms([{ name: "Odd", lengthFt: 0, widthFt: 0, sqftOverride: 220 }]),
    ).toEqual([]);
  });
});

describe("waste is one source of truth", () => {
  it("builder and questionnaire read flooring-profiles, not a second table", () => {
    expect(defaultWastePct("carpet")).toBe(10);
    expect(defaultWastePct("lvp")).toBe(8);
    expect(defaultWastePct("hardwood")).toBe(7);
    expect(defaultWastePct("laminate")).toBe(7);
    expect(defaultWastePct("tile")).toBe(12);
    expect(defaultWastePct("vinyl")).toBe(7);
    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/defaultWastePct/);
    expect(builder).not.toMatch(/WASTE_BY_CATEGORY/);
  });
});

describe("show_if all/any + knowledge overlay", () => {
  it("all requires every clause; any requires one", () => {
    const vals = { install_method: ["Floating / click"], attached_pad: ["No"] };
    expect(
      matchesShowIf(
        {
          all: [
            { key: "install_method", in: ["Floating / click"] },
            { key: "attached_pad", in: ["No"] },
          ],
        },
        vals,
      ),
    ).toBe(true);
    expect(
      matchesShowIf(
        {
          all: [
            { key: "install_method", in: ["Floating / click"] },
            { key: "attached_pad", in: ["Yes"] },
          ],
        },
        vals,
      ),
    ).toBe(false);
    expect(
      matchesShowIf(
        { any: [{ key: "install_method", in: ["Glue-down"] }, { key: "surface_type", in: ["Hardwood"] }] },
        { install_method: ["Glue-down"] },
      ),
    ).toBe(true);
  });

  it("hides adhesive on a floating laminate job once the method is known", () => {
    const ctxVals = {
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    };
    expect(
      questionApplies(
        {
          key: "adhesive",
          config: { show_if: { key: "install_method", in: ["Glue-down"] } },
        },
        ctxVals,
      ),
    ).toBe(false);
    // Leftover Glue-down on exclusive laminate does not switch the sole
    // floating system. Mixed LVP + laminate still asks adhesive.
    expect(
      questionApplies(
        {
          key: "adhesive",
          config: { show_if: { key: "install_method", in: ["Glue-down"] } },
        },
        { ...ctxVals, install_method: ["Glue-down"] },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        {
          key: "adhesive",
          config: { show_if: { key: "install_method", in: ["Glue-down"] } },
        },
        {
          project_type: ["Hard surface"],
          surface_type: ["Laminate", "LVP / LVT"],
          install_method: ["Glue-down"],
        },
      ),
    ).toBe(true);
  });

  it("does not hide family-specific questions before surface type is answered", () => {
    expect(
      questionApplies(
        { key: "acclimation", config: {} },
        { project_type: ["Hard surface"] },
      ),
    ).toBe(true);
  });

  it("acclimation is hardwood OR glue-down; moisture_test also follows a moisture-concern flag", () => {
    const anyWhen = knowledgeQuestionByKey("acclimation")?.any;
    expect(anyWhen).toEqual([{ families: ["hardwood"] }, { systems: ["glue", "carpet_tile"] }]);
    expect(knowledgeQuestionByKey("moisture_test")?.any).toEqual([
      { families: ["hardwood"] },
      { systems: ["glue", "carpet_tile"] },
      { subfloor: ["Moisture concerns"] },
    ]);

    const pending = installContextFromValByKey({ project_type: ["Hard surface"] });
    expect(knowledgeWhenApplies({ any: anyWhen }, pending)).toBe(true);

    const lam = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(knowledgeWhenApplies({ any: anyWhen }, lam)).toBe(false);
    expect(knowledgeWhenApplies(knowledgeQuestionByKey("moisture_test")!, lam)).toBe(false);

    const lamMoisture = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(knowledgeWhenApplies(knowledgeQuestionByKey("moisture_test")!, lamMoisture)).toBe(true);
    expect(knowledgeWhenApplies({ any: anyWhen }, lamMoisture)).toBe(false);

    const engNail = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Nail-down"],
    });
    expect(knowledgeWhenApplies({ any: anyWhen }, engNail)).toBe(true);

    const lvpGlue = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(knowledgeWhenApplies({ any: anyWhen }, lvpGlue)).toBe(true);

    const carpetGlue = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(knowledgeWhenApplies({ any: anyWhen }, carpetGlue)).toBe(true);
    expect(jobNeedsAcclimationClimate({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    })).toBe(true);
    expect(jobNeedsAcclimationClimate({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    })).toBe(true);
    expect(jobNeedsAcclimationClimate({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    })).toBe(false);
    expect(jobNeedsAcclimationClimate({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    })).toBe(false);
  });

  it("hides separate underlayment when attached pad is Yes", () => {
    const vals = {
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      attached_pad: ["Yes"],
    };
    expect(questionApplies({ key: "hs_underlayment", config: {} }, vals)).toBe(false);
    expect(
      questionApplies(
        { key: "hs_underlayment", config: {} },
        { ...vals, attached_pad: ["No"] },
      ),
    ).toBe(true);
  });
});

describe("install context from questionnaire keys", () => {
  it("treats Engineered hardwood as hardwood + engineered construction", () => {
    const ctx = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Glue-down"],
    });
    expect(ctx.families).toEqual(["hardwood"]);
    expect(ctx.hardwoodConstruction).toBe("engineered");
    expect(ctx.systems).toEqual(["glue"]);
  });
});

describe("questionnaire is wired to the knowledge engine", () => {
  it("uses overlay visibility, takeoff review, and does not treat sqyd as a second copy of sqft", () => {
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/resolveQuestionVisibility/);
    expect(q).toMatch(/amountUnitLabelForQuestion/);
    expect(q).toMatch(/computeMaterialTakeoff/);
    expect(q).toMatch(/equivalent area — not an order qty/);
    expect(q).toMatch(/Continue to Builder/);
    expect(q).toMatch(/formatTakeoffStrip/);
    expect(q).toMatch(/Running takeoff — measured is not order quantity/);
    expect(q).toMatch(/lineDisplayUnit/);
    expect(q).toMatch(/reviewBucketForQuestion/);
    expect(q).toMatch(/formatMeasuredLabel/);
    expect(q).toMatch(/materialWastePctForEmit/);
    expect(q).toMatch(/billing quantity, and unit of/);
  });
});

describe("migration 0190 is the knowledge seed (not 0188)", () => {
  it("adds the new question keys and family labels without inventing coverage", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0190_flooring_knowledge_engine.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0190_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/carpet_install/);
    expect(sql).toMatch(/attached_pad/);
    expect(sql).toMatch(/prep_confidence/);
    expect(sql).toMatch(/Field verify \/ TBD/);
    expect(sql).toMatch(/LVP \/ LVT/);
    expect(sql).toMatch(/Sheet vinyl/);
    expect(sql).not.toMatch(/create table public\.products/);
    const migrations = readdirSync(join(root, "supabase/migrations"));
    expect(migrations.some((f) => f.startsWith("0188"))).toBe(false);
  });
});

describe("sheet vinyl, tile, stairs, and existing-bond follow-ups", () => {
  it("vinyl cuts are order quantity in sq yd, separate from carpet cuts", () => {
    const t = computeMaterialTakeoff({
      family: "vinyl",
      measuredSqft: 180,
      cutsSqft: 216,
    });
    expect(t.orderBasis).toBe("cuts");
    expect(t.billingUnit).toBe("sqyd");
    expect(t.billingQty).toBe(24);
    expect(t.warnings).toEqual([]);
  });

  it("tile overlay hides tile_layout on LVP jobs once the surface is known", () => {
    expect(
      questionApplies(
        { key: "tile_layout", config: { show_if: { key: "surface_type", in: ["Tile"] } } },
        { project_type: ["Hard surface"], surface_type: ["LVP / LVT"] },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        { key: "tile_layout", config: { show_if: { key: "surface_type", in: ["Tile"] } } },
        { project_type: ["Hard surface"], surface_type: ["Tile"] },
      ),
    ).toBe(true);
  });

  it("nail/staple fasteners hide on floating laminate", () => {
    expect(
      questionApplies(
        {
          key: "hardwood_fasteners",
          config: { show_if: { key: "install_method", in: ["Nail-down", "Staple-down"] } },
        },
        {
          project_type: ["Hard surface"],
          surface_type: ["Laminate"],
          install_method: ["Floating / click"],
        },
      ),
    ).toBe(false);
  });

  it("0191 parks stair extras after HS stairs and adds vinyl layout", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0191_flooring_knowledge_roll_tile_stairs.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0191_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/position = 265/);
    expect(sql).toMatch(/vinyl_layout/);
    expect(sql).toMatch(/tile_layout/);
    expect(sql).toMatch(/existing_bond/);
    expect(sql).toMatch(/"widths":\[6,12\]/);
    expect(sql).toMatch(/position = 270/);
  });
});

describe("trim / accessory units never mix with area", () => {
  it("quarter round, shoe, and base are linear feet — never square feet", () => {
    expect(accessoryUnitForType("Quarter round")).toBe("lnft");
    expect(accessoryUnitForType("Shoe molding")).toBe("lnft");
    expect(accessoryUnitForType("Baseboard")).toBe("lnft");
    expect(coerceTrimUnit("Quarter round", "sqft")).toBe("lnft");
    expect(coerceTrimUnit("Quarter round", "sq yd")).toBe("lnft");
    expect(coerceTrimUnit("Quarter round", "")).toBe("lnft");
  });

  it("T-mold is each and is not classified as linear molding", () => {
    expect(accessoryUnitForType("T-mold")).toBe("each");
    expect(accessoryUnitForType("Reducer")).toBe("each");
    expect(accessoryUnitForType("Stair nose")).toBe("each");
    expect(accessoryUnitForType("Stair tread")).toBe("each");
    expect(accessoryUnitForType("Stair riser")).toBe("each");
    expect(accessoryUnitForType("Vent / register")).toBe("each");
    expect(coerceTrimUnit("Stair nose", "sqft")).toBe("each");
    expect(coerceTrimUnit("T-mold", "sqft")).toBe("each");
    expect(coerceTrimUnit("Vent / register", "lnft")).toBe("each");
  });
});

describe("tack strip, expansion, and tile setting overlay", () => {
  it("hides tack strip once carpet is glue-down", () => {
    expect(
      questionApplies(
        {
          key: "tack_strip",
          config: { show_if: { key: "project_type", in: ["Carpet"] } },
        },
        { project_type: ["Carpet"], carpet_install: ["Glue-down"] },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        {
          key: "tack_strip",
          config: { show_if: { key: "project_type", in: ["Carpet"] } },
        },
        { project_type: ["Carpet"], carpet_install: ["Stretch-in"] },
      ),
    ).toBe(true);
  });

  it("hides floating expansion on glue-down LVP", () => {
    expect(
      questionApplies(
        {
          key: "laminate_expansion",
          config: { show_if: { key: "install_method", in: ["Floating / click"] } },
        },
        {
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
        },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        {
          key: "laminate_expansion",
          config: { show_if: { key: "install_method", in: ["Floating / click"] } },
        },
        {
          project_type: ["Hard surface"],
          surface_type: ["Laminate"],
          install_method: ["Floating / click"],
        },
      ),
    ).toBe(true);
  });

  it("hides tile setting materials on laminate", () => {
    expect(
      questionApplies(
        {
          key: "tile_setting",
          config: { show_if: { key: "surface_type", in: ["Tile"] } },
        },
        { project_type: ["Hard surface"], surface_type: ["Laminate"] },
      ),
    ).toBe(false);
  });
});

describe("migration 0192 closes remaining estimator gaps", () => {
  it("keys tack strip, expansion, tile setting, vents without inventing prices", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0192_flooring_knowledge_estimator_gaps.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0192_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/tack_strip/);
    expect(sql).toMatch(/laminate_expansion/);
    expect(sql).toMatch(/tile_setting/);
    expect(sql).toMatch(/vents_registers/);
    expect(sql).toMatch(/Unknown \/ field verify/);
    expect(sql).not.toMatch(/create table public\.products/);
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/sortEstimateQuestions/);
    expect(q).toMatch(/coerceTrimUnit/);
    expect(q).toMatch(/never square feet/);
    expect(q).toMatch(/Order \(estimate — not a cut plan\)/);
    expect(q).toMatch(/rollWidthFt/);
    expect(q).toMatch(/defaultCutWidthFt/);
  });
});

describe("product metadata overrides generic roll-width defaults", () => {
  it("uses the catalog roll width when present, and does not invent one", () => {
    expect(
      defaultCutWidthFt({ family: "carpet", productWidthFt: 15, configWidths: [12, 15] }),
    ).toBe(15);
    expect(
      defaultCutWidthFt({ family: "vinyl", productWidthFt: null, configWidths: [6, 12] }),
    ).toBe(0);
    expect(defaultCutWidthFt({ family: "carpet" })).toBe(0);
    expect(defaultCutWidthFt({ family: "vinyl" })).toBe(0);
    expect(cutWidthChoicesFt({ family: "carpet" })).toEqual([12, 15]);
    expect(cutWidthChoicesFt({ family: "vinyl" })).toEqual([6, 12]);
    expect(enteredCutWidthFt("")).toBe(0);
    expect(enteredCutWidthFt("0")).toBe(0);
    expect(enteredCutWidthFt(null)).toBe(0);
    expect(enteredCutWidthFt("12")).toBe(12);
    expect(enteredCutWidthFt(15)).toBe(15);
    expect(cutWidthChoicesFt({ family: "carpet", productWidthFt: 13.5 })).toEqual([12, 13.5, 15]);
    expect(cutWidthChoicesFt({ family: "vinyl", configWidths: [6, 12], productWidthFt: 12 })).toEqual([
      6, 12,
    ]);
  });

  it("PO and Builder do not invent a 12' roll when catalog width is missing", () => {
    const po = readFileSync(join(root, "src/lib/po-build.ts"), "utf8");
    expect(po).toMatch(/lineUnitKey/);
    expect(po).toMatch(/billedQtyToSqyd/);
    expect(po).toMatch(/roll width TBD/);
    expect(po).not.toMatch(/: 12;/);
    expect(po).not.toMatch(/measure_unit === "sqyd" \? orderQty : orderQty \/ 9/);
    expect(po).not.toMatch(/unit === "sqyd" \? orderQty : unit === "sqft" \? orderQty \/ 9 : orderQty/);
    const scope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(scope).toMatch(/lineUnitKey/);
    expect(scope).toMatch(/padRollCount/);
    expect(scope).toMatch(/billedQtyToSqyd/);
    expect(scope).not.toMatch(/unit\.toLowerCase\(\)\.includes\(["']yd["']\)/);
    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/cutWidthChoicesFt/);
    expect(builder).toMatch(/Width TBD/);
    expect(builder).not.toMatch(/roll_width_ft: line\.roll_width_ft \|\| "12"/);
  });

  it("reads engineered construction from catalog species text", () => {
    expect(hardwoodConstructionFromSpecies("White oak, engineered")).toBe("engineered");
    expect(hardwoodConstructionFromSpecies("Solid white oak")).toBe("solid");
    expect(hardwoodConstructionFromSpecies(null)).toBe("unknown");
  });
});

describe("family → system asks the right keys (not every question)", () => {
  const has = (keys: string[], k: string) => keys.includes(k);

  it("every registry question has a purpose and vents are each", () => {
    expect(KNOWLEDGE_QUESTIONS.length).toBeGreaterThan(10);
    for (const q of KNOWLEDGE_QUESTIONS) {
      expect(q.purpose).toBeTruthy();
      expect(q.phase).toBeTruthy();
    }
    expect(knowledgeQuestionByKey("vents_registers")?.quantityUnit).toBe("each");
    expect(knowledgeQuestionByKey("toilets")?.quantityUnit).toBe("each");
    expect(knowledgeQuestionByKey("appliances")?.quantityUnit).toBe("each");
    expect(knowledgeQuestionByKey("doors_shave")?.quantityUnit).toBe("each");
    expect(knowledgeQuestionByKey("tack_strip")?.systems).toEqual(["stretch_in"]);
    expect(knowledgeQuestionByKey("carpet_pad")?.systems).toEqual(["stretch_in"]);
    expect(knowledgeQuestionByKey("tack_strip")?.require).toEqual({
      key: "carpet_install",
      in: ["Stretch-in"],
    });
    expect(knowledgeQuestionByKey("carpet_pad")?.require).toEqual({
      key: "carpet_install",
      in: ["Stretch-in"],
    });
    expect(amountUnitLabelForQuestion({ key: "toilets", config: { emit: { unit: "sqft" } } })).toBe(
      "each",
    );
    expect(amountUnitLabelForQuestion({ key: "vents_registers" })).toBe("each");
    expect(amountUnitLabelForQuestion({ key: "metals_qty" })).toBe("each");
    expect(amountUnitLabelForQuestion({ key: "tack_strip_qty" })).toBe("ln ft");
    expect(amountUnitLabelForQuestion({ key: "pattern_repeat" })).toMatch(/inches of repeat/);
  });

  it("floating laminate: no adhesive, no fasteners, no tack, expansion on", () => {
    const keys = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(has(keys, "adhesive")).toBe(false);
    expect(has(keys, "hardwood_fasteners")).toBe(false);
    expect(has(keys, "tack_strip")).toBe(false);
    expect(has(keys, "tile_setting")).toBe(false);
    expect(has(keys, "vinyl_layout")).toBe(false);
    expect(has(keys, "laminate_expansion")).toBe(true);
    expect(has(keys, "attached_pad")).toBe(true);
    expect(has(keys, "hs_underlayment")).toBe(true);
    expect(has(keys, "hs_direction")).toBe(true);
    expect(has(keys, "hardwood_finish")).toBe(false);
    expect(has(keys, "acclimation")).toBe(false);
    expect(has(keys, "moisture_test")).toBe(false);
    expect(has(keys, "moisture_mitigation")).toBe(false);
  });

  it("laminate / tile / sheet vinyl do not wait for a click on their only legal method", () => {
    const lam = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    });
    expect(has(lam, "adhesive")).toBe(false);
    expect(has(lam, "laminate_expansion")).toBe(true);
    expect(has(lam, "attached_pad")).toBe(true);
    expect(has(lam, "hardwood_fasteners")).toBe(false);
    expect(has(lam, "hardwood_finish")).toBe(false);

    const tile = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(has(tile, "adhesive")).toBe(false);
    expect(has(tile, "tile_setting")).toBe(true);
    expect(has(tile, "attached_pad")).toBe(false);

    const vinyl = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
    });
    expect(has(vinyl, "adhesive")).toBe(true);
    expect(has(vinyl, "vinyl_layout")).toBe(true);
    expect(has(vinyl, "attached_pad")).toBe(false);
    expect(has(vinyl, "moisture_test")).toBe(true);

    const lvp = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(has(lvp, "adhesive")).toBe(true);
    expect(has(lvp, "attached_pad")).toBe(true);
  });

  it("glue-down LVP: adhesive on, floating follow-ups off", () => {
    const keys = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(has(keys, "adhesive")).toBe(true);
    expect(has(keys, "attached_pad")).toBe(false);
    expect(has(keys, "laminate_expansion")).toBe(false);
    expect(has(keys, "hs_underlayment")).toBe(false);
    expect(has(keys, "hardwood_fasteners")).toBe(false);
    expect(has(keys, "hardwood_finish")).toBe(false);
    expect(has(keys, "tile_layout")).toBe(false);
    expect(has(keys, "acclimation")).toBe(true);
    expect(has(keys, "moisture_test")).toBe(true);
    expect(has(keys, "moisture_mitigation")).toBe(true);
  });

  it("each family asks its own follow-ups and hides the others", () => {
    const on = (keys: string[], want: string[]) => want.forEach((k) => expect(keys, k).toContain(k));
    const off = (keys: string[], hide: string[]) => hide.forEach((k) => expect(keys, k).not.toContain(k));

    const carpetStretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    on(carpetStretch, ["work_type", "wet_area", "carpet_install", "carpet_cuts", "pattern_match", "tack_strip", "carpet_pad", "existing_pad", "existing_tack", "hs_demo", "substrate", "radiant_heat", "carpet_stairs"]);
    off(carpetStretch, ["adhesive", "attached_pad", "tile_setting", "vinyl_layout", "hardwood_fasteners", "hardwood_finish", "laminate_expansion", "acclimation", "moisture_test", "hs_direction", "carpet_tile_stairs", "install_method", "surface_type"]);

    const carpetGlue = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    on(carpetGlue, ["adhesive", "vapor_barrier"]);
    off(carpetGlue, ["tack_strip", "carpet_pad", "attached_pad", "tile_layout", "install_method", "surface_type"]);

    const carpetTile = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    on(carpetTile, ["adhesive", "carpet_cuts", "carpet_tile_stairs", "acclimation", "moisture_test", "moisture_mitigation"]);
    off(carpetTile, ["tack_strip", "carpet_pad", "laminate_expansion", "carpet_stairs", "pattern_match", "pattern_repeat", "carpet_direction", "vapor_barrier", "install_method", "surface_type"]);

    const lam = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    on(lam, ["attached_pad", "laminate_expansion", "hs_direction"]);
    off(lam, ["adhesive", "hardwood_fasteners", "hardwood_finish", "tile_setting", "vinyl_layout", "tack_strip", "acclimation", "moisture_test", "moisture_mitigation"]);

    const lvpGlue = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    on(lvpGlue, ["adhesive", "acclimation", "moisture_test", "moisture_mitigation"]);
    off(lvpGlue, ["attached_pad", "laminate_expansion", "hardwood_fasteners", "hardwood_finish", "tile_setting"]);

    const engNail = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Nail-down"],
    });
    on(engNail, ["hardwood_fasteners", "hardwood_finish", "acclimation", "moisture_test", "moisture_mitigation"]);
    off(engNail, ["adhesive", "attached_pad", "tile_setting", "vinyl_layout"]);

    const vinyl = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Glue-down"],
    });
    on(vinyl, ["vinyl_layout", "vinyl_skim", "adhesive", "moisture_test", "moisture_mitigation"]);
    off(vinyl, ["attached_pad", "carpet_pad", "tile_application", "hardwood_fasteners", "hardwood_finish"]);

    const tile = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      install_method: ["Thinset / mortar"],
    });
    on(tile, ["tile_application", "tile_body", "tile_format", "tile_layout", "tile_setting"]);
    off(tile, ["adhesive", "attached_pad", "vinyl_layout", "hardwood_fasteners", "hardwood_finish", "laminate_expansion", "acclimation", "moisture_test", "hs_direction", "vapor_barrier"]);
  });

  it("stretch-in carpet: tack strip on, adhesive off", () => {
    const keys = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(has(keys, "tack_strip")).toBe(true);
    expect(has(keys, "adhesive")).toBe(false);
    expect(has(keys, "pattern_match")).toBe(true);
    expect(has(keys, "carpet_pad")).toBe(true);
    expect(has(keys, "hs_prep")).toBe(true);
    expect(has(keys, "subfloor_needed")).toBe(true);
    expect(has(keys, "selflevel_needed")).toBe(true); // unanswered hs_prep does not hide
    expect(has(keys, "tile_setting")).toBe(false);
    expect(
      has(
        visibleKnowledgeKeys({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_prep: ["None"],
        }),
        "selflevel_needed",
      ),
    ).toBe(false);
    expect(
      has(
        visibleKnowledgeKeys({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_prep: ["Self-leveling"],
        }),
        "selflevel_needed",
      ),
    ).toBe(true);
    expect(
      has(
        visibleKnowledgeKeys({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          pattern_match: ["Pattern match required"],
        }),
        "pattern_repeat",
      ),
    ).toBe(true);
    expect(
      has(
        visibleKnowledgeKeys({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          pattern_match: ["No pattern / no match"],
        }),
        "pattern_repeat",
      ),
    ).toBe(false);
  });

  it("glue-down carpet: tack strip off, adhesive on", () => {
    const keys = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(has(keys, "tack_strip")).toBe(false);
    expect(has(keys, "adhesive")).toBe(true);
    expect(has(keys, "carpet_pad")).toBe(false);
  });

  it("tile: setting materials and layout, not floating pad", () => {
    const keys = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      install_method: ["Thinset / mortar"],
    });
    expect(has(keys, "tile_setting")).toBe(true);
    expect(has(keys, "tile_layout")).toBe(true);
    expect(has(keys, "tile_application")).toBe(true);
    expect(has(keys, "tile_body")).toBe(true);
    expect(has(keys, "tile_format")).toBe(true);
    expect(has(keys, "adhesive")).toBe(false);
    expect(has(keys, "attached_pad")).toBe(false);
    expect(has(keys, "laminate_expansion")).toBe(false);
  });

  it("0193 adds subfloor condition without inventing bag counts", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0193_flooring_knowledge_subfloor_condition.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0193_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/subfloor_condition/);
    expect(sql).toMatch(/Unknown \/ field verify/);
    expect(sql).toMatch(/Berber \/ loop/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(has(visibleKnowledgeKeys({ project_type: ["Hard surface"] }), "subfloor_condition")).toBe(
      true,
    );
  });

  it("0200 asks moisture_test from substrate moisture concerns without inventing a reading", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0200_flooring_knowledge_prep_gates.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0200_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Moisture concerns/);
    expect(sql).toMatch(/subfloor/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);

    const lam = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    });
    expect(has(lam, "moisture_test")).toBe(false);

    const flagged = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(has(flagged, "moisture_test")).toBe(true);
    expect(has(flagged, "moisture_mitigation")).toBe(true);
    expect(has(lam, "moisture_mitigation")).toBe(false);

    const uneven = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      subfloor_condition: ["Uneven"],
      hs_prep: ["None"],
    });
    expect(knowledgeWarnings(uneven).some((w) => w.id === "subfloor-uneven")).toBe(true);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          subfloor_condition: ["Uneven"],
          hs_prep: ["Self-leveling"],
        }),
      ).some((w) => w.id === "subfloor-uneven"),
    ).toBe(false);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          subfloor_condition: ["Damage / soft spots"],
          subfloor_needed: ["No"],
        }),
      ).some((w) => w.id === "subfloor-damage"),
    ).toBe(true);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Carpet"],
          subfloor_condition: ["Damage / soft spots"],
          subfloor_needed: ["Field verify / TBD"],
        }),
      ).some((w) => w.id === "subfloor-damage"),
    ).toBe(false);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          subfloor_condition: ["Unknown / field verify"],
          prep_confidence: ["Known"],
        }),
      ).some((w) => w.id === "subfloor-unknown-known"),
    ).toBe(true);
  });
});

describe("estimator conversation order (not SQL position)", () => {
  const q = (row: {
    key?: string;
    kind?: string;
    position: number;
    id?: string;
    section?: string;
    show_if?: { key: string; in: string[] };
    trim_list?: boolean;
  }) => ({
    id: row.id ?? row.key ?? `p${row.position}`,
    key: row.key ?? null,
    kind: row.kind ?? "choice",
    section: row.section ?? "",
    position: row.position,
    config: {
      ...(row.show_if ? { show_if: row.show_if } : {}),
      ...(row.trim_list ? { trim_list: true } : {}),
    },
  });

  it("walks area → product → measure → install even when SQL positions are inverted", () => {
    const sorted = sortEstimateQuestions([
      q({
        key: "adhesive",
        position: 1,
        show_if: { key: "install_method", in: ["Glue-down"] },
      }),
      q({ key: "install_method", position: 2 }),
      q({ key: "project_type", position: 50 }),
      q({ key: "pattern_match", position: 3 }),
      q({ key: "surface_type", position: 40 }),
    ]);
    expect(sorted.map((x) => x.key)).toEqual([
      "project_type",
      "surface_type",
      "pattern_match",
      "install_method",
      "adhesive",
    ]);
  });

  it("keeps a show_if gate ahead of its dependent inside the same phase", () => {
    const sorted = sortEstimateQuestions([
      q({
        key: "adhesive",
        position: 1,
        show_if: { key: "install_method", in: ["Glue-down"] },
      }),
      q({ key: "install_method", position: 99 }),
    ]);
    expect(sorted.map((x) => x.key)).toEqual(["install_method", "adhesive"]);
  });

  it("places floor_map after rooms so mixed jobs assign product to measured areas", () => {
    const sorted = sortEstimateQuestions([
      q({ id: "map", kind: "floor_map", position: 10 }),
      q({ id: "rooms", kind: "areas", position: 90, key: "rooms" }),
      q({ key: "project_type", position: 5 }),
    ]);
    expect(sorted.map((x) => x.id)).toEqual(["project_type", "rooms", "map"]);
  });

  it("puts prep_scope with measure (after area, before rooms) not in front of project_type", () => {
    expect(estimatorPhaseForQuestion(q({ key: "prep_scope", position: 4 }))).toBe("measure");
    const sorted = sortEstimateQuestions([
      q({ key: "prep_scope", position: 4 }),
      q({ key: "project_type", position: 10 }),
      q({ id: "rooms", kind: "areas", position: 20 }),
    ]);
    expect(sorted.map((x) => x.key ?? x.id)).toEqual(["project_type", "prep_scope", "rooms"]);
  });

  it("labels phases for the salesperson rail", () => {
    expect(estimatorPhaseLabel("area")).toBe("Area");
    expect(estimatorPhaseLabel("install")).toBe("Installation");
    expect(showIfReferencedKeys({ all: [{ key: "install_method", in: ["Glue-down"] }, { key: "attached_pad", in: ["No"] }] })).toEqual(
      ["install_method", "attached_pad"],
    );
  });
});

describe("unknown conditions stay unknown", () => {
  it("withholds bag/sheet counts only when prep is field verify / TBD", () => {
    expect(prepQuantitiesAreFinal([])).toBe(true);
    expect(prepQuantitiesAreFinal(["Known"])).toBe(true);
    expect(prepQuantitiesAreFinal(["Estimated"])).toBe(true);
    expect(prepQuantitiesAreFinal(["Allowance"])).toBe(true);
    expect(prepQuantitiesAreFinal(["Field verify / TBD"])).toBe(false);
    expect(prepQuantitySuffix(["Estimated"])).toBe(" (estimated)");
    expect(prepQuantitySuffix(["Allowance"])).toBe(" (allowance)");
    expect(prepQuantitySuffix(["Field verify / TBD"])).toBe("");
  });

  it("classifies live orphan keys instead of dumping them in details", () => {
    expect(knowledgeQuestionByKey("stairs")?.phase).toBe("details");
    expect(knowledgeQuestionByKey("hs_prep")?.phase).toBe("prep");
    expect(knowledgeQuestionByKey("selflevel_needed")?.purpose).toBe("PREP");
    expect(knowledgeQuestionByKey("selflevel_needed")?.require).toEqual({
      key: "hs_prep",
      in: ["Self-leveling"],
    });
    expect(knowledgeQuestionByKey("existing_bond")?.require).toEqual({
      key: "hs_demo",
      in: ["LVP", "Laminate", "Sheet vinyl", "LVP / Vinyl"],
    });
    expect(knowledgeQuestionByKey("subfloor_needed")?.purpose).toBe("PREP");
    expect(knowledgeQuestionByKey("climate_control")?.phase).toBe("install");
    expect(knowledgeQuestionByKey("hs_product")).toBeUndefined();
  });

  it("loose-lay LVP does not ask glue or floating follow-ups", () => {
    const keys = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Loose-lay"],
    });
    expect(keys.includes("adhesive")).toBe(false);
    expect(keys.includes("attached_pad")).toBe(false);
    expect(keys.includes("hs_underlayment")).toBe(false);
    expect(keys.includes("laminate_expansion")).toBe(false);
  });

  it("does not invent a hard-surface stair labor rate", () => {
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).not.toMatch(/DEFAULT_STAIR_LABOR_PER_SQFT/);
    expect(q).toMatch(/prepQuantitiesAreFinal/);
    expect(q).toMatch(/labor rate TBD/);
    expect(q).not.toMatch(/derive one cut from the area @ 12/);
    expect(q).not.toMatch(/width_in: 144/);
    const scope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(scope).not.toMatch(/Derive one cut from the area at the roll/);
    const fake = carpetCutList([
      {
        room: "Living",
        description: "Mohawk",
        category: "carpet",
        length_in: null,
        width_in: null,
        sqft: 450,
        roll_width_ft: 12,
        measurements: null,
      },
    ]);
    expect(fake.cuts).toEqual([]);
    expect(fake.totalSqyd).toBe(0);
    const real = carpetCutList([
      {
        room: "Living",
        description: "Mohawk",
        category: "carpet",
        length_in: 25 * 12,
        width_in: 12 * 12,
        sqft: 300,
        roll_width_ft: 12,
        measurements: [{ label: "Living", length_in: 25 * 12, width_in: 12 * 12, op: "add" }],
      },
    ]);
    expect(real.cuts.length).toBe(1);
    expect(real.totalSqyd).toBeGreaterThan(0);
    const tileRoom = carpetCutList([
      {
        room: "Living",
        description: "Carpet tile",
        category: "carpet",
        length_in: 12 * 12,
        width_in: 14 * 12,
        sqft: 168,
        roll_width_ft: null,
        measurements: null,
        order_as_roll: false,
      },
    ]);
    expect(tileRoom.cuts).toEqual([]);
    expect(tileRoom.totalSqyd).toBe(0);
  });
});

describe("SQL show_if + overlay + phase sort (no live database)", () => {
  /** Configs copied from 0190–0194 so the walk matches what owner apply installs. */
  const catalogRows: { key: string; position: number; show_if: ShowIfClause | null }[] = [
    { key: "project_type", position: 10, show_if: null },
    { key: "surface_type", position: 200, show_if: { key: "project_type", in: ["Hard surface"] } },
    { key: "install_method", position: 205, show_if: { key: "project_type", in: ["Hard surface"] } },
    { key: "carpet_install", position: 105, show_if: { key: "project_type", in: ["Carpet"] } },
    { key: "carpet_cuts", position: 100, show_if: { key: "project_type", in: ["Carpet"] } },
    { key: "pattern_match", position: 106, show_if: { key: "project_type", in: ["Carpet"] } },
    {
      key: "pattern_repeat",
      position: 107,
      show_if: { key: "pattern_match", in: ["Pattern match required"] },
    },
    { key: "carpet_direction", position: 108, show_if: { key: "project_type", in: ["Carpet"] } },
    { key: "tack_strip", position: 109, show_if: { key: "carpet_install", in: ["Stretch-in"] } },
    {
      key: "tack_strip_qty",
      position: 110,
      show_if: { key: "tack_strip", in: ["Replace / new tack strip"] },
    },
    {
      key: "attached_pad",
      position: 206,
      show_if: { key: "install_method", in: ["Floating / click"] },
    },
    {
      key: "adhesive",
      position: 225,
      show_if: {
        any: [
          { key: "install_method", in: ["Glue-down"] },
          { key: "carpet_install", in: ["Glue-down", "Carpet tile"] },
        ],
      },
    },
    {
      key: "hs_underlayment",
      position: 220,
      show_if: { key: "install_method", in: ["Floating / click"] },
    },
    {
      key: "laminate_expansion",
      position: 217,
      show_if: { key: "install_method", in: ["Floating / click"] },
    },
    {
      key: "hardwood_fasteners",
      position: 228,
      show_if: { key: "install_method", in: ["Nail-down", "Staple-down"] },
    },
    {
      key: "hardwood_finish",
      position: 211,
      show_if: { key: "surface_type", in: ["Hardwood", "Engineered hardwood"] },
    },
    {
      key: "acclimation",
      position: 230,
      show_if: {
        any: [
          { key: "surface_type", in: ["Hardwood", "Engineered hardwood"] },
          { key: "install_method", in: ["Glue-down"] },
          { key: "carpet_install", in: ["Glue-down", "Carpet tile"] },
        ],
      },
    },
    {
      key: "moisture_test",
      position: 235,
      show_if: {
        any: [
          { key: "install_method", in: ["Glue-down"] },
          { key: "carpet_install", in: ["Glue-down", "Carpet tile"] },
          { key: "surface_type", in: ["Hardwood", "Engineered hardwood"] },
          { key: "subfloor_condition", in: ["Moisture concerns"] },
        ],
      },
    },
    {
      key: "moisture_mitigation",
      position: 245,
      show_if: {
        any: [
          { key: "install_method", in: ["Glue-down"] },
          { key: "carpet_install", in: ["Glue-down", "Carpet tile"] },
          { key: "surface_type", in: ["Hardwood", "Engineered hardwood"] },
          { key: "subfloor_condition", in: ["Moisture concerns"] },
        ],
      },
    },
    { key: "vinyl_layout", position: 216, show_if: { key: "surface_type", in: ["Sheet vinyl"] } },
    { key: "vinyl_skim", position: 353, show_if: { key: "surface_type", in: ["Sheet vinyl"] } },
    { key: "tile_application", position: 217, show_if: { key: "surface_type", in: ["Tile"] } },
    { key: "tile_body", position: 218, show_if: { key: "surface_type", in: ["Tile"] } },
    { key: "tile_format", position: 219, show_if: { key: "surface_type", in: ["Tile"] } },
    { key: "tile_layout", position: 220, show_if: { key: "surface_type", in: ["Tile"] } },
    { key: "tile_setting", position: 221, show_if: { key: "surface_type", in: ["Tile"] } },
    { key: "subfloor_condition", position: 352, show_if: { key: "project_type", in: ["Carpet", "Hard surface"] } },
    { key: "existing_pad", position: 271, show_if: { any: [{ key: "project_type", in: ["Carpet"] }, { key: "hs_demo", in: ["Carpet"] }] } },
    { key: "existing_tack", position: 272, show_if: { any: [{ key: "project_type", in: ["Carpet"] }, { key: "hs_demo", in: ["Carpet"] }] } },
    { key: "metals_needed", position: 112, show_if: { key: "project_type", in: ["Carpet"] } },
    { key: "carpet_pad", position: 110, show_if: { key: "carpet_install", in: ["Stretch-in"] } },
    {
      key: "wet_area",
      position: 254,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    { key: "toilets", position: 255, show_if: { key: "project_type", in: ["Carpet", "Hard surface"] } },
    { key: "vents_registers", position: 602, show_if: { key: "project_type", in: ["Carpet", "Hard surface"] } },
    { key: "appliances", position: 260, show_if: { key: "project_type", in: ["Carpet", "Hard surface"] } },
    { key: "delivery_scope", position: 535, show_if: { key: "project_type", in: ["Carpet", "Hard surface"] } },
    { key: "carpet_stairs", position: 250, show_if: { key: "project_type", in: ["Carpet"] } },
    {
      key: "carpet_tile_stairs",
      position: 251,
      show_if: { key: "carpet_install", in: ["Carpet tile"] },
    },
    {
      key: "carpet_tile_stair_count",
      position: 252,
      show_if: { key: "carpet_tile_stairs", in: ["Yes"] },
    },
    { key: "hs_plank_stairs", position: 250, show_if: { key: "project_type", in: ["Hard surface"] } },
    { key: "stairs", position: 249, show_if: { key: "project_type", in: ["Carpet", "Hard surface"] } },
    {
      key: "stair_landings",
      position: 265,
      show_if: {
        any: [
          { key: "stairs", in: ["Yes"] },
          { key: "carpet_stairs", in: ["Yes"] },
          { key: "hs_plank_stairs", in: ["Yes"] },
          { key: "carpet_tile_stairs", in: ["Yes"] },
        ],
      },
    },
    {
      key: "stair_open_sides",
      position: 266,
      show_if: {
        any: [
          { key: "stairs", in: ["Yes"] },
          { key: "carpet_stairs", in: ["Yes"] },
          { key: "hs_plank_stairs", in: ["Yes"] },
          { key: "carpet_tile_stairs", in: ["Yes"] },
        ],
      },
    },
    {
      key: "hs_direction",
      position: 214,
      show_if: { key: "surface_type", in: ["Laminate", "LVP / LVT", "Hardwood", "Engineered hardwood"] },
    },
    {
      key: "work_type",
      position: 268,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "hs_demo",
      position: 270,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "demo_disposal",
      position: 275,
      show_if: {
        key: "hs_demo",
        in: [
          "Carpet",
          "Ceramic WITH mortar bed",
          "Ceramic WITHOUT mortar bed",
          "Sheet vinyl",
          "Luan",
          "LVP",
          "Laminate",
          "Glue-down hardwood",
          "Nailed hardwood",
          "Other",
        ],
      },
    },
    {
      key: "bulk_pickup",
      position: 276,
      show_if: { key: "demo_disposal", in: ["Placed at curb"] },
    },
    {
      key: "substrate",
      position: 350,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "vapor_barrier",
      position: 207,
      show_if: {
        any: [
          { key: "install_method", in: ["Floating / click", "Glue-down"] },
          { key: "carpet_install", in: ["Glue-down"] },
          { key: "substrate", in: ["Concrete"] },
        ],
      },
    },
    {
      key: "radiant_heat",
      position: 208,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "construction_grade",
      position: 209,
      show_if: {
        any: [
          { key: "project_type", in: ["Hard surface"] },
          { key: "carpet_install", in: ["Glue-down", "Carpet tile"] },
        ],
      },
    },
    {
      key: "asbestos_risk",
      position: 277,
      show_if: {
        key: "hs_demo",
        in: ["Ceramic WITH mortar bed", "Ceramic WITHOUT mortar bed", "Sheet vinyl"],
      },
    },
    {
      key: "hs_transitions",
      position: 291,
      show_if: { key: "project_type", in: ["Hard surface"] },
    },
    {
      key: "hs_base_trim",
      position: 292,
      show_if: { key: "project_type", in: ["Hard surface"] },
    },
    {
      key: "prep_scope",
      position: 300,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "hs_prep",
      position: 310,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "selflevel_needed",
      position: 320,
      show_if: { key: "hs_prep", in: ["Self-leveling"] },
    },
    {
      key: "subfloor_needed",
      position: 355,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "existing_bond",
      position: 273,
      show_if: { key: "hs_demo", in: ["LVP", "Laminate", "Sheet vinyl", "LVP / Vinyl"] },
    },
    {
      key: "doors_shave",
      position: 500,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "furniture_level",
      position: 510,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "occupancy",
      position: 505,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "access_conditions",
      position: 532,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "furniture_heavy",
      position: 512,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "climate_control",
      position: 520,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "crew_entry",
      position: 530,
      show_if: { key: "project_type", in: ["Carpet", "Hard surface"] },
    },
    {
      key: "metals_qty",
      position: 113,
      show_if: { key: "metals_needed", in: ["Yes"] },
    },
    {
      key: "metal_type",
      position: 114,
      show_if: { key: "metals_needed", in: ["Yes"] },
    },
    {
      key: "metal_color",
      position: 115,
      show_if: { key: "metals_needed", in: ["Yes"] },
    },
  ];
  const catalog = catalogRows.map((row) => ({
    id: row.key,
    key: row.key,
    kind: "choice",
    section: "",
    position: row.position,
    config: { show_if: row.show_if },
  }));

  const walk = (answers: Record<string, string[]>) => {
    const vis = resolveQuestionVisibility(catalog, (q) => (q.key && answers[q.key]) || []);
    return sortEstimateQuestions(catalog.filter((q) => vis[q.id])).map((q) => q.key);
  };

  it("floating laminate: expansion and attached pad, no adhesive or fasteners", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(keys.indexOf("project_type")).toBeLessThan(keys.indexOf("surface_type")!);
    expect(keys.indexOf("surface_type")).toBeLessThan(keys.indexOf("install_method")!);
    expect(keys.indexOf("install_method")).toBeLessThan(keys.indexOf("attached_pad")!);
    expect(keys).toContain("laminate_expansion");
    expect(keys).toContain("hs_underlayment");
    expect(keys).toContain("hs_direction");
    expect(keys).toContain("hs_transitions");
    expect(keys).toContain("hs_base_trim");
    expect(keys).not.toContain("adhesive");
    expect(keys).not.toContain("hardwood_fasteners");
    expect(keys).not.toContain("hardwood_finish");
    expect(keys).not.toContain("tack_strip");
    expect(keys).not.toContain("tile_setting");
    expect(keys).not.toContain("vinyl_layout");
    expect(keys).not.toContain("carpet_install");
    expect(keys).not.toContain("acclimation");
    expect(keys).not.toContain("moisture_test");
    expect(keys).not.toContain("moisture_mitigation");
  });

  it("laminate without clicking install_method still hides adhesive and shows floating follow-ups", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    });
    expect(keys).toContain("attached_pad");
    expect(keys).toContain("laminate_expansion");
    expect(keys).not.toContain("adhesive");
    expect(keys).not.toContain("hardwood_fasteners");
    expect(keys).not.toContain("hardwood_finish");
    expect(synthesizeSoleInstallMethod({ project_type: ["Hard surface"], surface_type: ["Laminate"] }).install_method).toEqual([
      "Floating / click",
    ]);
  });

  it("laminate + moisture concerns still asks moisture_test (not a fake reading)", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(keys).toContain("moisture_test");
    expect(keys).toContain("moisture_mitigation");
    expect(keys).not.toContain("adhesive");
  });

  it("glue-down LVP: adhesive on, floating follow-ups off", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(keys).toContain("adhesive");
    expect(keys).toContain("acclimation");
    expect(keys).toContain("moisture_test");
    expect(keys).toContain("moisture_mitigation");
    expect(keys).not.toContain("attached_pad");
    expect(keys).not.toContain("laminate_expansion");
    expect(keys).not.toContain("hs_underlayment");
  });

  it("stretch-in carpet: tack strip on via overlay, adhesive off", () => {
    const keys = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(keys).toContain("tack_strip");
    expect(keys).toContain("pattern_match");
    expect(keys).toContain("carpet_pad");
    expect(keys).toContain("toilets");
    expect(keys).toContain("hs_demo");
    expect(keys).toContain("substrate");
    expect(keys).toContain("existing_pad");
    expect(keys).toContain("metals_needed");
    expect(keys).toContain("radiant_heat");
    expect(keys).toContain("hs_prep");
    expect(keys).toContain("subfloor_needed");
    expect(keys).toContain("prep_scope");
    expect(keys).toContain("doors_shave");
    expect(keys).toContain("furniture_level");
    expect(keys).toContain("occupancy");
    expect(keys).toContain("access_conditions");
    expect(keys).toContain("furniture_heavy");
    expect(keys).toContain("climate_control");
    expect(keys).toContain("crew_entry");
    expect(keys).not.toContain("metals_qty");
    expect(keys).not.toContain("selflevel_needed");
    expect(keys).not.toContain("existing_bond");
    expect(keys).not.toContain("pattern_repeat");
    expect(keys).not.toContain("adhesive");
    expect(keys).not.toContain("surface_type");
    expect(keys).not.toContain("hs_transitions");
    expect(keys).not.toContain("hs_base_trim");
    expect(keys).not.toContain("stair_landings");
    expect(keys).not.toContain("stair_open_sides");
    expect(keys).not.toContain("demo_disposal");
    expect(keys).not.toContain("hardwood_finish");
  });

  it("vapor_barrier: floating or concrete; not nail-down over plywood", () => {
    expect(
      walk({
        project_type: ["Hard surface"],
        surface_type: ["Laminate"],
        install_method: ["Floating / click"],
      }),
    ).toContain("vapor_barrier");
    expect(
      walk({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
        install_method: ["Nail-down"],
        substrate: ["Concrete"],
      }),
    ).toContain("vapor_barrier");
    expect(
      walk({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
        install_method: ["Nail-down"],
        substrate: ["Plywood / OSB"],
      }),
    ).not.toContain("vapor_barrier");
    expect(
      walk({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
      }),
    ).not.toContain("vapor_barrier");
    expect(
      walk({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
        substrate: ["Concrete"],
      }),
    ).toContain("vapor_barrier");
    expect(
      walk({
        project_type: ["Carpet"],
        carpet_install: ["Glue-down"],
      }),
    ).toContain("vapor_barrier");
  });

  it("glue-down carpet: SQL + overlay hide tack strip and pad (not just overlay)", () => {
    const keys = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(keys).toContain("adhesive");
    expect(keys).toContain("vapor_barrier");
    expect(keys).toContain("metals_needed");
    expect(keys).toContain("existing_pad");
    expect(keys).not.toContain("tack_strip");
    expect(keys).not.toContain("carpet_pad");
  });

  it("carpet-only walk uses shared typed demo (0142/0201), not a second $0.50 tear-out", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0201_flooring_knowledge_shared_demo.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0201_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/789950c7-06a2-4c4b-81ca-15b5a4a54f29/);
    expect(sql).toMatch(/\["Carpet","Hard surface"\]/);
    expect(sql).toMatch(/demo_disposal/);
    expect(sql).toMatch(/vapor_barrier/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).toMatch(/retired the generic/);
    expect(sql).not.toMatch(/insert into public\.estimate_questions/);

    const beforeDemo = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(beforeDemo).toContain("hs_demo");
    expect(beforeDemo).toContain("substrate");
    expect(beforeDemo).not.toContain("demo_disposal");
    expect(beforeDemo).not.toContain("existing_bond");
    expect(beforeDemo).not.toContain("asbestos_risk");

    const afterCarpetDemo = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
    });
    expect(afterCarpetDemo).toContain("demo_disposal");
    expect(afterCarpetDemo).not.toContain("asbestos_risk");
    expect(afterCarpetDemo).not.toContain("existing_bond");

    const overlay = visibleKnowledgeKeys({ project_type: ["Carpet"] });
    expect(overlay).toContain("hs_demo");
    expect(overlay).toContain("substrate");
    expect(overlay).toContain("demo_disposal");
  });

  it("attached pad Yes hides separate underlayment", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
      attached_pad: ["Yes"],
    });
    expect(keys).toContain("attached_pad");
    expect(keys).not.toContain("hs_underlayment");
  });

  it("sheet vinyl: layout + skim, no carton pad questions", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Glue-down"],
    });
    expect(keys).toContain("vinyl_layout");
    expect(keys).toContain("vinyl_skim");
    expect(keys).toContain("adhesive");
    expect(keys).not.toContain("attached_pad");
    expect(keys).not.toContain("tile_application");
    expect(keys).not.toContain("carpet_pad");
    expect(keys).not.toContain("hs_direction");
  });

  it("asbestos risk only after ceramic or sheet-vinyl demo", () => {
    const without = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      hs_demo: ["LVP"],
    });
    expect(without).not.toContain("asbestos_risk");
    const withVinyl = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Glue-down"],
      hs_demo: ["Sheet vinyl"],
    });
    expect(withVinyl).toContain("asbestos_risk");
  });

  it("tile: floor vs wall before layout, no floating follow-ups", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      install_method: ["Thinset / mortar"],
    });
    expect(keys.indexOf("tile_application")).toBeLessThan(keys.indexOf("tile_body")!);
    expect(keys.indexOf("tile_body")).toBeLessThan(keys.indexOf("tile_format")!);
    expect(keys.indexOf("tile_format")).toBeLessThan(keys.indexOf("tile_layout")!);
    expect(keys).toContain("tile_setting");
    expect(keys).not.toContain("attached_pad");
    expect(keys).not.toContain("vinyl_skim");
    expect(keys).not.toContain("hs_direction");
    expect(keys).not.toContain("asbestos_risk");
  });

  it("carpet tile asks adhesive, not pad or tack strip", () => {
    const keys = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(keys).toContain("adhesive");
    expect(keys).toContain("carpet_cuts");
    expect(keys).toContain("carpet_tile_stairs");
    expect(keys).toContain("acclimation");
    expect(keys).toContain("moisture_test");
    expect(keys).toContain("moisture_mitigation");
    expect(keys).not.toContain("carpet_stairs");
    expect(keys).not.toContain("tack_strip");
    expect(keys).not.toContain("carpet_pad");
    expect(keys).not.toContain("vapor_barrier");
    expect(keys).not.toContain("pattern_match");
    expect(keys).not.toContain("carpet_direction");
    expect(keys).not.toContain("pattern_repeat");
  });

  it("0202 gates moisture mitigation and adds tile body without inventing a category", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0202_flooring_knowledge_tile_carpet_prep.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0202_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/moisture_mitigation/);
    expect(sql).toMatch(/tile_body/);
    expect(sql).toMatch(/Carpet tile/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const ceramicNew = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
        install_method: ["Thinset / mortar"],
      }),
      { pickedLabels: ["Ceramic"] },
    );
    expect(ceramicNew.some((w) => w.id === "ceramic_substrate")).toBe(false);
    expect(ceramicNew.some((w) => w.id === "tile-stone")).toBe(false);

    const demoCeramic = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
        hs_demo: ["Ceramic WITHOUT mortar bed"],
      }),
    );
    expect(demoCeramic.some((w) => w.id === "ceramic_substrate")).toBe(true);

    const stone = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
      }),
      { pickedLabels: ["Natural stone"] },
    );
    expect(stone.some((w) => w.id === "tile-stone")).toBe(true);
    expect(knowledgeQuestionByKey("adhesive")?.systems).toEqual(["glue", "carpet_tile"]);
    expect(knowledgeQuestionByKey("tile_body")?.families).toEqual(["tile"]);
  });

  it("0203 asks radiant on carpet and tile format without inventing waste", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0203_flooring_knowledge_radiant_tile_format.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0203_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/radiant_heat/);
    expect(sql).toMatch(/tile_format/);
    expect(sql).toMatch(/\["Carpet","Hard surface"\]/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const large = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
      }),
      { pickedLabels: ['Large format (24" or larger)'] },
    );
    expect(large.some((w) => w.id === "tile-large-format")).toBe(true);

    const mixedGlue = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT", "Hardwood"],
        install_method: ["Glue-down"],
      }),
    );
    expect(mixedGlue.some((w) => w.id === "mixed-hs-install")).toBe(true);

    const mixedFloat = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT", "Hardwood"],
        install_method: ["Floating / click"],
      }),
    );
    expect(mixedFloat.some((w) => w.id === "mixed-hs-method")).toBe(true);

    const unanswered = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT", "Hardwood"],
      }),
    );
    expect(unanswered.some((w) => w.id === "mixed-hs-install" || w.id === "mixed-hs-method")).toBe(false);

    const mixedBoth = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT", "Hardwood"],
        install_method: ["Floating / click", "Nail-down"],
      }),
    );
    expect(mixedBoth.some((w) => w.id === "mixed-hs-install" || w.id === "mixed-hs-method")).toBe(
      false,
    );

    const bothBranches = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT", "Hardwood"],
      install_method: ["Floating / click", "Nail-down"],
    });
    expect(bothBranches).toContain("attached_pad");
    expect(bothBranches).toContain("hardwood_fasteners");
    expect(bothBranches).toContain("hardwood_finish");
    expect(bothBranches).not.toContain("adhesive");

    expect(jobNeedsMixedInstallMethodPicks(["lvp", "hardwood"])).toBe(true);
    expect(jobNeedsMixedInstallMethodPicks(["lvp"])).toBe(false);

    const keys = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(keys).toContain("radiant_heat");
    expect(keys).not.toContain("tile_format");
    expect(
      walk({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
        install_method: ["Thinset / mortar"],
      }),
    ).toContain("tile_format");
  });

  it("0204 asks grade on glue-down carpet, not stretch-in, without inventing a ban", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0204_flooring_knowledge_grade_carpet.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0204_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/construction_grade/);
    expect(sql).toMatch(/Glue-down/);
    expect(sql).toMatch(/Carpet tile/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(knowledgeQuestionByKey("construction_grade")?.any).toEqual([
      { families: ["hardwood", "lvp", "laminate", "vinyl", "tile"] },
      { systems: ["glue", "carpet_tile"] },
    ]);

    expect(
      walk({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
      }),
    ).not.toContain("construction_grade");
    expect(
      walk({
        project_type: ["Carpet"],
        carpet_install: ["Glue-down"],
      }),
    ).toContain("construction_grade");
    expect(
      walk({
        project_type: ["Carpet"],
        carpet_install: ["Carpet tile"],
      }),
    ).toContain("construction_grade");
    expect(
      walk({
        project_type: ["Hard surface"],
        surface_type: ["Laminate"],
        install_method: ["Floating / click"],
      }),
    ).toContain("construction_grade");

    const below = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Glue-down"],
        construction_grade: ["Below grade"],
      }),
    );
    expect(below.some((w) => w.id === "carpet-glue-below-grade")).toBe(true);
    expect(below.some((w) => /do not assume a ban/i.test(w.text))).toBe(true);

    const stretchBelow = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
        construction_grade: ["Below grade"],
      }),
    );
    expect(stretchBelow.some((w) => w.id === "carpet-glue-below-grade")).toBe(false);
  });

  it("0205 asks acclimation and moisture on glue-down carpet, not stretch-in", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0205_flooring_knowledge_carpet_glue_climate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0205_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/acclimation/);
    expect(sql).toMatch(/moisture_test/);
    expect(sql).toMatch(/moisture_mitigation/);
    expect(sql).toMatch(/carpet_install/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).not.toContain("acclimation");
    expect(stretch).not.toContain("moisture_test");
    expect(stretch).not.toContain("moisture_mitigation");

    const glue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("acclimation");
    expect(glue).toContain("moisture_test");
    expect(glue).toContain("moisture_mitigation");
    expect(glue).toContain("adhesive");
    expect(glue).not.toContain("tack_strip");
  });

  it("0206 shares Floor prep and subfloor on carpet, bags only after Self-leveling", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0206_flooring_knowledge_shared_prep.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0206_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/subfloor_needed/);
    expect(sql).toMatch(/hs_prep/);
    expect(sql).toMatch(/selflevel_needed/);
    expect(sql).toMatch(/prep_scope/);
    expect(sql).toMatch(/Field verify \/ TBD/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.estimate_questions/);

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("hs_prep");
    expect(stretch).toContain("subfloor_needed");
    expect(stretch).toContain("prep_scope");
    expect(stretch).not.toContain("selflevel_needed");

    const selfLevel = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_prep: ["Self-leveling"],
    });
    expect(selfLevel).toContain("selflevel_needed");
    expect(selfLevel.indexOf("hs_prep")).toBeLessThan(selfLevel.indexOf("selflevel_needed")!);

    const tbd = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      subfloor_needed: ["Field verify / TBD"],
    });
    expect(tbd).toContain("subfloor_needed");
    expect(tbd).toContain("hs_prep");

    const lam = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    });
    expect(lam).toContain("hs_prep");
    expect(lam).toContain("subfloor_needed");
    expect(lam).not.toContain("selflevel_needed");
  });

  it("0207 asks glued-vs-floating only after LVP/laminate/vinyl demo", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0207_flooring_knowledge_bond_site.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0207_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/existing_bond/);
    expect(sql).toMatch(/doors_shave/);
    expect(sql).toMatch(/furniture_level/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.estimate_questions/);

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("doors_shave");
    expect(stretch).toContain("furniture_level");
    expect(stretch).not.toContain("existing_bond");

    const carpetDemo = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
    });
    expect(carpetDemo).not.toContain("existing_bond");

    const lvpDemo = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["LVP"],
    });
    expect(lvpDemo).toContain("existing_bond");
    expect(lvpDemo.indexOf("hs_demo")).toBeLessThan(lvpDemo.indexOf("existing_bond")!);

    const lamDemo = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      hs_demo: ["Laminate"],
    });
    expect(lamDemo).toContain("existing_bond");
    expect(lamDemo).toContain("doors_shave");
    expect(lamDemo).toContain("furniture_level");

    expect(
      visibleKnowledgeKeys({
        project_type: ["Carpet"],
        hs_demo: ["Carpet"],
      }).includes("existing_bond"),
    ).toBe(false);
    expect(
      visibleKnowledgeKeys({
        project_type: ["Carpet"],
        hs_demo: ["LVP"],
      }).includes("existing_bond"),
    ).toBe(true);
  });

  it("0208 asks tack strip and new pad only after stretch-in", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0208_flooring_knowledge_stretch_pad.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0208_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/tack_strip/);
    expect(sql).toMatch(/carpet_pad/);
    expect(sql).toMatch(/Stretch-in/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.estimate_questions/);

    const unanswered = walk({
      project_type: ["Carpet"],
    });
    expect(unanswered).toContain("carpet_install");
    expect(unanswered).toContain("existing_pad");
    expect(unanswered).toContain("metals_needed");
    expect(unanswered).not.toContain("tack_strip");
    expect(unanswered).not.toContain("carpet_pad");

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("tack_strip");
    expect(stretch).toContain("carpet_pad");
    expect(stretch).toContain("existing_pad");
    expect(stretch).toContain("metals_needed");

    const glue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("existing_pad");
    expect(glue).toContain("metals_needed");
    expect(glue).not.toContain("tack_strip");
    expect(glue).not.toContain("carpet_pad");
  });

  it("0209 lets mixed hard-surface jobs pick more than one install method", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0209_flooring_knowledge_mixed_install.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0209_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/install_method/);
    expect(sql).toMatch(/\{multi\}/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.estimate_questions/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/jobNeedsMixedInstallMethodPicks/);
    expect(q).toMatch(/mixedHsInstall/);

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT", "Hardwood"],
      install_method: ["Floating / click", "Nail-down"],
    });
    expect(mixed).toContain("attached_pad");
    expect(mixed).toContain("hardwood_fasteners");
    expect(mixed).toContain("hardwood_finish");
    expect(mixed).not.toContain("adhesive");
    expect(mixed).toContain("occupancy");
    expect(mixed).toContain("climate_control");
    expect(mixed).toContain("crew_entry");
    expect(mixed).not.toContain("metals_needed");
    expect(mixed).not.toContain("metals_qty");
  });

  it("0210 counts carpet metals in EACH and keys crew entry", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0210_flooring_knowledge_metals_entry.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0210_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/metals_qty/);
    expect(sql).toMatch(/crew_entry/);
    expect(sql).toMatch(/occupancy/);
    expect(sql).toMatch(/climate_control/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.products/);

    expect(knowledgeQuestionByKey("metals_qty")?.quantityUnit).toBe("each");
    expect(knowledgeQuestionByKey("metals_qty")?.families).toEqual(["carpet"]);
    expect(knowledgeQuestionByKey("crew_entry")?.purpose).toBe("SCHEDULING");
    expect(knowledgeQuestionByKey("metals_qty")?.require).toEqual({
      key: "metals_needed",
      in: ["Yes"],
    });
    expect(amountUnitLabelForQuestion({ key: "metals_qty" })).toBe("each");
    expect(reviewBucketForQuestion({ key: "metals_qty", label: "How many metals" })).toBe(
      "accessories",
    );
    expect(reviewBucketForQuestion({ key: "crew_entry", label: "Site access" })).toBe("specials");
    expect(reviewBucketForQuestion({ key: "occupancy", label: "Occupied or vacant?" })).toBe(
      "specials",
    );
    expect(reviewBucketForQuestion({ key: "climate_control", label: "Climate control on site?" })).toBe(
      "installation",
    );

    const unanswered = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(unanswered).toContain("metals_needed");
    expect(unanswered).toContain("occupancy");
    expect(unanswered).toContain("access_conditions");
    expect(unanswered).toContain("furniture_heavy");
    expect(unanswered).toContain("climate_control");
    expect(unanswered).toContain("crew_entry");
    expect(unanswered).not.toContain("metals_qty");
    expect(unanswered).not.toContain("metal_type");

    const withMetals = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      metals_needed: ["Yes"],
    });
    expect(withMetals).toContain("metals_qty");
    expect(withMetals).toContain("metal_type");
    expect(withMetals).toContain("metal_color");
    expect(withMetals.indexOf("metals_needed")).toBeLessThan(withMetals.indexOf("metals_qty")!);

    const noMetals = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
      metals_needed: ["No"],
    });
    expect(noMetals).toContain("metals_needed");
    expect(noMetals).not.toContain("metals_qty");
    expect(noMetals).toContain("climate_control");
    expect(noMetals).toContain("occupancy");
  });

  it("0211 asks linear feet of new tack strip only after Replace", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0211_flooring_knowledge_tack_lnft.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0211_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/tack_strip_qty/);
    expect(sql).toMatch(/linear feet/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.products/);

    expect(knowledgeQuestionByKey("tack_strip_qty")?.quantityUnit).toBe("lnft");
    expect(knowledgeQuestionByKey("tack_strip_qty")?.require).toEqual({
      key: "tack_strip",
      in: ["Replace / new tack strip"],
    });
    expect(amountUnitLabelForQuestion({ key: "tack_strip_qty" })).toBe("ln ft");
    expect(reviewBucketForQuestion({ key: "tack_strip_qty", label: "New tack strip" })).toBe(
      "accessories",
    );

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("tack_strip");
    expect(stretch).not.toContain("tack_strip_qty");

    const keep = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      tack_strip: ["Keep existing — in good shape"],
    });
    expect(keep).not.toContain("tack_strip_qty");

    const tbd = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      tack_strip: ["Unknown / field verify"],
    });
    expect(tbd).not.toContain("tack_strip_qty");

    const replace = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      tack_strip: ["Replace / new tack strip"],
    });
    expect(replace).toContain("tack_strip_qty");
    expect(replace.indexOf("tack_strip")).toBeLessThan(replace.indexOf("tack_strip_qty")!);

    const glue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
      tack_strip: ["Replace / new tack strip"],
    });
    expect(glue).not.toContain("tack_strip");
    expect(glue).not.toContain("tack_strip_qty");
  });

  it("0212 does not order adhesive from taped square feet", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0212_flooring_knowledge_adhesive_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0212_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/taped square feet is not a glue order/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/q\.key !== "adhesive"/);
    expect(q).toMatch(/qty TBD \(\$\{countPhrase\} — not taped sq ft\)/);
    expect(q).toMatch(/productUnit: p\.unit/);

    expect(areaDerivedMaterialAllowed("other", "gal")).toBe(false);
    expect(
      areaDerivedMaterialQty({
        family: "other",
        measuredSqft: 500,
        billingUnit: "sqft",
        productUnit: "kit",
      }),
    ).toBeNull();
    expect(
      areaDerivedMaterialQty({
        family: "lvp",
        measuredSqft: 500,
        billingUnit: "sqft",
        productUnit: "sqft",
      }),
    ).toBe(500);
  });

  it("0213 treats exclusive carpet tile as modular, not a roll cut plan", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0213_flooring_knowledge_carpet_tile.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0213_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/carpet_cuts/);
    expect(sql).toMatch(/Carpet tile is modular/);
    expect(sql).toMatch(/do not invent a box size/i);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.products/);
    expect(sql).not.toMatch(/create table public\.estimate_questions/);

    const unanswered = walk({
      project_type: ["Carpet"],
    });
    expect(unanswered).toContain("carpet_install");
    expect(unanswered).toContain("carpet_cuts");

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("carpet_cuts");

    const glue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("carpet_cuts");

    const tile = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(tile).toContain("carpet_cuts");
    expect(tile).not.toContain("tack_strip");

    const mixedHsGlue = walk({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      carpet_install: ["Carpet tile"],
    });
    expect(mixedHsGlue).toContain("carpet_cuts");
    expect(mixedHsGlue).toContain("adhesive");

    const tileWarn = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Carpet tile"],
      }),
      { measuredSqft: 450, hasCuts: false },
    );
    expect(tileWarn.some((w) => w.id === "carpet-no-cuts")).toBe(false);

    const stretchWarn = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
      }),
      { measuredSqft: 450, hasCuts: false },
    );
    expect(stretchWarn.some((w) => w.id === "carpet-no-cuts")).toBe(true);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/modularTile/);
    expect(q).toMatch(/rollGoodsNeedCuts/);
    expect(q).toMatch(/order_as_roll: false/);
    expect(knowledgeQuestionByKey("carpet_cuts")?.purpose).toBe("WAREHOUSE");
  });

  it("0214 hides waterfall stairs on exclusive carpet tile and asks a step count in EACH", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0214_flooring_knowledge_carpet_tile_stairs.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0214_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/carpet_tile_stairs/);
    expect(sql).toMatch(/carpet_tile_stair_count/);
    expect(sql).toMatch(/waterfall/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.products/);

    const unanswered = walk({ project_type: ["Carpet"] });
    expect(unanswered).toContain("carpet_stairs");
    expect(unanswered).not.toContain("carpet_tile_stairs");

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("carpet_stairs");
    expect(stretch).not.toContain("carpet_tile_stairs");
    expect(stretch).not.toContain("carpet_tile_stair_count");

    const tile = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(tile).toContain("carpet_tile_stairs");
    expect(tile).not.toContain("carpet_stairs");
    expect(tile).not.toContain("carpet_tile_stair_count");

    const counted = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      carpet_tile_stairs: ["Yes"],
    });
    expect(counted).toContain("carpet_tile_stair_count");
    expect(counted).toContain("stair_landings");
    expect(counted.indexOf("carpet_tile_stairs")).toBeLessThan(
      counted.indexOf("carpet_tile_stair_count")!,
    );
    expect(amountUnitLabelForQuestion({ key: "carpet_tile_stair_count" })).toBe("each");
    expect(synthesizeStairGate({ carpet_tile_stairs: ["Yes"] }).stairs).toEqual(["Yes"]);

    const tbd = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      carpet_tile_stairs: ["Field verify / TBD"],
    });
    expect(tbd).not.toContain("carpet_tile_stair_count");
    expect(tbd).not.toContain("stair_landings");

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/Exclusive carpet tile is modular — do not emit wrap/);
    expect(knowledgeQuestionByKey("carpet_stairs")?.require?.in).toEqual(
      expect.arrayContaining(["Stretch-in", "Glue-down"]),
    );

    const glue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("carpet_stairs");
    expect(glue).not.toContain("carpet_tile_stairs");
  });

  it("install-day labels distinguish LVP from sheet vinyl without inventing a second capacity", () => {
    const line = (category: "lvp" | "vinyl") =>
      ({
        id: "1",
        option_id: "o",
        position: 0,
        room: null,
        description: category,
        note: null,
        line_type: "mat_labor" as const,
        sqft: 250,
        length_in: null,
        width_in: null,
        measure_unit: "sqft" as const,
        material_rate: 0,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 0,
        labor_cost: 0,
        quantity: 250,
        unit: "sq ft",
        category,
        from_stock: false,
        order_as_roll: false,
        roll_width_ft: null,
        sqft_per_box: null,
        is_fill: false,
        is_optional: false,
        measurements: null,
      });
    const lvp = installDaysForJob([line("lvp")], SCHEDULING_DEFAULTS);
    expect(lvp.breakdown.map((b) => b.label)).toEqual(["LVP / LVT"]);
    expect(lvp.breakdown[0]?.unit).toBe("sq ft");
    const vinyl = installDaysForJob([line("vinyl")], SCHEDULING_DEFAULTS);
    expect(vinyl.breakdown.map((b) => b.label)).toEqual(["Sheet vinyl"]);
    expect(vinyl.breakdown[0]?.unit).toBe("sq ft");
    expect(vinyl.days).toBe(lvp.days);
    const src = readFileSync(join(root, "src/lib/scheduling.ts"), "utf8");
    expect(src).not.toMatch(/Luxury \/ sheet vinyl/);
  });

  it("0215 does not order a 12-foot roll when cut width is empty", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0215_flooring_knowledge_cut_width.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0215_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/empty width is not a 12-foot/);
    expect(sql).toMatch(/vinyl_layout/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/enteredCutWidthFt/);
    expect(q).not.toMatch(/numv\(c\.width\) \|\| fallback/);
    expect(q).not.toMatch(/width = "12"/);
    expect(q).toMatch(/newCutRow = \(width = ""\)/);
    expect(carpetYardageFromCuts([{ lengthFt: 20, lengthIn: 0, rollWidthFt: "" }]).sqyd).toBe(0);
    expect(carpetYardageFromCuts([{ lengthFt: 20, lengthIn: 0, rollWidthFt: 12 }]).sqyd).toBe(26.67);
  });

  it("0216 keeps the roll-goods SKU as order TBD instead of dropping it or inventing yardage", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0216_flooring_knowledge_roll_tbd_sku.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0216_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/order TBD/);
    expect(sql).toMatch(/sq ft ÷ 9/);
    expect(sql).toMatch(/vinyl_layout/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const desc = rollGoodsOrderTbdDescription("Shaw Renovate", 450);
    expect(desc).toMatch(/order TBD \(enter cuts — not sq ft ÷ 9\)/);
    expect(desc).toMatch(/Measured 450 sq ft/);
    expect(desc).toMatch(/equivalent area — not an order qty/);
    expect(parseCutsFromText(desc)).toEqual([]);
    expect(lineQty({ line_type: "mat_labor", unit: "sq yd", measure_unit: "sqyd", sqft: null, quantity: null })).toBe(0);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/rollGoodsTbdLine/);
    expect(q).toMatch(/tbdRoll/);
    expect(q).toMatch(/sqft: null/);
    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/rollGoodsOrderTbdDescription/);
  });

  it("0217 does not order 8 sq ft of hard-surface flooring per stair", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0217_flooring_knowledge_hs_stair_units.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0217_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/8 sq ft\/step/);
    expect(sql).toMatch(/hs_plank_stairs/);
    expect(sql).toMatch(/per step/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).not.toMatch(/STAIR_SQFT_TREAD_RISER/);
    expect(q).not.toMatch(/STAIR_SQFT_TREAD_ONLY/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/Stair labor \$ \/ step/);
    expect(q).toMatch(/unit: "step"/);
    expect(q).not.toMatch(/sfPerStep/);
    expect(lineQty({ line_type: "mat_labor", category: "labor", unit: "step", measure_unit: "sqft", sqft: null, quantity: 12 })).toBe(12);

    const rules = readFileSync(join(root, "src/lib/flooring-knowledge/rules.ts"), "utf8");
    expect(rules).toMatch(/not an automatic 8 sq ft\/step order/);
  });

  it("0218 does not turn a floor-map room into a carpet-tile warehouse cut", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0218_flooring_knowledge_tile_not_room_cut.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0218_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/warehouse cut/);
    expect(sql).toMatch(/order_as_roll/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/isRollGoodCategory\(cat\) \? null : rm\.lenIn/);
    expect(q).toMatch(/order_as_roll: isRollGoodCategory\(cat\) \? false/);
    const scope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(scope).toMatch(/order_as_roll === false && !measured\.length/);
  });

  it("0219 Builder treats exclusive carpet tile as modular coverage, not Cuts vs Roll", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0219_flooring_knowledge_builder_tile_coverage.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0219_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Cuts vs Roll/);
    expect(sql).toMatch(/measured coverage/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(
      carpetLineIsModularCoverage({
        category: "carpet",
        order_as_roll: false,
        sqft: 450,
        quantity: 50,
        length_in: null,
        width_in: null,
        measurements: null,
      }),
    ).toBe(true);
    expect(
      carpetLineIsModularCoverage({
        category: "carpet",
        order_as_roll: false,
        sqft: null,
        quantity: null,
      }),
    ).toBe(false);
    expect(
      carpetLineIsModularCoverage({
        category: "carpet",
        order_as_roll: true,
        sqft: 450,
        quantity: 50,
      }),
    ).toBe(false);
    expect(
      carpetLineIsModularCoverage({
        category: "carpet",
        order_as_roll: false,
        sqft: 450,
        measurements: [{ op: "add", length_in: 144, width_in: 168 }],
      }),
    ).toBe(false);
    expect(
      carpetLineIsModularCoverage({
        category: "vinyl",
        order_as_roll: false,
        sqft: 450,
      }),
    ).toBe(false);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/modularCarpet/);
    expect(builder).toMatch(/Carpet tile is modular/);
    expect(builder).toMatch(/isRollGoodCategory\(line\.category\) && !modularCarpet/);
    expect(builder).toMatch(/carpetSystems: \["carpet_tile"\]/);
    expect(builder).toMatch(/Switch to warehouse cut plan/);

    const rules = readFileSync(join(root, "src/lib/flooring-knowledge/rules.ts"), "utf8");
    expect(rules).toMatch(/Builder shows measured coverage and carton math, not Cuts vs Roll/);
  });

  it("0220 does not invent $6/yd or $2/ft install labor", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0220_flooring_knowledge_install_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0220_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/\$6\/yd/);
    expect(sql).toMatch(/install_yd/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/install_yd',\s*6/);

    expect(configuredInstallRate({ billing: "yd", config: { install_yd: 6 } })).toBe(6);
    expect(configuredInstallRate({ billing: "yd", config: {} })).toBe(0);
    expect(configuredInstallRate({ billing: "ft", config: {} })).toBe(0);
    expect(configuredInstallRate({ billing: "yd", config: { install_yd: 0 } })).toBe(0);
    expect(
      configuredInstallRate({ billing: "yd", config: { install_yd: 6 }, productLabor: 8 }),
    ).toBe(8);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/configuredInstallRate/);
    expect(q).not.toMatch(/install_yd \?\? 6/);
    expect(q).not.toMatch(/install_ft \?\? 2/);
    expect(q).not.toMatch(/: 6;/);
    expect(q).not.toMatch(/: 2;/);

    const form = readFileSync(
      join(root, "src/app/(app)/settings/estimate-questions/question-form.tsx"),
      "utf8",
    );
    expect(form).toMatch(/shop rate — do not invent/);
    expect(form).not.toMatch(/install_yd \?\? 6/);
    expect(form).not.toMatch(/install_ft \?\? 2/);

    const actions = readFileSync(
      join(root, "src/app/(app)/settings/estimate-questions/actions.ts"),
      "utf8",
    );
    expect(actions).toMatch(/optionalRate/);
    expect(actions).not.toMatch(/numOr\(formData\.get\("cfg_install_yd"\), 6\)/);
    expect(actions).not.toMatch(/numOr\(formData\.get\("cfg_install_ft"\), 2\)/);
  });

  it("0221 does not invent trim chip prices and keeps extra roll goods as order TBD", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0221_flooring_knowledge_trim_price.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0221_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/\$1\/lnft/);
    expect(sql).toMatch(/order TBD/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).not.toMatch(/cost: 2\.6/);
    expect(q).not.toMatch(/cost: 45/);
    expect(q).not.toMatch(/DEFAULT_RR_PER_LNFT/);
    expect(q).toMatch(/catalog or type — do not invent/);
    expect(q).toMatch(/rollGoodsTbdLine\(ex\.product/);
    expect(q).toMatch(/qty TBD \(\$\{countPhrase\} — not taped sq ft\)/);

    const rules = readFileSync(join(root, "src/lib/flooring-knowledge/rules.ts"), "utf8");
    expect(rules).toMatch(/Clicking a chip does not invent \$1\/lnft or \$45\/nose/);
  });

  it("0222 does not bill roll goods from taped sq ft without cuts", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0222_flooring_knowledge_roll_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0222_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/sq ft ÷ 9/);
    expect(sql).toMatch(/not billed as an order/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(
      rollGoodsLineHasCuts({
        line_type: "mat_labor",
        category: "carpet",
        sqft: 450,
      }),
    ).toBe(false);
    expect(
      lineQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 450,
        quantity: null,
      }),
    ).toBe(0);
    expect(
      lineQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 450,
        quantity: 50,
      }),
    ).toBe(50);
    expect(
      lineQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 360,
        length_in: 360,
        width_in: 144,
        quantity: null,
      }),
    ).toBe(40);

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/never sq ft ÷ 9/);
    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/measurements: l\.measurements\.filter\(rowHasDims\)\.map\(rowToMeasurement\)/);
  });

  it("0223 does not invent a 94\" stick when piece length is missing", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0223_flooring_knowledge_piece_length.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0223_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/do not invent 94/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(resolvedPieceLengthIn(null)).toBeNull();
    expect(resolvedPieceLengthIn(0)).toBeNull();
    expect(resolvedPieceLengthIn(94)).toBe(94);
    expect(piecesForLinearFeet(24, null)).toBe(0);
    expect(piecesForLinearFeet(24, 94)).toBe(4);
    expect(accessoryQuantity({ linearFeet: 24, unit: "each" })).toBe(0);
    expect(accessoryQuantity({ linearFeet: 24, unit: "each", pieceLengthIn: 94 })).toBe(4);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/resolvedPieceLengthIn/);
    expect(q).toMatch(/showTrimLinearFt/);
    expect(q).toMatch(/is typical but not assumed/);
    expect(q).not.toMatch(/row\.product\.pieceLengthIn \|\| DEFAULT_PIECE_LENGTH_IN/);
    expect(q).not.toMatch(/numOr0\(input\.piece_length_in\) \|\| DEFAULT_PIECE_LENGTH_IN/);

    const catalog = readFileSync(join(root, "src/app/(app)/catalog/actions.ts"), "utf8");
    expect(catalog).not.toMatch(/DEFAULT_PIECE_LENGTH_IN/);
    expect(catalog).toMatch(/numOrNull\(input\.piece_length_in\)/);

    const engine = readFileSync(join(root, "src/lib/accessory-engine.ts"), "utf8");
    expect(engine).not.toMatch(/DEFAULT_PIECE_LENGTH_IN/);
    expect(engine).toMatch(/resolvedPieceLengthIn/);

    const picker = readFileSync(join(root, "src/app/(app)/estimates/product-picker.tsx"), "utf8");
    expect(picker).toMatch(/blank leaves TBD/);
    expect(picker).not.toMatch(/blank uses/);

    const rules = readFileSync(join(root, "src/lib/flooring-knowledge/rules.ts"), "utf8");
    expect(rules).toMatch(/we do not invent 94/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/Installed \/\$\{lineDisplayUnit\(line\)\}/);
    expect(builder).not.toMatch(/Installed \/\$\{line\.measure_unit === "sqyd"/);
  });

  it("0224 catalog conversion uses the printed billing unit, not leftover measure_unit", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0224_flooring_knowledge_billing_unit.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0224_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Leftover measure_unit is not the billing unit/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/catalogUnitFactor\(d\.unit, lineUnitKey\(l\) === "sqyd"\)/);
    expect(builder).toMatch(/measureUnit: lineUnitKey\(l\) === "sqyd" \? "sqyd" : "sqft"/);
    expect(builder).not.toMatch(/catalogFactor\(l\.measure_unit/);

    const catalog = readFileSync(join(root, "src/app/(app)/catalog/actions.ts"), "utf8");
    expect(catalog).toMatch(/catalogUnitFactor\(p\.unit/);
    expect(catalog).not.toMatch(/input\.measureUnit === catUnit/);

    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/areaDerivedMaterialQty/);
    expect(ai).toMatch(/materialWastePctForEmit/);
    expect(ai).not.toMatch(/Math\.ceil\(sqft \* \(1 \+ profile\.waste/);
    expect(ai).toMatch(/sqft: round2\(sqft\)/);
  });

  it("0225 does not invent 6/8 sq ft of carpet per stair or $15/bag self-level labor", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0225_flooring_knowledge_stair_allowance.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0225_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/do not invent 6\/8 sq ft/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(stairsCarpet(13, "waterfall").sqft).toBe(0);
    expect(stairsCarpet(13, "upholstered").sqft).toBe(0);
    expect(stairsCarpet(13, "waterfall", 6).sqft).toBe(78);
    expect(stairsCarpet(13, "upholstered", 8).sqft).toBe(104);

    const calc = readFileSync(join(root, "src/lib/questionnaire-calc.ts"), "utf8");
    expect(calc).not.toMatch(/waterfall: 6/);
    expect(calc).not.toMatch(/STAIR_ALLOWANCE_SQFT/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/We do not invent yardage from step count/);
    expect(q).not.toMatch(/Needs ≈/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).not.toMatch(/DEFAULT_LABOR_PER_BAG/);
    expect(builder).not.toMatch(/DEFAULT_LABOR_PER_SQFT/);
    expect(builder).toMatch(/enter the shop rate/);

    const rules = readFileSync(join(root, "src/lib/flooring-knowledge/rules.ts"), "utf8");
    expect(rules).toMatch(/We do not invent 6\/8 sq ft of carpet per step as an order/);
  });

  it("0226 does not invent a 1/4 inch self-level pour or count qty from taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0226_flooring_knowledge_selflevel_pour.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0226_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/do not invent 1\/4/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(selfLevelPourThicknessIn(undefined)).toBe(0);
    expect(selfLevelPourThicknessIn({})).toBe(0);
    expect(selfLevelPourThicknessIn({ coverage_thickness_in: 0.125 })).toBe(0.125);
    expect(selfLevelPourThicknessIn({ default_thickness_in: 0.25, coverage_thickness_in: 0.125 })).toBe(0.25);
    expect(selfLevelPourThicknessIn({ default_thickness_in: 0.25 }, 0.375)).toBe(0.375);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/selfLevelPourThicknessIn/);
    expect(q).not.toMatch(/default_thickness_in \?\? 0\.25/);
    expect(q).toMatch(/We do not invent 1\/4/);

    const emit = readFileSync(join(root, "src/lib/questionnaire-emit.ts"), "utf8");
    expect(emit).toMatch(/Taped square feet is not gallons/);
  });

  it("0227 does not invent a 4×8 (32 sq ft) subfloor sheet", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0227_flooring_knowledge_subfloor_sheet.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0227_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/4×8/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(resolvedSheetSqft(undefined)).toBeNull();
    expect(resolvedSheetSqft(0)).toBeNull();
    expect(resolvedSheetSqft(32)).toBe(32);
    expect(subfloorSheets(200)).toBe(0);
    expect(subfloorSheets(200, 32)).toBe(7);
    expect(subfloorSheets(320, 32)).toBe(10);

    const calc = readFileSync(join(root, "src/lib/questionnaire-calc.ts"), "utf8");
    expect(calc).toMatch(/TYPICAL_SUBFLOOR_SHEET_SQFT/);
    expect(calc).not.toMatch(/sheetSqft > 0 \? num\(sheetSqft\) : 32/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/resolvedSheetSqft/);
    expect(q).not.toMatch(/sheet_sqft \?\? 32/);
    expect(q).toMatch(/We do not invent a 4×8/);
  });

  it("0228 customer order cuts do not invent a 12' width or sq yd unit", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0228_flooring_knowledge_order_cut_width.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0228_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/12'/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(cutLabel({ width_ft: 12, length_ft: 14, length_in: 6 })).toMatch(/12'/);
    expect(cutLabel({ width_ft: 0, length_ft: 14, length_in: 6 })).toMatch(/width TBD/);
    expect(cutSqYd({ width_ft: 12, length_ft: 30, length_in: 0 })).toBe(40);
    expect(cutSqYd({ width_ft: 0, length_ft: 30, length_in: 0 })).toBeNull();

    const form = readFileSync(join(root, "src/components/order-form.tsx"), "utf8");
    expect(form).toMatch(/width: ""/);
    expect(form).not.toMatch(/width: "12"/);
    expect(form).toMatch(/unit: ""/);
    expect(form).not.toMatch(/unit: "sq yd"/);
    expect(form).toMatch(/Width TBD/);

    const actions = readFileSync(join(root, "src/app/order/actions.ts"), "utf8");
    expect(actions).not.toMatch(/\|\| "sq yd"/);
    expect(actions).toMatch(/length_ft > 0 \|\| c\.length_in > 0/);
  });

  it("0229 does not treat taped sq ft as bags of thinset or invent sq yd on order products", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0229_flooring_knowledge_tile_bags.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0229_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/bags of thinset/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const tile = profileFor("tile")!;
    const thinset = tile.companions.find((c) => c.key === "thinset")!;
    const grout = tile.companions.find((c) => c.key === "grout")!;
    expect(thinset.defaultOn).toBe(false);
    expect(grout.defaultOn).toBe(false);
    expect(thinset.unit).toBe("bag");
    expect(grout.unit).toBe("bag");
    expect(companionQty(thinset, 500, 0)).toBe(0);
    expect(companionQty(grout, 500, 0)).toBe(0);

    const pad = profileFor("carpet")!.companions.find((c) => c.key === "pad")!;
    expect(companionQty(pad, 450, 0)).toBe(60);

    expect(profileFor("hardwood")!.companions.find((c) => c.key === "underlayment")!.defaultOn).toBe(false);
    expect(profileFor("laminate")!.companions.find((c) => c.key === "underlayment")!.defaultOn).toBe(false);

    const orders = readFileSync(join(root, "src/lib/data/orders.ts"), "utf8");
    expect(orders).not.toMatch(/\|\| "sq yd"/);

    const inv = readFileSync(join(root, "src/app/(app)/inventory/[id]/page.tsx"), "utf8");
    expect(inv).not.toMatch(/placeholder="12"/);
  });

  it("0230 does not invent a count of 1 or sq yd on unknown roll units", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0230_flooring_knowledge_count_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0230_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/quantity of 1/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const lvp = profileFor("lvp")!;
    const transitions = lvp.companions.find((c) => c.key === "transitions")!;
    expect(transitions.sizeBy).toBe("each");
    expect(companionQty(transitions, 500, 80)).toBe(0);

    expect(rollReceiveUnit("sq yd")).toBe("sqyd");
    expect(rollReceiveUnit("SY")).toBe("sqyd");
    expect(rollReceiveUnit("lnft")).toBe("lnft");
    expect(rollReceiveUnit("lf")).toBe("lnft");
    expect(rollReceiveUnit("")).toBe("");
    expect(rollReceiveUnit(null)).toBe("");
    expect(rollReceiveUnit("bag")).toBe("");

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).not.toMatch(/quantity: prepLine \? l\.quantity : "1"/);
    expect(builder).not.toMatch(/l\.quantity \|\| "1"/);
    expect(builder).toMatch(/never invent a count of 1/);
    expect(builder).not.toMatch(/quantity: confirm \? "" : "1"/);
    expect(builder).toMatch(/description: confirm \? "Subfloor/);
    expect(builder).toMatch(/quantity: "",\n    \};/);

    const invPage = readFileSync(join(root, "src/app/(app)/inventory/[id]/page.tsx"), "utf8");
    expect(invPage).toMatch(/Unit TBD/);
    expect(invPage).toMatch(/rollReceiveUnit/);
    expect(invPage).not.toMatch(/: "sqyd"\}/);

    const invActions = readFileSync(join(root, "src/app/(app)/inventory/actions.ts"), "utf8");
    expect(invActions).not.toMatch(/\|\| "sqyd"/);
    expect(invActions).toMatch(/if \(!unit\) return/);

    const rolls = readFileSync(join(root, "src/lib/data/stock-rolls.ts"), "utf8");
    expect(rolls).not.toMatch(/\|\| "sqyd"/);
  });

  it("0231 does not plant a 12-foot or 6-foot cut width before the salesperson enters one", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0231_flooring_knowledge_cut_width_init.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0231_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not plant 12/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(defaultCutWidthFt({ family: "carpet" })).toBe(0);
    expect(defaultCutWidthFt({ family: "vinyl" })).toBe(0);
    expect(defaultCutWidthFt({ family: "carpet", productWidthFt: 13.5 })).toBe(13.5);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/newCarpetGroup\(""\)/);
    expect(q).not.toMatch(/String\(chipW\)/);
    expect(q).toMatch(/placeholder="Width TBD"/);
    expect(q).not.toMatch(/placeholder=\{defaultWidthFor/);

    const qty = readFileSync(join(root, "src/lib/flooring-knowledge/quantities.ts"), "utf8");
    expect(qty).toMatch(/return 0;/);
    expect(qty).not.toMatch(/cutWidthChoicesFt\(\{ \.\.\.opts, productWidthFt: null \}\)\[0\]/);
  });

  it("0232 does not invent a count of 1 on per=each without Amount, or label unknown units as each", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0232_flooring_knowledge_each_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0232_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/do not invent 1/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const emit = readFileSync(join(root, "src/lib/questionnaire-emit.ts"), "utf8");
    expect(emit).toMatch(/per === "flat"/);
    expect(emit).toMatch(/Do not invent 1 T-mold/);
    expect(emit).not.toMatch(/else \{\s*qty = 1;/);

    const units = readFileSync(join(root, "src/lib/units.ts"), "utf8");
    expect(units).toMatch(/unknown, not square feet and not "each"/);
    expect(units).toMatch(/return "";/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/countUnitForTbd/);
    expect(q).toMatch(/unit TBD/);
    expect(q).not.toMatch(/\|\| "each"/);
  });

  it("0233 does not invent each as the default unit when metadata is missing", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0233_flooring_knowledge_unit_tbd.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0233_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/unit TBD/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(formatBillingQty(2, "each")).toBe("2 each");
    expect(formatBillingQty(12, "lnft")).toBe("12 lnft");
    expect(formatBillingQty(3, "")).toBe("3 (unit TBD)");

    const emit = readFileSync(join(root, "src/lib/questionnaire-emit.ts"), "utf8");
    expect(emit).not.toMatch(/\|\| "each"/);

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).not.toMatch(/\|\| "each"/);

    const inv = readFileSync(join(root, "src/app/(app)/inventory/actions.ts"), "utf8");
    expect(inv).not.toMatch(/\|\| "each"/);

    const qty = readFileSync(join(root, "src/lib/flooring-knowledge/quantities.ts"), "utf8");
    expect(qty).toMatch(/unit TBD/);
    expect(qty).not.toMatch(/\|\| "each"/);
  });

  it("0234 review always labels measured, waste, order, billing, and unit of measure", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0234_flooring_knowledge_review_units.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0234_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/UNIT of measure/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const lvp = computeMaterialTakeoff({ family: "lvp", measuredSqft: 500, wastePct: 10 });
    const lvpRows = takeoffConceptRows(lvp);
    expect(lvpRows.map((r) => r.label)).toEqual(
      expect.arrayContaining([
        "Measured area",
        "Waste",
        "Order quantity",
        "Billing quantity",
        "Unit of measure",
      ]),
    );
    expect(lvpRows.find((r) => r.label === "Measured area")?.value).toBe("500 sq ft");
    expect(lvpRows.find((r) => r.label === "Waste")?.value).toBe("10% (50 sq ft)");
    expect(lvpRows.find((r) => r.label === "Order quantity")?.value).toBe("550 sq ft");
    expect(lvpRows.find((r) => r.label === "Billing quantity")?.value).toBe(
      "550 sq ft (this number is square feet, not yards)",
    );
    expect(lvpRows.find((r) => r.label === "Unit of measure")?.value).toMatch(/^sq ft/);
    expect(lvpRows.find((r) => r.label === "Billing quantity")?.value).not.toMatch(/sq yd/);
    expect(takeoffUnitKeyLabel("sqft")).toBe("sq ft");
    expect(formatTakeoffStrip(lvp)).toMatch(/Billing 550 sq ft \(this number is square feet, not yards\)/);
    expect(formatTakeoffStrip(lvp)).toMatch(/Unit sq ft/);
    expect(formatTakeoffStrip(lvp)).not.toMatch(/sq yd/);

    const review = buildSalespersonReview({
      rooms: [],
      products: ["LVP sample"],
      takeoffs: [lvp],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [],
      prep: [],
      accessories: [],
      specials: [],
    });
    const takeoffSec = review.sections.find((s) => s.id === "takeoff-0");
    expect(takeoffSec?.rows.find((r) => r.label === "Billing quantity")?.value).toMatch(/550 sq ft/);
    expect(takeoffSec?.rows.find((r) => r.label === "Unit of measure")?.value).toMatch(/sq ft/);

    const carpet = computeMaterialTakeoff({ family: "carpet", measuredSqft: 450 });
    const carpetRows = takeoffConceptRows(carpet);
    expect(carpetRows.find((r) => r.label === "Order quantity")?.value).toMatch(/TBD — enter cuts/);
    expect(carpetRows.find((r) => r.label === "Billing quantity")?.value).toBe(
      "TBD — not 50 sq yd from taped area",
    );
    expect(carpetRows.find((r) => r.label === "Unit of measure")?.value).toMatch(/^sq yd/);
    expect(carpet.billingQty).toBe(0);

    const cuts = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 450,
      cutsSqft: 540,
    });
    const cutRows = takeoffConceptRows(cuts);
    expect(cutRows.find((r) => r.label === "Waste")?.value).toMatch(/already in the entered cuts/);
    expect(cutRows.find((r) => r.label === "Billing quantity")?.value).toBe(
      "60 sq yd (this number is yards, not square feet)",
    );

    const qty = readFileSync(join(root, "src/lib/flooring-knowledge/quantities.ts"), "utf8");
    expect(qty).toMatch(/takeoffConceptRows/);
    expect(qty).toMatch(/UNIT/);
    const takeoffSrc = readFileSync(join(root, "src/lib/flooring-knowledge/takeoff.ts"), "utf8");
    expect(takeoffSrc).toMatch(/takeoffConceptRows/);
    expect(takeoffSrc).not.toMatch(/if \(t\.billingUnit === "sqyd"\)/);
  });

  it("0235 does not plant square feet on a picked SKU that forgot its unit", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0235_flooring_knowledge_product_unit.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0235_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not plant sq ft/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(pickedProductUnit("gal", "other")).toBe("gal");
    expect(pickedProductUnit("", "carpet")).toBe("sqyd");
    expect(pickedProductUnit("", "vinyl")).toBe("sqyd");
    expect(pickedProductUnit("", "lvp")).toBe("sqft");
    expect(pickedProductUnit("", "hardwood")).toBe("sqft");
    expect(pickedProductUnit("", "other")).toBe("");
    expect(pickedProductUnit("", "trim")).toBe("");
    expect(pickedProductUnit("", "underlayment")).toBe("");
    expect(pickedProductUnit("SY", "carpet")).toBe("SY");

    expect(areaDerivedMaterialAllowed("other")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "sqyd")).toBe(true);
    expect(areaDerivedMaterialAllowed("lvp", "")).toBe(true);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/pickedProductUnit/);
    expect(q).not.toMatch(/\|\| "sqft"/);

    const catalog = readFileSync(join(root, "src/app/(app)/catalog/actions.ts"), "utf8");
    expect(catalog).toMatch(/pickedProductUnit/);
    expect(catalog).not.toMatch(/\|\| "sqft"/);

    const po = readFileSync(join(root, "src/app/(app)/purchase-orders/po-builder.tsx"), "utf8");
    expect(po).toMatch(/pickedProductUnit/);
    expect(po).not.toMatch(/\|\| "sqft"/);
    expect(po).not.toMatch(/\|\| "lnft"/);
  });

  it("0236 does not plant sq ft on Other, or LVP on a carpet floor-map fill", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0236_flooring_knowledge_other_unit.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0236_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not plant LVP/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(defaultUnitForCategory("other")).toBe("");
    expect(defaultUnitForCategory("lvp")).toBe("sqft");
    expect(defaultUnitForCategory("carpet")).toBe("sqyd");

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/mapDefaultCategory/);
    expect(q).toMatch(/defaultCategory=\{mapDefaultCategory\}/);

    const picker = readFileSync(join(root, "src/app/(app)/estimates/product-picker.tsx"), "utf8");
    expect(picker).toMatch(/Unit TBD — pick how this is sold/);
    expect(picker).toMatch(/do not plant sq ft on Other/);

    const units = readFileSync(join(root, "src/lib/units.ts"), "utf8");
    expect(units).toMatch(/Planting sq ft here is how a pail/);
    expect(units).not.toMatch(/default:\s*return "sqft";/);
  });

  it("0237 hard-surface stair wrap follows the job family, not planted LVP", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0237_flooring_knowledge_stair_wrap.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0237_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not plant LVP/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/hsWrapCat/);
    expect(q).toMatch(/isHardSurfaceFamily/);
    expect(q).toMatch(/defaultCategory=\{hsWrapCat\}/);
    expect(q).not.toMatch(/defaultCategory="lvp"/);

    const picker = readFileSync(join(root, "src/app/(app)/estimates/product-picker.tsx"), "utf8");
    expect(picker).toMatch(/initialCategory \|\| ""/);
    expect(picker).not.toMatch(/initialCategory \|\| "lvp"/);
  });

  it("0238 mixed jobs do not clone whole-job sq ft onto every family", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0238_flooring_knowledge_mixed_takeoff.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0238_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/per family/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/measuredSqftForFamilyTakeoff/);
    expect(q).toMatch(/groupMeasuredSqftByFamily/);
    expect(q).toMatch(/flooring.length === 1/);
  });

  it("0239 mixed Builder emit does not clone whole-job sq ft onto every family", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0239_flooring_knowledge_mixed_emit.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0239_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/per family/);
    expect(sql).toMatch(/Builder lines/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/measuredSqftForQuestionCover/);
    expect(q).toMatch(/floorMapOwnsFlooring/);
    expect(q).toMatch(/roomsForPrepTakeoff/);
    expect(q).toMatch(/questionCoverSf/);
  });

  it("0240 mixed prep labor does not clone whole-job sq ft onto carpet rooms", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0240_flooring_knowledge_prep_area.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0240_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/prep labor/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/emitAreaSqftForQuestion/);
    expect(q).toMatch(/questionPurpose/);
  });

  it("0241 per-room prep uses this room's sq ft, not the whole mixed job", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0241_flooring_knowledge_room_prep.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0241_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/By-room/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/not the whole mixed job/);
    expect(q).toMatch(/roomFamilySqft/);
    expect(q).toMatch(/totalSqft=\{sf\}/);
  });

  it("0242 mixed jobs without a floor-map assignment warn instead of cloning sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0242_flooring_knowledge_mixed_unassigned.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0242_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Unassigned rooms stay off the takeoff/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/unassignedRoomSqft/);
    expect(q).toMatch(/byFamily: floorMapAssignments.byFamily/);
    expect(q).toMatch(/mixed-unassigned|mixedUnassigned/);
  });

  it("0243 adhesive TBD in Builder is How many / Unit TBD, not Sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0243_flooring_knowledge_adhesive_qty_ui.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0243_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/How many \/ Unit TBD/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/isCountPricedLine/);
    expect(builder).toMatch(/category === "other"/);
    expect(builder).toMatch(/Unit TBD/);
    expect(builder).toMatch(/unitValue \?\? ""\)\.trim\(\)/);
    expect(knowledgeHelpFor({ key: "adhesive" }, emptyInstallContext())).toMatch(/Unit TBD/);
  });

  it("0244 pad TBD and lineQty share isCountPricedLine — leftover sqft is not an order", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0244_flooring_knowledge_pad_count_ui.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0244_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/How many \/ Unit TBD/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/isCountPricedLine\(line\)/);
    expect(calc).not.toMatch(/const countUnit = line\.unit != null && line\.unit !== ""/);

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/category === "underlayment"/);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(/Unit TBD/);
  });

  it("0245 hardwood prefinished vs unfinished is SCOPE, not invented sand/finish labor", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0245_flooring_knowledge_hardwood_finish.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0245_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/hardwood_finish/);
    expect(sql).toMatch(/Unfinished \(site finish\)/);
    expect(sql).toMatch(/Prefinished/);
    expect(sql).toMatch(/Field verify \/ TBD/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/insert into public\.products/);

    expect(knowledgeQuestionByKey("hardwood_finish")?.purpose).toBe("SCOPE");
    expect(knowledgeQuestionByKey("hardwood_finish")?.phase).toBe("product");
    expect(knowledgeQuestionByKey("hardwood_finish")?.families).toEqual(["hardwood"]);
    expect(reviewBucketForQuestion({ key: "hardwood_finish", label: "Prefinished or unfinished?" })).toBe(
      "specials",
    );
    expect(knowledgeHelpFor({ key: "hardwood_finish" }, emptyInstallContext())).toMatch(
      /sand\/finish/,
    );

    const solid = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
    });
    expect(solid).toContain("hardwood_finish");
    expect(solid).toContain("hardwood_fasteners");

    const engineered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Glue-down"],
    });
    expect(engineered).toContain("hardwood_finish");
    expect(engineered).toContain("adhesive");
    expect(engineered).not.toContain("hardwood_fasteners");

    const lam = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(lam).not.toContain("hardwood_finish");

    // Overlay stays open on unanswered HS (surfacePending). SQL show_if hides
    // until Hardwood / Engineered hardwood is picked.
    expect(
      visibleKnowledgeKeys({ project_type: ["Hard surface"] }),
    ).toContain("hardwood_finish");
    expect(walk({ project_type: ["Hard surface"] })).not.toContain("hardwood_finish");
    expect(
      walk({ project_type: ["Hard surface"], surface_type: ["Hardwood"] }),
    ).toContain("hardwood_finish");

    const unfinished = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
      }),
      { pickedLabels: ["Unfinished (site finish)"] },
    );
    expect(unfinished.some((w) => w.id === "hardwood-unfinished")).toBe(true);
    expect(unfinished.find((w) => w.id === "hardwood-unfinished")?.text).toMatch(/Do not invent/);

    const prefinished = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
      }),
      { pickedLabels: ["Prefinished"] },
    );
    expect(prefinished.some((w) => w.id === "hardwood-unfinished")).toBe(false);

    const unknown = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
      }),
      { pickedLabels: ["Unknown"] },
    );
    expect(unknown.some((w) => w.id === "hardwood-unfinished")).toBe(false);

    const shoe = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Laminate"],
      }),
      { pickedLabels: ["Shoe (unfinished)"] },
    );
    expect(shoe.some((w) => w.id === "hardwood-unfinished")).toBe(false);
  });

  it("0246 stair wrap TBD is How many / Unit TBD, never taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0246_flooring_knowledge_stair_wrap_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0246_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/wrap qty TBD/);
    expect(sql).toMatch(/How many \/ Unit TBD/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/lineIsStairWrapTbd/);
    expect(calc).toMatch(/if \(lineIsStairWrapTbd\(line\)\) return num\(line\.quantity\)/);

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/lineIsStairWrapTbd/);
    expect(builder).toMatch(/flooringAreaUi/);
    expect(builder).toMatch(/description: l\.description/);
    expect(builder).toMatch(/description: line\.description/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);

    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /How many \/ Unit TBD/,
    );
    expect(lineQty({
      line_type: "mat_labor",
      category: "hardwood",
      unit: "sqft",
      sqft: 104,
      quantity: null,
      description: "Oak wrap — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
    })).toBe(0);
  });

  it("0247 new construction hides tear-out; unanswered keeps demo visible", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0247_flooring_knowledge_work_type.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0247_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/work_type/);
    expect(sql).toMatch(/New construction/);
    expect(sql).toMatch(/Replacement \(tear-out\)/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(knowledgeQuestionByKey("work_type")?.purpose).toBe("SCOPE");
    expect(knowledgeQuestionByKey("work_type")?.phase).toBe("existing");
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(/New construction hides tear-out/);

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(unanswered).toContain("work_type");
    expect(unanswered).toContain("hs_demo");
    expect(unanswered).toContain("existing_pad");

    const replacement = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["Replacement (tear-out)"],
    });
    expect(replacement).toContain("hs_demo");
    expect(replacement).toContain("existing_pad");
    expect(replacement).toContain("demo_disposal");

    const neu = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["New construction"],
    });
    expect(neu).toContain("work_type");
    expect(neu).toContain("substrate");
    expect(neu).toContain("hs_prep");
    expect(neu).not.toContain("hs_demo");
    expect(neu).not.toContain("existing_pad");
    expect(neu).not.toContain("existing_tack");
    expect(neu).not.toContain("demo_disposal");
    expect(neu).not.toContain("asbestos_risk");
    expect(neu).not.toContain("existing_bond");
    expect(neu).not.toContain("vinyl_skim");

    const walkNew = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      work_type: ["New construction"],
    });
    expect(walkNew).toContain("work_type");
    expect(walkNew).toContain("substrate");
    expect(walkNew).not.toContain("hs_demo");
    expect(walkNew).not.toContain("demo_disposal");
    expect(walkNew).not.toContain("existing_bond");
    expect(walkNew.indexOf("work_type")).toBeLessThan(walkNew.indexOf("substrate")!);

    const walkReplace = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["Replacement (tear-out)"],
      hs_demo: ["Carpet"],
    });
    expect(walkReplace).toContain("hs_demo");
    expect(walkReplace).toContain("demo_disposal");
    expect(walkReplace).toContain("existing_pad");

    const warn = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT"],
        work_type: ["New construction"],
      }),
    );
    expect(warn.some((w) => w.id === "new-construction")).toBe(true);
    expect(warn.find((w) => w.id === "new-construction")?.text).toMatch(/Do not invent a demo charge/);

    const ceramicNew = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
        work_type: ["New construction"],
        hs_demo: ["Ceramic WITH mortar bed"],
      }),
    );
    expect(ceramicNew.some((w) => w.id === "mortar")).toBe(false);
    expect(ceramicNew.some((w) => w.id === "new-construction")).toBe(true);
  });

  it("0248 wet area warns without inventing a waterproof SKU", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0248_flooring_knowledge_wet_area.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0248_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/wet_area/);
    expect(sql).toMatch(/Yes — bath \/ laundry \/ mudroom/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(knowledgeQuestionByKey("wet_area")?.purpose).toBe("WARNING");
    expect(knowledgeQuestionByKey("wet_area")?.phase).toBe("details");
    expect(knowledgeHelpFor({ key: "wet_area" }, emptyInstallContext())).toMatch(
      /no waterproof column/,
    );

    const keys = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(keys).toContain("wet_area");
    expect(keys).toContain("toilets");

    const dry = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      wet_area: ["No"],
    });
    expect(dry).toContain("wet_area");

    const lvpWet = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT"],
      }),
      { pickedLabels: ["Yes — bath / laundry / mudroom"] },
    );
    expect(lvpWet.some((w) => w.id === "wet-area")).toBe(true);
    expect(lvpWet.find((w) => w.id === "wet-area")?.text).toMatch(/Do not invent/);
    expect(lvpWet.some((w) => w.id === "wet-area-hardwood")).toBe(false);
    expect(lvpWet.some((w) => w.id === "wet-area-carpet")).toBe(false);

    const oakWet = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
      }),
      { pickedLabels: ["Some rooms"] },
    );
    expect(oakWet.some((w) => w.id === "wet-area")).toBe(true);
    expect(oakWet.some((w) => w.id === "wet-area-hardwood")).toBe(true);

    const carpetWet = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
      }),
      { pickedLabels: ["Yes — bath / laundry / mudroom"] },
    );
    expect(carpetWet.some((w) => w.id === "wet-area-carpet")).toBe(true);

    const unknown = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT"],
      }),
      { pickedLabels: ["Unknown / field verify"] },
    );
    expect(unknown.some((w) => w.id === "wet-area")).toBe(false);
  });

  it("0249 Builder roll goods without cuts label leftover sq ft as measured, not the order", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0249_flooring_knowledge_builder_measured.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0249_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Measured sq ft \(not the order\)/);
    expect(sql).toMatch(/Sq ft ÷ 9/);
    expect(sql).toMatch(/do not invent a 12-foot width/i);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(
      builderAreaFallbackLabel({ isRollGood: true, hasWarehouseCuts: false }),
    ).toBe("Measured sq ft (not the order)");
    expect(
      builderAreaFallbackLabel({ isRollGood: true, hasWarehouseCuts: true }),
    ).toBe("Or enter sq ft directly");
    expect(
      builderAreaFallbackLabel({ isRollGood: false, hasWarehouseCuts: false }),
    ).toBe("Or enter sq ft directly");
    expect(ROLL_GOODS_CUTS_MISSING_CAPTION).toMatch(/Order TBD until you enter warehouse cuts/);
    expect(ROLL_GOODS_CUTS_MISSING_CAPTION).toMatch(/Sq ft ÷ 9 is equivalent area, not a cut plan/);

    expect(
      rollGoodsLineHasCuts({
        line_type: "mat_labor",
        category: "carpet",
        sqft: 450,
      }),
    ).toBe(false);
    expect(
      lineQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 450,
        quantity: null,
      }),
    ).toBe(0);

    expect(
      knowledgeHelpFor({ kind: "cuts" }, emptyInstallContext()),
    ).toMatch(/Builder warehouse cuts show Order TBD/);
    expect(
      knowledgeHelpFor({ kind: "cuts", config: { category: "vinyl" } }, emptyInstallContext()),
    ).toMatch(/Builder warehouse cuts show Order TBD/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderAreaFallbackLabel/);
    expect(builder).toMatch(/ROLL_GOODS_CUTS_MISSING_CAPTION/);
    expect(builder).toMatch(/rollGoodsLineHasCuts/);
    expect(builder).not.toMatch(/label="Or enter sq ft directly"/);
    expect(builder).not.toMatch(/width_ft: 12/);

    const qty = readFileSync(join(root, "src/lib/flooring-knowledge/quantities.ts"), "utf8");
    expect(qty).toMatch(/Measured sq ft \(not the order\)/);
    expect(qty).toMatch(/Sq ft ÷ 9 is equivalent area, not a cut plan/);
  });

  it("0250 existing pad follows tearing out carpet, not only a new-carpet job", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0250_flooring_knowledge_existing_pad.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0250_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/existing_pad/);
    expect(sql).toMatch(/hs_demo/);
    expect(sql).toMatch(/position = 271/);
    expect(sql).toMatch(/Do not invent a second demo rate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(knowledgeQuestionByKey("existing_pad")?.any).toEqual([
      { families: ["carpet"] },
      { demo: ["Carpet"] },
    ]);
    expect(knowledgeHelpFor({ key: "existing_pad" }, emptyInstallContext())).toMatch(
      /follows the existing floor/,
    );

    const carpet = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(carpet).toContain("existing_pad");
    expect(carpet).toContain("hs_demo");
    expect(carpet.indexOf("hs_demo")).toBeLessThan(carpet.indexOf("existing_pad")!);

    const lvpQuiet = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(lvpQuiet).toContain("hs_demo");
    expect(lvpQuiet).not.toContain("existing_pad");

    const lvpOverCarpet = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      hs_demo: ["Carpet"],
    });
    expect(lvpOverCarpet).toContain("existing_pad");
    expect(lvpOverCarpet).toContain("demo_disposal");
    expect(lvpOverCarpet.indexOf("hs_demo")).toBeLessThan(lvpOverCarpet.indexOf("existing_pad")!);

    const tileOverTile = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      hs_demo: ["Ceramic WITHOUT mortar bed"],
    });
    expect(tileOverTile).not.toContain("existing_pad");

    const overlayLvp = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      hs_demo: ["Carpet"],
    });
    expect(overlayLvp).toContain("existing_pad");

    const neu = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      work_type: ["New construction"],
      hs_demo: ["Carpet"],
    });
    expect(neu).not.toContain("existing_pad");
    expect(neu).not.toContain("existing_tack");
    expect(neu).not.toContain("hs_demo");
  });

  it("0251 Builder warehouse cuts panel is the order, not measured 0 sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0251_flooring_knowledge_cuts_panel.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0251_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Order TBD — not measured sq ft/);
    expect(sql).toMatch(/warehouse cuts/);
    expect(sql).toMatch(/do not plant 12/i);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(ROLL_GOODS_CUTS_HEADER).toBe("Warehouse cuts (the order)");
    expect(lineMeasurementsRollTotalLabel(0)).toBe("Order TBD — not measured sq ft");
    expect(lineMeasurementsRollTotalLabel(450)).toBe("450 sq ft · 50 sq yd from cuts (the order)");
    expect(ROLL_GOODS_CUTS_EMPTY_HINT).toMatch(/we do not plant 12/);
    expect(ROLL_GOODS_CUTS_EMPTY_HINT).toMatch(/Measured room sq ft is not a cut plan/);

    expect(
      knowledgeHelpFor({ kind: "cuts" }, emptyInstallContext()),
    ).toMatch(/Builder warehouse cuts show Order TBD/);

    const panel = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(panel).toMatch(/ROLL_GOODS_CUTS_HEADER/);
    expect(panel).toMatch(/lineMeasurementsRollTotalLabel/);
    expect(panel).not.toMatch(/Cuts & areas/);
    expect(panel).not.toMatch(/They add up to the sq yd/);
    expect(panel).not.toMatch(/width_ft: 12/);
  });

  it("0252 Guided Estimate cut totals are Order TBD when width × length is missing", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0252_flooring_knowledge_cuts_tbd.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0252_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/0 sq yd is not an order/);
    expect(sql).toMatch(/Order TBD/);
    expect(sql).toMatch(/do not plant 12/i);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(questionnaireCutGroupOrderLabel(0)).toBe(
      "Order TBD — width × length, not measured sq ft ÷ 9",
    );
    expect(questionnaireCutGroupOrderLabel(40)).toBe("40 sq yd to order (from cuts)");
    expect(questionnaireCutsGrandOrderLabel(0, "Carpet")).toEqual({
      title: "Carpet order TBD",
      note: "Enter cuts (width × length). Sq ft ÷ 9 is not an order.",
    });
    expect(questionnaireCutsGrandOrderLabel(60, "Sheet vinyl")).toEqual({
      title: "Sheet vinyl to order: 60 sq yd",
      note: "from cuts — not measured sq ft ÷ 9",
    });

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/questionnaireCutGroupOrderLabel/);
    expect(q).toMatch(/questionnaireCutsGrandOrderLabel/);
    expect(q).not.toMatch(/\{rollNounCap\} to order: <span className="tabular-nums">\{r2\(grandY\)\}<\/span> sq yd/);
    expect(q).not.toMatch(/width_ft: 12/);
  });

  it("0253 existing tack strip follows tearing out carpet, not only new stretch-in", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0253_flooring_knowledge_existing_tack.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0253_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/existing_tack/);
    expect(sql).toMatch(/Do not invent a linear-foot price/);
    expect(sql).toMatch(/Keep \(unusual\)/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(knowledgeQuestionByKey("existing_tack")?.any).toEqual([
      { families: ["carpet"] },
      { demo: ["Carpet"] },
    ]);
    expect(knowledgeQuestionByKey("existing_tack")?.purpose).toBe("LABOR");
    expect(knowledgeHelpFor({ key: "existing_tack" }, emptyInstallContext())).toMatch(
      /not new stretch-in tack strip/,
    );

    const carpet = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(carpet).toContain("existing_tack");
    expect(carpet).toContain("tack_strip");
    expect(carpet.indexOf("existing_pad")).toBeLessThan(carpet.indexOf("existing_tack")!);
    expect(carpet.indexOf("hs_demo")).toBeLessThan(carpet.indexOf("existing_tack")!);

    const glue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("existing_tack");
    expect(glue).not.toContain("tack_strip");

    const lvpQuiet = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(lvpQuiet).not.toContain("existing_tack");

    const lvpOverCarpet = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      hs_demo: ["Carpet"],
    });
    expect(lvpOverCarpet).toContain("existing_tack");
    expect(lvpOverCarpet).toContain("existing_pad");
    expect(lvpOverCarpet).not.toContain("tack_strip");
    expect(lvpOverCarpet.indexOf("existing_pad")).toBeLessThan(lvpOverCarpet.indexOf("existing_tack")!);

    const neu = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      work_type: ["New construction"],
      hs_demo: ["Carpet"],
    });
    expect(neu).not.toContain("existing_tack");
  });

  it("0254 exclusive wall tile hides floor-only questions; mixed carpet/LVP still asks them", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0254_flooring_knowledge_wall_tile.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0254_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/tile_application/);
    expect(sql).toMatch(/Exclusive wall tile hides toilets/);
    expect(sql).toMatch(/Do NOT SQL-gate toilets on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*tile_application.*toilets|toilets.*show_if.*tile_application/);

    expect(TILE_WALL_HIDES_KEYS).toEqual(
      expect.arrayContaining([
        "toilets",
        "vents_registers",
        "doors_shave",
        "hs_plank_stairs",
        "construction_grade",
        "radiant_heat",
      ]),
    );
    expect(tileJobIsWallOnly({})).toBe(false);
    expect(tileJobIsWallOnly({ tile_application: ["Wall"] })).toBe(true);
    expect(tileJobIsWallOnly({ tile_application: ["Floor"] })).toBe(false);
    expect(tileJobIsWallOnly({ tile_application: ["Both"] })).toBe(false);
    expect(tileJobIsWallOnly({ tile_application: ["Unknown / field verify"] })).toBe(false);
    expect(tileJobIsWallOnly({ tile_application: ["Wall", "Floor"] })).toBe(false);
    expect(
      jobHasNonTileFloorFamily(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHasNonTileFloorFamily(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile", "LVP / LVT"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHasNonTileFloorFamily(
        installContextFromValByKey({ project_type: ["Carpet", "Hard surface"], surface_type: ["Tile"] }),
      ),
    ).toBe(true);

    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile hides toilets/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(unanswered).toContain("tile_application");
    expect(unanswered).toContain("toilets");
    expect(unanswered).toContain("vents_registers");
    expect(unanswered).toContain("doors_shave");
    expect(unanswered).toContain("hs_plank_stairs");
    expect(unanswered).toContain("construction_grade");
    expect(unanswered).toContain("radiant_heat");
    expect(unanswered).toContain("wet_area");
    expect(unanswered).toContain("appliances");
    expect(unanswered).toContain("hs_prep");
    expect(unanswered).toContain("tile_setting");

    const wall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(wall).toContain("tile_application");
    expect(wall).not.toContain("toilets");
    expect(wall).not.toContain("vents_registers");
    expect(wall).not.toContain("doors_shave");
    expect(wall).not.toContain("hs_plank_stairs");
    expect(wall).not.toContain("construction_grade");
    expect(wall).not.toContain("radiant_heat");
    expect(wall).toContain("wet_area");
    expect(wall).toContain("appliances");
    expect(wall).toContain("hs_prep");
    expect(wall).toContain("tile_setting");

    const floor = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
    });
    expect(floor).toContain("toilets");
    expect(floor).toContain("hs_plank_stairs");
    expect(floor).toContain("radiant_heat");

    const both = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Both"],
    });
    expect(both).toContain("toilets");
    expect(both).toContain("doors_shave");

    const unknown = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Unknown / field verify"],
    });
    expect(unknown).toContain("toilets");
    expect(unknown).toContain("radiant_heat");

    const mixedCarpet = walk({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      carpet_install: ["Stretch-in"],
    });
    expect(mixedCarpet).toContain("toilets");
    expect(mixedCarpet).toContain("vents_registers");
    expect(mixedCarpet).toContain("doors_shave");
    expect(mixedCarpet).toContain("radiant_heat");
    expect(mixedCarpet).toContain("hs_plank_stairs");

    const mixedLvp = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
    });
    expect(mixedLvp).toContain("toilets");
    expect(mixedLvp).toContain("doors_shave");
    expect(mixedLvp).toContain("hs_plank_stairs");
    expect(mixedLvp).toContain("construction_grade");
    expect(mixedLvp).toContain("radiant_heat");

    const overlayWall = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(overlayWall).not.toContain("toilets");
    expect(overlayWall).not.toContain("vents_registers");
    expect(overlayWall).toContain("wet_area");
    expect(overlayWall).toContain("appliances");
  });

  it("0255 exclusive wall tile also hides floor T-molds, subfloor sheets, vapor, and self-level", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0255_flooring_knowledge_wall_floor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0255_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/doorway T-molds/);
    expect(sql).toMatch(/Do NOT SQL-gate these on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect([...TILE_WALL_HIDES_KEYS]).toEqual([
      "toilets",
      "vents_registers",
      "doors_shave",
      "hs_plank_stairs",
      "construction_grade",
      "radiant_heat",
      "hs_transitions",
      "subfloor_needed",
      "selflevel_needed",
      "vapor_barrier",
      "moisture_mitigation",
      "moisture_test",
      "subfloor_condition",
      "stairs",
      "stair_landings",
      "stair_open_sides",
      "existing_pad",
      "existing_tack",
      "existing_bond",
      "climate_control",
      "acclimation",
    ]);

    const wallCtx = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(jobIsExclusiveWallTile(wallCtx)).toBe(true);
    expect(jobIsExclusiveWallTile(emptyInstallContext())).toBe(false);
    expect(
      jobIsExclusiveWallTile(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile", "LVP / LVT"],
          tile_application: ["Wall"],
        }),
      ),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /doorway T-molds/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(unanswered).toContain("hs_transitions");
    expect(unanswered).toContain("subfloor_needed");
    expect(unanswered).toContain("tile_setting");
    expect(unanswered).toContain("hs_base_trim");
    expect(unanswered).toContain("hs_prep");

    const wall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      hs_prep: ["Self-leveling"],
      substrate: ["Concrete"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(wall).not.toContain("hs_transitions");
    expect(wall).not.toContain("subfloor_needed");
    expect(wall).not.toContain("selflevel_needed");
    expect(wall).not.toContain("vapor_barrier");
    expect(wall).not.toContain("moisture_mitigation");
    expect(wall).toContain("hs_base_trim");
    expect(wall).toContain("hs_prep");
    expect(wall).toContain("tile_setting");
    expect(wall).toContain("wet_area");
    expect(wall).toContain("substrate");

    const mixedLvp = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
      hs_prep: ["Self-leveling"],
      substrate: ["Concrete"],
    });
    expect(mixedLvp).toContain("hs_transitions");
    expect(mixedLvp).not.toContain("subfloor_needed");
    expect(mixedLvp).toContain("selflevel_needed");
    expect(mixedLvp).toContain("vapor_barrier");

    expect(
      knowledgeWarnings(wallCtx, {
        hsStairSteps: 13,
        hasStairNose: false,
        neededTransitionTrims: ["T-mold"],
        presentTrimTypes: [],
      }).some((w) => w.id === "hs-transitions" || w.id === "hs-stair-nose"),
    ).toBe(false);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/jobIsExclusiveWallTile\(flooringCtx\)/);
  });

  it("0256 vacant occupancy hides furniture moving; unanswered and occupied still ask", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0256_flooring_knowledge_vacant_furniture.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0256_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Vacant hides furniture moving/);
    expect(sql).toMatch(/Do NOT SQL-gate furniture on occupancy/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*occupancy.*furniture|furniture.*show_if.*occupancy/);

    expect([...FURNITURE_MOVING_KEYS]).toEqual(["furniture_level", "furniture_heavy"]);
    expect(jobIsVacant({})).toBe(false);
    expect(jobIsVacant({ occupancy: ["Vacant"] })).toBe(true);
    expect(jobIsVacant({ occupancy: ["Occupied"] })).toBe(false);
    expect(jobIsVacant({ occupancy: ["Unknown"] })).toBe(false);
    expect(knowledgeHelpFor({ key: "occupancy" }, emptyInstallContext())).toMatch(
      /Vacant hides furniture moving/,
    );

    const unanswered = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(unanswered).toContain("occupancy");
    expect(unanswered).toContain("furniture_level");
    expect(unanswered).toContain("furniture_heavy");
    expect(unanswered.indexOf("occupancy")).toBeLessThan(unanswered.indexOf("furniture_level")!);

    const occupied = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      occupancy: ["Occupied"],
    });
    expect(occupied).toContain("furniture_level");
    expect(occupied).toContain("furniture_heavy");

    const vacant = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      occupancy: ["Vacant"],
    });
    expect(vacant).toContain("occupancy");
    expect(vacant).not.toContain("furniture_level");
    expect(vacant).not.toContain("furniture_heavy");
    expect(vacant).toContain("toilets");
    expect(vacant).toContain("appliances");

    const unknown = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      occupancy: ["Unknown"],
    });
    expect(unknown).toContain("furniture_level");
    expect(unknown).toContain("furniture_heavy");
  });

  it("0257 new construction hides existing-vinyl skim; unanswered and replacement still ask", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0257_flooring_knowledge_new_build_skim.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0257_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/existing-vinyl skim/);
    expect(sql).toMatch(/Do NOT SQL-gate vinyl_skim on work_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);

    expect(knowledgeHelpFor({ key: "vinyl_skim" }, emptyInstallContext())).toMatch(
      /New construction hides this/,
    );
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(
      /existing-vinyl skim/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
    });
    expect(unanswered).toContain("work_type");
    expect(unanswered).toContain("vinyl_skim");
    expect(unanswered).toContain("vinyl_layout");

    const replacement = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      work_type: ["Replacement (tear-out)"],
    });
    expect(replacement).toContain("vinyl_skim");
    expect(replacement).toContain("hs_demo");

    const neu = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      work_type: ["New construction"],
    });
    expect(neu).toContain("work_type");
    expect(neu).toContain("vinyl_layout");
    expect(neu).toContain("adhesive");
    expect(neu).toContain("substrate");
    expect(neu).not.toContain("vinyl_skim");
    expect(neu).not.toContain("hs_demo");
  });

  it("0258 new construction hides toilet pull/reset; appliances and door shaves stay", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0258_flooring_knowledge_new_build_toilets.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0258_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/toilet pull/);
    expect(sql).toMatch(/Do NOT SQL-gate toilets on work_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*work_type.*toilets|toilets.*show_if.*work_type/);

    expect([...NEW_CONSTRUCTION_HIDES_KEYS]).toEqual(["toilets"]);
    expect(knowledgeHelpFor({ key: "toilets" }, emptyInstallContext())).toMatch(
      /New construction hides this/,
    );
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(
      /toilet pull/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(unanswered).toContain("work_type");
    expect(unanswered).toContain("toilets");
    expect(unanswered).toContain("appliances");
    expect(unanswered).toContain("doors_shave");

    const replacement = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["Replacement (tear-out)"],
    });
    expect(replacement).toContain("toilets");
    expect(replacement).toContain("hs_demo");

    const neu = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      work_type: ["New construction"],
    });
    expect(neu).toContain("work_type");
    expect(neu).toContain("appliances");
    expect(neu).toContain("doors_shave");
    expect(neu).toContain("substrate");
    expect(neu).not.toContain("toilets");
    expect(neu).not.toContain("hs_demo");
  });

  it("0259 climate / radiant / moisture warnings live on the overlay, not a second questionnaire list", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0259_flooring_knowledge_climate_sot.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0259_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/knowledgeWarnings/);
    expect(sql).toMatch(/Do NOT SQL-gate climate on install_method/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*install_method.*climate_control|climate_control.*show_if.*install_method/);

    expect(knowledgeHelpFor({ key: "climate_control" }, emptyInstallContext())).toMatch(
      /not a second questionnaire list/,
    );
    expect(knowledgeHelpFor({ key: "radiant_heat" }, emptyInstallContext())).toMatch(
      /overlay purchasing warning/,
    );
    expect(knowledgeHelpFor({ key: "moisture_test" }, emptyInstallContext())).toMatch(
      /moisture-untested/,
    );

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).not.toMatch(/Hardwood \/ glue-down without confirmed AC/);
    expect(q).not.toMatch(/Radiant heat present — confirm the selected flooring/);
    expect(q).not.toMatch(/Glue-down \/ hardwood without a moisture test/);
    expect(q).not.toMatch(/Hardwood \/ glue-down \/ carpet tile without confirmed AC/);
    expect(q).not.toMatch(/Glue-down \/ hardwood \/ carpet tile without a moisture test/);
    expect(q).not.toMatch(/Ceramic WITH mortar bed demo — expect a floor-height change/);

    const hardwood = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
    });
    expect(installNeedsAcclimationClimate(hardwood)).toBe(true);
    expect(climateControlConfirmed(hardwood)).toBe(false);
    const climateWarn = knowledgeWarnings(hardwood);
    expect(climateWarn.some((w) => w.id === "climate")).toBe(true);
    expect(climateWarn.find((w) => w.id === "climate")?.text).toMatch(/Confirm climate control/);

    const climateOk = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
        climate_control: ["AC", "Heat"],
      }),
    );
    expect(climateOk.some((w) => w.id === "climate")).toBe(false);

    const legacyOk = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
        ac_available: ["Yes"],
        heat_available: ["Yes"],
      }),
    );
    expect(legacyOk.some((w) => w.id === "climate")).toBe(false);

    const stretch = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
      }),
    );
    expect(installNeedsAcclimationClimate(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
      }),
    )).toBe(false);
    expect(stretch.some((w) => w.id === "climate")).toBe(false);
    expect(stretch.some((w) => w.id === "moisture-untested")).toBe(false);

    const laminate = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Laminate"],
        install_method: ["Floating / click"],
      }),
    );
    expect(laminate.some((w) => w.id === "climate")).toBe(false);

    const radiant = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
        radiant_heat: ["Yes"],
      }),
    );
    expect(radiant.some((w) => w.id === "radiant")).toBe(true);

    const untested = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
        climate_control: ["AC", "Heat"],
        moisture_test: ["No"],
      }),
    );
    expect(untested.some((w) => w.id === "moisture-untested")).toBe(true);
    expect(untested.some((w) => w.id === "climate")).toBe(false);

    const unansweredMoisture = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Hardwood"],
        climate_control: ["AC", "Heat"],
      }),
    );
    expect(unansweredMoisture.some((w) => w.id === "moisture-untested")).toBe(false);

    const neu = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT"],
        work_type: ["New construction"],
      }),
    );
    expect(neu.find((w) => w.id === "new-construction")?.text).toMatch(/toilet pull/);
    expect(neu.find((w) => w.id === "new-construction")?.text).toMatch(/appliances/);
  });

  it("0260 exclusive wall tile hides slab moisture tests; wet area stays", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0260_flooring_knowledge_wall_moisture.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0260_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/slab moisture/);
    expect(sql).toMatch(/Do NOT SQL-gate moisture_test on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*tile_application.*moisture_test|moisture_test.*show_if.*tile_application/);

    expect(TILE_WALL_HIDES_KEYS).toContain("moisture_test");
    expect(TILE_WALL_HIDES_KEYS).toContain("moisture_mitigation");
    expect(knowledgeHelpFor({ key: "moisture_test" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /slab moisture tests/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(unanswered).toContain("moisture_test");
    expect(unanswered).toContain("moisture_mitigation");
    expect(unanswered).toContain("wet_area");
    expect(unanswered).toContain("toilets");

    const wall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(wall).not.toContain("moisture_test");
    expect(wall).not.toContain("moisture_mitigation");
    expect(wall).not.toContain("vapor_barrier");
    expect(wall).toContain("wet_area");
    expect(wall).toContain("hs_prep");
    expect(wall).toContain("tile_setting");
    expect(wall).toContain("substrate");

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "Hardwood"],
      tile_application: ["Wall"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(mixed).toContain("moisture_test");
    expect(mixed).toContain("wet_area");
  });

  it("0261 exclusive wall tile hides Self-leveling and grinding chips; patch/skim stays", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0261_flooring_knowledge_wall_prep.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0261_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Self-leveling and grinding/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_prep on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*tile_application.*hs_prep|hs_prep.*show_if.*tile_application/);

    expect(tileWallHidesPrepOptionLabel("Self-leveling")).toBe(true);
    expect(tileWallHidesPrepOptionLabel("Self-level")).toBe(true);
    expect(tileWallHidesPrepOptionLabel("Grinding / high spots")).toBe(true);
    expect(tileWallHidesPrepOptionLabel("Patch / skim coat")).toBe(false);
    expect(tileWallHidesPrepOptionLabel("None")).toBe(false);

    const wall = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(choiceOptionApplies({ key: "hs_prep" }, "Self-leveling", wall)).toBe(false);
    expect(choiceOptionApplies({ key: "hs_prep" }, "Grinding / high spots", wall)).toBe(false);
    expect(choiceOptionApplies({ key: "hs_prep" }, "Patch / skim coat", wall)).toBe(true);
    expect(choiceOptionApplies({ key: "hs_prep" }, "None", wall)).toBe(true);

    const unanswered = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(choiceOptionApplies({ key: "hs_prep" }, "Self-leveling", unanswered)).toBe(true);

    const mixed = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
    });
    expect(choiceOptionApplies({ key: "hs_prep" }, "Self-leveling", mixed)).toBe(true);

    const walkWall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(walkWall).toContain("hs_prep");
    expect(walkWall).toContain("tile_setting");
    expect(walkWall).not.toContain("selflevel_needed");

    expect(knowledgeHelpFor({ key: "hs_prep" }, emptyInstallContext())).toMatch(
      /hides Self-leveling and grinding/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /grinding chips/,
    );

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/choiceOptionApplies/);
  });

  it("0262 exclusive carpet tile hides roll-goods layout; stretch-in and glue-down keep it", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0262_flooring_knowledge_carpet_tile_layout.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0262_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Exclusive carpet tile hides this/);
    expect(sql).toMatch(/Do NOT SQL-gate pattern_match on carpet_install/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*pattern_match|pattern_match.*show_if.*carpet_install/,
    );

    expect(CARPET_TILE_HIDES_KEYS).toEqual(
      expect.arrayContaining([
        "pattern_match",
        "pattern_repeat",
        "carpet_direction",
      ]),
    );
    expect(
      jobIsExclusiveCarpetTile(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Carpet tile"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveCarpetTile(
        installContextFromValByKey({ project_type: ["Carpet"] }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetTile(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetTile(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Glue-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetTile(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in", "Carpet tile"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetTile(
        installContextFromValByKey({
          project_type: ["Carpet", "Hard surface"],
          surface_type: ["LVP / LVT"],
          carpet_install: ["Carpet tile"],
        }),
      ),
    ).toBe(true);

    const unanswered = walk({ project_type: ["Carpet"] });
    expect(unanswered).toContain("pattern_match");
    expect(unanswered).toContain("carpet_direction");
    expect(unanswered).not.toContain("pattern_repeat");

    const stretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("pattern_match");
    expect(stretch).toContain("carpet_direction");

    const glue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("pattern_match");
    expect(glue).toContain("carpet_direction");

    const tile = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(tile).toContain("carpet_cuts");
    expect(tile).toContain("adhesive");
    expect(tile).toContain("carpet_tile_stairs");
    expect(tile).not.toContain("pattern_match");
    expect(tile).not.toContain("carpet_direction");
    expect(tile).not.toContain("pattern_repeat");

    const leftover = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      pattern_match: ["Pattern match required"],
    });
    expect(leftover).not.toContain("pattern_repeat");
    expect(leftover).not.toContain("pattern_match");

    const mixed = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in", "Carpet tile"],
    });
    expect(mixed).toContain("pattern_match");
    expect(mixed).toContain("carpet_direction");
    expect(mixed).toContain("carpet_stairs");
    expect(mixed).toContain("carpet_tile_stairs");

    const mixedLvp = walk({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Carpet tile"],
    });
    expect(mixedLvp).not.toContain("pattern_match");
    expect(mixedLvp).not.toContain("carpet_direction");
    expect(mixedLvp).toContain("hs_direction");

    expect(knowledgeHelpFor({ key: "pattern_match" }, emptyInstallContext())).toMatch(
      /Exclusive carpet tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /hides pattern match/,
    );
    expect(knowledgeHelpFor({ key: "carpet_direction" }, emptyInstallContext())).toMatch(
      /there is no roll/,
    );
  });

  it("0263 exclusive wall tile hides floor subfloor condition; leftover Uneven does not warn to self-level", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0263_flooring_knowledge_wall_subfloor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0263_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/floor subfloor condition/);
    expect(sql).toMatch(/Do NOT SQL-gate subfloor_condition on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*tile_application.*subfloor_condition|subfloor_condition.*show_if.*tile_application/,
    );

    expect(TILE_WALL_HIDES_KEYS).toContain("subfloor_condition");
    expect(knowledgeHelpFor({ key: "subfloor_condition" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /floor subfloor condition/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(unanswered).toContain("subfloor_condition");
    expect(unanswered).toContain("hs_prep");
    expect(unanswered).toContain("substrate");

    const wall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(wall).not.toContain("subfloor_condition");
    expect(wall).not.toContain("subfloor_needed");
    expect(wall).not.toContain("selflevel_needed");
    expect(wall).toContain("hs_prep");
    expect(wall).toContain("substrate");
    expect(wall).toContain("tile_setting");
    expect(wall).toContain("wet_area");

    const floor = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
    });
    expect(floor).toContain("subfloor_condition");

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
    });
    expect(mixed).toContain("subfloor_condition");

    const leftoverWall = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
        tile_application: ["Wall"],
        subfloor_condition: ["Uneven"],
        hs_prep: ["Patch / skim coat"],
      }),
    );
    expect(leftoverWall.some((w) => w.id === "subfloor-uneven")).toBe(false);

    const leftoverDamage = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["Tile"],
        tile_application: ["Wall"],
        subfloor_condition: ["Damage / soft spots"],
        subfloor_needed: ["No"],
      }),
    );
    expect(leftoverDamage.some((w) => w.id === "subfloor-damage")).toBe(false);

    const lvpUneven = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT"],
        subfloor_condition: ["Uneven"],
        hs_prep: ["None"],
      }),
    );
    expect(lvpUneven.some((w) => w.id === "subfloor-uneven")).toBe(true);
  });

  it("0264 exclusive wall tile hides stairs, landings, and open sides", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0264_flooring_knowledge_wall_stairs.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0264_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/stair landings/);
    expect(sql).toMatch(/Do NOT SQL-gate stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*tile_application.*stairs|stairs.*show_if.*tile_application/);

    expect(TILE_WALL_HIDES_KEYS).toEqual(
      expect.arrayContaining(["stairs", "stair_landings", "stair_open_sides", "hs_plank_stairs"]),
    );
    expect(knowledgeHelpFor({ key: "stairs" }, emptyInstallContext())).toMatch(
      /a backsplash is not a stair job/,
    );
    expect(knowledgeHelpFor({ key: "stair_landings" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /stair landings/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(unanswered).not.toContain("stairs");
    expect(unanswered).toContain("hs_plank_stairs");
    expect(unanswered).not.toContain("stair_landings");

    const wall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(wall).not.toContain("stairs");
    expect(wall).not.toContain("hs_plank_stairs");
    expect(wall).not.toContain("stair_landings");
    expect(wall).not.toContain("stair_open_sides");
    expect(wall).toContain("hs_prep");
    expect(wall).toContain("tile_setting");

    const leftover = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      hs_plank_stairs: ["Yes"],
      stairs: ["Yes"],
    });
    expect(leftover).not.toContain("stair_landings");
    expect(leftover).not.toContain("stair_open_sides");

    const floor = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
      hs_plank_stairs: ["Yes"],
    });
    expect(floor).not.toContain("stairs");
    expect(floor).toContain("hs_plank_stairs");
    expect(floor).toContain("stair_landings");
    expect(floor).toContain("stair_open_sides");

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
    });
    expect(mixed).not.toContain("stairs");
    expect(mixed).toContain("hs_plank_stairs");

    const overlayWall = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(overlayWall).not.toContain("stairs");
    expect(overlayWall).not.toContain("stair_landings");
    expect(overlayWall).not.toContain("stair_open_sides");

    const overlayUnanswered = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(overlayUnanswered).not.toContain("stairs");
    expect(overlayUnanswered).toContain("hs_plank_stairs");
    expect(overlayUnanswered).not.toContain("stair_landings");
    expect(overlayUnanswered).not.toContain("stair_open_sides");
  });

  it("0265 exclusive wall tile hides floor demo chips; ceramic mortar stays", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0265_flooring_knowledge_wall_demo.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0265_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Floor demo chips/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_demo on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*tile_application.*hs_demo|hs_demo.*show_if.*tile_application/);

    expect(tileWallHidesDemoOptionLabel("Carpet")).toBe(true);
    expect(tileWallHidesDemoOptionLabel("LVP")).toBe(true);
    expect(tileWallHidesDemoOptionLabel("Laminate")).toBe(true);
    expect(tileWallHidesDemoOptionLabel("Sheet vinyl")).toBe(true);
    expect(tileWallHidesDemoOptionLabel("Luan")).toBe(true);
    expect(tileWallHidesDemoOptionLabel("Glue-down hardwood")).toBe(true);
    expect(tileWallHidesDemoOptionLabel("Nailed hardwood")).toBe(true);
    expect(tileWallHidesDemoOptionLabel("Ceramic WITH mortar bed")).toBe(false);
    expect(tileWallHidesDemoOptionLabel("Ceramic WITHOUT mortar bed")).toBe(false);
    expect(tileWallHidesDemoOptionLabel("None")).toBe(false);
    expect(tileWallHidesDemoOptionLabel("Other")).toBe(false);

    const wall = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(choiceOptionApplies({ key: "hs_demo" }, "Carpet", wall)).toBe(false);
    expect(choiceOptionApplies({ key: "hs_demo" }, "LVP", wall)).toBe(false);
    expect(choiceOptionApplies({ key: "hs_demo" }, "Ceramic WITH mortar bed", wall)).toBe(true);
    expect(choiceOptionApplies({ key: "hs_demo" }, "None", wall)).toBe(true);

    const unanswered = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(choiceOptionApplies({ key: "hs_demo" }, "Carpet", unanswered)).toBe(true);

    const mixed = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
    });
    expect(choiceOptionApplies({ key: "hs_demo" }, "Carpet", mixed)).toBe(true);

    const walkUnanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(walkUnanswered).toContain("hs_demo");
    expect(walkUnanswered).not.toContain("existing_pad");

    const walkWall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      hs_demo: ["Carpet"],
    });
    expect(walkWall).toContain("hs_demo");
    expect(walkWall).not.toContain("existing_pad");
    expect(walkWall).not.toContain("existing_tack");
    expect(walkWall).not.toContain("existing_bond");
    expect(walkWall).toContain("tile_setting");
    expect(walkWall).toContain("wet_area");

    const walkFloor = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
      hs_demo: ["Carpet"],
    });
    expect(walkFloor).toContain("existing_pad");
    expect(walkFloor).toContain("existing_tack");

    expect(TILE_WALL_HIDES_KEYS).toEqual(
      expect.arrayContaining(["existing_pad", "existing_tack", "existing_bond"]),
    );
    expect(knowledgeHelpFor({ key: "hs_demo" }, emptyInstallContext())).toMatch(
      /hides floor demo chips/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /Floor demo chips/,
    );
  });

  it("0266 exclusive wall tile hides furniture moving; appliances stay", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0266_flooring_knowledge_wall_furniture.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0266_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/a backsplash is not a furniture-moving job/);
    expect(sql).toMatch(/Do NOT SQL-gate furniture on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*tile_application.*furniture|furniture.*show_if.*tile_application/);

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(unanswered).toContain("furniture_level");
    expect(unanswered).toContain("furniture_heavy");
    expect(unanswered).toContain("appliances");

    const wall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      occupancy: ["Occupied"],
    });
    expect(wall).toContain("occupancy");
    expect(wall).not.toContain("furniture_level");
    expect(wall).not.toContain("furniture_heavy");
    expect(wall).toContain("appliances");
    expect(wall).toContain("wet_area");

    const floor = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
      occupancy: ["Occupied"],
    });
    expect(floor).toContain("furniture_level");
    expect(floor).toContain("furniture_heavy");

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
      occupancy: ["Occupied"],
    });
    expect(mixed).toContain("furniture_level");
    expect(mixed).toContain("furniture_heavy");

    expect(knowledgeHelpFor({ key: "furniture_level" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile hides this too/,
    );
    expect(knowledgeHelpFor({ key: "occupancy" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile also hides it/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /Furniture moving hides too/,
    );
  });

  it("0267 solid hardwood hides floating follow-ups; engineered keeps them", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0267_flooring_knowledge_solid_floating.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0267_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/floating is not a permitted system/);
    expect(sql).toMatch(/Do NOT SQL-gate attached_pad on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*attached_pad|attached_pad.*show_if.*surface_type/);

    expect(SOLID_HARDWOOD_HIDES_KEYS).toEqual([
      "attached_pad",
      "hs_underlayment",
      "laminate_expansion",
    ]);
    expect(
      jobIsExclusiveSolidHardwood(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveSolidHardwood(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Engineered hardwood"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveSolidHardwood(
        installContextFromValByKey({ project_type: ["Hard surface"] }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveSolidHardwood(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood", "LVP / LVT"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveSolidHardwood(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood", "Engineered hardwood"],
        }),
      ),
    ).toBe(false);

    const unansweredHs = visibleKnowledgeKeys({ project_type: ["Hard surface"] });
    expect(unansweredHs).toContain("attached_pad");

    const lvp = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(lvp).toContain("attached_pad");

    const solid = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
    });
    expect(solid).not.toContain("attached_pad");
    expect(solid).not.toContain("hs_underlayment");
    expect(solid).not.toContain("laminate_expansion");
    expect(solid).toContain("hardwood_fasteners");
    expect(solid).toContain("hardwood_finish");
    expect(solid).toContain("adhesive");

    const leftover = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Floating / click"],
    });
    expect(leftover).not.toContain("attached_pad");
    expect(leftover).not.toContain("laminate_expansion");
    expect(leftover).toContain("hardwood_fasteners");
    expect(leftover).toContain("adhesive");
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Floating / click"],
        }),
      ).some((w) => w.id === "solid-floating"),
    ).toBe(true);

    const engineered = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
    });
    expect(engineered).toContain("attached_pad");
    expect(engineered).toContain("laminate_expansion");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood", "Engineered hardwood"],
    });
    expect(mixed).toContain("attached_pad");

    const nail = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
    });
    expect(nail).toContain("hardwood_fasteners");
    expect(nail).not.toContain("attached_pad");
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
        }),
      ).some((w) => w.id === "solid-floating"),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "attached_pad" }, emptyInstallContext())).toMatch(
      /Solid hardwood hides this/,
    );
    expect(knowledgeHelpFor({ key: "laminate_expansion" }, emptyInstallContext())).toMatch(
      /Solid hardwood hides this/,
    );
  });

  it("0268 exclusive tile hides 6-mil vapor barrier; mixed LVP + tile still asks", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0268_flooring_knowledge_tile_vapor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0268_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/thinset is not a 6-mil click-floor vapor barrier/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*vapor_barrier|vapor_barrier.*show_if.*surface_type/,
    );

    expect(TILE_THINSET_HIDES_KEYS).toEqual(["vapor_barrier"]);
    expect(
      jobIsExclusiveTile(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveTile(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile"],
          tile_application: ["Floor"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveTile(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile", "LVP / LVT"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveTile(
        installContextFromValByKey({ project_type: ["Hard surface"] }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveWallTile(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile"],
          tile_application: ["Floor"],
        }),
      ),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /Exclusive floor tile also hides the 6-mil/,
    );

    const floorConcrete = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
      substrate: ["Concrete"],
    });
    expect(floorConcrete).not.toContain("vapor_barrier");
    expect(floorConcrete).toContain("tile_setting");
    expect(floorConcrete).toContain("hs_prep");
    expect(floorConcrete).toContain("substrate");
    expect(floorConcrete).toContain("construction_grade");

    const overlayFloor = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
      substrate: ["Concrete"],
    });
    expect(overlayFloor).not.toContain("vapor_barrier");
    expect(overlayFloor).toContain("tile_setting");

    const unansweredTile = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      substrate: ["Concrete"],
    });
    expect(unansweredTile).not.toContain("vapor_barrier");
    expect(unansweredTile).toContain("tile_application");

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Floor"],
      install_method: ["Floating / click"],
      substrate: ["Concrete"],
    });
    expect(mixed).toContain("vapor_barrier");
    expect(mixed).toContain("tile_setting");
    expect(mixed).toContain("attached_pad");

    const hardwoodSlab = walk({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Concrete"],
    });
    expect(hardwoodSlab).toContain("vapor_barrier");
  });

  it("0269 glue-down hides Included with underlayment; floating still offers it", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0269_flooring_knowledge_vapor_underlayment.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0269_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/floating-floor sheet, not an adhesive moisture system/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier options on install_method/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*install_method.*vapor_barrier|vapor_barrier.*show_if.*install_method/,
    );

    expect(vaporBarrierHidesUnderlaymentOptionLabel("Included with underlayment")).toBe(true);
    expect(vaporBarrierHidesUnderlaymentOptionLabel("Required")).toBe(false);
    expect(vaporBarrierHidesUnderlaymentOptionLabel("Not needed")).toBe(false);

    const glue = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(glue)).toBe(false);
    expect(choiceOptionApplies({ key: "vapor_barrier" }, "Included with underlayment", glue)).toBe(
      false,
    );
    expect(choiceOptionApplies({ key: "vapor_barrier" }, "Required", glue)).toBe(true);

    const floating = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(floating)).toBe(true);
    expect(
      choiceOptionApplies({ key: "vapor_barrier" }, "Included with underlayment", floating),
    ).toBe(true);

    const unansweredLvp = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(unansweredLvp)).toBe(true);

    const solid = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(solid)).toBe(false);

    const engineered = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(engineered)).toBe(true);

    const nail = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Concrete"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(nail)).toBe(false);
    expect(choiceOptionApplies({ key: "vapor_barrier" }, "Included with underlayment", nail)).toBe(
      false,
    );
    expect(choiceOptionApplies({ key: "vapor_barrier" }, "Required", nail)).toBe(true);

    const stretch = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      substrate: ["Concrete"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(stretch)).toBe(false);

    const mixed = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT", "Laminate"],
      install_method: ["Glue-down", "Floating / click"],
    });
    expect(jobAllowsFloatingVaporUnderlayment(mixed)).toBe(true);

    const glueWalk = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(glueWalk).toContain("vapor_barrier");
    expect(glueWalk).toContain("moisture_mitigation");
    expect(glueWalk).not.toContain("attached_pad");

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /hide Included with underlayment/,
    );
  });

  it("0270 exclusive concrete hides 4x8 plywood overlay; wood still asks", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0270_flooring_knowledge_concrete_sheets.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0270_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/a slab is patch \/ self-level, not plywood overlay/);
    expect(sql).toMatch(/Do NOT SQL-gate subfloor_needed on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*subfloor_needed|subfloor_needed.*show_if.*substrate/,
    );

    expect(CONCRETE_HIDES_KEYS).toEqual(["subfloor_needed"]);
    expect(
      jobIsExclusiveConcrete(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveConcrete(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveConcrete(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          substrate: ["Concrete", "Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveConcrete(
        installContextFromValByKey({ project_type: ["Hard surface"] }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveConcrete(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          substrate: ["Unknown / field verify"],
        }),
      ),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "subfloor_needed" }, emptyInstallContext())).toMatch(
      /Exclusive Concrete hides this/,
    );
    expect(knowledgeHelpFor({ key: "substrate" }, emptyInstallContext())).toMatch(
      /Exclusive Concrete hides 4×8/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(unanswered).toContain("subfloor_needed");
    expect(unanswered).toContain("substrate");
    expect(unanswered.indexOf("substrate")).toBeLessThan(unanswered.indexOf("subfloor_needed")!);

    const concrete = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Concrete"],
    });
    expect(concrete).not.toContain("subfloor_needed");
    expect(concrete).toContain("hs_prep");
    expect(concrete).toContain("vapor_barrier");
    expect(concrete).toContain("moisture_mitigation");

    const plywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(plywood).toContain("subfloor_needed");

    const mixed = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      substrate: ["Concrete", "Plywood / OSB"],
    });
    expect(mixed).toContain("subfloor_needed");

    const unknown = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      substrate: ["Unknown / field verify"],
    });
    expect(unknown).toContain("subfloor_needed");

    const stretchConcrete = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      substrate: ["Concrete"],
    });
    expect(stretchConcrete).not.toContain("subfloor_needed");
    expect(stretchConcrete).toContain("hs_prep");
  });

  it("0271 exclusive carpet tile hides 6-mil vapor barrier; moisture and acclimation still ask", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0271_flooring_knowledge_carpet_tile_vapor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0271_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/modular tile uses adhesive, not a floating-floor sheet/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on carpet_install/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*vapor_barrier|vapor_barrier.*show_if.*carpet_install/,
    );

    expect(CARPET_TILE_VAPOR_HIDES_KEYS).toEqual(["vapor_barrier"]);
    expect(
      jobIsExclusiveCarpetTileOnly(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Carpet tile"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveCarpetTileOnly(
        installContextFromValByKey({
          project_type: ["Carpet", "Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          carpet_install: ["Carpet tile"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetTileOnly(
        installContextFromValByKey({
          project_type: ["Carpet", "Hard surface"],
          carpet_install: ["Carpet tile"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetTileOnly(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Glue-down", "Carpet tile"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetTileOnly(
        installContextFromValByKey({ project_type: ["Carpet"] }),
      ),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Exclusive carpet tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "acclimation" }, emptyInstallContext())).toMatch(
      /carpet tile need acclimation/,
    );
    expect(knowledgeHelpFor({ key: "moisture_test" }, emptyInstallContext())).toMatch(
      /Exclusive carpet tile asks this instead of 6-mil/,
    );

    const tileConcrete = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      substrate: ["Concrete"],
    });
    expect(tileConcrete).not.toContain("vapor_barrier");
    expect(tileConcrete).toContain("adhesive");
    expect(tileConcrete).toContain("acclimation");
    expect(tileConcrete).toContain("moisture_test");
    expect(tileConcrete).toContain("moisture_mitigation");
    expect(tileConcrete).not.toContain("pattern_match");
    expect(tileConcrete).not.toContain("tack_strip");

    const overlayTile = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      substrate: ["Concrete"],
    });
    expect(overlayTile).not.toContain("vapor_barrier");
    expect(overlayTile).toContain("moisture_test");

    const unanswered = walk({
      project_type: ["Carpet"],
      substrate: ["Concrete"],
    });
    expect(unanswered).toContain("vapor_barrier");
    expect(unanswered).toContain("carpet_install");

    const stretchConcrete = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      substrate: ["Concrete"],
    });
    expect(stretchConcrete).toContain("vapor_barrier");
    expect(stretchConcrete).not.toContain("acclimation");
    expect(stretchConcrete).not.toContain("moisture_test");

    const mixedLvp = walk({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      carpet_install: ["Carpet tile"],
      substrate: ["Concrete"],
    });
    expect(mixedLvp).toContain("vapor_barrier");
    expect(mixedLvp).toContain("attached_pad");
    expect(mixedLvp).toContain("moisture_test");

    const glueCarpet = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
      substrate: ["Concrete"],
    });
    expect(glueCarpet).toContain("vapor_barrier");
    expect(glueCarpet).toContain("acclimation");

    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Carpet tile"],
        }),
      ).some((w) => w.id === "climate"),
    ).toBe(true);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
        }),
      ).some((w) => w.id === "climate"),
    ).toBe(false);
  });

  it("0272 exclusive hardwood over plywood hides slab moisture; concrete and glue still ask", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0272_flooring_knowledge_wood_moisture.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0272_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/glue-down or wood over concrete, not a wood deck/);
    expect(sql).toMatch(/Do NOT SQL-gate moisture_test on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*moisture_test|moisture_test.*show_if.*substrate/,
    );

    expect(WOOD_DECK_MOISTURE_HIDES_KEYS).toEqual(["moisture_test", "moisture_mitigation"]);
    expect(labelsAreWoodDeckOnly(["Plywood / OSB"])).toBe(true);
    expect(labelsAreWoodDeckOnly(["Wood"])).toBe(true);
    expect(labelsAreWoodDeckOnly(["Plywood / OSB", "Wood"])).toBe(true);
    expect(labelsAreWoodDeckOnly(["Concrete"])).toBe(false);
    expect(labelsAreWoodDeckOnly(["Plywood / OSB", "Concrete"])).toBe(false);
    expect(labelsAreWoodDeckOnly(["Existing flooring"])).toBe(false);
    expect(labelsAreWoodDeckOnly(["Unknown / field verify"])).toBe(false);
    expect(labelsAreWoodDeckOnly([])).toBe(false);

    expect(
      jobHidesSlabMoistureOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesSlabMoistureOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesSlabMoistureOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Engineered hardwood"],
          install_method: ["Glue-down"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesSlabMoistureOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesSlabMoistureOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood", "LVP / LVT"],
          install_method: ["Nail-down"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "moisture_test" }, emptyInstallContext())).toMatch(
      /Exclusive hardwood nail\/staple\/floating over plywood hides this/,
    );
    expect(knowledgeHelpFor({ key: "moisture_mitigation" }, emptyInstallContext())).toMatch(
      /Aqua bar is a slab system/,
    );

    const nailPlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(nailPlywood).not.toContain("moisture_test");
    expect(nailPlywood).not.toContain("moisture_mitigation");
    expect(nailPlywood).toContain("acclimation");
    expect(nailPlywood).toContain("hardwood_fasteners");
    expect(nailPlywood).not.toContain("vapor_barrier");

    const overlayPlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(overlayPlywood).not.toContain("moisture_test");
    expect(overlayPlywood).toContain("acclimation");

    const nailConcrete = walk({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Concrete"],
    });
    expect(nailConcrete).toContain("moisture_test");
    expect(nailConcrete).toContain("moisture_mitigation");
    expect(nailConcrete).toContain("vapor_barrier");

    const unansweredSub = walk({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
    });
    expect(unansweredSub).toContain("moisture_test");
    expect(unansweredSub).toContain("substrate");

    const gluePlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(gluePlywood).toContain("moisture_test");
    expect(gluePlywood).toContain("adhesive");

    const floatingPlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(floatingPlywood).not.toContain("moisture_test");
    expect(floatingPlywood).not.toContain("vapor_barrier");
    expect(floatingPlywood).toContain("attached_pad");

    const mixedLvp = walk({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood", "LVP / LVT"],
      install_method: ["Nail-down", "Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(mixedLvp).toContain("moisture_test");

    const moistureFlag = walk({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Plywood / OSB"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(moistureFlag).toContain("moisture_test");
  });

  it("0273 leftover illegal chips do not switch sole-system follow-ups", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0273_flooring_knowledge_sole_leftover.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0273_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/do not switch follow-ups/);
    expect(sql).toMatch(/Do NOT SQL-gate adhesive on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*adhesive|adhesive.*show_if.*surface_type/,
    );

    const lamGlue = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Glue-down"],
    });
    expect(lamGlue).toContain("laminate_expansion");
    expect(lamGlue).toContain("attached_pad");
    expect(lamGlue).toContain("hs_underlayment");
    expect(lamGlue).not.toContain("adhesive");
    expect(lamGlue).not.toContain("hardwood_fasteners");

    const overlayLam = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Glue-down"],
    });
    expect(overlayLam).toContain("laminate_expansion");
    expect(overlayLam).not.toContain("adhesive");
    expect(
      synthesizeSoleInstallMethod({
        project_type: ["Hard surface"],
        surface_type: ["Laminate"],
        install_method: ["Glue-down"],
      }).install_method,
    ).toEqual(["Floating / click"]);

    const vinylFloat = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Floating / click"],
    });
    expect(vinylFloat).toContain("adhesive");
    expect(vinylFloat).toContain("vinyl_layout");
    expect(vinylFloat).toContain("moisture_test");
    expect(vinylFloat).not.toContain("attached_pad");
    expect(vinylFloat).not.toContain("laminate_expansion");
    expect(vinylFloat).not.toContain("hs_underlayment");

    const overlayVinyl = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Floating / click"],
    });
    expect(overlayVinyl).toContain("adhesive");
    expect(overlayVinyl).not.toContain("laminate_expansion");

    const tileFloat = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      install_method: ["Floating / click"],
    });
    expect(tileFloat).toContain("tile_setting");
    expect(tileFloat).not.toContain("attached_pad");
    expect(tileFloat).not.toContain("laminate_expansion");
    expect(tileFloat).not.toContain("hs_underlayment");
    expect(tileFloat).not.toContain("adhesive");

    const unansweredLam = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    });
    expect(unansweredLam).toContain("laminate_expansion");
    expect(unansweredLam).not.toContain("adhesive");

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate", "LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(mixed).toContain("adhesive");
    expect(mixed).not.toContain("laminate_expansion");

    const mixedBoth = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate", "LVP / LVT"],
      install_method: ["Glue-down", "Floating / click"],
    });
    expect(mixedBoth).toContain("adhesive");
    expect(mixedBoth).toContain("laminate_expansion");
    expect(mixedBoth).toContain("attached_pad");

    const legalLam = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(legalLam).toContain("laminate_expansion");
    expect(legalLam).not.toContain("adhesive");

    expect(
      leftoverIllegalSoleInstallLabels(
        ["laminate"],
        "unknown",
        ["Glue-down"],
      ),
    ).toEqual(["Glue-down"]);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Laminate"],
          install_method: ["Glue-down"],
        }),
      ).some((w) => w.id === "sole-system-leftover"),
    ).toBe(true);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Laminate"],
          install_method: ["Floating / click"],
        }),
      ).some((w) => w.id === "sole-system-leftover"),
    ).toBe(false);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Floating / click"],
        }),
      ).some((w) => w.id === "sole-system-leftover"),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "adhesive" }, emptyInstallContext())).toMatch(
      /Exclusive laminate leftover Glue-down does not show this/,
    );
    expect(knowledgeHelpFor({ key: "install_method" }, installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    }))).toMatch(/Leftover Glue-down/);
    expect(knowledgeHelpFor({ key: "laminate_expansion" }, emptyInstallContext())).toMatch(
      /Exclusive laminate leftover Glue-down still asks this/,
    );
  });

  it("0274 carpet-only leftover HS chips do not reopen click-floor follow-ups", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0274_flooring_knowledge_carpet_hs_leftover.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0274_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not open expansion, underlayment, or click-floor vapor/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on install_method/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*install_method.*vapor_barrier|vapor_barrier.*show_if.*install_method/,
    );

    expect(
      jobHasHardSurfaceInstallScope(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          install_method: ["Floating / click"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHasHardSurfaceInstallScope(
        installContextFromValByKey({
          project_type: ["Carpet", "Hard surface"],
          surface_type: ["LVP / LVT"],
          carpet_install: ["Stretch-in"],
          install_method: ["Floating / click"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHasHardSurfaceInstallScope(
        installContextFromValByKey({ project_type: ["Hard surface"] }),
      ),
    ).toBe(true);

    const stretchFloat = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      install_method: ["Floating / click"],
    });
    expect(stretchFloat).toContain("tack_strip");
    expect(stretchFloat).toContain("carpet_pad");
    expect(stretchFloat).not.toContain("laminate_expansion");
    expect(stretchFloat).not.toContain("hs_underlayment");
    expect(stretchFloat).not.toContain("attached_pad");
    expect(stretchFloat).not.toContain("adhesive");
    expect(stretchFloat).not.toContain("vapor_barrier");

    const overlayStretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      install_method: ["Floating / click"],
    });
    expect(overlayStretch).not.toContain("laminate_expansion");
    expect(overlayStretch).toContain("tack_strip");

    const tileFloat = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      install_method: ["Floating / click"],
      substrate: ["Concrete"],
    });
    expect(tileFloat).not.toContain("vapor_barrier");
    expect(tileFloat).not.toContain("laminate_expansion");
    expect(tileFloat).toContain("adhesive");
    expect(tileFloat).toContain("moisture_test");
    expect(
      jobIsExclusiveCarpetTileOnly(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Carpet tile"],
          install_method: ["Floating / click"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(true);

    const stretchGlue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      install_method: ["Glue-down"],
    });
    expect(stretchGlue).toContain("tack_strip");
    expect(stretchGlue).not.toContain("adhesive");
    expect(stretchGlue).not.toContain("acclimation");
    expect(stretchGlue).not.toContain("moisture_test");

    const unanswered = walk({
      project_type: ["Carpet"],
      install_method: ["Floating / click"],
      substrate: ["Concrete"],
    });
    expect(unanswered).toContain("carpet_install");
    expect(unanswered).toContain("vapor_barrier");

    const mixed = walk({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Stretch-in"],
      install_method: ["Floating / click"],
    });
    expect(mixed).toContain("laminate_expansion");
    expect(mixed).toContain("attached_pad");
    expect(mixed).toContain("tack_strip");
    expect(mixed).toContain("vapor_barrier");

    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          install_method: ["Floating / click"],
        }),
      ).some((w) => w.id === "carpet-hs-leftover"),
    ).toBe(true);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Carpet", "Hard surface"],
          surface_type: ["LVP / LVT"],
          carpet_install: ["Stretch-in"],
          install_method: ["Floating / click"],
        }),
      ).some((w) => w.id === "carpet-hs-leftover"),
    ).toBe(false);

    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /Leftover Floating \/ Glue-down on a carpet-only job/,
    );
    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Leftover Floating on a carpet-only job does not reopen this/,
    );
  });

  it("0275 exclusive glue-down over plywood hides 6-mil vapor; concrete and floating still ask", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0275_flooring_knowledge_glue_wood_vapor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0275_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/you cannot glue to 6-mil poly/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*vapor_barrier|vapor_barrier.*show_if.*substrate/,
    );

    expect(GLUE_WOOD_VAPOR_HIDES_KEYS).toEqual(["vapor_barrier"]);
    expect(
      jobHidesVaporOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down", "Floating / click"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Plywood / OSB", "Concrete"],
        }),
      ),
    ).toBe(false);

    const gluePlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(gluePlywood).not.toContain("vapor_barrier");
    expect(gluePlywood).toContain("adhesive");
    expect(gluePlywood).toContain("moisture_test");

    const overlayPlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(overlayPlywood).not.toContain("vapor_barrier");
    expect(overlayPlywood).toContain("moisture_test");

    const glueConcrete = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Concrete"],
    });
    expect(glueConcrete).toContain("vapor_barrier");
    expect(glueConcrete).toContain("moisture_mitigation");

    const unansweredSub = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(unansweredSub).toContain("vapor_barrier");
    expect(unansweredSub).toContain("substrate");

    const floatingPlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(floatingPlywood).not.toContain("vapor_barrier");
    expect(floatingPlywood).toContain("attached_pad");

    const mixedFloat = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down", "Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(mixedFloat).not.toContain("vapor_barrier");
    expect(mixedFloat).toContain("attached_pad");
    expect(mixedFloat).toContain("adhesive");

    const mixedSub = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB", "Concrete"],
    });
    expect(mixedSub).toContain("vapor_barrier");

    const vinylPlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      substrate: ["Plywood / OSB"],
    });
    expect(vinylPlywood).not.toContain("vapor_barrier");
    expect(vinylPlywood).toContain("adhesive");
    expect(vinylPlywood).toContain("moisture_test");

    const glueCarpetPlywood = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(glueCarpetPlywood).not.toContain("vapor_barrier");
    expect(glueCarpetPlywood).toContain("adhesive");
    expect(glueCarpetPlywood).toContain("moisture_test");

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Exclusive glue-down over plywood hides this/,
    );
  });

  it("0276 exclusive glue-down over plywood hides Aqua bar; moisture test and concrete still ask", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0276_flooring_knowledge_glue_wood_aqua.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0276_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Aqua bar is a slab coating/);
    expect(sql).toMatch(/Do NOT SQL-gate moisture_mitigation on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*moisture_mitigation|moisture_mitigation.*show_if.*substrate/,
    );

    expect(WOOD_DECK_AQUA_HIDES_KEYS).toEqual(["moisture_mitigation"]);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Plywood / OSB", "Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down", "Floating / click"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Carpet tile"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Plywood / OSB"],
          subfloor_condition: ["Moisture concerns"],
        }),
      ),
    ).toBe(false);

    const gluePlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(gluePlywood).not.toContain("moisture_mitigation");
    expect(gluePlywood).not.toContain("vapor_barrier");
    expect(gluePlywood).toContain("adhesive");
    expect(gluePlywood).toContain("moisture_test");

    const overlayPlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(overlayPlywood).not.toContain("moisture_mitigation");
    expect(overlayPlywood).toContain("moisture_test");

    const glueConcrete = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Concrete"],
    });
    expect(glueConcrete).toContain("moisture_mitigation");
    expect(glueConcrete).toContain("vapor_barrier");
    expect(glueConcrete).toContain("moisture_test");

    const unansweredSub = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(unansweredSub).toContain("moisture_mitigation");
    expect(unansweredSub).toContain("substrate");

    const mixedSub = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB", "Concrete"],
    });
    expect(mixedSub).toContain("moisture_mitigation");

    const moistureFlag = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(moistureFlag).toContain("moisture_mitigation");

    const vinylPlywood = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      substrate: ["Plywood / OSB"],
    });
    expect(vinylPlywood).not.toContain("moisture_mitigation");
    expect(vinylPlywood).toContain("adhesive");
    expect(vinylPlywood).toContain("moisture_test");

    const carpetTilePlywood = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      substrate: ["Plywood / OSB"],
    });
    expect(carpetTilePlywood).not.toContain("moisture_mitigation");
    expect(carpetTilePlywood).toContain("moisture_test");
    expect(carpetTilePlywood).toContain("adhesive");
    expect(carpetTilePlywood).not.toContain("vapor_barrier");

    const glueCarpetPlywood = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(glueCarpetPlywood).not.toContain("moisture_mitigation");
    expect(glueCarpetPlywood).toContain("moisture_test");
    expect(glueCarpetPlywood).toContain("adhesive");

    const mixedFloat = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down", "Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(mixedFloat).not.toContain("moisture_mitigation");
    expect(mixedFloat).not.toContain("vapor_barrier");
    expect(mixedFloat).toContain("attached_pad");

    expect(knowledgeHelpFor({ key: "moisture_mitigation" }, emptyInstallContext())).toMatch(
      /Exclusive glue-down or carpet tile over plywood also hides this/,
    );
    expect(knowledgeHelpFor({ key: "moisture_test" }, emptyInstallContext())).toMatch(
      /Exclusive glue-down or carpet tile over plywood hides Aqua bar/,
    );
  });

  it("0277 unkeyed underlayment does not clone carpet sq ft onto mixed LVP foam", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0277_flooring_knowledge_underlayment_cover.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0277_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/cloning pad onto LVP/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Quantity follows carpet rooms on a mixed job/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /Unkeyed extra underlayment on a mixed job stays 0/,
    );
  });

  it("0278 hard-surface foam bills square feet, not pad yards", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0278_flooring_knowledge_underlayment_units.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0278_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/hs_underlayment is not converted to yards/);
    expect(sql).toMatch(/Do not invent a 30-yard foam roll/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect([...SQYD_CATEGORIES]).toEqual(["carpet", "vinyl", "underlayment"]);
    expect(billsBySquareYard("underlayment")).toBe(true);
    expect(billingUnitForCategory("underlayment")).toBe("sqyd");

    expect(areaBillsBySquareYard({ category: "underlayment", key: "hs_underlayment" })).toBe(false);
    expect(areaBillsBySquareYard({ category: "underlayment", key: "carpet_pad" })).toBe(true);
    expect(areaBillsBySquareYard({ category: "underlayment" })).toBe(true);
    expect(areaBillsBySquareYard({ category: "lvp" })).toBe(false);
    expect(areaBillsBySquareYard({ category: "carpet" })).toBe(true);
    expect(
      areaBillsBySquareYard({
        category: "underlayment",
        key: "hs_underlayment",
        productUnit: "sqyd",
      }),
    ).toBe(true);
    expect(
      areaBillsBySquareYard({
        category: "underlayment",
        key: "carpet_pad",
        productUnit: "sqft",
      }),
    ).toBe(false);
    expect(
      areaBillsBySquareYard({
        category: "underlayment",
        key: "hs_underlayment",
        productUnit: "SY",
      }),
    ).toBe(true);
    expect(billingUnitForArea({ category: "underlayment", key: "hs_underlayment" })).toBe("sqft");
    expect(billingUnitForArea({ category: "underlayment", key: "carpet_pad" })).toBe("sqyd");

    expect(
      areaDerivedMaterialQty({
        family: "other",
        measuredSqft: 270,
        billingUnit: billingUnitForArea({
          category: "underlayment",
          key: "hs_underlayment",
          productUnit: "sqft",
        }),
        productUnit: "sqft",
      }),
    ).toBe(270);
    expect(
      areaDerivedMaterialQty({
        family: "other",
        measuredSqft: 270,
        billingUnit: billingUnitForArea({
          category: "underlayment",
          key: "carpet_pad",
          productUnit: "sqyd",
        }),
        productUnit: "sqyd",
      }),
    ).toBe(30);

    expect(padRollCount("underlayment", 270, "sqft")).toBe(0);
    expect(padRollCount("underlayment", 30, "sqyd")).toBe(1);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Billed in square yards unless the SKU itself is sold by the square foot/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Do not invent a 30-yard foam roll/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /Billed in square feet unless the SKU itself is sold by the square yard/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /this question is foam/,
    );

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/areaBillsBySquareYard/);
    expect(q).not.toMatch(/function billing\(category: string\)/);
    expect(q).toMatch(/q\.key === "hs_underlayment"/);
    expect(q).toMatch(/key: q\.key/);

    const units = readFileSync(join(root, "src/lib/units.ts"), "utf8");
    expect(units).toMatch(/areaBillsBySquareYard/);
    expect(units).toMatch(/SQYD_CATEGORIES = \["carpet", "vinyl", "underlayment"\]/);
  });

  it("0279 keys bulk pickup so overlay can hide it on new construction", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0279_flooring_knowledge_bulk_pickup.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0279_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Key it as bulk_pickup/);
    expect(sql).toMatch(/Do NOT SQL-gate bulk_pickup on work_type/);
    expect(sql).toMatch(/Do NOT SQL-gate bulk_pickup on demo_disposal/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*work_type.*bulk_pickup|bulk_pickup.*show_if.*work_type/,
    );

    expect(knowledgeQuestionByKey("bulk_pickup")?.purpose).toBe("SCHEDULING");
    expect(knowledgeQuestionByKey("bulk_pickup")?.phase).toBe("existing");
    expect(knowledgeQuestionByKey("bulk_pickup")?.require).toEqual({
      key: "demo_disposal",
      in: ["Placed at curb"],
    });
    expect(REMOVAL_QUESTION_KEYS).toContain("bulk_pickup");
    expect(reviewBucketForQuestion({ key: "bulk_pickup" })).toBe("removal");
    expect(knowledgeHelpFor({ key: "bulk_pickup" }, emptyInstallContext())).toMatch(
      /Municipal bulk pickup day/,
    );
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(
      /bulk pickup day/,
    );

    const unanswered = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
    });
    expect(unanswered).toContain("demo_disposal");
    expect(unanswered).not.toContain("bulk_pickup");

    const curb = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
      demo_disposal: ["Placed at curb"],
    });
    expect(curb).toContain("bulk_pickup");
    expect(curb.indexOf("demo_disposal")).toBeLessThan(curb.indexOf("bulk_pickup")!);

    const haul = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
      demo_disposal: ["Haul away"],
    });
    expect(haul).toContain("demo_disposal");
    expect(haul).not.toContain("bulk_pickup");

    const neu = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["New construction"],
      hs_demo: ["Carpet"],
      demo_disposal: ["Placed at curb"],
    });
    expect(neu).not.toContain("bulk_pickup");
    expect(neu).not.toContain("demo_disposal");
  });

  it("0280 exclusive wall tile hides site AC/heat; occupancy and appliances stay", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0280_flooring_knowledge_wall_climate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0280_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/a backsplash is not a hardwood acclimation job/);
    expect(sql).toMatch(/Do NOT SQL-gate climate_control on tile_application/);
    expect(sql).toMatch(/Do NOT SQL-gate acclimation on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*tile_application.*climate_control|climate_control.*show_if.*tile_application/,
    );

    expect(TILE_WALL_HIDES_KEYS).toEqual(expect.arrayContaining(["climate_control", "acclimation"]));
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /Site AC\/heat and acclimation hide too/,
    );
    expect(knowledgeHelpFor({ key: "climate_control" }, emptyInstallContext())).toMatch(
      /a backsplash is not a hardwood acclimation job/,
    );
    expect(knowledgeHelpFor({ key: "acclimation" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile hides this/,
    );

    const unanswered = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
    });
    expect(unanswered).toContain("climate_control");
    expect(unanswered).toContain("occupancy");
    expect(unanswered).toContain("appliances");
    expect(unanswered).toContain("wet_area");

    const wall = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      install_method: ["Glue-down"],
    });
    expect(wall).toContain("occupancy");
    expect(wall).toContain("appliances");
    expect(wall).toContain("wet_area");
    expect(wall).toContain("delivery_scope");
    expect(wall).toContain("access_conditions");
    expect(wall).not.toContain("climate_control");
    expect(wall).not.toContain("acclimation");

    const floor = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
    });
    expect(floor).toContain("climate_control");

    const mixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      tile_application: ["Wall"],
      install_method: ["Glue-down"],
    });
    expect(mixed).toContain("climate_control");
    expect(mixed).toContain("acclimation");
  });

  it("0281 leftover AC/heat yes-no stay off the overlay; climate_control is SOT", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0281_flooring_knowledge_legacy_climate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0281_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/climate_control is the source of truth/);
    expect(sql).toMatch(/Do NOT SQL-gate climate_control on install_method/);
    expect(sql).toMatch(/Do NOT SQL-gate climate_control on tile_application/);
    expect(sql).toMatch(/Do NOT drop legacy Yes reading from climateControlConfirmed/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*install_method.*climate_control|climate_control.*show_if.*install_method/,
    );
    expect(sql).not.toMatch(
      /show_if.*tile_application.*climate_control|climate_control.*show_if.*tile_application/,
    );
    expect(sql).toMatch(/e3bcf27d-cddd-44ee-8fef-70ff38056649/);
    expect(sql).not.toMatch(/4f2ec418-e4bf-4019-bf99-65dbc5de025f/);

    expect([...MERGED_CLIMATE_HIDES_KEYS]).toEqual(["ac_available", "heat_available"]);
    expect(knowledgeQuestionByKey("ac_available")?.key).toBe("ac_available");
    expect(knowledgeQuestionByKey("heat_available")?.key).toBe("heat_available");
    expect(knowledgeHelpFor({ key: "climate_control" }, emptyInstallContext())).toMatch(
      /climate_control is the source of truth/,
    );
    expect(knowledgeHelpFor({ key: "climate_control" }, emptyInstallContext())).toMatch(
      /Legacy AC\/heat yes-no answers still count/,
    );

    expect(
      questionApplies({ key: "ac_available", config: {} }, {}, emptyInstallContext()),
    ).toBe(false);
    expect(
      questionApplies({ key: "heat_available", config: {} }, {}, emptyInstallContext()),
    ).toBe(false);
    expect(
      questionApplies({ key: "climate_control", config: {} }, {}, emptyInstallContext()),
    ).toBe(true);

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("climate_control");
    expect(stretch).not.toContain("ac_available");
    expect(stretch).not.toContain("heat_available");

    const lam = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(lam).toContain("climate_control");
    expect(lam).not.toContain("ac_available");
    expect(lam).not.toContain("heat_available");

    const hardwood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
    });
    expect(hardwood).toContain("climate_control");
    expect(hardwood).not.toContain("ac_available");
    expect(hardwood).not.toContain("heat_available");

    const unanswered = visibleKnowledgeKeys({ project_type: ["Hard surface"] });
    expect(unanswered).toContain("climate_control");
    expect(unanswered).not.toContain("ac_available");
    expect(unanswered).not.toContain("heat_available");

    const wall = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
    });
    expect(wall).not.toContain("climate_control");
    expect(wall).not.toContain("ac_available");
    expect(wall).not.toContain("heat_available");

    expect(
      climateControlConfirmed(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          ac_available: ["Yes"],
          heat_available: ["Yes"],
        }),
      ),
    ).toBe(true);
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          ac_available: ["Yes"],
          heat_available: ["Yes"],
        }),
      ).some((w) => w.id === "climate"),
    ).toBe(false);
  });

  it("0282 leftover Placed-on-the-curb yes-no stays off the overlay; demo_disposal is SOT", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0282_flooring_knowledge_legacy_curb.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0282_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/demo_disposal is the source of truth/);
    expect(sql).toMatch(/Do NOT SQL-gate bulk_pickup on demo_disposal/);
    expect(sql).toMatch(/Do NOT SQL-gate demo_disposal on carpet_curb/);
    expect(sql).toMatch(/Do NOT drop leftover carpet_curb from review SPECIAL_KEYS/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*demo_disposal.*carpet_curb|carpet_curb.*show_if.*demo_disposal/,
    );
    expect(sql).toMatch(/a07eb68f-91b6-4fe7-8237-bb0e9986076c/);
    expect(sql).not.toMatch(/81a746cb-5374-46d2-b828-c7f0053b3c8f/);

    expect([...MERGED_CURB_HIDES_KEYS]).toEqual(["carpet_curb"]);
    expect(knowledgeQuestionByKey("carpet_curb")?.key).toBe("carpet_curb");
    expect(reviewBucketForQuestion({ key: "carpet_curb" })).toBe("specials");
    expect(knowledgeHelpFor({ key: "demo_disposal" }, emptyInstallContext())).toMatch(
      /demo_disposal is the source of truth/,
    );
    expect(knowledgeHelpFor({ key: "bulk_pickup" }, emptyInstallContext())).toMatch(
      /Placed at curb on demo_disposal/,
    );

    expect(
      questionApplies({ key: "carpet_curb", config: {} }, { project_type: ["Carpet"] }, emptyInstallContext()),
    ).toBe(false);
    expect(
      questionApplies(
        { key: "demo_disposal", config: {} },
        { project_type: ["Carpet"], hs_demo: ["Carpet"] },
        emptyInstallContext(),
      ),
    ).toBe(true);

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("demo_disposal");
    expect(stretch).not.toContain("carpet_curb");

    const curb = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
      demo_disposal: ["Placed at curb"],
    });
    expect(curb).toContain("demo_disposal");
    expect(curb).toContain("bulk_pickup");
    expect(curb).not.toContain("carpet_curb");

    const haul = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
      demo_disposal: ["Haul away"],
    });
    expect(haul).toContain("demo_disposal");
    expect(haul).not.toContain("bulk_pickup");
    expect(haul).not.toContain("carpet_curb");

    const lvp = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(lvp).not.toContain("carpet_curb");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Stretch-in"],
    });
    expect(mixed).toContain("demo_disposal");
    expect(mixed).not.toContain("carpet_curb");
  });

  it("0283 exclusive carpet hides hard-surface Install method; mixed Carpet + LVP still asks", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0283_flooring_knowledge_carpet_install_method.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0283_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Exclusive carpet hides this hard-surface method picker/);
    expect(sql).toMatch(/Do NOT SQL-gate install_method on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate install_method on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*install_method|install_method.*show_if.*carpet_install/,
    );

    expect(CARPET_ONLY_HIDES_KEYS).toEqual(expect.arrayContaining(["install_method"]));
    expect(
      jobIsExclusiveCarpetOnly(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveCarpetOnly(
        installContextFromValByKey({
          project_type: ["Carpet", "Hard surface"],
          surface_type: ["LVP / LVT"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveCarpetOnly(
        installContextFromValByKey({ project_type: ["Hard surface"] }),
      ),
    ).toBe(false);
    expect(jobIsExclusiveCarpetOnly(emptyInstallContext())).toBe(false);

    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /hides the hard-surface Install method picker/,
    );
    expect(
      knowledgeHelpFor(
        { key: "install_method" },
        installContextFromValByKey({ project_type: ["Carpet"] }),
      ),
    ).toMatch(/Exclusive carpet hides this hard-surface method picker/);

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      install_method: ["Floating / click"],
    });
    expect(stretch).toContain("carpet_install");
    expect(stretch).not.toContain("install_method");
    expect(stretch).not.toContain("laminate_expansion");

    const glue = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("carpet_install");
    expect(glue).toContain("adhesive");
    expect(glue).not.toContain("install_method");

    const unansweredHs = visibleKnowledgeKeys({ project_type: ["Hard surface"] });
    expect(unansweredHs).toContain("install_method");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Stretch-in"],
    });
    expect(mixed).toContain("install_method");
    expect(mixed).toContain("carpet_install");

    const unanswered = visibleKnowledgeKeys({});
    expect(unanswered).toContain("install_method");
  });

  it("0284 exclusive carpet hides Surface type; mixed Carpet + LVP still asks", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0284_flooring_knowledge_carpet_surface.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0284_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Exclusive carpet hides this/);
    expect(sql).toMatch(/Do NOT SQL-gate surface_type on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate surface_type on project_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*surface_type|surface_type.*show_if.*carpet_install/,
    );

    expect([...CARPET_ONLY_HIDES_KEYS]).toEqual(["install_method", "surface_type"]);
    expect(knowledgeHelpFor({ key: "surface_type" }, emptyInstallContext())).toMatch(
      /Exclusive carpet hides this/,
    );
    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /Install method picker and Surface type/,
    );

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("carpet_install");
    expect(stretch).not.toContain("surface_type");
    expect(stretch).not.toContain("install_method");

    const leftoverHs = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      surface_type: ["Hardwood"],
    });
    expect(leftoverHs).not.toContain("surface_type");
    expect(leftoverHs).not.toContain("install_method");

    const unansweredHs = visibleKnowledgeKeys({ project_type: ["Hard surface"] });
    expect(unansweredHs).toContain("surface_type");
    expect(unansweredHs).toContain("install_method");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Stretch-in"],
    });
    expect(mixed).toContain("surface_type");
    expect(mixed).toContain("install_method");
    expect(mixed).toContain("carpet_install");

    const unanswered = visibleKnowledgeKeys({});
    expect(unanswered).toContain("surface_type");
  });

  it("0285 leftover Surface type on exclusive carpet does not reopen HS follow-ups", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0285_flooring_knowledge_carpet_surface_leftover.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0285_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not switch finish, fasteners, vapor/);
    expect(sql).toMatch(/Do NOT SQL-gate hardwood_finish on project_type/);
    expect(sql).toMatch(/Do NOT SQL-gate surface_type on carpet_install/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*project_type.*hardwood_finish|hardwood_finish.*show_if.*project_type/,
    );

    const leftoverCtx = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      surface_type: ["Hardwood"],
    });
    expect(jobIsExclusiveCarpetOnly(leftoverCtx)).toBe(true);
    expect(jobHasHardSurfaceInstallScope(leftoverCtx)).toBe(false);
    expect(leftoverCtx.families).toEqual(["carpet"]);
    expect(
      knowledgeWarnings(leftoverCtx).some((w) => w.id === "carpet-surface-leftover"),
    ).toBe(true);

    expect(knowledgeHelpFor({ key: "surface_type" }, emptyInstallContext())).toMatch(
      /do not reopen finish, fasteners, vapor/,
    );
    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /Leftover Hardwood on Surface type does not reopen/,
    );

    const leftover = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      surface_type: ["Hardwood"],
    });
    expect(leftover).toContain("carpet_install");
    expect(leftover).toContain("tack_strip");
    expect(leftover).not.toContain("surface_type");
    expect(leftover).not.toContain("install_method");
    expect(leftover).not.toContain("hardwood_finish");
    expect(leftover).not.toContain("hardwood_fasteners");
    expect(leftover).not.toContain("adhesive");
    expect(leftover).not.toContain("vapor_barrier");
    expect(leftover).not.toContain("hs_direction");
    expect(leftover).not.toContain("hs_plank_stairs");
    expect(leftover).not.toContain("hs_transitions");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["Hardwood"],
      carpet_install: ["Stretch-in"],
    });
    expect(mixed).toContain("surface_type");
    expect(mixed).toContain("hardwood_finish");
    expect(mixed).toContain("carpet_install");

    const unansweredProject = visibleKnowledgeKeys({
      surface_type: ["Hardwood"],
    });
    expect(unansweredProject).toContain("hardwood_finish");
    expect(unansweredProject).toContain("surface_type");

    const assigned = withProductFamilies(leftoverCtx, ["hardwood"]);
    expect(assigned.families).toEqual(["carpet", "hardwood"]);
    expect(jobHasHardSurfaceInstallScope(assigned)).toBe(true);
    expect(knowledgeWarnings(assigned).some((w) => w.id === "unscoped-products")).toBe(true);

    const leftoverLvp = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      surface_type: ["LVP / LVT"],
    });
    expect(leftoverLvp).not.toContain("attached_pad");
    expect(leftoverLvp).not.toContain("laminate_expansion");
    expect(leftoverLvp).not.toContain("hs_underlayment");
  });

  it("0286 leftover Carpet install on exclusive HS does not reopen adhesive", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0286_flooring_knowledge_hs_carpet_leftover.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0286_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not switch adhesive, moisture test, or acclimation/);
    expect(sql).toMatch(/Do NOT SQL-gate adhesive on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_install on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*adhesive|adhesive.*show_if.*carpet_install/,
    );

    const leftoverCtx = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      carpet_install: ["Glue-down"],
    });
    expect(jobHasCarpetInstallScope(leftoverCtx)).toBe(false);
    expect(jobHasHardSurfaceInstallScope(leftoverCtx)).toBe(true);
    expect(leftoverCtx.families).toEqual(["lvp"]);
    expect(leftoverCtx.systems).toEqual(["floating"]);
    expect(leftoverCtx.installLabels).toEqual(["Floating / click"]);
    expect(
      knowledgeWarnings(leftoverCtx).some((w) => w.id === "hs-carpet-leftover"),
    ).toBe(true);

    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /Leftover Glue-down \/ Carpet tile on exclusive LVP/,
    );

    const leftoverGlue = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      carpet_install: ["Glue-down"],
    });
    expect(leftoverGlue).toContain("attached_pad");
    expect(leftoverGlue).not.toContain("adhesive");
    expect(leftoverGlue).not.toContain("moisture_test");
    expect(leftoverGlue).not.toContain("acclimation");
    expect(leftoverGlue).not.toContain("carpet_install");
    expect(leftoverGlue).not.toContain("tack_strip");

    const leftoverTile = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      carpet_install: ["Carpet tile"],
    });
    expect(leftoverTile).not.toContain("adhesive");
    expect(leftoverTile).not.toContain("moisture_test");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      carpet_install: ["Glue-down"],
    });
    expect(mixed).toContain("carpet_install");
    expect(mixed).toContain("adhesive");
    expect(mixed).toContain("attached_pad");

    const exclusiveCarpetGlue = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(exclusiveCarpetGlue).toContain("adhesive");
    expect(exclusiveCarpetGlue).toContain("carpet_install");

    const assigned = withProductFamilies(leftoverCtx, ["carpet"]);
    expect(assigned.families).toEqual(["lvp", "carpet"]);
    expect(jobHasCarpetInstallScope(assigned)).toBe(true);
  });

  it("0287 extra pad area is measured sq ft, not a roll or billing unit", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0287_flooring_knowledge_extra_pad_measured.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0287_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/MEASURED sq ft/);
    expect(sql).toMatch(/not a 30-yard roll/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(EXTRA_AREA_MEASURED_LABEL).toBe("Measured sq ft");
    expect(EXTRA_AREA_MEASURED_PLACEHOLDER).toBe("measured sq ft");
    expect(EXTRA_AREA_MEASURED_HINT).toMatch(/not a 30-yard roll/);
    expect(EXTRA_AREA_MEASURED_HINT).toMatch(/not the billing unit/);
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/EXTRA_AREA_MEASURED_LABEL/);
    expect(q).toMatch(/EXTRA_AREA_MEASURED_PLACEHOLDER/);
    expect(q).toMatch(/EXTRA_AREA_MEASURED_HINT/);
    expect(q).toMatch(/`sqft` is MEASURED area, not the order/);
  });

  it("0288 leftover Stairs yes/no hides once family-specific stairs are live", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0288_flooring_knowledge_dead_stair_gate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0288_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/leftover synthesizer/);
    expect(sql).toMatch(/Do NOT SQL-gate stair_landings on carpet_stairs/);
    expect(sql).toMatch(/Do NOT SQL-gate stairs on carpet_install/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*stairs|stairs.*show_if.*carpet_install/,
    );

    expect([...DEAD_STAIR_GATE_HIDES_KEYS]).toEqual(["stairs"]);
    expect(
      jobHidesDeadStairGate(installContextFromValByKey({ project_type: ["Carpet"] })),
    ).toBe(true);
    expect(
      jobHidesDeadStairGate(installContextFromValByKey({ project_type: ["Hard surface"] })),
    ).toBe(true);
    expect(jobHidesDeadStairGate(emptyInstallContext())).toBe(false);
    expect(knowledgeHelpFor({ key: "stairs" }, emptyInstallContext())).toMatch(
      /leftover yes\/no hides once Carpet stairs/,
    );

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("carpet_stairs");
    expect(stretch).not.toContain("stairs");

    const tile = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(tile).toContain("carpet_tile_stairs");
    expect(tile).not.toContain("stairs");

    const lvp = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(lvp).toContain("hs_plank_stairs");
    expect(lvp).not.toContain("stairs");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Stretch-in"],
    });
    expect(mixed).toContain("carpet_stairs");
    expect(mixed).toContain("hs_plank_stairs");
    expect(mixed).not.toContain("stairs");

    const unanswered = visibleKnowledgeKeys({});
    expect(unanswered).toContain("stairs");

    const after = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      carpet_stairs: ["Yes"],
    });
    expect(after).not.toContain("stairs");
    expect(after).toContain("carpet_stairs");
    expect(after).toContain("stair_landings");
  });

  it("0289 exclusive carpet tile hides gripper/flat metals; stretch-in and glue-down keep them", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0289_flooring_knowledge_carpet_tile_metals.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0289_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/binder bars for roll goods/);
    expect(sql).toMatch(/Do NOT SQL-gate metals_needed on carpet_install/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*metals_needed|metals_needed.*show_if.*carpet_install/,
    );

    expect([...CARPET_TILE_HIDES_KEYS]).toEqual([
      "pattern_match",
      "pattern_repeat",
      "carpet_direction",
      "metals_needed",
      "metals_qty",
      "metal_type",
      "metal_color",
    ]);
    expect(knowledgeHelpFor({ key: "metals_needed" }, emptyInstallContext())).toMatch(
      /Exclusive carpet tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "metals_qty" }, emptyInstallContext())).toMatch(
      /Exclusive carpet tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "metal_type" }, emptyInstallContext())).toMatch(
      /Exclusive carpet tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "metal_color" }, emptyInstallContext())).toMatch(
      /Exclusive carpet tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "carpet_install" }, emptyInstallContext())).toMatch(
      /hides gripper\/flat metals/,
    );

    const unanswered = visibleKnowledgeKeys({ project_type: ["Carpet"] });
    expect(unanswered).toContain("metals_needed");
    expect(unanswered).toContain("metals_qty");

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("metals_needed");

    const glue = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(glue).toContain("metals_needed");

    const tile = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(tile).not.toContain("metals_needed");
    expect(tile).not.toContain("metals_qty");
    expect(tile).not.toContain("metal_type");
    expect(tile).not.toContain("metal_color");
    expect(tile).toContain("adhesive");
    expect(tile).toContain("carpet_tile_stairs");

    const leftoverYes = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      metals_needed: ["Yes"],
    });
    expect(leftoverYes).not.toContain("metals_needed");
    expect(leftoverYes).not.toContain("metals_qty");
    expect(leftoverYes).not.toContain("metal_type");
    expect(leftoverYes).not.toContain("metal_color");

    const mixedStretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in", "Carpet tile"],
    });
    expect(mixedStretch).toContain("metals_needed");

    const mixedLvp = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Carpet tile"],
    });
    expect(mixedLvp).not.toContain("metals_needed");
    expect(mixedLvp).toContain("hs_transitions");

    const leftoverHs = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      install_method: ["Floating / click"],
    });
    expect(leftoverHs).not.toContain("metals_needed");

    const walkTile = walk({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(walkTile).not.toContain("metals_needed");
    expect(walkTile).not.toContain("metals_qty");

    const walkStretch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(walkStretch).toContain("metals_needed");
    expect(walkStretch).not.toContain("metals_qty");

    const walkGlue = walk({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
      metals_needed: ["Yes"],
    });
    expect(walkGlue).toContain("metals_needed");
    expect(walkGlue).toContain("metals_qty");
  });

  it("0290 exclusive glue over existing flooring hides 6-mil and Aqua bar; moisture test still asks", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0290_flooring_knowledge_glue_existing_vapor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0290_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/existing-floor primer/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on substrate/);
    expect(sql).toMatch(/Do NOT SQL-gate moisture_mitigation on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*vapor_barrier|vapor_barrier.*show_if.*substrate/,
    );
    expect(sql).not.toMatch(
      /show_if.*substrate.*moisture_mitigation|moisture_mitigation.*show_if.*substrate/,
    );

    expect([...GLUE_EXISTING_VAPOR_HIDES_KEYS]).toEqual(["vapor_barrier"]);
    expect([...EXISTING_FLOOR_AQUA_HIDES_KEYS]).toEqual(["moisture_mitigation"]);
    expect(labelsAreExistingFlooringOnly(["Existing flooring"])).toBe(true);
    expect(labelsAreExistingFlooringOnly(["existing flooring"])).toBe(true);
    expect(labelsAreExistingFlooringOnly(["Plywood / OSB"])).toBe(false);
    expect(labelsAreExistingFlooringOnly(["Existing flooring", "Concrete"])).toBe(false);
    expect(labelsAreExistingFlooringOnly(["Unknown / field verify"])).toBe(false);
    expect(labelsAreExistingFlooringOnly([])).toBe(false);

    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Glue-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down", "Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Existing flooring", "Concrete"],
        }),
      ),
    ).toBe(false);

    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Carpet tile"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Existing flooring", "Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down", "Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Existing flooring"],
          subfloor_condition: ["Moisture concerns"],
        }),
      ),
    ).toBe(false);

    const glueExisting = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Existing flooring"],
    });
    expect(glueExisting).not.toContain("vapor_barrier");
    expect(glueExisting).not.toContain("moisture_mitigation");
    expect(glueExisting).toContain("adhesive");
    expect(glueExisting).toContain("moisture_test");

    const glueCarpetExisting = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
      substrate: ["Existing flooring"],
    });
    expect(glueCarpetExisting).not.toContain("vapor_barrier");
    expect(glueCarpetExisting).not.toContain("moisture_mitigation");
    expect(glueCarpetExisting).toContain("adhesive");
    expect(glueCarpetExisting).toContain("moisture_test");

    const tileExisting = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      substrate: ["Existing flooring"],
    });
    expect(tileExisting).not.toContain("vapor_barrier");
    expect(tileExisting).not.toContain("moisture_mitigation");
    expect(tileExisting).toContain("moisture_test");
    expect(tileExisting).toContain("adhesive");

    const floatExisting = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Existing flooring"],
    });
    expect(floatExisting).not.toContain("vapor_barrier");
    expect(floatExisting).not.toContain("moisture_mitigation");

    const glueConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Concrete"],
    });
    expect(glueConcrete).toContain("vapor_barrier");
    expect(glueConcrete).toContain("moisture_mitigation");

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
    });
    expect(unanswered).toContain("vapor_barrier");
    expect(unanswered).toContain("moisture_mitigation");
    expect(unanswered).toContain("substrate");

    const mixedSub = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Existing flooring", "Concrete"],
    });
    expect(mixedSub).toContain("vapor_barrier");
    expect(mixedSub).toContain("moisture_mitigation");

    const mixedFloat = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down", "Floating / click"],
      substrate: ["Existing flooring"],
    });
    expect(mixedFloat).not.toContain("vapor_barrier");
    expect(mixedFloat).not.toContain("moisture_mitigation");
    expect(mixedFloat).toContain("attached_pad");

    const moistureFlag = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Existing flooring"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(moistureFlag).not.toContain("vapor_barrier");
    expect(moistureFlag).toContain("moisture_mitigation");
    expect(moistureFlag).toContain("moisture_test");

    const leftoverPlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Existing flooring"],
      carpet_install: ["Stretch-in"],
    });
    expect(leftoverPlywood).not.toContain("vapor_barrier");
    expect(leftoverPlywood).not.toContain("moisture_mitigation");

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Exclusive glue-down over existing flooring hides this/,
    );
    expect(knowledgeHelpFor({ key: "moisture_mitigation" }, emptyInstallContext())).toMatch(
      /Exclusive glue-down or carpet tile over existing flooring also hides this/,
    );

    const walkGlue = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Existing flooring"],
    });
    expect(walkGlue).not.toContain("vapor_barrier");
    expect(walkGlue).not.toContain("moisture_mitigation");
    expect(walkGlue).toContain("moisture_test");
  });

  it("0291 exclusive plywood hides 6-mil for floating, glue, and mixed; concrete stays open", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0291_flooring_knowledge_wood_deck_vapor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0291_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/not a wood-deck underlayment/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*vapor_barrier|vapor_barrier.*show_if.*substrate/,
    );

    expect([...WOOD_DECK_VAPOR_HIDES_KEYS]).toEqual(["vapor_barrier"]);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down", "Floating / click"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Laminate"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnGlueWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down", "Floating / click"],
          substrate: ["Plywood / OSB", "Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnWoodDeck(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          substrate: ["Plywood / OSB"],
        }),
      ),
    ).toBe(false);

    const floatingPlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(floatingPlywood).not.toContain("vapor_barrier");
    expect(floatingPlywood).toContain("attached_pad");

    const overlayGlue = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(overlayGlue).not.toContain("vapor_barrier");
    expect(overlayGlue).toContain("adhesive");
    expect(overlayGlue).toContain("moisture_test");

    const mixedFloat = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down", "Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(mixedFloat).not.toContain("vapor_barrier");
    expect(mixedFloat).toContain("attached_pad");
    expect(mixedFloat).toContain("adhesive");

    const laminatePlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      substrate: ["Plywood / OSB"],
    });
    expect(laminatePlywood).not.toContain("vapor_barrier");
    expect(laminatePlywood).toContain("attached_pad");

    const floatingConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Concrete"],
    });
    expect(floatingConcrete).toContain("vapor_barrier");
    expect(floatingConcrete).toContain("attached_pad");

    const glueConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Concrete"],
    });
    expect(glueConcrete).toContain("vapor_barrier");
    expect(glueConcrete).toContain("adhesive");

    const unansweredSub = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(unansweredSub).toContain("vapor_barrier");
    expect(unansweredSub).toContain("substrate");

    const unansweredMethod = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      substrate: ["Plywood / OSB"],
    });
    expect(unansweredMethod).toContain("vapor_barrier");
    expect(unansweredMethod).toContain("install_method");

    const mixedSub = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB", "Concrete"],
    });
    expect(mixedSub).toContain("vapor_barrier");

    const leftoverStretch = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB"],
      carpet_install: ["Stretch-in"],
    });
    expect(leftoverStretch).not.toContain("vapor_barrier");

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Exclusive plywood also hides this/,
    );

    const walkFloat = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(walkFloat).not.toContain("vapor_barrier");
    expect(walkFloat).toContain("attached_pad");
  });

  it("0292 exclusive existing flooring hides 6-mil for floating, glue, and mixed; concrete stays open", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0292_flooring_knowledge_existing_floor_vapor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0292_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/not an existing-floor underlayment/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*vapor_barrier|vapor_barrier.*show_if.*substrate/,
    );

    expect([...EXISTING_FLOOR_VAPOR_HIDES_KEYS]).toEqual(["vapor_barrier"]);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down", "Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Laminate"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Glue-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          substrate: ["Existing flooring", "Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(false);

    const floatingExisting = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Existing flooring"],
    });
    expect(floatingExisting).not.toContain("vapor_barrier");
    expect(floatingExisting).toContain("attached_pad");

    const overlayGlue = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Existing flooring"],
    });
    expect(overlayGlue).not.toContain("vapor_barrier");
    expect(overlayGlue).toContain("adhesive");
    expect(overlayGlue).toContain("moisture_test");

    const mixedFloat = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down", "Floating / click"],
      substrate: ["Existing flooring"],
    });
    expect(mixedFloat).not.toContain("vapor_barrier");
    expect(mixedFloat).toContain("attached_pad");
    expect(mixedFloat).toContain("adhesive");

    const laminateExisting = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      substrate: ["Existing flooring"],
    });
    expect(laminateExisting).not.toContain("vapor_barrier");
    expect(laminateExisting).toContain("attached_pad");

    const floatingConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Concrete"],
    });
    expect(floatingConcrete).toContain("vapor_barrier");
    expect(floatingConcrete).toContain("attached_pad");

    const glueConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Glue-down"],
      substrate: ["Concrete"],
    });
    expect(glueConcrete).toContain("vapor_barrier");
    expect(glueConcrete).toContain("adhesive");

    const unansweredSub = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(unansweredSub).toContain("vapor_barrier");
    expect(unansweredSub).toContain("substrate");

    const unansweredMethod = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      substrate: ["Existing flooring"],
    });
    expect(unansweredMethod).toContain("vapor_barrier");
    expect(unansweredMethod).toContain("install_method");

    const mixedSub = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Existing flooring", "Concrete"],
    });
    expect(mixedSub).toContain("vapor_barrier");

    const leftoverStretch = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Existing flooring"],
      carpet_install: ["Stretch-in"],
    });
    expect(leftoverStretch).not.toContain("vapor_barrier");

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Exclusive existing flooring also hides this/,
    );

    const walkFloat = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      substrate: ["Existing flooring"],
    });
    expect(walkFloat).not.toContain("vapor_barrier");
    expect(walkFloat).toContain("attached_pad");
  });

  it("0293 leftover landings hide until a family stair question is Yes", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0293_flooring_knowledge_dead_stair_followups.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0293_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/hidden parent/);
    expect(sql).toMatch(/Do NOT SQL-gate stair_landings on carpet_stairs/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_stairs.*stair_landings|stair_landings.*show_if.*carpet_stairs/,
    );

    expect([...DEAD_STAIR_FOLLOWUP_HIDES_KEYS]).toEqual(["stair_landings", "stair_open_sides"]);
    expect(
      jobHidesDeadStairFollowups(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
        }),
        { carpet_install: ["Stretch-in"] },
      ),
    ).toBe(true);
    expect(
      jobHidesDeadStairFollowups(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          carpet_stairs: ["Yes"],
        }),
        { carpet_stairs: ["Yes"] },
      ),
    ).toBe(false);
    expect(
      jobHidesDeadStairFollowups(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
        }),
        {},
      ),
    ).toBe(true);
    expect(
      jobHidesDeadStairFollowups(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Floating / click"],
          hs_plank_stairs: ["Yes"],
        }),
        { hs_plank_stairs: ["Yes"] },
      ),
    ).toBe(false);
    expect(
      jobHidesDeadStairFollowups(emptyInstallContext(), {}),
    ).toBe(false);
    expect(
      jobHidesDeadStairFollowups(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
        }),
        { stairs: ["Yes"] },
      ),
    ).toBe(true);

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).toContain("carpet_stairs");
    expect(stretch).not.toContain("stairs");
    expect(stretch).not.toContain("stair_landings");
    expect(stretch).not.toContain("stair_open_sides");

    const stretchYes = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      carpet_stairs: ["Yes"],
    });
    expect(stretchYes).toContain("stair_landings");
    expect(stretchYes).toContain("stair_open_sides");

    const leftoverStairs = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      stairs: ["Yes"],
    });
    expect(leftoverStairs).not.toContain("stair_landings");
    expect(leftoverStairs).not.toContain("stair_open_sides");

    const lvp = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(lvp).toContain("hs_plank_stairs");
    expect(lvp).not.toContain("stair_landings");

    const lvpYes = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
      hs_plank_stairs: ["Yes"],
    });
    expect(lvpYes).toContain("stair_landings");
    expect(lvpYes).toContain("stair_open_sides");

    const tile = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(tile).toContain("carpet_tile_stairs");
    expect(tile).not.toContain("stair_landings");

    const tileYes = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
      carpet_tile_stairs: ["Yes"],
    });
    expect(tileYes).toContain("stair_landings");
    expect(tileYes).toContain("carpet_tile_stair_count");

    const unanswered = visibleKnowledgeKeys({});
    expect(unanswered).toContain("stairs");
    expect(unanswered).toContain("stair_landings");
    expect(unanswered).toContain("stair_open_sides");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      carpet_install: ["Stretch-in"],
    });
    expect(mixed).toContain("carpet_stairs");
    expect(mixed).toContain("hs_plank_stairs");
    expect(mixed).not.toContain("stair_landings");

    expect(knowledgeHelpFor({ key: "stair_landings" }, emptyInstallContext())).toMatch(
      /Leftover Stairs yes\/no hides this/,
    );
    expect(knowledgeHelpFor({ key: "stair_open_sides" }, emptyInstallContext())).toMatch(
      /Leftover Stairs yes\/no hides this/,
    );

    const walkBefore = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(walkBefore).not.toContain("stair_landings");
    const walkAfter = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      carpet_stairs: ["Yes"],
    });
    expect(walkAfter).toContain("stair_landings");
    expect(walkAfter).toContain("stair_open_sides");
  });

  it("0294 pad/foam Review takeoff is measured vs billing, not a 30-yard roll", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0294_flooring_knowledge_pad_takeoff.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0294_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/not a 30-yard roll/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(familyFromCatalogCategory("underlayment")).toBe("other");
    expect(padFoamTakeoffLabel({ key: "carpet_pad", category: "underlayment" })).toBe(
      "Carpet pad",
    );
    expect(padFoamTakeoffLabel({ key: "hs_underlayment", category: "underlayment" })).toBe(
      "Underlayment",
    );

    const pad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 450,
      billingUnit: billingUnitForArea({ category: "underlayment", key: "carpet_pad" }),
      takeoffLabel: padFoamTakeoffLabel({ key: "carpet_pad", category: "underlayment" }),
    });
    expect(pad.family).toBe("other");
    expect(pad.takeoffLabel).toBe("Carpet pad");
    expect(takeoffDisplayTitle(pad)).toBe("Carpet pad");
    expect(pad.billingUnit).toBe("sqyd");
    expect(pad.measured.sqft).toBe(450);
    expect(pad.measured.sqydEquivalent).toBe(50);
    expect(pad.wastePct).toBe(0);
    expect(pad.orderBasis).toBe("measured_plus_waste");
    expect(pad.billingQty).toBe(50);
    expect(pad.orderSqft).toBe(450);
    expect(pad.cartons).toBeNull();
    expect(pad.notes.some((n) => /not a 30-yard roll/i.test(n))).toBe(true);
    expect(formatTakeoffStrip(pad)).toMatch(/Billing 50 sq yd/);
    expect(formatTakeoffStrip(pad)).not.toMatch(/60 sq yd/);

    const padCompanion = profileFor("carpet")!.companions.find((c) => c.key === "pad")!;
    expect(companionQty(padCompanion, 450, 0)).toBe(60);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);

    const foam = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 450,
      billingUnit: billingUnitForArea({
        category: "underlayment",
        key: "hs_underlayment",
      }),
      takeoffLabel: padFoamTakeoffLabel({
        key: "hs_underlayment",
        category: "underlayment",
      }),
    });
    expect(foam.billingUnit).toBe("sqft");
    expect(foam.billingQty).toBe(450);
    expect(foam.wastePct).toBe(0);
    expect(foam.cartons).toBeNull();
    expect(foam.notes.some((n) => /not pad yards/i.test(n))).toBe(true);
    expect(formatTakeoffStrip(foam)).toMatch(/Billing 450 sq ft/);
    expect(formatTakeoffStrip(foam)).not.toMatch(/sq yd/);

    const boxed = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 450,
      billingUnit: "sqft",
      sqftPerBox: 20,
      takeoffLabel: "Underlayment",
    });
    expect(boxed.cartons?.cartonCount).toBe(23);
    const noBox = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 450,
      billingUnit: "sqyd",
    });
    expect(noBox.cartons).toBeNull();

    const review = buildSalespersonReview({
      rooms: [],
      products: ["Rebond pad"],
      takeoffs: [pad, foam],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [],
      prep: [],
      accessories: [],
      specials: [],
    });
    expect(review.sections.map((s) => s.title)).toEqual(
      expect.arrayContaining(["Carpet pad takeoff", "Underlayment takeoff"]),
    );
    expect(review.sections.map((s) => s.title)).not.toContain("Other takeoff");
    const notes = reviewToJobNotes(review);
    expect(notes).toMatch(/Carpet pad takeoff:/);
    expect(notes).toMatch(/Billing quantity: 50 sq yd/);
    expect(notes).not.toMatch(/Billing quantity: 60 sq yd/);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Review takeoff shows measured area vs billing yards/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /Review takeoff shows measured area vs billing square feet/,
    );

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/padFoamTakeoffLabel/);
    expect(q).toMatch(/billingUnitForArea/);
    expect(q).toMatch(/takeoffDisplayTitle/);
    expect(q).not.toMatch(/if \(family === "other"\) return;/);
  });

  it("0295 exclusive existing flooring hides Aqua bar for nail/staple/floating hardwood", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0295_flooring_knowledge_existing_floor_aqua.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0295_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/not an existing-floor primer/);
    expect(sql).toMatch(/Do NOT SQL-gate moisture_mitigation on substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*substrate.*moisture_mitigation|moisture_mitigation.*show_if.*substrate/,
    );

    expect([...EXISTING_FLOOR_AQUA_HIDES_KEYS]).toEqual(["moisture_mitigation"]);
    expect(
      jobHidesAquaBarOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Staple-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Engineered hardwood"],
          install_method: ["Floating / click"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesAquaBarOnGlueExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
          substrate: ["Existing flooring"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
          substrate: ["Existing flooring", "Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesAquaBarOnExistingFloor(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Nail-down"],
          substrate: ["Existing flooring"],
          subfloor_condition: ["Moisture concerns"],
        }),
      ),
    ).toBe(false);

    const nail = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Existing flooring"],
    });
    expect(nail).not.toContain("moisture_mitigation");
    expect(nail).not.toContain("vapor_barrier");
    expect(nail).toContain("moisture_test");
    expect(nail).toContain("hardwood_fasteners");

    const staple = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Staple-down"],
      substrate: ["Existing flooring"],
    });
    expect(staple).not.toContain("moisture_mitigation");
    expect(staple).toContain("moisture_test");

    const engFloat = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Floating / click"],
      substrate: ["Existing flooring"],
    });
    expect(engFloat).not.toContain("moisture_mitigation");
    expect(engFloat).not.toContain("vapor_barrier");
    expect(engFloat).toContain("moisture_test");
    expect(engFloat).toContain("attached_pad");

    const nailConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Concrete"],
    });
    expect(nailConcrete).toContain("moisture_mitigation");
    expect(nailConcrete).toContain("moisture_test");

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
    });
    expect(unanswered).toContain("moisture_mitigation");
    expect(unanswered).toContain("moisture_test");

    const mixedSub = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Existing flooring", "Concrete"],
    });
    expect(mixedSub).toContain("moisture_mitigation");

    const moistureFlag = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Existing flooring"],
      subfloor_condition: ["Moisture concerns"],
    });
    expect(moistureFlag).toContain("moisture_mitigation");

    expect(knowledgeHelpFor({ key: "moisture_mitigation" }, emptyInstallContext())).toMatch(
      /Exclusive hardwood nail\/staple\/floating over existing flooring also hides this/,
    );
    expect(knowledgeHelpFor({ key: "moisture_test" }, emptyInstallContext())).toMatch(
      /Exclusive hardwood nail\/staple\/floating over existing flooring also hides Aqua bar/,
    );

    const walkNail = walk({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Existing flooring"],
    });
    expect(walkNail).not.toContain("moisture_mitigation");
    expect(walkNail).toContain("moisture_test");
  });

  it("0296 exclusive new construction hides furniture moving", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0296_flooring_knowledge_new_build_furniture.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0296_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/no furniture to move/);
    expect(sql).toMatch(/Do NOT SQL-gate furniture on work_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*work_type.*furniture|furniture.*show_if.*work_type/);

    expect([...FURNITURE_MOVING_KEYS]).toEqual(["furniture_level", "furniture_heavy"]);
    expect(jobIsExclusiveNewConstruction({})).toBe(false);
    expect(jobIsExclusiveNewConstruction({ work_type: ["New construction"] })).toBe(true);
    expect(
      jobIsExclusiveNewConstruction({
        work_type: ["New construction", "Replacement (tear-out)"],
      }),
    ).toBe(false);
    expect(jobIsNewConstruction({ work_type: ["New construction", "Replacement (tear-out)"] })).toBe(
      true,
    );

    const neu = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["New construction"],
    });
    expect(neu).not.toContain("furniture_level");
    expect(neu).not.toContain("furniture_heavy");
    expect(neu).toContain("occupancy");
    expect(neu).toContain("appliances");
    expect(neu).toContain("doors_shave");
    expect(neu).not.toContain("toilets");

    const occupiedNew = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["New construction"],
      occupancy: ["Occupied"],
    });
    expect(occupiedNew).not.toContain("furniture_level");
    expect(occupiedNew).not.toContain("furniture_heavy");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["New construction", "Replacement (tear-out)"],
    });
    expect(mixed).toContain("furniture_level");
    expect(mixed).toContain("furniture_heavy");

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(unanswered).toContain("furniture_level");
    expect(unanswered).toContain("work_type");

    const replacement = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      work_type: ["Replacement (tear-out)"],
      occupancy: ["Occupied"],
    });
    expect(replacement).toContain("furniture_level");
    expect(replacement).toContain("furniture_heavy");

    expect(knowledgeHelpFor({ key: "furniture_level" }, emptyInstallContext())).toMatch(
      /Exclusive new construction hides this/,
    );
    expect(knowledgeHelpFor({ key: "furniture_heavy" }, emptyInstallContext())).toMatch(
      /Exclusive new construction hides this/,
    );
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(
      /furniture moving/,
    );

    const walkNew = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      work_type: ["New construction"],
    });
    expect(walkNew).not.toContain("furniture_level");
    expect(walkNew).not.toContain("furniture_heavy");
    expect(walkNew).toContain("appliances");
  });

  it("0297 exclusive loose-lay hides 6-mil vapor, including over concrete", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0297_flooring_knowledge_loose_lay_vapor.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0297_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/not a click-floor/);
    expect(sql).toMatch(/Do NOT SQL-gate vapor_barrier on install_method/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*install_method.*vapor_barrier|vapor_barrier.*show_if.*install_method/,
    );

    expect([...LOOSE_LAY_VAPOR_HIDES_KEYS]).toEqual(["vapor_barrier"]);
    expect(
      jobHidesVaporOnLooseLay(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Loose-lay"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVaporOnLooseLay(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Loose-lay", "Floating / click"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnLooseLay(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnLooseLay(
        installContextFromValByKey({
          project_type: ["Carpet", "Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Loose-lay"],
          carpet_install: ["Stretch-in"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnLooseLay(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          install_method: ["Loose-lay"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnLooseLay(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Laminate"],
          install_method: ["Loose-lay"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVaporOnLooseLay(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Loose-lay"],
          substrate: ["Concrete"],
        }),
      ),
    ).toBe(false);

    const looseConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Loose-lay"],
      substrate: ["Concrete"],
    });
    expect(looseConcrete).not.toContain("vapor_barrier");
    expect(looseConcrete).not.toContain("adhesive");
    expect(looseConcrete).not.toContain("attached_pad");
    expect(looseConcrete).not.toContain("moisture_test");

    const mixedFloat = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Loose-lay", "Floating / click"],
      substrate: ["Concrete"],
    });
    expect(mixedFloat).toContain("vapor_barrier");

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      substrate: ["Concrete"],
    });
    expect(unanswered).toContain("vapor_barrier");

    const leftoverCarpet = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      install_method: ["Loose-lay"],
      substrate: ["Concrete"],
    });
    expect(leftoverCarpet).toContain("vapor_barrier");

    const leftoverLam = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Loose-lay"],
      substrate: ["Concrete"],
    });
    expect(leftoverLam).toContain("vapor_barrier");
    expect(leftoverLam).toContain("attached_pad");

    const leftoverVinyl = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Loose-lay"],
      substrate: ["Concrete"],
    });
    expect(leftoverVinyl).toContain("vapor_barrier");

    const mixedCarpetLvp = visibleKnowledgeKeys({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Loose-lay"],
      carpet_install: ["Stretch-in"],
      substrate: ["Concrete"],
    });
    expect(mixedCarpetLvp).toContain("vapor_barrier");

    const leftoverHardwood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Loose-lay"],
      substrate: ["Concrete"],
    });
    expect(leftoverHardwood).toContain("vapor_barrier");

    expect(knowledgeHelpFor({ key: "vapor_barrier" }, emptyInstallContext())).toMatch(
      /Exclusive loose-lay hides this/,
    );

    const walkLoose = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Loose-lay"],
      substrate: ["Concrete"],
    });
    expect(walkLoose).not.toContain("vapor_barrier");
    expect(walkLoose).not.toContain("adhesive");
  });

  it("0298 exclusive non-vinyl demo hides existing-vinyl skim", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0298_flooring_knowledge_non_vinyl_skim.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0298_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/not embossed vinyl/);
    expect(sql).toMatch(/Do NOT SQL-gate vinyl_skim on hs_demo/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*hs_demo.*vinyl_skim|vinyl_skim.*show_if.*hs_demo/,
    );

    expect([...NON_VINYL_DEMO_SKIM_HIDES_KEYS]).toEqual(["vinyl_skim"]);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["Carpet"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["LVP"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["Nailed hardwood"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["Ceramic WITHOUT mortar bed"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["Sheet vinyl"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["None"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["Other"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesVinylSkimOnNonVinylDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Sheet vinyl"],
          hs_demo: ["Carpet", "Sheet vinyl"],
        }),
      ),
    ).toBe(false);

    const carpetDemo = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Glue-down"],
      hs_demo: ["Carpet"],
    });
    expect(carpetDemo).not.toContain("vinyl_skim");
    expect(carpetDemo).toContain("vinyl_layout");
    expect(carpetDemo).toContain("adhesive");

    const lvpDemo = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      hs_demo: ["LVP"],
    });
    expect(lvpDemo).not.toContain("vinyl_skim");

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
    });
    expect(unanswered).toContain("vinyl_skim");

    const vinylDemo = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      hs_demo: ["Sheet vinyl"],
    });
    expect(vinylDemo).toContain("vinyl_skim");

    const encapsulate = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      hs_demo: ["None"],
    });
    expect(encapsulate).toContain("vinyl_skim");

    const mixedDemo = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      hs_demo: ["Carpet", "Sheet vinyl"],
    });
    expect(mixedDemo).toContain("vinyl_skim");

    expect(knowledgeHelpFor({ key: "vinyl_skim" }, emptyInstallContext())).toMatch(
      /Exclusive carpet \/ LVP \/ hardwood \/ ceramic \/ luan tear-out also hides this/,
    );

    const walkCarpet = walk({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      install_method: ["Glue-down"],
      hs_demo: ["Carpet"],
    });
    expect(walkCarpet).not.toContain("vinyl_skim");
    expect(walkCarpet).toContain("vinyl_layout");
  });

  it("0299 leftover illegal install chips do not switch overlay follow-ups", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0299_flooring_knowledge_illegal_leftover.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0299_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/leftover illegal chips do not switch/);
    expect(sql).toMatch(/Do NOT SQL-gate hardwood_fasteners on install_method/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*install_method.*hardwood_fasteners|hardwood_fasteners.*show_if.*install_method/,
    );

    expect(stripIllegalInstallSystems(["hardwood"], "solid", ["floating"])).toEqual([]);
    expect(stripIllegalInstallSystems(["hardwood"], "solid", ["floating", "nail"])).toEqual(["nail"]);
    expect(stripIllegalInstallSystems(["lvp"], "unknown", ["nail"])).toEqual([]);
    expect(stripIllegalInstallSystems(["lvp"], "unknown", ["glue"])).toEqual(["glue"]);
    expect(stripIllegalInstallSystems(["hardwood"], "engineered", ["loose_lay"])).toEqual([]);
    expect(stripIllegalInstallSystems(["laminate"], "unknown", ["floating"])).toEqual(["floating"]);
    expect(stripIllegalInstallSystems(["lvp", "hardwood"], "solid", ["floating"])).toEqual([
      "floating",
    ]);

    const leftoverSolid = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Floating / click"],
    });
    expect(leftoverSolid).toContain("hardwood_fasteners");
    expect(leftoverSolid).toContain("adhesive");
    expect(leftoverSolid).not.toContain("attached_pad");
    expect(leftoverSolid).not.toContain("laminate_expansion");
    expect(
      knowledgeWarnings(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
          install_method: ["Floating / click"],
        }),
      ).some((w) => w.id === "solid-floating"),
    ).toBe(true);

    const leftoverPlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Floating / click"],
      substrate: ["Plywood / OSB"],
    });
    expect(leftoverPlywood).not.toContain("vapor_barrier");
    expect(leftoverPlywood).toContain("hardwood_fasteners");

    const leftoverLvpNail = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Nail-down"],
    });
    expect(leftoverLvpNail).toContain("attached_pad");
    expect(leftoverLvpNail).toContain("adhesive");
    expect(leftoverLvpNail).toContain("laminate_expansion");
    expect(leftoverLvpNail).not.toContain("hardwood_fasteners");

    const leftoverEngLoose = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Engineered hardwood"],
      install_method: ["Loose-lay"],
    });
    expect(leftoverEngLoose).toContain("attached_pad");
    expect(leftoverEngLoose).toContain("hardwood_fasteners");
    expect(leftoverEngLoose).toContain("adhesive");

    const lamGlue = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Glue-down"],
    });
    expect(lamGlue).not.toContain("adhesive");
    expect(lamGlue).toContain("attached_pad");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT", "Hardwood"],
      install_method: ["Floating / click"],
    });
    expect(mixed).toContain("attached_pad");

    expect(knowledgeHelpFor({ key: "hardwood_fasteners" }, emptyInstallContext())).toMatch(
      /Leftover Floating on exclusive solid hardwood does not hide this/,
    );
    expect(
      knowledgeHelpFor(
        { key: "install_method" },
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Hardwood"],
        }),
      ),
    ).toMatch(/Leftover Floating does not hide fasteners/);
  });

  it("0300 exclusive LVP hides hardwood fasteners, including leftover Nail-down", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0300_flooring_knowledge_lvp_fasteners.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0300_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/leftover Nail-down on LVP does not reopen/);
    expect(sql).toMatch(/Do NOT SQL-gate hardwood_fasteners on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*hardwood_fasteners|hardwood_fasteners.*show_if.*surface_type/,
    );

    expect(knowledgeQuestionByKey("hardwood_fasteners")?.families).toEqual(["hardwood"]);
    expect(knowledgeQuestionByKey("hardwood_fasteners")?.systems).toEqual(["nail", "staple"]);
    expect([...NON_HARDWOOD_FASTENER_HIDES_KEYS]).toEqual(["hardwood_fasteners"]);

    const liveLeftoverNail = {
      key: "hardwood_fasteners",
      config: {
        show_if: { key: "install_method", in: ["Nail-down", "Staple-down"] },
        knowledge_when: { systems: ["nail", "staple"], purpose: "MATERIAL" as const },
      },
    };
    expect(
      questionApplies(liveLeftoverNail, {
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT"],
        install_method: ["Nail-down"],
      }),
    ).toBe(false);
    expect(
      questionApplies(liveLeftoverNail, {
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT", "Hardwood"],
        install_method: ["Nail-down"],
      }),
    ).toBe(true);
    expect(
      questionApplies(
        { key: "hardwood_fasteners", config: {} },
        { project_type: ["Hard surface"] },
      ),
    ).toBe(true);

    expect(
      jobHidesFastenersOnNonHardwood(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          install_method: ["Nail-down"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesFastenersOnNonHardwood(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT", "Hardwood"],
          install_method: ["Nail-down"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesFastenersOnNonHardwood(
        installContextFromValByKey({ project_type: ["Hard surface"] }),
      ),
    ).toBe(false);

    const lvp = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(lvp).not.toContain("hardwood_fasteners");
    expect(lvp).toContain("attached_pad");
    expect(lvp).toContain("adhesive");

    const leftoverNail = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Nail-down"],
    });
    expect(leftoverNail).not.toContain("hardwood_fasteners");
    expect(leftoverNail).toContain("attached_pad");

    const walkLeftover = walk({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Nail-down"],
    });
    expect(walkLeftover).not.toContain("hardwood_fasteners");
    expect(walkLeftover).toContain("install_method");

    const unansweredHs = visibleKnowledgeKeys({ project_type: ["Hard surface"] });
    expect(unansweredHs).toContain("hardwood_fasteners");

    const solid = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
    });
    expect(solid).toContain("hardwood_fasteners");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT", "Hardwood"],
      install_method: ["Nail-down"],
    });
    expect(mixed).toContain("hardwood_fasteners");

    const leftoverSolid = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Floating / click"],
    });
    expect(leftoverSolid).toContain("hardwood_fasteners");

    for (const surface of ["Laminate", "Sheet vinyl", "Tile"] as const) {
      expect(
        visibleKnowledgeKeys({
          project_type: ["Hard surface"],
          surface_type: [surface],
          install_method: ["Nail-down"],
        }),
      ).not.toContain("hardwood_fasteners");
    }

    expect(knowledgeHelpFor({ key: "hardwood_fasteners" }, emptyInstallContext())).toMatch(
      /Exclusive LVP \/ laminate \/ vinyl \/ tile hide this/,
    );
  });

  it("0301 exclusive non-carpet demo hides existing pad and tack", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0301_flooring_knowledge_non_carpet_pad.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0301_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/that demo is not old carpet/);
    expect(sql).toMatch(/Do NOT SQL-gate existing_pad on hs_demo/);
    expect(sql).toMatch(/Do NOT SQL-gate existing_tack on hs_demo/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*hs_demo.*existing_pad|existing_pad.*show_if.*hs_demo/);
    expect(sql).not.toMatch(/show_if.*hs_demo.*existing_tack|existing_tack.*show_if.*hs_demo/);

    expect([...NON_CARPET_DEMO_PAD_HIDES_KEYS]).toEqual(["existing_pad", "existing_tack"]);

    const liveCarpetShowIf = {
      show_if: {
        any: [
          { key: "project_type", in: ["Carpet"] },
          { key: "hs_demo", in: ["Carpet"] },
        ],
      },
      knowledge_when: {
        any: [{ families: ["carpet"] }, { demo: ["Carpet"] }],
        purpose: "LABOR" as const,
      },
    };
    expect(
      questionApplies(
        { key: "existing_pad", config: liveCarpetShowIf },
        { project_type: ["Carpet"], carpet_install: ["Stretch-in"], hs_demo: ["LVP"] },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        { key: "existing_tack", config: liveCarpetShowIf },
        { project_type: ["Carpet"], carpet_install: ["Stretch-in"], hs_demo: ["Ceramic WITH mortar bed"] },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        { key: "existing_pad", config: liveCarpetShowIf },
        { project_type: ["Carpet"], carpet_install: ["Stretch-in"] },
      ),
    ).toBe(true);
    expect(
      questionApplies(
        { key: "existing_pad", config: liveCarpetShowIf },
        { project_type: ["Carpet"], carpet_install: ["Stretch-in"], hs_demo: ["Carpet", "LVP"] },
      ),
    ).toBe(true);

    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["LVP"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Nailed hardwood"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Sheet vinyl"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Luan"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Carpet"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["None"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Other"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesPadOnNonCarpetDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Carpet", "LVP"],
        }),
      ),
    ).toBe(false);

    const lvpDemo = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["LVP"],
    });
    expect(lvpDemo).not.toContain("existing_pad");
    expect(lvpDemo).not.toContain("existing_tack");
    expect(lvpDemo).toContain("hs_demo");
    expect(lvpDemo).toContain("carpet_pad");

    const ceramicWalk = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Ceramic WITHOUT mortar bed"],
    });
    expect(ceramicWalk).not.toContain("existing_pad");
    expect(ceramicWalk).not.toContain("existing_tack");
    expect(ceramicWalk).toContain("asbestos_risk");

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(unanswered).toContain("existing_pad");
    expect(unanswered).toContain("existing_tack");

    const carpetDemo = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      hs_demo: ["Carpet"],
    });
    expect(carpetDemo).toContain("existing_pad");
    expect(carpetDemo).toContain("existing_tack");

    const noneDemo = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["None"],
    });
    expect(noneDemo).toContain("existing_pad");
    expect(noneDemo).toContain("existing_tack");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet", "LVP"],
    });
    expect(mixed).toContain("existing_pad");
    expect(mixed).toContain("existing_tack");

    expect(knowledgeHelpFor({ key: "existing_pad" }, emptyInstallContext())).toMatch(
      /Exclusive LVP \/ hardwood \/ ceramic \/ luan \/ sheet vinyl tear-out hides this/,
    );
    expect(knowledgeHelpFor({ key: "existing_tack" }, emptyInstallContext())).toMatch(
      /Exclusive LVP \/ hardwood \/ ceramic \/ luan \/ sheet vinyl tear-out hides this/,
    );
    expect(knowledgeHelpFor({ key: "hs_demo" }, emptyInstallContext())).toMatch(
      /hides existing pad and tack/,
    );
  });

  it("0302 exclusive None demo hides haul-away and bulk pickup", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0302_flooring_knowledge_none_disposal.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0302_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/nothing is coming up/);
    expect(sql).toMatch(/Do NOT SQL-gate demo_disposal on hs_demo/);
    expect(sql).toMatch(/Do NOT SQL-gate bulk_pickup on hs_demo/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*hs_demo.*demo_disposal|demo_disposal.*show_if.*hs_demo/);
    expect(sql).not.toMatch(/show_if.*hs_demo.*bulk_pickup|bulk_pickup.*show_if.*hs_demo/);

    expect([...NONE_DEMO_DISPOSAL_HIDES_KEYS]).toEqual(["demo_disposal", "bulk_pickup"]);

    const liveCurbShowIf = {
      show_if: { key: "demo_disposal", in: ["Placed at curb"] },
    };
    expect(
      questionApplies(
        { key: "bulk_pickup", config: liveCurbShowIf },
        {
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["None"],
          demo_disposal: ["Placed at curb"],
        },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        { key: "bulk_pickup", config: liveCurbShowIf },
        {
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Carpet"],
          demo_disposal: ["Placed at curb"],
        },
      ),
    ).toBe(true);

    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["None"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["LVP / LVT"],
          hs_demo: ["None"],
        }),
      ),
    ).toBe(true);
    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Carpet"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["LVP"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Other"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["Unknown / field verify"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
        }),
      ),
    ).toBe(false);
    expect(
      jobHidesDisposalOnNoDemo(
        installContextFromValByKey({
          project_type: ["Carpet"],
          carpet_install: ["Stretch-in"],
          hs_demo: ["None", "Carpet"],
        }),
      ),
    ).toBe(false);

    const noneDemo = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["None"],
    });
    expect(noneDemo).not.toContain("demo_disposal");
    expect(noneDemo).not.toContain("bulk_pickup");
    expect(noneDemo).toContain("existing_pad");
    expect(noneDemo).toContain("hs_demo");

    const leftoverCurb = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["None"],
      demo_disposal: ["Placed at curb"],
    });
    expect(leftoverCurb).not.toContain("demo_disposal");
    expect(leftoverCurb).not.toContain("bulk_pickup");

    const carpetDemo = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
    });
    expect(carpetDemo).toContain("demo_disposal");

    const lvpDemo = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      hs_demo: ["LVP"],
    });
    expect(lvpDemo).toContain("demo_disposal");

    const unanswered = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(unanswered).toContain("demo_disposal");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["None", "Carpet"],
    });
    expect(mixed).toContain("demo_disposal");

    const other = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Other"],
    });
    expect(other).toContain("demo_disposal");

    expect(knowledgeHelpFor({ key: "demo_disposal" }, emptyInstallContext())).toMatch(
      /Exclusive None demo hides this/,
    );
    expect(knowledgeHelpFor({ key: "bulk_pickup" }, emptyInstallContext())).toMatch(
      /Exclusive None demo hides this/,
    );
    expect(knowledgeHelpFor({ key: "hs_demo" }, emptyInstallContext())).toMatch(
      /Exclusive None hides haul-away/,
    );
    expect(knowledgeHelpFor({ key: "demo_disposal" }, emptyInstallContext())).toMatch(
      /demo_disposal is the source of truth/,
    );
    expect(knowledgeHelpFor({ key: "bulk_pickup" }, emptyInstallContext())).toMatch(
      /Municipal bulk pickup day/,
    );

    const walkNone = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["None"],
    });
    expect(walkNone).not.toContain("demo_disposal");
    expect(walkNone).not.toContain("bulk_pickup");
    expect(walkNone).toContain("existing_pad");

    const walkCarpet = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      hs_demo: ["Carpet"],
    });
    expect(walkCarpet).toContain("demo_disposal");
  });

  it("0303 exclusive tile hides hard-surface Install method; mixed LVP + tile still asks", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0303_flooring_knowledge_tile_install_method.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0303_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/thinset stays on Tile setting/);
    expect(sql).toMatch(/Do NOT SQL-gate install_method on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate install_method on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*install_method|install_method.*show_if.*surface_type/,
    );
    expect(sql).not.toMatch(
      /show_if.*tile_application.*install_method|install_method.*show_if.*tile_application/,
    );

    expect([...EXCLUSIVE_TILE_METHOD_HIDES_KEYS]).toEqual(["install_method"]);

    const liveHsShowIf = {
      show_if: { key: "project_type", in: ["Hard surface"] },
    };
    expect(
      questionApplies(
        { key: "install_method", config: liveHsShowIf },
        { project_type: ["Hard surface"], surface_type: ["Tile"] },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        { key: "install_method", config: liveHsShowIf },
        {
          project_type: ["Hard surface"],
          surface_type: ["Tile"],
          tile_application: ["Wall"],
          install_method: ["Floating / click"],
        },
      ),
    ).toBe(false);
    expect(
      questionApplies(
        { key: "install_method", config: liveHsShowIf },
        { project_type: ["Hard surface"] },
      ),
    ).toBe(true);
    expect(
      questionApplies(
        { key: "install_method", config: liveHsShowIf },
        { project_type: ["Hard surface"], surface_type: ["Tile", "LVP / LVT"] },
      ),
    ).toBe(true);

    expect(
      jobIsExclusiveTile(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile"],
        }),
      ),
    ).toBe(true);
    expect(
      jobIsExclusiveTile(
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile", "LVP / LVT"],
        }),
      ),
    ).toBe(false);
    expect(
      jobIsExclusiveTile(
        installContextFromValByKey({ project_type: ["Hard surface"] }),
      ),
    ).toBe(false);

    const floor = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
    });
    expect(floor).not.toContain("install_method");
    expect(floor).toContain("tile_setting");
    expect(floor).toContain("tile_application");

    const wall = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Wall"],
      install_method: ["Floating / click"],
    });
    expect(wall).not.toContain("install_method");
    expect(wall).toContain("tile_setting");
    expect(wall).not.toContain("attached_pad");

    const unansweredHs = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
    });
    expect(unansweredHs).toContain("install_method");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
    });
    expect(mixed).toContain("install_method");
    expect(mixed).toContain("tile_setting");
    expect(mixed).toContain("attached_pad");

    const laminate = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
    });
    expect(laminate).toContain("install_method");

    expect(
      knowledgeHelpFor(
        { key: "install_method" },
        installContextFromValByKey({
          project_type: ["Hard surface"],
          surface_type: ["Tile"],
        }),
      ),
    ).toMatch(/Exclusive tile hides this hard-surface method picker/);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the hard-surface Install method picker/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /Exclusive floor tile also hides the 6-mil/,
    );

    const walkTile = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      tile_application: ["Floor"],
    });
    expect(walkTile).not.toContain("install_method");
    expect(walkTile).toContain("tile_setting");

    const walkMixed = walk({
      project_type: ["Hard surface"],
      surface_type: ["Tile", "LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(walkMixed).toContain("install_method");
  });

  it("0304 extra pad TBD does not require measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0304_flooring_knowledge_extra_pad_tbd.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0304_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/without typing measured sq ft/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(EXTRA_AREA_COUNT_TBD_HINT).toMatch(/How many \/ Unit TBD/);
    expect(EXTRA_AREA_COUNT_TBD_HINT).toMatch(/Do not plant leftover sq ft/);
    expect(EXTRA_AREA_MEASURED_LABEL).toBe("Measured sq ft");
    expect(areaDerivedMaterialAllowed("other", "")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "sqyd")).toBe(true);
    expect(areaDerivedMaterialAllowed("other", "sqft")).toBe(true);
    expect(
      isCountPricedLine({ unit: "", measure_unit: "sqft", sqft: null }),
    ).toBe(true);
    expect(
      isCountPricedLine({ unit: "sqyd", measure_unit: "sqyd", sqft: 450 }),
    ).toBe(false);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/extraNeedsMeasured/);
    expect(q).toMatch(/without requiring measured/);
    expect(q).toMatch(/do not plant leftover sq ft/);
    expect(q).not.toMatch(/if \(!ex\.product \|\| numv\(ex\.sqft\) <= 0\) continue/);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /without typing measured sq ft/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /without typing measured sq ft/,
    );
  });

  it("0305 leftover extra sq ft is not Review pad yards", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0305_flooring_knowledge_extra_leftover_sqft.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0305_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/leftover measured sq ft on a count or TBD extra/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(extraMeasuredSqftForTakeoff({ family: "other", productUnit: "", measuredSqft: 450 })).toBe(
      null,
    );
    expect(extraMeasuredSqftForTakeoff({ family: "other", productUnit: "gal", measuredSqft: 450 })).toBe(
      null,
    );
    expect(extraMeasuredSqftForTakeoff({ family: "other", productUnit: "roll", measuredSqft: 450 })).toBe(
      null,
    );
    expect(extraMeasuredSqftForTakeoff({ family: "other", productUnit: "each", measuredSqft: 450 })).toBe(
      null,
    );
    expect(extraMeasuredSqftForTakeoff({ family: "other", productUnit: "sqyd", measuredSqft: 450 })).toBe(
      450,
    );
    expect(extraMeasuredSqftForTakeoff({ family: "other", productUnit: "sqft", measuredSqft: 200 })).toBe(
      200,
    );
    expect(extraMeasuredSqftForTakeoff({ family: "lvp", productUnit: "sqft", measuredSqft: 180 })).toBe(
      180,
    );
    expect(
      extraMeasuredSqftForTakeoff({
        family: "carpet",
        productUnit: "sqyd",
        measuredSqft: 450,
      }),
    ).toBe(null);
    expect(
      extraMeasuredSqftForTakeoff({
        family: "carpet",
        productUnit: "sqyd",
        measuredSqft: 450,
        carpetInstallSystems: ["carpet_tile"],
      }),
    ).toBe(450);
    expect(extraMeasuredSqftForTakeoff({ family: "other", productUnit: "sqyd", measuredSqft: 0 })).toBe(
      null,
    );

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/extraMeasuredSqftForTakeoff/);
    expect(q).toMatch(/extraKeepMeasured/);
    expect(q).toMatch(/leftover measured sq ft/);
    expect(q).toMatch(/is not pad yards/);

    expect(EXTRA_AREA_COUNT_TBD_HINT).toMatch(/Review takeoff ignores leftover taped sq ft/);
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /leftover measured sq ft on a count or TBD extra/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /leftover measured sq ft on a count or TBD extra/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /without typing measured sq ft/,
    );
  });

  it("0306 extra count SKU asks How many, not leftover sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0306_flooring_knowledge_extra_count_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0306_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/asks How many in that unit/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(extraAsksCountQty({ family: "other", productUnit: "roll" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "gal" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "each" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "lnft" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "" })).toBe(false);
    expect(extraAsksCountQty({ family: "other", productUnit: "sqyd" })).toBe(false);
    expect(extraAsksCountQty({ family: "other", productUnit: "sqft" })).toBe(false);
    expect(extraAsksCountQty({ family: "carpet", productUnit: "sqyd" })).toBe(false);
    expect(
      extraAsksCountQty({
        family: "carpet",
        productUnit: "each",
        carpetInstallSystems: ["carpet_tile"],
      }),
    ).toBe(true);

    expect(
      extraCountQtyForEmit({ family: "other", productUnit: "roll", qty: 2 }),
    ).toEqual({ quantity: 2, unit: "roll" });
    expect(
      extraCountQtyForEmit({ family: "other", productUnit: "gal", qty: 1.5 }),
    ).toEqual({ quantity: 1.5, unit: "gallon" });
    expect(extraCountQtyForEmit({ family: "other", productUnit: "roll", qty: 0 })).toBe(null);
    expect(extraCountQtyForEmit({ family: "other", productUnit: "", qty: 2 })).toBe(null);
    expect(extraCountQtyForEmit({ family: "other", productUnit: "sqyd", qty: 50 })).toBe(null);
    expect(
      extraCountQtyForEmit({ family: "other", productUnit: "roll", qty: 450 }),
    ).toEqual({ quantity: 450, unit: "roll" });
    expect(EXTRA_AREA_COUNT_QTY_LABEL).toBe("How many");
    expect(EXTRA_AREA_COUNT_QTY_HINT).toMatch(/not a 30-yard roll/);
    expect(EXTRA_AREA_COUNT_TBD_HINT).toMatch(/How many \/ Unit TBD/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/extraAsksCountQty/);
    expect(q).toMatch(/extraCountQtyForEmit/);
    expect(q).toMatch(/extraKeepCountQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_QTY_LABEL/);
    expect(q).toMatch(/not leftover sq ft and not a 30-yard roll/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /asks How many in that unit/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /asks How many in that unit/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /without typing measured sq ft/,
    );
  });

  it("0307 extra count How many rides onto Review, not leftover sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0307_flooring_knowledge_extra_count_review.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0307_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Typed How many rides onto Review as that count/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(
      extraCountReviewLine({
        family: "other",
        productUnit: "roll",
        qty: 2,
        label: "Stair pad",
      }),
    ).toBe("Stair pad: 2 roll — not taped square feet and not a 30-yard roll");
    expect(
      extraCountReviewLine({
        family: "other",
        productUnit: "gal",
        qty: 1.5,
        label: "Foam kit",
      }),
    ).toBe("Foam kit: 1.5 gallon — not taped square feet and not a 30-yard roll");
    expect(
      extraCountReviewLine({ family: "other", productUnit: "roll", qty: 0, label: "Stair pad" }),
    ).toBe(null);
    expect(
      extraCountReviewLine({ family: "other", productUnit: "", qty: 2, label: "Stair pad" }),
    ).toBe(null);
    expect(
      extraCountReviewLine({ family: "other", productUnit: "sqyd", qty: 50, label: "Pad" }),
    ).toBe(null);
    expect(
      extraCountReviewLine({
        family: "other",
        productUnit: "roll",
        qty: 2,
        label: "Stair pad",
      }),
    ).not.toMatch(/450|50 sq yd|pad yards/);
    expect(
      extraMeasuredSqftForTakeoff({ family: "other", productUnit: "roll", measuredSqft: 450 }),
    ).toBe(null);
    expect(EXTRA_AREA_COUNT_QTY_HINT).toMatch(/Review prints that count/);
    expect(EXTRA_AREA_COUNT_TBD_HINT).toMatch(/Typed How many rides onto Review as that count/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/extraCountReviewLine/);
    expect(q).toMatch(/Typed How many rides onto Review as that count/);
    expect(q).toMatch(/leftover measured sq ft/);
    expect(q).toMatch(/is not pad yards/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).toMatch(/accessories\.push\(\.\.\.extraCountReview\)/);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Typed How many rides onto Review as that count/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /Typed How many rides onto Review as that count/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /asks How many in that unit/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /without typing measured sq ft/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /leftover measured sq ft on a count or TBD extra/,
    );
  });

  it("0308 main count pad does not convert room sq ft into Review yards", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0308_flooring_knowledge_main_pad_count.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0308_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/does not convert room square feet into pad yards/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );

    expect(areaDerivedMaterialAllowed("other", "roll")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "gal")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "")).toBe(false);
    expect(areaDerivedMaterialAllowed("other", "sqyd")).toBe(true);
    expect(areaDerivedMaterialAllowed("other", "sqft")).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "roll" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "" })).toBe(false);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/Count \/ TBD main pad skips area Review takeoff/);
    expect(q).toMatch(/is not pad yards and not a 30-yard roll/);
    expect(q).toMatch(/isPadOrFoam &&/);
    expect(q).toMatch(/!areaDerivedMaterialAllowed/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /does not convert room square feet into pad yards on Review/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /does not convert room square feet into foam feet on Review/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Typed How many rides onto Review as that count/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /without typing measured sq ft/,
    );
  });

  it("0309 main count SKU asks How many, not room sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0309_flooring_knowledge_main_count_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0309_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/asks How many in that unit — room square feet is not pad yards/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_pad on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate adhesive on surface_type/);
    expect(sql).toMatch(/Do NOT drop underlayment from SQYD_CATEGORIES/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*surface_type.*carpet_pad|carpet_pad.*show_if.*surface_type/,
    );
    expect(sql).not.toMatch(
      /show_if.*surface_type.*adhesive|adhesive.*show_if.*surface_type/,
    );

    expect(extraAsksCountQty({ family: "other", productUnit: "roll" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "gal" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "kit" })).toBe(true);
    expect(extraAsksCountQty({ family: "other", productUnit: "" })).toBe(false);
    expect(extraAsksCountQty({ family: "other", productUnit: "sqyd" })).toBe(false);
    expect(
      extraCountQtyForEmit({ family: "other", productUnit: "roll", qty: 2 }),
    ).toEqual({ quantity: 2, unit: "roll" });
    expect(
      extraCountQtyForEmit({ family: "other", productUnit: "gal", qty: 3 }),
    ).toEqual({ quantity: 3, unit: "gallon" });
    expect(
      extraCountReviewLine({
        family: "other",
        productUnit: "roll",
        qty: 2,
        label: "Rebond pad",
      }),
    ).toBe("Rebond pad: 2 roll — not taped square feet and not a 30-yard roll");
    expect(
      extraCountReviewLine({
        family: "other",
        productUnit: "gal",
        qty: 3,
        label: "Pressure sensitive",
      }),
    ).toBe("Pressure sensitive: 3 gallon — not taped square feet and not a 30-yard roll");

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/mainAsksCount/);
    expect(q).toMatch(/setMainQty/);
    expect(q).toMatch(/Main count SKU emits How many in that unit/);
    expect(q).toMatch(/mainCountLine/);
    expect(q).toMatch(/qty\?: string/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — room square feet is not pad yards/,
    );
    expect(knowledgeHelpFor({ key: "hs_underlayment" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — room square feet is not foam feet/,
    );
    expect(knowledgeHelpFor({ key: "adhesive" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — taped square feet is not a glue order/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /does not convert room square feet into pad yards on Review/,
    );
    expect(knowledgeHelpFor({ key: "adhesive" }, emptyInstallContext())).toMatch(/Unit TBD/);
  });

  it("0310 stair wrap count SKU asks How many, not steps × 8", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0310_flooring_knowledge_stair_wrap_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0310_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/asks How many in that unit — not 8 sq ft\/step/);
    expect(sql).toMatch(/Area-unit wrap stays wrap qty TBD/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*tile_application.*hs_plank_stairs|hs_plank_stairs.*show_if.*tile_application/,
    );

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "each" })).toBe(true);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "roll" })).toBe(true);
    expect(extraAsksCountQty({ family: "hardwood", productUnit: "box" })).toBe(true);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "sqft" })).toBe(false);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "" })).toBe(false);
    expect(extraAsksCountQty({ family: "hardwood", productUnit: "sqyd" })).toBe(false);
    expect(
      extraCountQtyForEmit({ family: "lvp", productUnit: "box", qty: 4 }),
    ).toEqual({ quantity: 4, unit: "box" });
    expect(
      extraCountQtyForEmit({ family: "lvp", productUnit: "sqft", qty: 104 }),
    ).toBe(null);
    expect(
      extraCountQtyForEmit({ family: "lvp", productUnit: "box", qty: 0 }),
    ).toBe(null);
    expect(
      extraCountReviewLine({
        family: "lvp",
        productUnit: "box",
        qty: 4,
        label: "Lifeproof Oak wrap",
      }),
    ).toBe("Lifeproof Oak wrap: 4 box — not taped square feet and not a 30-yard roll");
    expect(
      extraCountReviewLine({
        family: "lvp",
        productUnit: "sqft",
        qty: 104,
        label: "Lifeproof Oak wrap",
      }),
    ).toBe(null);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrapAsksCount/);
    expect(q).toMatch(/setWrapProduct/);
    expect(q).toMatch(/Count wrap SKU emits How many in that unit/);
    expect(q).toMatch(/not leftover sq ft and not 8 sq ft\/step/);
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/extraCountQtyForEmit/);
    expect(q).toMatch(/extraCountReviewLine/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/STAIR_SQFT_TREAD_RISER/);

    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /How many \/ Unit TBD/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Area-unit wrap stays wrap qty TBD/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Exclusive wall tile hides this/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_application" }, emptyInstallContext())).toMatch(
      /Exclusive floor tile also hides the 6-mil/,
    );
  });

  it("0311 Review prints self-level bags and subfloor sheets, not taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0311_flooring_knowledge_prep_count_review.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0311_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Review prints the bag count — taped square feet is not a bag order/);
    expect(sql).toMatch(/Review prints the sheet count — taped square feet is not a plywood order/);
    expect(sql).toMatch(/Do NOT SQL-gate selflevel_needed on tile_application/);
    expect(sql).toMatch(/Do NOT SQL-gate subfloor_needed on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*tile_application.*selflevel_needed|selflevel_needed.*show_if.*tile_application/,
    );
    expect(sql).not.toMatch(
      /show_if.*surface_type.*subfloor_needed|subfloor_needed.*show_if.*surface_type/,
    );

    expect(bagsNeeded(450, 30, null, null)).toBe(15);
    expect(subfloorSheets(320, 32)).toBe(10);
    expect(
      prepCountReviewLine({ label: "Self-leveler", qty: 15, unit: "bag" }),
    ).toBe("Self-leveler: 15 bag — not taped square feet");
    expect(
      prepCountReviewLine({ label: "Subfloor 3/4\"", qty: 10, unit: "sheet" }),
    ).toBe("Subfloor 3/4\": 10 sheet — not taped square feet");
    expect(prepCountReviewLine({ label: "Self-leveler", qty: 0, unit: "bag" })).toBe(null);
    expect(prepCountReviewLine({ label: "Subfloor", qty: 7, unit: "" })).toBe(null);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/prepCountReview/);
    expect(q).toMatch(/Review prints the bag count — taped square feet is not a bag order/);
    expect(q).toMatch(/Review prints the sheet count — taped square feet is not a plywood order/);
    expect(q).toMatch(/prepCountReviewLine/);
    expect(q).toMatch(/coverage_sqft: cov/);
    expect(q).toMatch(/resolvedSheetSqft/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/sheet_sqft \?\? 32/);
    expect(q).not.toMatch(/default_thickness_in \?\? 0\.25/);

    expect(knowledgeHelpFor({ key: "selflevel_needed" }, emptyInstallContext())).toMatch(
      /Review prints the bag count — taped square feet is not a bag order/,
    );
    expect(knowledgeHelpFor({ key: "selflevel_needed" }, emptyInstallContext())).toMatch(
      /we do not invent 1\/4 inch/,
    );
    expect(knowledgeHelpFor({ key: "subfloor_needed" }, emptyInstallContext())).toMatch(
      /Review prints the sheet count — taped square feet is not a plywood order/,
    );
    expect(knowledgeHelpFor({ key: "subfloor_needed" }, emptyInstallContext())).toMatch(
      /Exclusive Concrete hides this/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0312 boxed carton SKU with coverage takeoffs from measured area, not How many boxes", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0312_flooring_knowledge_boxed_carton_area.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0312_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(sql).toMatch(/Missing coverage stays TBD; do not invent a box size/);
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "carton",
        sqftPerBox: 20,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "hardwood",
        productUnit: "box",
        sqftPerBox: 20,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "laminate",
        productUnit: "box",
        sqftPerBox: 18,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "tile",
        productUnit: "ctn",
        sqftPerBox: 15,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({ family: "lvp", productUnit: "box", sqftPerBox: 0 }),
    ).toBe(false);
    expect(boxedCartonAreaTakeoffAllowed({ family: "lvp", productUnit: "box" })).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "sqft",
        sqftPerBox: 23.64,
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "each",
        sqftPerBox: 23.64,
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "other",
        productUnit: "box",
        sqftPerBox: 30,
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 20,
      }),
    ).toBe(false);

    expect(
      areaDerivedMaterialQty({
        family: "lvp",
        measuredSqft: 500,
        billingUnit: "sqft",
        productUnit: "box",
      }),
    ).toBeNull();
    expect(
      areaDerivedMaterialQty({
        family: "lvp",
        measuredSqft: 500,
        billingUnit: "sqft",
        productUnit: "box",
        sqftPerBox: 23.64,
      }),
    ).toBe(500);
    expect(
      areaDerivedMaterialQty({
        family: "hardwood",
        measuredSqft: 450,
        billingUnit: "sqft",
        productUnit: "carton",
        sqftPerBox: 20,
      }),
    ).toBe(450);
    expect(
      areaDerivedMaterialQty({
        family: "lvp",
        measuredSqft: 500,
        billingUnit: "sqft",
        productUnit: "each",
        sqftPerBox: 23.64,
      }),
    ).toBeNull();
    expect(
      extraMeasuredSqftForTakeoff({
        family: "lvp",
        productUnit: "box",
        measuredSqft: 500,
      }),
    ).toBeNull();
    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(extraAsksCountQty({ family: "hardwood", productUnit: "box" })).toBe(true);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "sqft" })).toBe(false);

    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "box",
        label: "Lifeproof Oak",
      }),
    ).toBe("Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)");
    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
        label: "Lifeproof Oak",
      }),
    ).toBe(null);
    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "sqft",
        label: "Lifeproof Oak",
      }),
    ).toBe(null);
    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "each",
        label: "Lifeproof Oak",
      }),
    ).toBe(null);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(q).toMatch(/boxedCartonCoverageTbdDescription/);
    expect(q).toMatch(/tbdBoxed/);
    expect(q).toMatch(/sqftPerBox: spb > 0 \? spb : null/);
    expect(q).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(q).toMatch(/do not invent a box size/);
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(ai).toMatch(/boxedCartonCoverageTbdDescription/);
    expect(ai).toMatch(/sqftPerBox/);
    expect(ai).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(ai).toMatch(/rollGoodsOrderTbdDescription/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /A boxed LVP \/ hardwood SKU sold by the carton with coverage still takeoffs from measured area/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /not How many boxes from leftover taped sq ft/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Missing coverage stays TBD; do not invent a box size/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Unassigned rooms stay off the takeoff/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "carpet_pad" }, emptyInstallContext())).toMatch(
      /Additional pad for a specific area is MEASURED sq ft/,
    );
    expect(knowledgeHelpFor({ key: "selflevel_needed" }, emptyInstallContext())).toMatch(
      /Review prints the bag count — taped square feet is not a bag order/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0313 exclusive carpet-tile carton SKU takeoffs from measured area, not How many boxes", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0313_flooring_knowledge_carpet_tile_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0313_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/A carpet-tile SKU sold by the carton with coverage still takeoffs from measured area/);
    expect(sql).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(sql).toMatch(/Missing coverage stays TBD; do not invent a box size/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(
      /show_if.*carpet_install.*carpet_cuts|carpet_cuts.*show_if.*carpet_install/,
    );

    const tile = carpetInstallSystemsFromLabels(["Carpet tile"]);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 20,
        carpetInstallSystems: tile,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "carton",
        sqftPerBox: 18,
        carpetInstallSystems: tile,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 20,
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 20,
        carpetInstallSystems: ["stretch_in"],
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 20,
        carpetInstallSystems: ["stretch_in", "carpet_tile"],
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        carpetInstallSystems: tile,
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "sqyd",
        sqftPerBox: 20,
        carpetInstallSystems: tile,
      }),
    ).toBe(false);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "vinyl",
        productUnit: "box",
        sqftPerBox: 20,
      }),
    ).toBe(false);

    expect(
      areaDerivedMaterialQty({
        family: "carpet",
        measuredSqft: 450,
        billingUnit: "sqyd",
        productUnit: "box",
        sqftPerBox: 20,
        carpetInstallSystems: tile,
      }),
    ).toBe(50);
    expect(
      areaDerivedMaterialQty({
        family: "carpet",
        measuredSqft: 450,
        billingUnit: "sqyd",
        productUnit: "box",
        carpetInstallSystems: tile,
      }),
    ).toBeNull();
    expect(
      extraAsksCountQty({
        family: "carpet",
        productUnit: "box",
        carpetInstallSystems: tile,
      }),
    ).toBe(true);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      boxedCartonCoverageTbdDescription({
        family: "carpet",
        productUnit: "box",
        label: "Shaw Tile",
        carpetInstallSystems: tile,
      }),
    ).toBe("Shaw Tile — carton coverage TBD (not How many boxes from leftover taped sq ft)");
    expect(
      boxedCartonCoverageTbdDescription({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 20,
        label: "Shaw Tile",
        carpetInstallSystems: tile,
      }),
    ).toBe(null);
    expect(
      boxedCartonCoverageTbdDescription({
        family: "carpet",
        productUnit: "box",
        label: "Shaw Tile",
        carpetInstallSystems: ["stretch_in"],
      }),
    ).toBe(null);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/boxedCartonCoverageTbdDescription/);
    expect(q).toMatch(/sqftPerBox: spb/);
    expect(q).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(q).toMatch(/do not invent a box size/);
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const tileCtx = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(knowledgeHelpFor({ kind: "cuts" }, tileCtx)).toMatch(
      /A carpet-tile SKU sold by the carton with coverage still takeoffs from measured area/,
    );
    expect(knowledgeHelpFor({ kind: "cuts" }, tileCtx)).toMatch(
      /Builder shows measured coverage and carton math, not Cuts vs Roll/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, tileCtx)).toMatch(
      /A carpet-tile SKU sold by the carton with coverage still takeoffs from measured area/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /A boxed LVP \/ hardwood SKU sold by the carton with coverage still takeoffs from measured area/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0314 Review does not print taped sq ft as the order when carton coverage is missing", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0314_flooring_knowledge_carton_tbd_review.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0314_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Review does not print taped square feet as the order when carton coverage is missing/);
    expect(sql).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "box",
        label: "Lifeproof Oak",
      }),
    ).toBe("Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)");
    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
        label: "Lifeproof Oak",
      }),
    ).toBe(null);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      extraCountReviewLine({
        family: "lvp",
        productUnit: "box",
        qty: 4,
        label: "Lifeproof Oak wrap",
      }),
    ).toBe("Lifeproof Oak wrap: 4 box — not taped square feet and not a 30-yard roll");

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/Review does not print taped square feet as the order when carton coverage is missing/);
    expect(q).toMatch(/mainCartonTbd/);
    expect(q).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(q).toMatch(/addProduct\(/);
    expect(q).toMatch(/p\.unit/);
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Review does not print taped square feet as the order when carton coverage is missing/,
    );
    const tileCtx = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(knowledgeHelpFor({ kind: "cuts" }, tileCtx)).toMatch(
      /Review does not print taped square feet as the order when carton coverage is missing/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0315 Builder carton-coverage TBD is How many / Unit TBD, never taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0315_flooring_knowledge_carton_tbd_builder.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0315_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Builder carton-coverage TBD is How many \/ Unit TBD, never taped square feet/);
    expect(sql).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "box",
        label: "Lifeproof Oak",
      }),
    ).toBe("Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)");
    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
        label: "Lifeproof Oak",
      }),
    ).toBe(null);
    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      extraCountReviewLine({
        family: "lvp",
        productUnit: "box",
        qty: 4,
        label: "Lifeproof Oak wrap",
      }),
    ).toBe("Lifeproof Oak wrap: 4 box — not taped square feet and not a 30-yard roll");

    const carton = {
      line_type: "mat_labor" as const,
      category: "lvp",
      unit: "sqft",
      measure_unit: "sqft" as const,
      sqft: 500,
      quantity: null,
      description:
        "Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)",
    };
    expect(lineIsBoxedCartonTbd(carton)).toBe(true);
    expect(lineQty(carton)).toBe(0);
    expect(
      lineQty({
        ...carton,
        quantity: 22,
        unit: "box",
      }),
    ).toBe(22);
    expect(
      lineQty({
        ...carton,
        description: "Lifeproof Oak living room",
      }),
    ).toBe(500);

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/lineIsBoxedCartonTbd/);
    expect(calc).toMatch(/if \(lineIsBoxedCartonTbd\(line\)\) return num\(line\.quantity\)/);
    expect(calc).toMatch(/if \(lineIsStairWrapTbd\(line\)\) return num\(line\.quantity\)/);

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/lineIsBoxedCartonTbd/);
    expect(builder).toMatch(/boxedCartonTbd/);
    expect(builder).toMatch(/flooringAreaUi/);
    expect(builder).toMatch(/Builder carton-coverage TBD is How many \/ Unit TBD, never taped square feet/);
    expect(builder).toMatch(/description: l\.description/);
    expect(builder).toMatch(/description: line\.description/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/Builder carton-coverage TBD is How many \/ Unit TBD, never taped square feet/);
    expect(q).toMatch(/Review does not print taped square feet as the order when carton coverage is missing/);
    expect(q).toMatch(/boxedCartonCoverageTbdDescription/);
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/Builder carton-coverage TBD is How many \/ Unit TBD, never taped square feet/);
    expect(ai).toMatch(/boxedCartonCoverageTbdDescription/);
    expect(ai).toMatch(/isAreaUnit/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Builder carton-coverage TBD is How many \/ Unit TBD, never taped square feet/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Review does not print taped square feet as the order when carton coverage is missing/,
    );
    const exclusiveTileCuts = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(knowledgeHelpFor({ kind: "cuts" }, exclusiveTileCuts)).toMatch(
      /Builder carton-coverage TBD is How many \/ Unit TBD, never taped square feet/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /How many \/ Unit TBD/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0316 Builder count SKU / qty TBD is How many / Unit TBD, never taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0316_flooring_knowledge_count_tbd_builder.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0316_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/Builder count SKU \/ qty TBD lines are How many \/ Unit TBD, never taped square feet/);
    expect(sql).toMatch(/not How many boxes from leftover taped sq ft/);
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      extraCountReviewLine({
        family: "lvp",
        productUnit: "box",
        qty: 4,
        label: "Lifeproof Oak wrap",
      }),
    ).toBe("Lifeproof Oak wrap: 4 box — not taped square feet and not a 30-yard roll");
    expect(
      boxedCartonCoverageTbdDescription({
        family: "lvp",
        productUnit: "box",
        label: "Lifeproof Oak",
      }),
    ).toBe("Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)");

    const tbd = {
      line_type: "mat_labor" as const,
      category: "lvp",
      unit: "",
      measure_unit: "sqft" as const,
      sqft: 500,
      quantity: null,
      description: "Lifeproof Oak — qty TBD (unit TBD — not taped sq ft)",
    };
    expect(lineIsCountNotTapedSqft(tbd)).toBe(true);
    expect(lineQty(tbd)).toBe(0);
    expect(
      lineQty({
        ...tbd,
        quantity: 12,
        unit: "box",
        description: "Lifeproof Oak — 12 box (not taped sq ft)",
      }),
    ).toBe(12);
    expect(
      lineQty({
        ...tbd,
        description: "Lifeproof Oak living room",
      }),
    ).toBe(500);

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/lineIsCountNotTapedSqft/);
    expect(calc).toMatch(/if \(lineIsCountNotTapedSqft\(line\)\) return num\(line\.quantity\)/);
    expect(calc).toMatch(/if \(lineIsStairWrapTbd\(line\)\) return num\(line\.quantity\)/);
    expect(calc).toMatch(/if \(lineIsBoxedCartonTbd\(line\)\) return num\(line\.quantity\)/);
    expect(calc).toMatch(/wrap qty TBD\|not an automatic sq ft\\\/step order/);

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/lineIsCountNotTapedSqft/);
    expect(builder).toMatch(/countNotTaped/);
    expect(builder).toMatch(/boxedCartonTbd/);
    expect(builder).toMatch(/flooringAreaUi/);
    expect(builder).toMatch(/Builder count SKU \/ qty TBD lines are How many \/ Unit TBD, never taped square feet/);
    expect(builder).toMatch(/description: l\.description/);
    expect(builder).toMatch(/description: line\.description/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/Builder count SKU \/ qty TBD lines are How many \/ Unit TBD, never taped square feet/);
    expect(q).toMatch(/qty TBD \(\$\{countPhrase\} — not taped sq ft\)/);
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Builder count SKU \/ qty TBD lines are How many \/ Unit TBD, never taped square feet/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Builder carton-coverage TBD is How many \/ Unit TBD, never taped square feet/,
    );
    const exclusiveTileCuts = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Carpet tile"],
    });
    expect(knowledgeHelpFor({ kind: "cuts" }, exclusiveTileCuts)).toMatch(
      /Builder count SKU \/ qty TBD lines are How many \/ Unit TBD, never taped square feet/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Builder count SKU \/ qty TBD lines are How many \/ Unit TBD, never taped square feet/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /How many \/ Unit TBD/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0317 picking a boxed SKU with coverage takeoffs from measured area, not How many boxes", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0317_flooring_knowledge_pick_boxed_area.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0317_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Picking a boxed SKU with coverage still takeoffs from measured area — catalog unit box is not How many boxes/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
      }),
    ).toBe(true);
    expect(
      knowledgePickDescription(
        "Lifeproof Oak — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
        "Lifeproof Maple",
        { dropTbd: true },
      ),
    ).toBe(
      "Lifeproof Maple — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
    );
    expect(
      knowledgePickDescription(
        "Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)",
        "Lifeproof Maple",
        { dropTbd: true },
      ),
    ).toBe("Lifeproof Maple");

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/knowledgePickDescription/);
    expect(calc).toMatch(/if \(lineIsStairWrapTbd\(line\)\) return num\(line\.quantity\)/);
    expect(calc).toMatch(/if \(lineIsBoxedCartonTbd\(line\)\) return num\(line\.quantity\)/);
    expect(calc).toMatch(/if \(lineIsCountNotTapedSqft\(line\)\) return num\(line\.quantity\)/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(pricing).toMatch(
      /Picking a boxed SKU with coverage still takeoffs from measured area — catalog unit box is not How many boxes/,
    );

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/knowledgePickDescription/);
    expect(builder).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(builder).toMatch(/sqft_per_box: spb/);
    expect(builder).toMatch(/dropTbd: boxedArea && !wrap/);
    expect(builder).toMatch(/lineIsStairWrapTbd/);
    expect(builder).toMatch(/flooringAreaUi/);
    expect(builder).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/isAreaUnit\(p\.unit\) \? "" : p\.unit/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Picking a boxed SKU with coverage still takeoffs from measured area — catalog unit box is not How many boxes/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /asks How many in that unit — not 8 sq ft\/step/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Picking a wrap SKU keeps How many/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0318 PO / warehouse / work-order carton math skips wrap and qty TBD How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0318_flooring_knowledge_po_carton_count.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0318_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /PO \/ warehouse \/ work-order carton math from sq ft ÷ coverage does not apply to wrap \/ carton-coverage TBD \/ qty TBD How many — those are already the order, not taped square feet/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      lineSkipsAreaCartonMath({
        description:
          "Lifeproof Oak — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
        unit: "box",
      }),
    ).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description:
            "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)",
          category: "lvp",
          unit: "",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Lifeproof Oak — qty TBD (box — not taped sq ft)",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
        },
        10,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/export function lineSkipsAreaCartonMath/);
    expect(calc).toMatch(/export function hardSurfaceAreaCartonCount/);
    expect(calc).toMatch(
      /PO \/ warehouse \/ work-order carton math from sq ft ÷ coverage does not apply to wrap \/ carton-coverage TBD \/ qty TBD How many — those are already the order, not taped square feet/,
    );

    const scope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(scope).toMatch(/lineSkipsAreaCartonMath/);

    const po = readFileSync(join(root, "src/app/(app)/purchase-orders/po-builder.tsx"), "utf8");
    expect(po).toMatch(/lineSkipsAreaCartonMath/);
    expect(po).toMatch(/hardSurfaceAreaCartonCount/);
    expect(po).toMatch(/dropTbd: false/);
    expect(po).toMatch(/knowledgePickDescription/);

    const print = readFileSync(join(root, "src/app/(app)/purchase-orders/[id]/po-print.tsx"), "utf8");
    expect(print).toMatch(/hardSurfaceAreaCartonCount/);
    expect(print).toMatch(/lineSkipsAreaCartonMath/);

    const staging = readFileSync(join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"), "utf8");
    expect(staging).toMatch(/hardSurfaceAreaCartonCount/);
    expect(staging).toMatch(/lineSkipsAreaCartonMath/);

    const wo = readFileSync(join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"), "utf8");
    expect(wo).toMatch(/hardSurfaceAreaCartonCount/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /PO \/ warehouse \/ work-order carton math from sq ft ÷ coverage does not apply to wrap \/ carton-coverage TBD \/ qty TBD How many — those are already the order, not taped square feet/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /PO \/ warehouse \/ work-order carton math from sq ft ÷ coverage does not apply to wrap \/ carton-coverage TBD \/ qty TBD How many — those are already the order, not taped square feet/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0319 Builder hydrate does not plant How many as taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0319_flooring_knowledge_hydrate_count_sqft.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0319_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on wrap \/ carton-coverage TBD \/ qty TBD — leftover quantity is not measured area/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      recoverAreaSqftFromQuantity({
        unit: "",
        sqft: null,
        quantity: 8,
        description:
          "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
      }),
    ).toBe("");
    expect(
      recoverAreaSqftFromQuantity({
        unit: "sq ft",
        sqft: 300,
        quantity: 8,
        description:
          "Lifeproof Oak — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
      }),
    ).toBe("");
    expect(
      recoverAreaSqftFromQuantity({
        unit: "sq ft",
        sqft: null,
        quantity: 250,
        description: "Living room — Lifeproof Oak",
      }),
    ).toBe("250");

    const emit = readFileSync(join(root, "src/lib/questionnaire-emit.ts"), "utf8");
    expect(emit).toMatch(/lineSkipsAreaCartonMath/);
    expect(emit).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on wrap \/ carton-coverage TBD \/ qty TBD — leftover quantity is not measured area/,
    );

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/const identity = lineSkipsAreaCartonMath/);
    expect(builder).toMatch(/if \(lineSkipsAreaCartonMath\(l\)\) return l/);
    expect(builder).toMatch(/description: desc/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on wrap \/ carton-coverage TBD \/ qty TBD — leftover quantity is not measured area/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on wrap \/ carton-coverage TBD \/ qty TBD — leftover quantity is not measured area/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0320 Builder hydrate does not plant How many as taped sq ft on Unit TBD count lines", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0320_flooring_knowledge_unit_tbd_hydrate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0320_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on Unit TBD \(empty unit\) count lines — leftover quantity is not measured area/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      recoverAreaSqftFromQuantity({
        unit: "",
        sqft: null,
        quantity: 40,
        category: "other",
        description: "Adhesive",
      }),
    ).toBe("");
    expect(
      recoverAreaSqftFromQuantity({
        unit: "",
        sqft: 40,
        quantity: 40,
        category: "underlayment",
        description: "Rebond pad",
      }),
    ).toBe("");
    expect(
      recoverAreaSqftFromQuantity({
        unit: "sq ft",
        sqft: null,
        quantity: 250,
        category: "lvp",
        description: "Living room — Lifeproof Oak",
      }),
    ).toBe("250");

    const emit = readFileSync(join(root, "src/lib/questionnaire-emit.ts"), "utf8");
    expect(emit).toMatch(/isCountPricedLine/);
    expect(emit).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on Unit TBD \(empty unit\) count lines — leftover quantity is not measured area/,
    );

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/isCountPricedLine\(\{ unit: l\.unit, sqft: null \}\)/);
    expect(builder).toMatch(/category: l\.category/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on Unit TBD \(empty unit\) count lines — leftover quantity is not measured area/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Builder hydrate does not plant How many as taped sq ft on Unit TBD \(empty unit\) count lines — leftover quantity is not measured area/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0321 Customer / invoice / portal copy strips wrap / carton TBD How many identity", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0321_flooring_knowledge_customer_identity.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0321_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ invoice \/ portal copy strips wrap \/ carton-coverage TBD \/ qty TBD \/ order TBD identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const asLine = (description: string, extra?: Partial<EstimateLineItem>): EstimateLineItem =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 8,
        unit: "box",
        category: "lvp",
        ...extra,
      }) as EstimateLineItem;

    expect(
      stripCrewIdentityFromCustomerLabel(
        "Lifeproof Oak — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
      ),
    ).toBe("Lifeproof Oak");
    expect(
      stripCrewIdentityFromCustomerLabel(
        "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
      ),
    ).toBe("Lifeproof Oak");
    expect(
      stripCrewIdentityFromCustomerLabel(
        "Lifeproof Oak — carton coverage TBD (not How many boxes from leftover taped sq ft)",
      ),
    ).toBe("Lifeproof Oak");
    expect(
      stripCrewIdentityFromCustomerLabel("Rebond pad — qty TBD (unit TBD — not taped sq ft)"),
    ).toBe("Rebond pad");
    expect(
      stripCrewIdentityFromCustomerLabel("Rebond pad — 4 roll (not taped sq ft)"),
    ).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel(rollGoodsOrderTbdDescription("Mohawk", 300))).toBe(
      "Mohawk",
    );
    expect(stripCrewIdentityFromCustomerLabel("Living room — Lifeproof Oak")).toBe(
      "Living room — Lifeproof Oak",
    );

    const wrap = asLine(
      "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
    );
    expect(customerLineLabel(wrap)).toBe("Lifeproof Oak");
    expect(lineQty(wrap)).toBe(8);
    expect(lineIsStairWrapTbd(wrap)).toBe(true);

    const snap = { line_total: 320 } as ApprovalSnapshotLine;
    const invoiced = invoiceItemFromSnapshotLine("inv1", snap, wrap, 0);
    expect(invoiced.description).toBe("Lifeproof Oak");
    expect(invoiced.quantity).toBe(8);
    expect(invoiced.description).not.toMatch(/wrap qty TBD|not an automatic sq ft\/step order/i);

    const roomed = invoiceItemFromSnapshotLine(
      "inv1",
      snap,
      asLine(
        "Lifeproof Oak — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
        { room: "Stairs", quantity: null, unit: "" },
      ),
      1,
    );
    expect(roomed.description).toBe("Stairs — Lifeproof Oak");
    expect(roomed.description).not.toMatch(/Measured |order TBD|qty TBD/i);

    const scope = readFileSync(join(root, "src/lib/customer-scope.ts"), "utf8");
    expect(scope).toMatch(/export function stripCrewIdentityFromCustomerLabel/);
    expect(scope).toMatch(
      /Customer \/ invoice \/ portal copy strips wrap \/ carton-coverage TBD \/ qty TBD \/ order TBD identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );

    const invoice = readFileSync(join(root, "src/lib/invoice-from-approval.ts"), "utf8");
    expect(invoice).toMatch(/customerLineLabel/);
    expect(invoice).toMatch(
      /Customer \/ invoice \/ portal copy strips wrap \/ carton-coverage TBD \/ qty TBD \/ order TBD identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/not taped sq ft/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/const identity = lineSkipsAreaCartonMath/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);

    const po = readFileSync(join(root, "src/app/(app)/purchase-orders/po-builder.tsx"), "utf8");
    expect(po).toMatch(/lineSkipsAreaCartonMath/);
    expect(po).not.toMatch(/stripCrewIdentityFromCustomerLabel/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ invoice \/ portal copy strips wrap \/ carton-coverage TBD \/ qty TBD \/ order TBD identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ invoice \/ portal copy strips wrap \/ carton-coverage TBD \/ qty TBD \/ order TBD identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0322 Pricing does not treat leftover planted sqft as measured area on Unit TBD count lines", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0322_flooring_knowledge_unit_tbd_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0322_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Pricing does not treat leftover planted sqft as measured area on Unit TBD \(empty unit\) count lines — leftover quantity is How many, not taped square feet/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      isCountPricedLine({
        unit: "",
        measure_unit: "sqft",
        sqft: 300,
        category: "other",
      }),
    ).toBe(true);
    expect(
      lineQty({
        line_type: "mat_labor",
        unit: "",
        measure_unit: "sqft",
        sqft: 300,
        quantity: 2,
        category: "other",
        material_rate: 10,
        labor_rate: 0,
        waste_pct: 0,
      }),
    ).toBe(2);
    expect(
      lineUnitKey({
        unit: "",
        measure_unit: "sqft",
        sqft: 300,
        category: "other",
      }),
    ).toBe("");
    expect(
      lineDisplayUnit({
        unit: "",
        measure_unit: "sqft",
        sqft: 40,
        category: "underlayment",
      }),
    ).toBe("");
    expect(
      lineQty({
        line_type: "mat_labor",
        unit: "sq ft",
        measure_unit: "sqft",
        sqft: 300,
        quantity: 2,
        category: "lvp",
        material_rate: 10,
        labor_rate: 0,
        waste_pct: 0,
      }),
    ).toBe(300);
    expect(
      recoverAreaSqftFromQuantity({
        unit: "sq ft",
        sqft: null,
        quantity: 250,
        category: "lvp",
        description: "Living room — Lifeproof Oak",
      }),
    ).toBe("250");

    const units = readFileSync(join(root, "src/lib/units.ts"), "utf8");
    expect(units).toMatch(
      /Pricing does not treat leftover planted sqft as measured area on Unit TBD \(empty unit\) count lines — leftover quantity is How many, not taped square feet/,
    );

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(
      /Pricing does not treat leftover planted sqft as measured area on Unit TBD \(empty unit\) count lines — leftover quantity is How many, not taped square feet/,
    );

    const builder = readFileSync(join(root, "src/app/(app)/estimates/estimate-builder.tsx"), "utf8");
    expect(builder).toMatch(/const identity = lineSkipsAreaCartonMath/);
    expect(builder).toMatch(/category: l\.category/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Pricing does not treat leftover planted sqft as measured area on Unit TBD \(empty unit\) count lines — leftover quantity is How many, not taped square feet/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Pricing does not treat leftover planted sqft as measured area on Unit TBD \(empty unit\) count lines — leftover quantity is How many, not taped square feet/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0323 Customer project details strip Guided takeoff How many identity", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0323_flooring_knowledge_customer_notes.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0323_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print project details strip wrap \/ carton-coverage TBD \/ qty TBD \/ not-taped-sq-ft identity from Guided takeoff notes — those stamps stay in stored job_description so the crew still sees How many vs leftover taped sq ft/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      stripCrewIdentityFromCustomerLabel(
        "Rebond pad: 4 roll — not taped square feet and not a 30-yard roll",
      ),
    ).toBe("Rebond pad: 4 roll");
    expect(
      stripCrewIdentityFromCustomerLabel(
        "Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      ),
    ).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Living room — Lifeproof Oak")).toBe(
      "Living room — Lifeproof Oak",
    );

    const notes = [
      "Guided takeoff:",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      "• Lifeproof Oak — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
    ].join("\n");
    expect(customerFacingJobNotes(notes)).toMatch(/Guided takeoff:/);
    expect(customerFacingJobNotes(notes)).toMatch(/Stair wrap: 8 box/);
    expect(customerFacingJobNotes(notes)).not.toMatch(/not taped square feet/i);
    expect(customerFacingJobNotes(notes)).not.toMatch(/wrap qty TBD/i);
    expect(parseProjectDetails(notes).details.join("\n")).not.toMatch(/not taped square feet/i);
    expect(parseProjectDetails(notes).details).toContain("Stair wrap: 8 box");

    const scope = readFileSync(join(root, "src/lib/customer-scope.ts"), "utf8");
    expect(scope).toMatch(/export function customerFacingJobNotes/);
    expect(scope).toMatch(/not taped square feet/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    expect(print).not.toMatch(
      /\{estimate\.job_description \? \(/,
    );

    const view = readFileSync(join(root, "src/components/customer-scope-view.tsx"), "utf8");
    expect(view).toMatch(/customerFacingJobNotes/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/extraCountReviewLine/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const qty = readFileSync(join(root, "src/lib/flooring-knowledge/quantities.ts"), "utf8");
    expect(qty).toMatch(/not taped square feet and not a 30-yard roll/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print project details strip wrap \/ carton-coverage TBD \/ qty TBD \/ not-taped-sq-ft identity from Guided takeoff notes — those stamps stay in stored job_description so the crew still sees How many vs leftover taped sq ft/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print project details strip wrap \/ carton-coverage TBD \/ qty TBD \/ not-taped-sq-ft identity from Guided takeoff notes — those stamps stay in stored job_description so the crew still sees How many vs leftover taped sq ft/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0324 Customer project details strip Guided takeoff MEASURED / ORDER / BILLING math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0324_flooring_knowledge_customer_takeoff.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0324_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff MEASURED \/ WASTE \/ ORDER \/ BILLING math — those stay in stored job_description so the crew still sees measured vs order/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isGuidedTakeoffMathLine("Carpet takeoff:")).toBe(true);
    expect(isGuidedTakeoffMathLine("Guided takeoff:")).toBe(false);
    expect(
      isGuidedTakeoffMathLine("Measured area: 300 sq ft (33.3 sq yd equivalent)"),
    ).toBe(true);
    expect(
      isGuidedTakeoffMathLine(
        "Order quantity: TBD — enter cuts (sq ft ÷ 9 is not an order)",
      ),
    ).toBe(true);
    expect(
      isGuidedTakeoffMathLine(
        "Note: Billing is square yards from measured area — not a 30-yard roll.",
      ),
    ).toBe(true);
    expect(isGuidedTakeoffMathLine("Stair wrap: 8 box")).toBe(false);
    expect(isGuidedTakeoffMathLine("Occupancy: Occupied")).toBe(false);

    const notes = [
      "Guided takeoff:",
      "Carpet takeoff:",
      "• Measured area: 300 sq ft (33.3 sq yd equivalent)",
      "• Waste: 0% (not invented from sq ft ÷ 9 — layout waste lives in the cut list)",
      "• Order quantity: TBD — enter cuts (sq ft ÷ 9 is not an order)",
      "• Billing quantity: TBD — not 33.3 sq yd from taped area",
      "• Unit of measure: sq yd — billed by the yard. Taped sq ft is measured area, not an order.",
      "• Note: Billing is square yards from measured area — not a 30-yard roll.",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      "• Lifeproof Oak — wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
      "Conditions:",
      "• Occupancy: Occupied",
    ].join("\n");
    const shown = customerFacingJobNotes(notes);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Lifeproof Oak/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).not.toMatch(/Carpet takeoff/i);
    expect(shown).not.toMatch(/Measured area/i);
    expect(shown).not.toMatch(/Order quantity/i);
    expect(shown).not.toMatch(/Billing quantity/i);
    expect(shown).not.toMatch(/Unit of measure/i);
    expect(shown).not.toMatch(/33\.3 sq yd/i);
    expect(shown).not.toMatch(/not a 30-yard roll/i);
    expect(shown).not.toMatch(/wrap qty TBD/i);
    expect(shown).not.toMatch(/not taped square feet/i);
    expect(parseProjectDetails(notes).details).toContain("Stair wrap: 8 box");
    expect(parseProjectDetails(notes).details).toContain("Occupancy: Occupied");
    expect(parseProjectDetails(notes).details.join("\n")).not.toMatch(/Measured area/i);
    expect(parseProjectDetails(notes).details.join("\n")).not.toMatch(/Order quantity/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const takeoff = readFileSync(join(root, "src/lib/flooring-knowledge/takeoff.ts"), "utf8");
    expect(takeoff).toMatch(/export function reviewToJobNotes/);
    expect(takeoff).toMatch(/Guided takeoff:/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff MEASURED \/ WASTE \/ ORDER \/ BILLING math — those stay in stored job_description so the crew still sees measured vs order/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff MEASURED \/ WASTE \/ ORDER \/ BILLING math — those stay in stored job_description so the crew still sees measured vs order/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0325 Customer project details strip room MEASURED sq ft and crew Warnings", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0325_flooring_knowledge_customer_rooms.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0325_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff room MEASURED sq ft and crew Warnings — those stay in stored job_description so the crew still sees taped area vs order/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isGuidedTakeoffMathLine("Rooms:")).toBe(true);
    expect(
      isGuidedTakeoffMathLine("Living: 300 sq ft — Main: 12 × 14 (300 sq ft)"),
    ).toBe(true);
    expect(isGuidedTakeoffMathLine("Stair wrap: 8 box")).toBe(false);
    expect(isGuidedTakeoffMathLine("Occupancy: Occupied")).toBe(false);
    expect(isGuidedTakeoffMathLine("Guided takeoff:")).toBe(false);

    const notes = [
      "Guided takeoff:",
      "Rooms:",
      "• Living: 300 sq ft — Main: 12 × 14 (300 sq ft)",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      "Conditions:",
      "• Occupancy: Occupied",
      "Warnings:",
      "• Carpet measured by area only — converting sq ft ÷ 9 is equivalent area, not a cut plan. Enter cuts (roll width × length) before ordering.",
      "• Radiant heat present — confirm the selected flooring is rated for radiant heat before ordering.",
    ].join("\n");
    const shown = customerFacingJobNotes(notes);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).not.toMatch(/^Rooms:/m);
    expect(shown).not.toMatch(/300 sq ft/i);
    expect(shown).not.toMatch(/Warnings:/i);
    expect(shown).not.toMatch(/Carpet measured by area only/i);
    expect(shown).not.toMatch(/Radiant heat present/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(notes);
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/300 sq ft/i);
    expect(parsed.details.join("\n")).not.toMatch(/Radiant heat present/i);
    expect(parsed.flags.join("\n")).toMatch(/Carpet measured by area only/i);
    expect(parsed.flags.join("\n")).toMatch(/Radiant heat present/i);

    const takeoff = readFileSync(join(root, "src/lib/flooring-knowledge/takeoff.ts"), "utf8");
    expect(takeoff).toMatch(/lines\.push\("Warnings:"\)/);
    expect(takeoff).toMatch(/formatSqft\(r\.measuredSqft\)/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff room MEASURED sq ft and crew Warnings — those stay in stored job_description so the crew still sees taped area vs order/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff room MEASURED sq ft and crew Warnings — those stay in stored job_description so the crew still sees taped area vs order/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0326 Customer print / portal line notes strip How many and MEASURED sq ft identity", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0326_flooring_knowledge_customer_line_notes.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0326_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer print \/ portal itemized line notes strip wrap \/ carton-coverage TBD \/ qty TBD \/ room MEASURED sq ft identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      customerFacingLineNote(
        "13 steps, tread + riser — not an automatic sq ft/step order",
      ),
    ).toBe("");
    expect(
      customerFacingLineNote(
        "wrap qty TBD (13 steps, tread + riser — not an automatic sq ft/step order)",
      ),
    ).toBe("");
    expect(customerFacingLineNote("300 sq ft leftover taped area")).toBe("");
    expect(customerFacingLineNote("Match existing stair nose")).toBe(
      "Match existing stair nose",
    );

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingLineNote/);
    expect(print).not.toMatch(/\{l\.note\}/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/customerFacingLineNote/);
    expect(portal).not.toMatch(/note: \(l\.note \?\? ""\)\.trim\(\)/);

    const snap = readFileSync(join(root, "src/lib/approval-snapshot-view.ts"), "utf8");
    expect(snap).toMatch(/customerFacingLineNote/);
    expect(snap).not.toMatch(/note: \(l\.note \?\? ""\)\.trim\(\)/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/page.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/\{l\.note\}/);
    expect(wo).not.toMatch(/customerFacingLineNote/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer print \/ portal itemized line notes strip wrap \/ carton-coverage TBD \/ qty TBD \/ room MEASURED sq ft identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer print \/ portal itemized line notes strip wrap \/ carton-coverage TBD \/ qty TBD \/ room MEASURED sq ft identity — those stamps stay on stored lines so Builder \/ PO \/ WO \/ hydrate still skip leftover taped sq ft/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0327 Customer project details strip Guided takeoff crew Uncertainty", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0327_flooring_knowledge_customer_uncertainty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0327_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff crew Uncertainty — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const review = buildSalespersonReview({
      rooms: [],
      products: [],
      takeoffs: [],
      ctx: {
        ...emptyInstallContext(),
        prepConfidence: ["Field verify / TBD"],
      },
      removal: [],
      installation: [],
      prep: [],
      accessories: [
        "Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      ],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Uncertainty:/);
    expect(stored).toMatch(/Field verify \/ TBD/);
    expect(stored).toMatch(/Stair wrap: 8 box/);

    const notes = [
      "Guided takeoff:",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      "Uncertainty:",
      "• Prep: Field verify / TBD",
      "Conditions:",
      "• Occupancy: Occupied",
      "Warnings:",
      "• Prep is Field verify / TBD — do not treat bag counts or leveler quantities as final until the crew sees the substrate.",
    ].join("\n");

    const shown = customerFacingJobNotes(notes);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).not.toMatch(/Uncertainty:/i);
    expect(shown).not.toMatch(/Field verify \/ TBD/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(notes);
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/Uncertainty:/i);
    expect(parsed.details.join("\n")).not.toMatch(/Field verify \/ TBD/i);
    expect(parsed.flags.join("\n")).toMatch(/Field verify \/ TBD/i);

    const takeoff = readFileSync(join(root, "src/lib/flooring-knowledge/takeoff.ts"), "utf8");
    expect(takeoff).toMatch(/title: "Uncertainty"/);
    expect(takeoff).toMatch(/lines\.push\("Warnings:"\)/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff crew Uncertainty — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff crew Uncertainty — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0328 Exclusive New construction hides tear-out; mixed Replacement still asks", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0328_flooring_knowledge_mixed_new_build.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0328_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive New construction hides tear-out — mixed Replacement \+ New construction still asks demo, pad removal, and toilets/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*work_type.*hs_demo|hs_demo.*show_if.*work_type/);
    expect(sql).not.toMatch(/show_if.*work_type.*toilets|toilets.*show_if.*work_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const exclusive = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["New construction"],
    });
    expect(exclusive).not.toContain("hs_demo");
    expect(exclusive).not.toContain("existing_pad");
    expect(exclusive).not.toContain("demo_disposal");
    expect(exclusive).not.toContain("toilets");
    expect(exclusive).toContain("substrate");
    expect(exclusive).toContain("appliances");

    const mixed = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      work_type: ["New construction", "Replacement (tear-out)"],
    });
    expect(mixed).toContain("hs_demo");
    expect(mixed).toContain("existing_pad");
    expect(mixed).toContain("demo_disposal");
    expect(mixed).toContain("toilets");
    expect(mixed).toContain("furniture_level");

    const mixedWarn = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Carpet"],
        carpet_install: ["Stretch-in"],
        work_type: ["New construction", "Replacement (tear-out)"],
      }),
    );
    expect(mixedWarn.some((w) => w.id === "new-construction")).toBe(false);

    const exclusiveWarn = knowledgeWarnings(
      installContextFromValByKey({
        project_type: ["Hard surface"],
        surface_type: ["LVP / LVT"],
        work_type: ["New construction"],
      }),
    );
    expect(exclusiveWarn.some((w) => w.id === "new-construction")).toBe(true);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(
      /Exclusive New construction hides tear-out — mixed Replacement \+ New construction still asks demo, pad removal, and toilets/,
    );
    expect(knowledgeHelpFor({ key: "toilets" }, emptyInstallContext())).toMatch(
      /Mixed Replacement \+ New construction still asks/,
    );
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Exclusive New construction hides tear-out — mixed Replacement \+ New construction still asks demo, pad removal, and toilets/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Exclusive New construction hides tear-out — mixed Replacement \+ New construction still asks demo, pad removal, and toilets/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0329 Customer project details strip Guided takeoff crew prep confidence", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0329_flooring_knowledge_customer_prep_confidence.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0329_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff crew prep confidence — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewPrepConfidenceLine("How sure are we about the prep?: Field verify / TBD")).toBe(
      true,
    );
    expect(isCrewPrepConfidenceLine("Prep: Field verify / TBD")).toBe(true);
    expect(isCrewPrepConfidenceLine("Self-leveler: 15 bag — not taped square feet")).toBe(false);
    expect(isCrewPrepConfidenceLine("Stair wrap: 8 box")).toBe(false);

    const notes = [
      "Guided takeoff:",
      "Prep:",
      "• How sure are we about the prep?: Field verify / TBD",
      "• Self-leveler: 15 bag — not taped square feet",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      "Uncertainty:",
      "• Prep: Field verify / TBD",
      "Conditions:",
      "• Occupancy: Occupied",
    ].join("\n");

    const shown = customerFacingJobNotes(notes);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).not.toMatch(/Uncertainty:/i);
    expect(shown).not.toMatch(/How sure are we about the prep/i);
    expect(shown).not.toMatch(/Field verify \/ TBD/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(notes);
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/How sure are we about the prep/i);
    expect(parsed.details.join("\n")).not.toMatch(/Field verify \/ TBD/i);
    expect(parsed.flags.join("\n")).toMatch(/How sure are we about the prep/i);
    expect(parsed.flags.join("\n")).toMatch(/Field verify \/ TBD/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff crew prep confidence — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff crew prep confidence — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0330 Customer Site preparation strip Guided takeoff crew prep confidence", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0330_flooring_knowledge_customer_site_prep.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0330_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print Site preparation strip Guided takeoff crew prep confidence — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const notes = [
      "Job conditions:",
      "• How sure are we about the prep?: Field verify / TBD",
      "• Subfloor: Concrete",
      "",
      "Guided takeoff:",
      "Prep:",
      "• How sure are we about the prep?: Field verify / TBD",
      "• Self-leveler: 15 bag — not taped square feet",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
    ].join("\n");

    const scope = buildCustomerScope([], notes);
    expect(scope.conditions).toContain("Subfloor: Concrete");
    expect(scope.conditions.join("\n")).not.toMatch(/How sure are we about the prep/i);
    expect(scope.conditions.join("\n")).not.toMatch(/Field verify \/ TBD/i);
    expect(scope.notes).toMatch(/Stair wrap: 8 box/);
    expect(scope.notes).toMatch(/Self-leveler: 15 bag/);
    expect(scope.notes).not.toMatch(/How sure are we about the prep/i);
    expect(scope.notes).not.toMatch(/Field verify \/ TBD/i);
    expect(scope.notes).not.toMatch(/not taped square feet/i);

    const shown = customerFacingJobNotes(notes);
    expect(shown).toMatch(/Subfloor: Concrete/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).not.toMatch(/How sure are we about the prep/i);

    const parsed = parseProjectDetails(notes);
    expect(parsed.details).toContain("Subfloor: Concrete");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/How sure are we about the prep/i);
    expect(parsed.flags.join("\n")).toMatch(/How sure are we about the prep/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print Site preparation strip Guided takeoff crew prep confidence — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print Site preparation strip Guided takeoff crew prep confidence — those stay in stored job_description so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0331 Customer line labels strip stair-install step How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0331_flooring_knowledge_customer_stair_steps.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0331_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print line labels strip stair-install step How many — those stay on stored lines so Builder still prices per step/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(stripCrewIdentityFromCustomerLabel("Stair install — 8 steps (tread + riser)")).toBe(
      "Stair install",
    );
    expect(stripCrewIdentityFromCustomerLabel("Stair install — 1 step (tread only)")).toBe(
      "Stair install",
    );
    expect(
      stripCrewIdentityFromCustomerLabel(
        "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
      ),
    ).toBe("Lifeproof Oak");
    expect(stripCrewIdentityFromCustomerLabel("Living room — Lifeproof Oak")).toBe(
      "Living room — Lifeproof Oak",
    );

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 0,
        labor_rate: 45,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 0,
        labor_cost: 45,
        quantity: 8,
        unit: "step",
        category: "labor",
        ...extra,
      }) as EstimateLineItem;

    const labor = asLine("Stair install — 8 steps (tread + riser)");
    expect(customerLineLabel(labor)).toBe("Stair install");
    expect(customerLineLabel(labor)).not.toMatch(/\d+\s+steps?/i);
    expect(lineQty(labor)).toBe(8);

    const wrap = asLine(
      "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
      { category: "lvp", unit: "box", labor_rate: 0, labor_cost: 0, material_rate: 40, material_cost: 320 },
    );
    expect(customerLineLabel(wrap)).toBe("Lifeproof Oak");
    expect(lineQty(wrap)).toBe(8);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/Stair install — \$\{steps\} step/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print line labels strip stair-install step How many — those stay on stored lines so Builder still prices per step/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print line labels strip stair-install step How many — those stay on stored lines so Builder still prices per step/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0332 Customer line labels strip prep estimated / allowance suffix", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0332_flooring_knowledge_customer_prep_suffix.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0332_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print line labels strip prep estimated \/ allowance suffix — those stay on stored lines so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(stripCrewIdentityFromCustomerLabel("Self-leveler (estimated)")).toBe("Self-leveler");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler (allowance)")).toBe("Self-leveler");
    expect(stripCrewIdentityFromCustomerLabel("Subfloor 3/4 — Living (estimated)")).toBe(
      "Subfloor 3/4 — Living",
    );
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler")).toBe("Self-leveler");
    expect(stripCrewIdentityFromCustomerLabel("Stair install — 8 steps (tread + riser)")).toBe(
      "Stair install",
    );
    expect(stripCrewIdentityFromCustomerLabel("Living room — Lifeproof Oak")).toBe(
      "Living room — Lifeproof Oak",
    );

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 12,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 12,
        labor_cost: 0,
        quantity: 15,
        unit: "bag",
        category: "other",
        ...extra,
      }) as EstimateLineItem;

    const bags = asLine("Self-leveler (estimated)");
    expect(customerLineLabel(bags)).toBe("Self-leveler");
    expect(customerLineLabel(bags)).not.toMatch(/estimated|allowance/i);
    expect(lineQty(bags)).toBe(15);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/prepQuantitySuffix/);
    expect(q).toMatch(/Self-leveler\$\{suffix\}/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print line labels strip prep estimated \/ allowance suffix — those stay on stored lines so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print line labels strip prep estimated \/ allowance suffix — those stay on stored lines so the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0333 Customer line notes strip leftover stair-step How many and prep confidence", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0333_flooring_knowledge_customer_line_steps.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0333_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer print \/ portal itemized line notes strip leftover stair-install step How many and crew prep confidence — those stay on stored lines so Builder still prices per step and the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      customerFacingLineNote(
        "13 steps, tread + riser — not an automatic sq ft/step order",
      ),
    ).toBe("");
    expect(customerFacingLineNote("13 steps, tread + riser")).toBe("");
    expect(customerFacingLineNote("8 steps")).toBe("");
    expect(
      customerFacingLineNote("How sure are we about the prep?: Field verify / TBD"),
    ).toBe("");
    expect(isCrewStairStepHowManyLine("13 steps, tread + riser")).toBe(true);
    expect(isCrewPrepConfidenceLine("How sure are we about the prep?: Field verify / TBD")).toBe(
      true,
    );
    expect(customerFacingLineNote("Match existing stair nose")).toBe(
      "Match existing stair nose",
    );
    expect(customerFacingLineNote("Self-leveler (estimated)")).toBe("Self-leveler");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: "13 steps, tread + riser — not an automatic sq ft/step order",
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 0,
        labor_rate: 45,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 0,
        labor_cost: 45,
        quantity: 8,
        unit: "step",
        category: "labor",
        ...extra,
      }) as EstimateLineItem;

    const labor = asLine("Stair install — 8 steps (tread + riser)");
    expect(customerLineLabel(labor)).toBe("Stair install");
    expect(customerFacingLineNote(labor.note)).toBe("");
    expect(lineQty(labor)).toBe(8);

    const leftover = asLine("13 steps, tread + riser", { note: "8 steps" });
    expect(customerLineLabel(leftover)).not.toMatch(/\d+\s+steps?/i);
    expect(customerFacingLineNote(leftover.note)).toBe("");

    const wrap = asLine(
      "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
      {
        category: "lvp",
        unit: "box",
        labor_rate: 0,
        labor_cost: 0,
        material_rate: 40,
        material_cost: 320,
        quantity: 8,
        note: "13 steps, tread + riser",
      },
    );
    expect(customerLineLabel(wrap)).toBe("Lifeproof Oak");
    expect(customerFacingLineNote(wrap.note)).toBe("");
    expect(lineQty(wrap)).toBe(8);

    const notes = [
      "Guided takeoff:",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      "• 13 steps, tread + riser — not an automatic sq ft/step order",
      "Conditions:",
      "• Occupancy: Occupied",
    ].join("\n");
    const shown = customerFacingJobNotes(notes);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).not.toMatch(/13 steps/i);
    expect(shown).not.toMatch(/not an automatic/i);

    const parsed = parseProjectDetails(notes);
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/13 steps/i);
    expect(parsed.flags.join("\n")).toMatch(/13 steps/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/Stair install — \$\{steps\} step/);
    expect(q).toMatch(/wrap qty TBD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingLineNote/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/customerFacingLineNote/);
    const wo = readFileSync(join(root, "src/app/(app)/jobs/[id]/page.tsx"), "utf8");
    expect(wo).toMatch(/\{l\.note\}/);
    expect(wo).not.toMatch(/customerFacingLineNote/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer print \/ portal itemized line notes strip leftover stair-install step How many and crew prep confidence — those stay on stored lines so Builder still prices per step and the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer print \/ portal itemized line notes strip leftover stair-install step How many and crew prep confidence — those stay on stored lines so Builder still prices per step and the crew still sees Field verify \/ TBD vs Known bag counts/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0334 Customer copy strips Guided takeoff Review section headers", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0334_flooring_knowledge_customer_review_headers.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0334_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff Review section headers — those stay in stored job_description so the crew still sees Removal \/ Prep \/ Accessories grouping. Product names, accessory How many, and job conditions stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewReviewSectionHeader("Prep:")).toBe(true);
    expect(isCrewReviewSectionHeader("Accessories:")).toBe(true);
    expect(isCrewReviewSectionHeader("Removal:")).toBe(true);
    expect(isCrewReviewSectionHeader("Installation:")).toBe(true);
    expect(isCrewReviewSectionHeader("Special conditions:")).toBe(true);
    expect(isCrewReviewSectionHeader("Conditions:")).toBe(true);
    expect(isCrewReviewSectionHeader("Guided takeoff:")).toBe(false);
    expect(isCrewReviewSectionHeader("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewReviewSectionHeader("Stair wrap: 8 box")).toBe(false);
    expect(isCrewReviewSectionHeader("Prep: Field verify / TBD")).toBe(false);

    const notes = [
      "Guided takeoff:",
      "Prep:",
      "• Self-leveler: 15 bag — not taped square feet",
      "Accessories:",
      "• Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      "Special conditions:",
      "Conditions:",
      "• Occupancy: Occupied",
    ].join("\n");
    const shown = customerFacingJobNotes(notes);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).not.toMatch(/^Prep:/m);
    expect(shown).not.toMatch(/^Accessories:/m);
    expect(shown).not.toMatch(/^Special conditions:/m);
    expect(shown).not.toMatch(/^Conditions:/m);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(notes);
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/^Prep:/m);
    expect(parsed.details.join("\n")).not.toMatch(/^Accessories:/m);
    expect(parsed.details.join("\n")).not.toMatch(/^Guided takeoff:/m);
    expect(parsed.details.join("\n")).not.toMatch(/not taped square feet/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff Review section headers — those stay in stored job_description so the crew still sees Removal \/ Prep \/ Accessories grouping. Product names, accessory How many, and job conditions stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff Review section headers — those stay in stored job_description so the crew still sees Removal \/ Prep \/ Accessories grouping. Product names, accessory How many, and job conditions stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0335 Customer copy strips Guided takeoff Review bucket prefixes", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0335_flooring_knowledge_customer_review_buckets.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0335_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff Review bucket prefixes — those stay in stored job_description so the crew still sees Removal \/ Prep \/ Accessories grouping. Product names, accessory How many, and job conditions stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(stripCrewReviewBucketPrefix("Prep: Self-leveler: 15 bag")).toBe(
      "Self-leveler: 15 bag",
    );
    expect(stripCrewReviewBucketPrefix("Accessorie: Stair wrap: 8 box")).toBe(
      "Stair wrap: 8 box",
    );
    expect(stripCrewReviewBucketPrefix("Accessories: Stair wrap: 8 box")).toBe(
      "Stair wrap: 8 box",
    );
    expect(stripCrewReviewBucketPrefix("Special condition: Occupancy: Occupied")).toBe(
      "Occupancy: Occupied",
    );
    expect(stripCrewReviewBucketPrefix("Occupancy: Occupied")).toBe("Occupancy: Occupied");
    expect(stripCrewReviewBucketPrefix("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const review = buildSalespersonReview({
      rooms: [],
      products: ["Lifeproof Oak"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: [
        "Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      ],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Prep:/);
    expect(stored).toMatch(/Prep: Self-leveler: 15 bag/);
    expect(stored).toMatch(/Accessorie: Stair wrap: 8 box/);
    expect(stored).toMatch(/Special condition: Occupancy: Occupied/);

    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).toMatch(/Lifeproof Oak/);
    expect(shown).not.toMatch(/^Prep:/m);
    expect(shown).not.toMatch(/Prep: Self-leveler/i);
    expect(shown).not.toMatch(/Accessorie:/i);
    expect(shown).not.toMatch(/Special condition:/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/Prep: Self-leveler/i);
    expect(parsed.details.join("\n")).not.toMatch(/Accessorie:/i);
    expect(parsed.details.join("\n")).not.toMatch(/not taped square feet/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff Review bucket prefixes — those stay in stored job_description so the crew still sees Removal \/ Prep \/ Accessories grouping. Product names, accessory How many, and job conditions stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff Review bucket prefixes — those stay in stored job_description so the crew still sees Removal \/ Prep \/ Accessories grouping. Product names, accessory How many, and job conditions stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0336 Customer copy strips Guided takeoff stair-install step How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0336_flooring_knowledge_customer_review_steps.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0336_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff stair-install step How many — those stay in stored job_description so Builder still prices per step. Wrap How many still stays/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewStairStepHowManyLine("13 steps, tread + riser")).toBe(true);
    expect(isCrewStairStepHowManyLine("8 steps")).toBe(true);
    expect(
      isCrewStairStepHowManyLine("Hard-surface stairs: 13 steps (tread + riser)"),
    ).toBe(true);
    expect(
      isCrewStairStepHowManyLine(
        "Installation: Hard-surface stairs: 13 steps (tread + riser)",
      ),
    ).toBe(true);
    expect(isCrewStairStepHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewStairStepHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewStairStepHowManyLine("Occupancy: Occupied")).toBe(false);

    const review = buildSalespersonReview({
      rooms: [],
      products: ["Lifeproof Oak"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Hard-surface stairs: 13 steps (tread + riser)"],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: [
        "Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      ],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Installation: Hard-surface stairs: 13 steps/);
    expect(stored).toMatch(/Prep: Self-leveler: 15 bag/);
    expect(stored).toMatch(/Accessorie: Stair wrap: 8 box/);

    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).toMatch(/Lifeproof Oak/);
    expect(shown).not.toMatch(/\d+\s+steps?\b/i);
    expect(shown).not.toMatch(/Hard-surface stairs/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    expect(
      customerFacingLineNote("Hard-surface stairs: 13 steps (tread + riser)"),
    ).toBe("");
    expect(customerFacingLineNote("13 steps, tread + riser")).toBe("");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/\d+\s+steps?\b/i);
    expect(parsed.details.join("\n")).not.toMatch(/Hard-surface stairs/i);
    expect(parsed.flags.join("\n")).toMatch(/\d+\s+steps?\b/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff stair-install step How many — those stay in stored job_description so Builder still prices per step. Wrap How many still stays/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff stair-install step How many — those stay in stored job_description so Builder still prices per step. Wrap How many still stays/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0337 Customer copy strips Guided takeoff waterfall / upholstered stair How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0337_flooring_knowledge_customer_stair_types.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0337_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff waterfall \/ upholstered stair How many — those stay in stored job_description so Builder still prices wrap labor. Wrap How many still stays/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewStairStepHowManyLine("13 Waterfall")).toBe(true);
    expect(isCrewStairStepHowManyLine("8 Upholstered")).toBe(true);
    expect(isCrewStairStepHowManyLine("Carpet stairs: 13 Waterfall; 4 Upholstered")).toBe(
      true,
    );
    expect(
      isCrewStairStepHowManyLine("Installation: Carpet stairs: 13 Waterfall"),
    ).toBe(true);
    expect(isCrewStairStepHowManyLine("Hard-surface stairs: 13 steps (tread + riser)")).toBe(
      true,
    );
    expect(isCrewStairStepHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewStairStepHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewStairStepHowManyLine("Occupancy: Occupied")).toBe(false);

    const review = buildSalespersonReview({
      rooms: [],
      products: ["Lifeproof Oak"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Carpet stairs: 13 Waterfall; 4 Upholstered"],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: [
        "Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
      ],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Installation: Carpet stairs: 13 Waterfall/);
    expect(stored).toMatch(/Prep: Self-leveler: 15 bag/);
    expect(stored).toMatch(/Accessorie: Stair wrap: 8 box/);

    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).toMatch(/Lifeproof Oak/);
    expect(shown).not.toMatch(/Waterfall/i);
    expect(shown).not.toMatch(/Upholstered/i);
    expect(shown).not.toMatch(/Carpet stairs/i);
    expect(shown).not.toMatch(/\d+\s+steps?\b/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    expect(customerFacingLineNote("Carpet stairs: 13 Waterfall")).toBe("");
    expect(customerFacingLineNote("13 Upholstered")).toBe("");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/Waterfall/i);
    expect(parsed.details.join("\n")).not.toMatch(/Upholstered/i);
    expect(parsed.flags.join("\n")).toMatch(/Waterfall/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff waterfall \/ upholstered stair How many — those stay in stored job_description so Builder still prices wrap labor. Wrap How many still stays/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff waterfall \/ upholstered stair How many — those stay in stored job_description so Builder still prices wrap labor. Wrap How many still stays/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0338 Customer copy strips Guided takeoff labeled count How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0338_flooring_knowledge_customer_count_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0338_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff labeled count How many — those stay in stored job_description so the crew still sees toilets \/ trim \/ metals counts. Wrap How many and Self-leveler bag How many stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewLabeledCountHowManyLine("Toilets: 2")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Quarter round: 40 lnft")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Metals: 4")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Adhesive: 2 gal")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Stair landings: 1")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Occupancy: Occupied")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Subfloor: Concrete")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Lifeproof Oak")).toBe(false);

    const review = buildSalespersonReview({
      rooms: [],
      products: ["Lifeproof Oak"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: [
        "Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
        "Quarter round: 40 lnft",
      ],
      specials: ["Occupancy: Occupied", "Toilets: 2"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Prep: Self-leveler: 15 bag/);
    expect(stored).toMatch(/Accessorie: Stair wrap: 8 box/);
    expect(stored).toMatch(/Quarter round: 40 lnft/);
    expect(stored).toMatch(/Toilets: 2/);

    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).toMatch(/Lifeproof Oak/);
    expect(shown).not.toMatch(/Toilets:\s*2/);
    expect(shown).not.toMatch(/Quarter round: 40 lnft/);
    expect(shown).not.toMatch(/not taped square feet/i);

    expect(customerFacingLineNote("Toilets: 2")).toBe("");
    expect(customerFacingLineNote("Quarter round: 40 lnft")).toBe("");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Occupancy: Occupied");
    expect(parsed.details.join("\n")).not.toMatch(/Toilets:\s*2/);
    expect(parsed.details.join("\n")).not.toMatch(/Quarter round: 40 lnft/);
    expect(parsed.flags.join("\n")).toMatch(/Toilets:\s*2/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff labeled count How many — those stay in stored job_description so the crew still sees toilets \/ trim \/ metals counts. Wrap How many and Self-leveler bag How many stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip Guided takeoff labeled count How many — those stay in stored job_description so the crew still sees toilets \/ trim \/ metals counts. Wrap How many and Self-leveler bag How many stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0339 Customer line labels strip leftover em-dash How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0339_flooring_knowledge_customer_dash_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0339_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print line labels strip leftover em-dash How many — those stay on stored lines so Builder still prices How many. Wrap How many and Self-leveler bag How many stay in job notes/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(stripCrewIdentityFromCustomerLabel("Rebond pad — 4 roll")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("Rebond pad — 4 roll (not taped sq ft)")).toBe(
      "Rebond pad",
    );
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 8 box")).toBe("Lifeproof Oak");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler — 15 bag")).toBe("Self-leveler");
    expect(stripCrewIdentityFromCustomerLabel("Living room — Lifeproof Oak")).toBe(
      "Living room — Lifeproof Oak",
    );
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    const pad = asLine("Rebond pad — 4 roll");
    expect(customerLineLabel(pad)).toBe("Rebond pad");
    expect(customerLineLabel(pad)).not.toMatch(/\d+\s+roll/i);
    expect(customerFacingLineNote("Rebond pad — 4 roll")).toBe("Rebond pad");

    const wrap = asLine("Lifeproof Oak — 8 box", {
      category: "lvp",
      unit: "box",
      quantity: 8,
    });
    expect(customerLineLabel(wrap)).toBe("Lifeproof Oak");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const padLine = extraCountReviewLine({
      family: "other",
      productUnit: "roll",
      qty: 4,
      label: "Rebond pad",
    });
    expect(padLine).toMatch(/Rebond pad: 4 roll/);
    const review = buildSalespersonReview({
      rooms: [],
      products: ["Lifeproof Oak"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: [
        "Stair wrap: 8 box — not taped square feet and not a 30-yard roll",
        padLine!,
      ],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Rebond pad: 4 roll/);
    expect(stored).toMatch(/Accessorie: Stair wrap: 8 box/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).toMatch(/Lifeproof Oak/);
    expect(shown).not.toMatch(/4\s+roll/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details.join("\n")).not.toMatch(/4\s+roll/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print line labels strip leftover em-dash How many — those stay on stored lines so Builder still prices How many. Wrap How many and Self-leveler bag How many stay in job notes/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print line labels strip leftover em-dash How many — those stay on stored lines so Builder still prices How many. Wrap How many and Self-leveler bag How many stay in job notes/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0340 Customer copy strips leftover unlabeled and inch / percent How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0340_flooring_knowledge_customer_bare_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0340_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover unlabeled count How many and labeled inch \/ percent How many — those stay in stored job_description so the crew still sees pad rolls and pattern repeat. Wrap How many and Self-leveler bag How many stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("40 lnft")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("18\"")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Pattern repeat: 18\"")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Pattern repeat (inches): 18")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Occupancy: Occupied")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Lifeproof Oak")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("2nd floor")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);

    expect(customerFacingLineNote("4 roll")).toBe("");
    expect(customerFacingLineNote("Pattern repeat: 18\"")).toBe("");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const review = buildSalespersonReview({
      rooms: [],
      products: ["Lifeproof Oak"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Pattern repeat: 18\""],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Pattern repeat: 18"/);
    expect(stored).toMatch(/Accessorie: Stair wrap: 8 box/);
    const shown = customerFacingJobNotes(`${stored}\n• 4 roll`);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).toMatch(/Lifeproof Oak/);
    expect(shown).not.toMatch(/4\s+roll/i);
    expect(shown).not.toMatch(/Pattern repeat: 18/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(`${stored}\n• 4 roll`);
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details.join("\n")).not.toMatch(/4\s+roll/i);
    expect(parsed.details.join("\n")).not.toMatch(/Pattern repeat: 18/i);
    expect(parsed.flags.join("\n")).toMatch(/4\s+roll/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover unlabeled count How many and labeled inch \/ percent How many — those stay in stored job_description so the crew still sees pad rolls and pattern repeat. Wrap How many and Self-leveler bag How many stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover unlabeled count How many and labeled inch \/ percent How many — those stay in stored job_description so the crew still sees pad rolls and pattern repeat. Wrap How many and Self-leveler bag How many stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0341 Customer copy strips leftover dimension How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0341_flooring_knowledge_customer_dimensions.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0341_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap How many and Self-leveler bag How many stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewDimensionHowManyLine("12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("12' x 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("12 ft × 14 ft")).toBe(true);
    expect(isCrewDimensionHowManyLine("Living: 12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("12' 6\"")).toBe(true);
    expect(isCrewDimensionHowManyLine("2nd floor")).toBe(false);
    expect(isCrewDimensionHowManyLine("Lifeproof Oak 5mm")).toBe(false);
    expect(isCrewDimensionHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewDimensionHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewDimensionHowManyLine("Occupancy: Occupied")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Lifeproof Oak 5mm")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("Living room — 12' × 14'")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("Living room — Lifeproof Oak")).toBe(
      "Living room — Lifeproof Oak",
    );
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");

    expect(customerFacingLineNote("12' × 14'")).toBe("");
    expect(customerFacingLineNote("Living: 12' × 14'")).toBe("");
    expect(customerFacingLineNote("Living room — 12' × 14'")).toBe("Living room");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("Living room — 12' × 14'"))).toBe("Living room");
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );
    expect(customerLineLabel(asLine("OVF Del Mar - JETCORE 7.25\"", { category: "lvp" }))).toBe(
      "OVF Del Mar - JETCORE 7.25\"",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["Lifeproof Oak 5mm"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Run: 12' × 14'"],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied", "Access: 2nd floor"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Run: 12'/);
    expect(stored).toMatch(/Accessorie: Stair wrap: 8 box/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Occupancy: Occupied/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Access: 2nd floor/);
    expect(shown).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details).toContain("Self-leveler: 15 bag");
    expect(parsed.details).toContain("Access: 2nd floor");
    expect(parsed.details.join("\n")).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(parsed.flags.join("\n")).toMatch(/12'\s*[×x]\s*14'/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap How many and Self-leveler bag How many stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap How many and Self-leveler bag How many stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0342 Customer copy keeps 12' product names", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0342_flooring_knowledge_customer_product_ft.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0342_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print keep 12' product names — leftover dimension How many is a complete room \/ cut size, not a catalog name. Wrap How many and Self-leveler bag How many stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("8 box")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewDimensionHowManyLine("12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Name: 12' Shaw")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Living: 12' × 14'")).toBe(false);
    expect(isCrewDimensionHowManyLine("Living: 12' × 14'")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("Lifeproof Oak 5mm")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 8 box")).toBe("Lifeproof Oak");
    expect(stripCrewIdentityFromCustomerLabel("Rebond pad — 4 roll")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("Living room — 12' × 14'")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");

    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("12' × 14'")).toBe("");
    expect(customerFacingLineNote("Living: 12' × 14'")).toBe("");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "carpet",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("12' Shaw"))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Living room — 12' × 14'"))).toBe("Living room");
    expect(customerLineLabel(asLine("Lifeproof Oak — 8 box", { category: "lvp" }))).toBe(
      "Lifeproof Oak",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Run: 12' × 14'"],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Run: 12'/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(parsed.flags.join("\n")).toMatch(/12'\s*[×x]\s*14'/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print keep 12' product names — leftover dimension How many is a complete room \/ cut size, not a catalog name. Wrap How many and Self-leveler bag How many stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print keep 12' product names — leftover dimension How many is a complete room \/ cut size, not a catalog name. Wrap How many and Self-leveler bag How many stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0343 Customer copy strips leftover parenthetical dimension How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0343_flooring_knowledge_customer_paren_ft.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0343_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover parenthetical dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewDimensionHowManyLine("12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("(12' × 14')")).toBe(true);
    expect(isCrewDimensionHowManyLine("Living room (12' × 14')")).toBe(false);
    expect(isCrewDimensionHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewDimensionHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewDimensionHowManyLine("Lifeproof Oak 5mm")).toBe(false);
    expect(isCrewDimensionHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewDimensionHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("Living room (12' × 14')")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("(12' × 14')")).toBe("(12' × 14')");
    expect(stripCrewIdentityFromCustomerLabel("Shaw (12')")).toBe("Shaw (12')");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Mohawk Renovate II — cuts: 12' × 14'")).toBe(
      "Mohawk Renovate II",
    );
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    expect(customerFacingLineNote("Living room (12' × 14')")).toBe("Living room");
    expect(customerFacingLineNote("(12' × 14')")).toBe("");
    expect(customerFacingLineNote("Shaw (12')")).toBe("Shaw (12')");
    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("Mohawk Renovate II — cuts: 12' × 14'")).toBe(
      "Mohawk Renovate II",
    );
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "carpet",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("Living room (12' × 14')"))).toBe("Living room");
    expect(customerLineLabel(asLine("12' Shaw"))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Shaw (12')"))).toBe("Shaw (12')");
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm", "Shaw (12')"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Living room (12' × 14')", "Run: 12' × 14'"],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Living room \(12'/);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Shaw \(12'\)/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Shaw \(12'\)/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Living room/);
    expect(shown).not.toMatch(/Living room \(12'/);
    expect(shown).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Shaw (12')");
    expect(parsed.details).toContain("Living room");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(parsed.flags.join("\n")).toMatch(/12'\s*[×x]\s*14'/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover parenthetical dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover parenthetical dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0344 Customer copy strips leftover parenthetical count How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0344_flooring_knowledge_customer_paren_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0344_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover parenthetical count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("(4 roll)")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("(8 box)")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewDimensionHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewDimensionHowManyLine("Living room (12' × 14')")).toBe(false);
    expect(isCrewDimensionHowManyLine("(12' × 14')")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Lifeproof Oak 5mm")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("Rebond pad (4 roll)")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("(4 roll)")).toBe("(4 roll)");
    expect(stripCrewIdentityFromCustomerLabel("Shaw (12')")).toBe("Shaw (12')");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Living room (12' × 14')")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    expect(customerFacingLineNote("Rebond pad (4 roll)")).toBe("Rebond pad");
    expect(customerFacingLineNote("(4 roll)")).toBe("");
    expect(customerFacingLineNote("Shaw (12')")).toBe("Shaw (12')");
    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("Living room (12' × 14')")).toBe("Living room");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("Rebond pad (4 roll)"))).toBe("Rebond pad");
    expect(customerLineLabel(asLine("12' Shaw", { category: "carpet" }))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Shaw (12')", { category: "carpet" }))).toBe("Shaw (12')");
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm", "Shaw (12')"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Rebond pad (4 roll)", "(4 roll)", "Living room (12' × 14')"],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Rebond pad \(4 roll\)/);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Shaw \(12'\)/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Shaw \(12'\)/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Rebond pad/);
    expect(shown).not.toMatch(/Rebond pad \(4 roll\)/);
    expect(shown).not.toMatch(/\(4 roll\)/);
    expect(shown).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Shaw (12')");
    expect(parsed.details).toContain("Rebond pad");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/\(4 roll\)/);
    expect(parsed.details.join("\n")).not.toMatch(/12'\s*[×x]\s*14'/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover parenthetical count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover parenthetical count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0345 Customer copy strips leftover em-dash parenthetical How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0345_flooring_knowledge_customer_dash_paren.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0345_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover em-dash parenthetical How many — those stay in stored job_description so the crew still sees pad rolls and room sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("(4 roll)")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("— (4 roll)")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("(8 box)")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewDimensionHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewDimensionHowManyLine("Living room — (12' × 14')")).toBe(false);
    expect(isCrewDimensionHowManyLine("— (12' × 14')")).toBe(true);
    expect(isCrewDimensionHowManyLine("(12' × 14')")).toBe(true);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Lifeproof Oak 5mm")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("Rebond pad — (4 roll)")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("Rebond pad (4 roll)")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("Living room — (12' × 14')")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("(4 roll)")).toBe("(4 roll)");
    expect(stripCrewIdentityFromCustomerLabel("Shaw (12')")).toBe("Shaw (12')");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    expect(customerFacingLineNote("Rebond pad — (4 roll)")).toBe("Rebond pad");
    expect(customerFacingLineNote("Living room — (12' × 14')")).toBe("Living room");
    expect(customerFacingLineNote("(4 roll)")).toBe("");
    expect(customerFacingLineNote("— (4 roll)")).toBe("");
    expect(customerFacingLineNote("Shaw (12')")).toBe("Shaw (12')");
    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("Rebond pad — (4 roll)"))).toBe("Rebond pad");
    expect(customerLineLabel(asLine("12' Shaw", { category: "carpet" }))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Shaw (12')", { category: "carpet" }))).toBe("Shaw (12')");
    expect(customerLineLabel(asLine("Pad — 12' Shaw", { category: "carpet" }))).toBe(
      "Pad — 12' Shaw",
    );
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm", "Shaw (12')"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [
        "Rebond pad — (4 roll)",
        "Living room — (12' × 14')",
        "(4 roll)",
      ],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Rebond pad — \(4 roll\)/);
    expect(stored).toMatch(/Living room — \(12' × 14'\)/);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Shaw \(12'\)/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Shaw \(12'\)/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Rebond pad/);
    expect(shown).not.toMatch(/Rebond pad —/);
    expect(shown).not.toMatch(/Living room —/);
    expect(shown).not.toMatch(/\(4 roll\)/);
    expect(shown).not.toMatch(/12'\s*[×x]\s*14'/i);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Shaw (12')");
    expect(parsed.details).toContain("Rebond pad");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/Rebond pad —/);
    expect(parsed.details.join("\n")).not.toMatch(/\(4 roll\)/);
    expect(parsed.details.join("\n")).not.toMatch(/12'\s*[×x]\s*14'/i);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover em-dash parenthetical How many — those stay in stored job_description so the crew still sees pad rolls and room sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover em-dash parenthetical How many — those stay in stored job_description so the crew still sees pad rolls and room sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0346 Customer copy strips leftover leading count How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0346_flooring_knowledge_customer_lead_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0346_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover leading count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("4 roll Rebond pad")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewDimensionHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("2nd floor")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Lifeproof Oak 5mm")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("4 roll Rebond pad")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("4 roll — Rebond pad")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("(4 roll) Rebond pad")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("Rebond pad — (4 roll)")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("4 roll")).toBe("4 roll");
    expect(stripCrewIdentityFromCustomerLabel("Shaw (12')")).toBe("Shaw (12')");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("2nd floor")).toBe("2nd floor");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    expect(customerFacingLineNote("4 roll Rebond pad")).toBe("Rebond pad");
    expect(customerFacingLineNote("4 roll — Rebond pad")).toBe("Rebond pad");
    expect(customerFacingLineNote("(4 roll) Rebond pad")).toBe("Rebond pad");
    expect(customerFacingLineNote("4 roll")).toBe("");
    expect(customerFacingLineNote("Shaw (12')")).toBe("Shaw (12')");
    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("4 roll Rebond pad"))).toBe("Rebond pad");
    expect(customerLineLabel(asLine("4 roll — Rebond pad"))).toBe("Rebond pad");
    expect(customerLineLabel(asLine("12' Shaw", { category: "carpet" }))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Shaw (12')", { category: "carpet" }))).toBe("Shaw (12')");
    expect(customerLineLabel(asLine("Pad — 12' Shaw", { category: "carpet" }))).toBe(
      "Pad — 12' Shaw",
    );
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm", "Shaw (12')"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [
        "4 roll Rebond pad",
        "4 roll — Rebond pad",
        "(4 roll) Rebond pad",
      ],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/4 roll Rebond pad/);
    expect(stored).toMatch(/4 roll — Rebond pad/);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Shaw \(12'\)/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Shaw \(12'\)/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Rebond pad/);
    expect(shown).not.toMatch(/4 roll/);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Shaw (12')");
    expect(parsed.details).toContain("Rebond pad");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/4 roll/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover leading count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover leading count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0347 Customer copy strips leftover leading dimension How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0347_flooring_knowledge_customer_lead_ft.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0347_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover leading dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewDimensionHowManyLine("12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("12' × 14' Living room")).toBe(false);
    expect(isCrewDimensionHowManyLine("(12' × 14')")).toBe(true);
    expect(isCrewDimensionHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewDimensionHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewDimensionHowManyLine("Lifeproof Oak 5mm")).toBe(false);
    expect(isCrewStairStepHowManyLine("13 steps, tread + riser")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("4 roll Rebond pad")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("12' × 14' Living room")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("12' × 14' — Living room")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("(12' × 14') Living room")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("12' × 14'")).toBe("12' × 14'");
    expect(stripCrewIdentityFromCustomerLabel("(12' × 14')")).toBe("(12' × 14')");
    expect(stripCrewIdentityFromCustomerLabel("Living room (12' × 14')")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("Shaw (12')")).toBe("Shaw (12')");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("2nd floor")).toBe("2nd floor");
    expect(stripCrewIdentityFromCustomerLabel("13 steps, tread + riser")).toBe(
      "13 steps, tread + riser",
    );
    expect(stripCrewIdentityFromCustomerLabel("4 roll Rebond pad")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    expect(customerFacingLineNote("12' × 14' Living room")).toBe("Living room");
    expect(customerFacingLineNote("12' × 14' — Living room")).toBe("Living room");
    expect(customerFacingLineNote("(12' × 14') Living room")).toBe("Living room");
    expect(customerFacingLineNote("12' × 14'")).toBe("");
    expect(customerFacingLineNote("(12' × 14')")).toBe("");
    expect(customerFacingLineNote("Shaw (12')")).toBe("Shaw (12')");
    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("13 steps, tread + riser")).toBe("");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("12' × 14' Living room"))).toBe("Living room");
    expect(customerLineLabel(asLine("12' × 14' — Living room"))).toBe("Living room");
    expect(customerLineLabel(asLine("12' Shaw", { category: "carpet" }))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Shaw (12')", { category: "carpet" }))).toBe("Shaw (12')");
    expect(customerLineLabel(asLine("Pad — 12' Shaw", { category: "carpet" }))).toBe(
      "Pad — 12' Shaw",
    );
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm", "Shaw (12')"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [
        "12' × 14' Living room",
        "12' × 14' — Living room",
        "(12' × 14') Living room",
      ],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/12' × 14' Living room/);
    expect(stored).toMatch(/12' × 14' — Living room/);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Shaw \(12'\)/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Shaw \(12'\)/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Living room/);
    expect(shown).not.toMatch(/12' × 14'/);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Shaw (12')");
    expect(parsed.details).toContain("Living room");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/12' × 14'/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover leading dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover leading dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0348 Customer copy strips leftover trailing dimension How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0348_flooring_knowledge_customer_trail_ft.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0348_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover trailing dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewDimensionHowManyLine("12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("Living room 12' × 14'")).toBe(false);
    expect(isCrewDimensionHowManyLine("12' × 14' Living room")).toBe(false);
    expect(isCrewDimensionHowManyLine("Living: 12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewDimensionHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewDimensionHowManyLine("Lifeproof Oak 5mm")).toBe(false);
    expect(isCrewStairStepHowManyLine("13 steps, tread + riser")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("4 roll Rebond pad")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("Living room 12' × 14'")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("Living room 12 ft × 14 ft")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("12' × 14' Living room")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("Mohawk Renovate II — cuts: 12' × 14'")).toBe(
      "Mohawk Renovate II",
    );
    expect(stripCrewIdentityFromCustomerLabel("12' × 14'")).toBe("12' × 14'");
    expect(stripCrewIdentityFromCustomerLabel("Living room (12' × 14')")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("Shaw (12')")).toBe("Shaw (12')");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("2nd floor")).toBe("2nd floor");
    expect(stripCrewIdentityFromCustomerLabel("13 steps, tread + riser")).toBe(
      "13 steps, tread + riser",
    );
    expect(stripCrewIdentityFromCustomerLabel("4 roll Rebond pad")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    expect(customerFacingLineNote("Living room 12' × 14'")).toBe("Living room");
    expect(customerFacingLineNote("Living room 12 ft × 14 ft")).toBe("Living room");
    expect(customerFacingLineNote("12' × 14'")).toBe("");
    expect(customerFacingLineNote("Shaw (12')")).toBe("Shaw (12')");
    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("13 steps, tread + riser")).toBe("");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("Living room 12' × 14'"))).toBe("Living room");
    expect(customerLineLabel(asLine("12' Shaw", { category: "carpet" }))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Shaw (12')", { category: "carpet" }))).toBe("Shaw (12')");
    expect(customerLineLabel(asLine("Pad — 12' Shaw", { category: "carpet" }))).toBe(
      "Pad — 12' Shaw",
    );
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm", "Shaw (12')"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [
        "Living room 12' × 14'",
        "Living room 12 ft × 14 ft",
        "12' × 14' Living room",
      ],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Living room 12' × 14'/);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Shaw \(12'\)/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Shaw \(12'\)/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Living room/);
    expect(shown).not.toMatch(/12' × 14'/);
    expect(shown).not.toMatch(/12 ft × 14 ft/);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Shaw (12')");
    expect(parsed.details).toContain("Living room");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/12' × 14'/);
    expect(parsed.details.join("\n")).not.toMatch(/12 ft × 14 ft/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover trailing dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover trailing dimension How many — those stay in stored job_description so the crew still sees room \/ cut sizes. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0349 Customer copy strips leftover trailing count How many", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0349_flooring_knowledge_customer_trail_qty.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0349_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Customer \/ portal \/ print strip leftover trailing count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(isCrewUnlabeledCountHowManyLine("4 roll")).toBe(true);
    expect(isCrewUnlabeledCountHowManyLine("Rebond pad 4 roll")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("4 roll Rebond pad")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Stair wrap: 8 box")).toBe(false);
    expect(isCrewLabeledCountHowManyLine("Self-leveler: 15 bag")).toBe(false);
    expect(isCrewDimensionHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewDimensionHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("12' Shaw")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Shaw (12')")).toBe(false);
    expect(isCrewUnlabeledCountHowManyLine("Lifeproof Oak 5mm")).toBe(false);
    expect(isCrewStairStepHowManyLine("13 steps, tread + riser")).toBe(true);
    expect(isCrewDimensionHowManyLine("Living: 12' × 14'")).toBe(true);
    expect(isCrewDimensionHowManyLine("Living room 12' × 14'")).toBe(false);

    expect(stripCrewIdentityFromCustomerLabel("Rebond pad 4 roll")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("4 roll Rebond pad")).toBe("Rebond pad");
    expect(stripCrewIdentityFromCustomerLabel("4 roll")).toBe("4 roll");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 4 roll")).toBe("Pad");
    expect(stripCrewIdentityFromCustomerLabel("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(stripCrewIdentityFromCustomerLabel("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");
    expect(stripCrewIdentityFromCustomerLabel("Shaw (12')")).toBe("Shaw (12')");
    expect(stripCrewIdentityFromCustomerLabel("12' Shaw")).toBe("12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(stripCrewIdentityFromCustomerLabel("Lifeproof Oak — 5mm")).toBe("Lifeproof Oak — 5mm");
    expect(stripCrewIdentityFromCustomerLabel("2nd floor")).toBe("2nd floor");
    expect(stripCrewIdentityFromCustomerLabel("13 steps, tread + riser")).toBe(
      "13 steps, tread + riser",
    );
    expect(stripCrewIdentityFromCustomerLabel("Living room 12' × 14'")).toBe("Living room");
    expect(stripCrewIdentityFromCustomerLabel("Mohawk Renovate II — cuts: 12' × 14'")).toBe(
      "Mohawk Renovate II",
    );

    expect(customerFacingLineNote("Rebond pad 4 roll")).toBe("Rebond pad");
    expect(customerFacingLineNote("4 roll")).toBe("");
    expect(customerFacingLineNote("Pad — 4 roll")).toBe("Pad");
    expect(customerFacingLineNote("Stair wrap: 8 box")).toBe("Stair wrap: 8 box");
    expect(customerFacingLineNote("Self-leveler: 15 bag")).toBe("Self-leveler: 15 bag");
    expect(customerFacingLineNote("Shaw (12')")).toBe("Shaw (12')");
    expect(customerFacingLineNote("12' Shaw")).toBe("12' Shaw");
    expect(customerFacingLineNote("Pad — 12' Shaw")).toBe("Pad — 12' Shaw");
    expect(customerFacingLineNote("Lifeproof Oak 5mm")).toBe("Lifeproof Oak 5mm");
    expect(customerFacingLineNote("13 steps, tread + riser")).toBe("");

    const asLine = (description: string, extra: Partial<EstimateLineItem> = {}) =>
      ({
        id: "l1",
        option_id: "o1",
        position: 0,
        room: null,
        description,
        note: null,
        line_type: "mat_labor",
        sqft: null,
        length_in: null,
        width_in: null,
        measure_unit: "sqft",
        material_rate: 40,
        labor_rate: 0,
        installed_rate: null,
        flat_amount: null,
        waste_pct: 0,
        product_id: null,
        manufacturer: null,
        style: null,
        color: null,
        item_no: null,
        material_cost: 40,
        labor_cost: 0,
        quantity: 4,
        unit: "roll",
        category: "underlayment",
        ...extra,
      }) as EstimateLineItem;

    expect(customerLineLabel(asLine("Rebond pad 4 roll"))).toBe("Rebond pad");
    expect(customerLineLabel(asLine("4 roll Rebond pad"))).toBe("Rebond pad");
    expect(customerLineLabel(asLine("12' Shaw", { category: "carpet" }))).toBe("12' Shaw");
    expect(customerLineLabel(asLine("Shaw (12')", { category: "carpet" }))).toBe("Shaw (12')");
    expect(customerLineLabel(asLine("Pad — 12' Shaw", { category: "carpet" }))).toBe(
      "Pad — 12' Shaw",
    );
    expect(customerLineLabel(asLine("Lifeproof Oak 5mm", { category: "lvp" }))).toBe(
      "Lifeproof Oak 5mm",
    );

    const review = buildSalespersonReview({
      rooms: [],
      products: ["12' Shaw", "Lifeproof Oak 5mm", "Shaw (12')"],
      takeoffs: [],
      ctx: emptyInstallContext(),
      removal: [],
      installation: ["Rebond pad 4 roll", "4 roll Rebond pad", "Pad — 4 roll"],
      prep: ["Self-leveler: 15 bag — not taped square feet"],
      accessories: ["Stair wrap: 8 box — not taped square feet and not a 30-yard roll"],
      specials: ["Occupancy: Occupied"],
    });
    const stored = reviewToJobNotes(review);
    expect(stored).toMatch(/Rebond pad 4 roll/);
    expect(stored).toMatch(/4 roll Rebond pad/);
    expect(stored).toMatch(/12' Shaw/);
    expect(stored).toMatch(/Shaw \(12'\)/);
    const shown = customerFacingJobNotes(stored);
    expect(shown).toMatch(/Guided takeoff:/);
    expect(shown).toMatch(/12' Shaw/);
    expect(shown).toMatch(/Shaw \(12'\)/);
    expect(shown).toMatch(/Lifeproof Oak 5mm/);
    expect(shown).toMatch(/Stair wrap: 8 box/);
    expect(shown).toMatch(/Self-leveler: 15 bag/);
    expect(shown).toMatch(/Rebond pad/);
    expect(shown).toMatch(/\bPad\b/);
    expect(shown).not.toMatch(/4 roll/);
    expect(shown).not.toMatch(/not taped square feet/i);

    const parsed = parseProjectDetails(stored);
    expect(parsed.details).toContain("12' Shaw");
    expect(parsed.details).toContain("Shaw (12')");
    expect(parsed.details).toContain("Rebond pad");
    expect(parsed.details).toContain("Stair wrap: 8 box");
    expect(parsed.details.join("\n")).not.toMatch(/4 roll/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/reviewToJobNotes/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).toMatch(/customerFacingJobNotes/);
    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).toMatch(/parseProjectDetails/);
    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).not.toMatch(/customerFacingJobNotes/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover trailing count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Customer \/ portal \/ print strip leftover trailing count How many — those stay in stored job_description so the crew still sees pad rolls. Wrap colon How many and Self-leveler bag How many stay. 12' product names stay. 5mm product names stay/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
  });

  it("0350 Catalog box rate onto an area line is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0350_flooring_knowledge_boxed_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0350_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Catalog box rate onto an area line is \$\/coverage, not 1:1 — wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const boxed = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
      material_rate: 45.62,
    };
    const conv = catalogToLineMeasure(boxed);
    expect(conv.count).toBe(false);
    expect(conv.lineUnit).toBe("sq ft");
    expect(conv.factor).toBeCloseTo(1 / 23.64, 10);
    expect(catalogCostInLineUnit(boxed).amount).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(catalogRateInLineUnit(45.62, boxed, false)).toBe(
      Math.round((45.62 / 23.64) * 100) / 100,
    );
    expect(catalogToLineMeasure({ ...boxed, sqft_per_box: null }).count).toBe(true);
    expect(catalogToLineMeasure({ ...boxed, sqft_per_box: null }).factor).toBe(1);
    expect(catalogRateInLineUnit(45.62, { ...boxed, sqft_per_box: null }, false)).toBe(45.62);
    expect(catalogToLineMeasure({ ...boxed, category: "other" }).count).toBe(true);
    expect(catalogToLineMeasure({ ...boxed, category: "other" }).factor).toBe(1);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Catalog box rate onto an area line is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(/catalogRateInLineUnit/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/wrap \? \{ \.\.\.p, sqft_per_box: null \} : p/);
    expect(builder).toMatch(/catalogUnitFactor\(d\.unit, lineUnitKey\(l\) === "sqyd"\)/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/catalogRateInLineUnit/);
    expect(q).toMatch(/catalogRateToBillingUnit/);
    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/catalogRateInLineUnit/);
    expect(ai).toMatch(/catalogRateToBillingUnit/);

    const catalog = readFileSync(join(root, "src/app/(app)/catalog/actions.ts"), "utf8");
    expect(catalog).toMatch(/catalogUnitFactor\(p\.unit/);
    expect(catalog).toMatch(/Wrap \/ count How many stays 1:1/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Catalog box rate onto an area line is \$\/coverage, not 1:1 — wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Catalog box rate onto an area line is \$\/coverage, not 1:1 — wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Catalog box rate onto an area line/,
    );
  });

  it("0351 Exclusive carpet-tile catalog box rate onto that area line is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0351_flooring_knowledge_tile_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0351_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile still waits for cuts. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const lvp = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
      material_rate: 45.62,
    };
    expect(catalogToLineMeasure(lvp).count).toBe(false);
    expect(catalogToLineMeasure(lvp).lineUnit).toBe("sq ft");
    expect(catalogToLineMeasure(lvp).factor).toBeCloseTo(1 / 23.64, 10);

    const unanswered = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
      material_rate: 45.62,
    };
    expect(catalogToLineMeasure(unanswered).count).toBe(true);
    expect(catalogToLineMeasure(unanswered).factor).toBe(1);
    expect(catalogRateInLineUnit(45.62, unanswered, true)).toBe(45.62);

    const mixed = {
      ...unanswered,
      carpetInstallSystems: ["stretch_in", "carpet_tile"] as import("@/lib/flooring-knowledge").InstallSystem[],
    };
    expect(catalogToLineMeasure(mixed).count).toBe(true);
    expect(catalogToLineMeasure(mixed).factor).toBe(1);
    expect(catalogRateInLineUnit(45.62, mixed, true)).toBe(45.62);

    const exclusiveTile = {
      ...unanswered,
      carpetInstallSystems: ["carpet_tile"] as import("@/lib/flooring-knowledge").InstallSystem[],
    };
    const conv = catalogToLineMeasure(exclusiveTile);
    expect(conv.count).toBe(false);
    expect(conv.lineUnit).toBe("sq yd");
    expect(conv.factor).toBeCloseTo(9 / 23.64, 10);
    expect(catalogCostInLineUnit(exclusiveTile).amount).toBe(
      Math.round((45.62 * 9) / 23.64 * 100) / 100,
    );
    expect(catalogRateInLineUnit(45.62, exclusiveTile, true)).toBe(
      Math.round((45.62 * 9) / 23.64 * 100) / 100,
    );
    expect(catalogToLineMeasure({ ...exclusiveTile, sqft_per_box: null }).count).toBe(true);
    expect(catalogToLineMeasure({ ...exclusiveTile, sqft_per_box: null }).factor).toBe(1);
    expect(catalogRateInLineUnit(45.62, { ...exclusiveTile, sqft_per_box: null }, true)).toBe(45.62);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(/carpetInstallSystems: product\.carpetInstallSystems/);

    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/catalogRateInLineUnit/);
    expect(q).toMatch(/catalogRateToBillingUnit/);
    expect(q).toMatch(/sqft_per_box:[\s\S]{0,80}carpetInstallSystems: carpetSystems/);
    expect(q).toMatch(
      /rateFor\(p\.materialRate, p\.unit, true, \{\s*category: p\.category \|\| "carpet",\s*sqft_per_box:[\s\S]*?carpetInstallSystems: carpetSystems/,
    );

    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/catalogRateInLineUnit/);
    expect(ai).toMatch(/catalogRateToBillingUnit/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/wrap \? \{ \.\.\.p, sqft_per_box: null \} : p/);
    expect(builder).toMatch(/catalogUnitFactor\(d\.unit, lineUnitKey\(l\) === "sqyd"\)/);

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile still waits for cuts. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile still waits for cuts. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(
      /Exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile still waits for cuts. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile catalog box rate/,
    );
  });

  it("0352 Exclusive carpet-tile Builder boxed rate onto that area line is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0352_flooring_knowledge_builder_tile_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0352_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder boxed rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay How many. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const modular = {
      category: "carpet",
      order_as_roll: false,
      sqft: 200,
      quantity: 22.22,
    };
    expect(carpetLineIsModularCoverage(modular)).toBe(true);
    expect(carpetInstallSystemsForBoxedRate(modular)).toEqual(["carpet_tile"]);
    expect(
      carpetInstallSystemsForBoxedRate({
        category: "carpet",
        order_as_roll: false,
        sqft: null,
        quantity: null,
      }),
    ).toBeUndefined();
    expect(
      carpetInstallSystemsForBoxedRate({
        category: "carpet",
        order_as_roll: true,
        sqft: 200,
        quantity: 22.22,
      }),
    ).toBeUndefined();
    expect(
      carpetInstallSystemsForBoxedRate({
        category: "carpet",
        length_in: 144,
        width_in: 168,
        sqft: 168,
      }),
    ).toBeUndefined();
    expect(carpetInstallSystemsForBoxedRate({ category: "lvp", sqft: 200 })).toBeUndefined();

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
      material_rate: 45.62,
    };
    expect(catalogToLineMeasure(boxed).count).toBe(true);
    expect(catalogToLineMeasure(boxed).factor).toBe(1);
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);

    const onModular = {
      ...boxed,
      carpetInstallSystems: carpetInstallSystemsForBoxedRate(modular),
    };
    const conv = catalogToLineMeasure(onModular);
    expect(conv.count).toBe(false);
    expect(conv.lineUnit).toBe("sq yd");
    expect(conv.factor).toBeCloseTo(9 / 23.64, 10);
    expect(catalogRateInLineUnit(45.62, onModular, true)).toBe(
      Math.round((45.62 * 9) / 23.64 * 100) / 100,
    );

    const lvp = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogToLineMeasure(lvp).count).toBe(false);
    expect(catalogToLineMeasure(lvp).factor).toBeCloseTo(1 / 23.64, 10);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/carpetInstallSystemsForBoxedRate/);
    expect(builder).toMatch(/wrap \? \{ \.\.\.p, sqft_per_box: null \} : p/);
    expect(builder).toMatch(/catalogUnitFactor\(d\.unit, lineUnitKey\(l\) === "sqyd"\)/);
    expect(builder).toMatch(
      /carpetInstallSystems: wrap \|\| isCountLine\(l\) \? undefined : lineCarpetInstallSystems\(l\)/,
    );

    const catalog = readFileSync(join(root, "src/app/(app)/catalog/actions.ts"), "utf8");
    expect(catalog).toMatch(/catalogUnitFactor\(p\.unit/);
    expect(catalog).toMatch(/Wrap \/ count How many stays 1:1/);
    expect(catalog).toMatch(
      /carpetInstallSystems: input\.count \? undefined : input\.carpetInstallSystems/,
    );
    expect(catalog).toMatch(/Do not infer exclusive tile from unit=box/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder boxed rate onto that area line is \$\/coverage, not 1:1/,
    );

    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Exclusive carpet-tile Builder boxed rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay How many. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(
      /Exclusive carpet-tile Builder boxed rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay How many. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile Builder boxed rate/,
    );
  });

  it("0353 AI notes exclusive carpet-tile catalog box rate onto that area line is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0353_flooring_knowledge_ai_tile_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0353_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /AI notes exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(notesCarpetInstallSystemsForBoxedRate({ type: "carpet tile" })).toEqual(["carpet_tile"]);
    expect(notesCarpetInstallSystemsForBoxedRate({ type: "carpet-tile" })).toEqual(["carpet_tile"]);
    expect(notesCarpetInstallSystemsForBoxedRate({ type: "carpet tiles" })).toEqual(["carpet_tile"]);
    expect(
      notesCarpetInstallSystemsForBoxedRate({ type: "carpet", notes: "modular carpet" }),
    ).toEqual(["carpet_tile"]);
    expect(
      notesCarpetInstallSystemsForBoxedRate({
        type: "carpet",
        material: "Interface carpet tile",
      }),
    ).toEqual(["carpet_tile"]);
    expect(notesCarpetInstallSystemsForBoxedRate({ type: "carpet" })).toBeUndefined();
    expect(notesCarpetInstallSystemsForBoxedRate({ type: "broadloom" })).toBeUndefined();
    expect(notesCarpetInstallSystemsForBoxedRate({ type: "tile" })).toBeUndefined();
    expect(
      notesCarpetInstallSystemsForBoxedRate({
        type: "carpet tile",
        notes: "stretch-in living room",
      }),
    ).toBeUndefined();
    expect(
      notesCarpetInstallSystemsForBoxedRate({
        type: "carpet",
        notes: "stretch in + carpet tile",
      }),
    ).toBeUndefined();
    expect(
      notesCarpetInstallSystemsForBoxedRate({ type: "carpet", material: "Shaw 20 box" }),
    ).toBeUndefined();

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
      material_rate: 45.62,
    };
    expect(catalogToLineMeasure(boxed).count).toBe(true);
    expect(catalogToLineMeasure(boxed).factor).toBe(1);
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);

    const onNotesTile = {
      ...boxed,
      carpetInstallSystems: notesCarpetInstallSystemsForBoxedRate({ type: "carpet tile" }),
    };
    const conv = catalogToLineMeasure(onNotesTile);
    expect(conv.count).toBe(false);
    expect(conv.lineUnit).toBe("sq yd");
    expect(conv.factor).toBeCloseTo(9 / 23.64, 10);
    expect(catalogRateInLineUnit(45.62, onNotesTile, true)).toBe(
      Math.round((45.62 * 9) / 23.64 * 100) / 100,
    );
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 23.64,
        carpetInstallSystems: notesCarpetInstallSystemsForBoxedRate({ type: "carpet tile" }),
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "carpet",
        productUnit: "box",
        sqftPerBox: 23.64,
        carpetInstallSystems: notesCarpetInstallSystemsForBoxedRate({ type: "carpet" }),
      }),
    ).toBe(false);

    const lvp = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogToLineMeasure(lvp).count).toBe(false);
    expect(catalogToLineMeasure(lvp).factor).toBeCloseTo(1 / 23.64, 10);

    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/notesCarpetInstallSystemsForBoxedRate/);
    expect(ai).toMatch(/carpetInstallSystems: notesSystems/);
    expect(ai).toMatch(/catalogRateInLineUnit/);
    expect(ai).toMatch(/catalogRateToBillingUnit/);
    expect(ai).toMatch(
      /laborRate = rateFor\(Number\(match\.labor_rate\) \|\| 0, match\.unit, isYd\)/,
    );
    expect(ai).not.toMatch(/Do not pass carpetInstallSystems/);
    expect(ai).toMatch(/Do not infer exclusive tile from unit=box/);

    const extract = readFileSync(join(root, "src/lib/extract.ts"), "utf8");
    expect(extract).toMatch(/carpet tile \/ modular carpet→carpet/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /AI notes exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1/,
    );

    const help =
      /AI notes exclusive carpet-tile catalog box rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /AI notes exclusive carpet-tile catalog box rate/,
    );
  });

  it("0354 Exclusive carpet-tile PO / warehouse / work-order carton math from sq ft ÷ coverage", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0354_flooring_knowledge_tile_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0354_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile PO \/ warehouse \/ work-order carton math from sq ft ÷ coverage — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(billedQtyToSqft(22.22, "sqyd")).toBeCloseTo(199.98, 10);

    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);

    const tile = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false as const,
      sqft: 200,
      quantity: 22.22,
    };
    expect(lineUsesAreaCartonMath(tile)).toBe(true);
    expect(hardSurfaceAreaCartonCount(tile, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tile, 22.22)).not.toBe(1);

    expect(
      hardSurfaceAreaCartonCount(
        {
          ...tile,
          order_as_roll: true,
        },
        22.22,
      ),
    ).toBe(0);
    expect(
      lineUsesAreaCartonMath({
        category: "carpet",
        unit: "sq yd",
        sqft_per_box: 23.64,
        order_as_roll: true,
        quantity: 22.22,
      }),
    ).toBe(false);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          length_in: 144,
          width_in: 168,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description:
            "Lifeproof Oak — 8 box (13 steps, tread + riser — not an automatic sq ft/step order)",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(0);

    const calc = readFileSync(join(root, "src/lib/estimate-calc.ts"), "utf8");
    expect(calc).toMatch(/export function lineUsesAreaCartonMath/);
    expect(calc).toMatch(/export function hardSurfaceAreaCartonCount/);
    expect(calc).toMatch(
      /Exclusive carpet-tile PO \/ warehouse \/ work-order carton math from sq ft ÷ coverage/,
    );
    expect(calc).not.toMatch(/category === ["']carpet_tile["']/);

    const po = readFileSync(join(root, "src/app/(app)/purchase-orders/po-builder.tsx"), "utf8");
    expect(po).toMatch(/lineUsesAreaCartonMath/);
    expect(po).toMatch(/hardSurfaceAreaCartonCount/);

    const print = readFileSync(join(root, "src/app/(app)/purchase-orders/[id]/po-print.tsx"), "utf8");
    expect(print).toMatch(/lineUsesAreaCartonMath/);
    expect(print).toMatch(/billedQtyToSqft/);

    const staging = readFileSync(join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"), "utf8");
    expect(staging).toMatch(/lineUsesAreaCartonMath/);
    expect(staging).toMatch(/billedQtyToSqft/);

    const wo = readFileSync(join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"), "utf8");
    expect(wo).toMatch(/hardSurfaceAreaCartonCount/);
    expect(wo).toMatch(/Tile bills per sq yd/);

    const help =
      /Exclusive carpet-tile PO \/ warehouse \/ work-order carton math from sq ft ÷ coverage — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile PO \/ warehouse \/ work-order carton math/,
    );
  });

  it("0355 Exclusive carpet-tile PO boxed rate onto that area line is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0355_flooring_knowledge_po_tile_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0355_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile PO boxed rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      poCarpetInstallSystemsForBoxedRate({ category: "carpet", quantity: 22.22 }),
    ).toEqual(["carpet_tile"]);
    expect(
      poCarpetInstallSystemsForBoxedRate({ category: "carpet" }),
    ).toBeUndefined();
    expect(
      poCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        quantity: 22.22,
        roll_width_ft: 12,
      }),
    ).toBeUndefined();
    expect(
      poCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        quantity: 22.22,
        order_as_roll: true,
      }),
    ).toBeUndefined();
    expect(
      poCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        quantity: 22.22,
        length_in: 144,
        width_in: 168,
      }),
    ).toBeUndefined();
    expect(
      poCarpetInstallSystemsForBoxedRate({ category: "lvp", quantity: 300 }),
    ).toBeUndefined();

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
      material_rate: 45.62,
    };
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          carpetInstallSystems: poCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            quantity: 22.22,
          }),
        },
        true,
      ),
    ).toBe(Math.round((45.62 * 9) / 23.64 * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          sqft_per_box: null,
          carpetInstallSystems: poCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            quantity: 22.22,
          }),
        },
        true,
      ),
    ).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          carpetInstallSystems: poCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            quantity: 22.22,
            roll_width_ft: 12,
          }),
        },
        true,
      ),
    ).toBe(45.62);

    const po = readFileSync(join(root, "src/app/(app)/purchase-orders/po-builder.tsx"), "utf8");
    expect(po).toMatch(/catalogRateInLineUnit/);
    expect(po).toMatch(/poCarpetInstallSystemsForBoxedRate/);
    expect(po).toMatch(/skip \? base : converted\.unitCost/);
    expect(po).toMatch(
      /Exclusive carpet-tile PO boxed rate onto that area line is \$\/coverage, not 1:1/,
    );
    expect(po).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile PO boxed rate onto that area line is \$\/coverage, not 1:1/,
    );

    const help =
      /Exclusive carpet-tile PO boxed rate onto that area line is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile PO boxed rate/,
    );
  });

  it("0356 Hard-surface PO boxed rate onto that area line is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0356_flooring_knowledge_po_hs_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0356_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Hard-surface PO boxed rate onto that area line is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const boxed = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
      material_rate: 45.62,
    };
    expect(catalogRateInLineUnit(45.62, boxed, false)).toBe(
      Math.round((45.62 / 23.64) * 100) / 100,
    );
    expect(catalogRateInLineUnit(45.62, { ...boxed, sqft_per_box: null }, false)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        { category: "hardwood", unit: "box", sqft_per_box: 23.64 },
        false,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(45.62, { category: "lvp", unit: "box" }, false),
    ).toBe(45.62);

    const po = readFileSync(join(root, "src/app/(app)/purchase-orders/po-builder.tsx"), "utf8");
    expect(po).toMatch(/catalogRateInLineUnit/);
    expect(po).toMatch(/skip \? base : converted\.unitCost/);
    expect(po).toMatch(
      /Hard-surface PO boxed rate onto that area line is \$\/coverage, not 1:1/,
    );
    expect(po).toMatch(/cls === "hard"[\s\S]*?catalogRateInLineUnit/);
    expect(po).not.toMatch(/catalogRateToBillingUnit\(base, p\.unit, false\)/);
    expect(po).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Hard-surface PO boxed rate onto that area line is \$\/coverage, not 1:1/,
    );

    const help =
      /Hard-surface PO boxed rate onto that area line is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Hard-surface PO boxed rate/,
    );
  });

  it("0357 Exclusive carpet-tile catalog picker boxed rate onto sq yd is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0357_flooring_knowledge_picker_tile_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0357_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile catalog picker boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
      }),
    ).toEqual(["carpet_tile"]);
    expect(
      catalogCarpetInstallSystemsForBoxedRate({ category: "carpet" }),
    ).toBeUndefined();
    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
        roll_width_ft: 12,
      }),
    ).toBeUndefined();
    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "lvp",
        sqft_per_box: 23.64,
      }),
    ).toBeUndefined();

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate(boxed),
        },
        true,
      ),
    ).toBe(Math.round((45.62 * 9) / 23.64 * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          sqft_per_box: null,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            sqft_per_box: null,
          }),
        },
        true,
      ),
    ).toBe(45.62);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/catalogRateInLineUnit/);
    expect(picker).toMatch(/catalogCarpetInstallSystemsForBoxedRate/);
    expect(picker).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile catalog picker boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );

    const help =
      /Exclusive carpet-tile catalog picker boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile catalog picker boxed rate/,
    );
  });

  it("0358 Hard-surface catalog picker boxed rate onto sq ft is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0358_flooring_knowledge_picker_hs_rate.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0358_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Hard-surface catalog picker boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const boxed = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, false)).toBe(
      Math.round((45.62 / 23.64) * 100) / 100,
    );
    expect(catalogRateInLineUnit(45.62, { ...boxed, sqft_per_box: null }, false)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        { category: "hardwood", unit: "box", sqft_per_box: 23.64 },
        false,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        { category: "laminate", unit: "box", sqft_per_box: 23.64 },
        false,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        { category: "tile", unit: "box", sqft_per_box: 23.64 },
        false,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(45.62, { category: "lvp", unit: "box" }, false),
    ).toBe(45.62);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "box",
      }),
    ).toBe(false);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/catalogRateInLineUnit/);
    expect(picker).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(picker).toMatch(/const perSqft/);
    expect(picker).toMatch(/formatMoney\(perSqft\)\}\/sq ft/);
    expect(picker).toMatch(
      /Hard-surface catalog picker boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Hard-surface catalog picker boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );

    const help =
      /Hard-surface catalog picker boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Hard-surface catalog picker boxed rate/,
    );
  });

  it("0359 Hard-surface catalog picker swap boxed rate onto sq ft is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0359_flooring_knowledge_picker_hs_swap.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0359_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Hard-surface catalog picker swap boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const boxed = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, false)).toBe(
      Math.round((45.62 / 23.64) * 100) / 100,
    );
    expect(catalogRateInLineUnit(45.62, { ...boxed, sqft_per_box: null }, false)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        { category: "hardwood", unit: "box", sqft_per_box: 23.64 },
        false,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(45.62, { category: "lvp", unit: "box" }, false),
    ).toBe(45.62);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({ family: "lvp", productUnit: "box" }),
    ).toBe(false);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/pickerSwapRate/);
    expect(picker).toMatch(/catalogRateInLineUnit/);
    expect(picker).toMatch(/boxedCartonAreaTakeoffAllowed/);
    expect(picker).toMatch(/swapRate\?\.label\} per \{swapRate\?\.unit\}/);
    expect(picker).toMatch(
      /Hard-surface catalog picker swap boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Hard-surface catalog picker boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);
    expect(picker).not.toMatch(/pickerMoney\(pending, purpose\)\.label\} per \{pending\.unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Hard-surface catalog picker swap boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );

    const help =
      /Hard-surface catalog picker swap boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Hard-surface catalog picker swap boxed rate/,
    );
  });

  it("0360 Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0360_flooring_knowledge_picker_tile_swap.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0360_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
      }),
    ).toEqual(["carpet_tile"]);
    expect(
      catalogCarpetInstallSystemsForBoxedRate({ category: "carpet" }),
    ).toBeUndefined();
    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
        roll_width_ft: 12,
      }),
    ).toBeUndefined();

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate(boxed),
        },
        true,
      ),
    ).toBe(Math.round(((45.62 * 9) / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          sqft_per_box: null,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            sqft_per_box: null,
          }),
        },
        true,
      ),
    ).toBe(45.62);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/pickerSwapRate/);
    expect(picker).toMatch(/catalogCarpetInstallSystemsForBoxedRate/);
    expect(picker).toMatch(/isCarpet && \(isAreaUnit\(p\.unit\) \|\| boxedArea\)/);
    expect(picker).toMatch(/unit: "sq yd"/);
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Hard-surface catalog picker swap boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );

    const help =
      /Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile catalog picker swap boxed rate/,
    );
  });

  it("0361 Hard-surface catalog picker cost line boxed rate onto sq ft is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0361_flooring_knowledge_picker_hs_cost.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0361_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const boxed = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, false)).toBe(
      Math.round((45.62 / 23.64) * 100) / 100,
    );
    expect(catalogRateInLineUnit(45.62, { ...boxed, sqft_per_box: null }, false)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        { category: "hardwood", unit: "box", sqft_per_box: 23.64 },
        false,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(45.62, { category: "lvp", unit: "box" }, false),
    ).toBe(45.62);
    expect(
      boxedCartonAreaTakeoffAllowed({
        family: "lvp",
        productUnit: "box",
        sqftPerBox: 23.64,
      }),
    ).toBe(true);
    expect(
      boxedCartonAreaTakeoffAllowed({ family: "lvp", productUnit: "box" }),
    ).toBe(false);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/const costAmount = money\.cost\.amount/);
    expect(picker).toMatch(/!isCarpet && boxedArea && costAmount != null/);
    expect(picker).toMatch(/catalogRateInLineUnit\(\s*costAmount/);
    expect(picker).toMatch(/formatMoney\(costLine \?\? 0\)/);
    expect(picker).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Hard-surface catalog picker boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Hard-surface catalog picker swap boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);
    expect(picker).not.toMatch(/formatMoney\(money\.cost\.amount \?\? 0\)/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );

    const help =
      /Hard-surface catalog picker cost line boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Hard-surface catalog picker cost line boxed rate/,
    );
  });

  it("0362 Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0362_flooring_knowledge_picker_tile_cost.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0362_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
      }),
    ).toEqual(["carpet_tile"]);
    expect(
      catalogCarpetInstallSystemsForBoxedRate({ category: "carpet" }),
    ).toBeUndefined();
    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
        roll_width_ft: 12,
      }),
    ).toBeUndefined();

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate(boxed),
        },
        true,
      ),
    ).toBe(Math.round(((45.62 * 9) / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          sqft_per_box: null,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            sqft_per_box: null,
          }),
        },
        true,
      ),
    ).toBe(45.62);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/isCarpet && boxedArea && costAmount != null/);
    expect(picker).toMatch(/!isCarpet && boxedArea && costAmount != null/);
    expect(picker).toMatch(/catalogRateInLineUnit\(\s*costAmount/);
    expect(picker).toMatch(/carpetInstallSystems/);
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );

    const help =
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate/,
    );
  });

  it("0363 Exclusive carpet-tile PO carton helper boxed rate onto $/carton is sq ft coverage, not sq yd × coverage 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0363_flooring_knowledge_po_tile_carton_cost.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0363_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile PO carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(billedRateToCartonCost(1.93, "sqft", 23.64)).toBe(
      Math.round((1.93 / 1) * 23.64 * 100) / 100,
    );
    expect(billedRateToCartonCost(17.37, "sqyd", 23.64)).toBe(
      Math.round((17.37 / 9) * 23.64 * 100) / 100,
    );
    expect(billedRateToCartonCost(45.62, "box", 23.64)).toBeNull();
    expect(billedRateToCartonCost(17.37, "sqyd", 0)).toBeNull();
    expect(billedQtyToSqft(1, "sqyd")).toBe(9);

    const po = readFileSync(
      join(root, "src/app/(app)/purchase-orders/po-builder.tsx"),
      "utf8",
    );
    expect(po).toMatch(/billedRateToCartonCost/);
    expect(po).toMatch(/lineUnitKey\(it\)/);
    expect(po).toMatch(
      /Exclusive carpet-tile PO carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );
    expect(po).not.toMatch(
      /\(Number\(it\.unit_cost\) \|\| 0\) \* Number\(it\.sqft_per_box\)/,
    );
    expect(po).not.toMatch(/catalogRateToBillingUnit/);
    expect(po).not.toMatch(/category === ["']carpet_tile["']/);

    const units = readFileSync(join(root, "src/lib/units.ts"), "utf8");
    expect(units).toMatch(/export function billedRateToCartonCost/);
    expect(units).toMatch(
      /Exclusive carpet-tile PO carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile PO carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );

    const help =
      /Exclusive carpet-tile PO carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile PO carton helper boxed rate/,
    );
  });

  it("0364 Catalog picker clearance badge boxed rate onto sq ft / exclusive-tile sq yd is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0364_flooring_knowledge_picker_clearance.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0364_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Hard-surface catalog picker clearance badge boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(
      /Exclusive carpet-tile catalog picker clearance badge boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
      }),
    ).toEqual(["carpet_tile"]);
    expect(
      catalogCarpetInstallSystemsForBoxedRate({ category: "carpet" }),
    ).toBeUndefined();
    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
        roll_width_ft: 12,
      }),
    ).toBeUndefined();

    const boxedHs = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxedHs, false)).toBe(
      Math.round((45.62 / 23.64) * 100) / 100,
    );
    expect(catalogRateInLineUnit(45.62, { ...boxedHs, sqft_per_box: null }, false)).toBe(45.62);

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate(boxed),
        },
        true,
      ),
    ).toBe(Math.round(((45.62 * 9) / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          sqft_per_box: null,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            sqft_per_box: null,
          }),
        },
        true,
      ),
    ).toBe(45.62);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/const clearanceAmount = p\.clearance_price/);
    expect(picker).toMatch(/isCarpet && boxedArea && clearanceAmount != null/);
    expect(picker).toMatch(/!isCarpet && boxedArea && clearanceAmount != null/);
    expect(picker).toMatch(/catalogRateInLineUnit\(\s*clearanceAmount/);
    expect(picker).toMatch(/formatMoney\(clearanceLine \?\? 0\)/);
    expect(picker).toMatch(/isCarpet && boxedArea && costAmount != null/);
    expect(picker).toMatch(/!isCarpet && boxedArea && costAmount != null/);
    expect(picker).toMatch(
      /Hard-surface catalog picker clearance badge boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker clearance badge boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker swap boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);
    expect(picker).not.toMatch(/formatMoney\(p\.clearance_price\)/);

    const inventory = readFileSync(
      join(root, "src/app/(app)/inventory/page.tsx"),
      "utf8",
    );
    expect(inventory).toMatch(/formatMoney\(p\.clearance_price\)/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Hard-surface catalog picker clearance badge boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile catalog picker clearance badge boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile PO carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );

    const help =
      /Exclusive carpet-tile catalog picker clearance badge boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface catalog picker clearance badge boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /catalog picker clearance badge boxed rate/,
    );
  });

  it("0365 Exclusive carpet-tile PO print carton helper boxed rate onto $/carton is sq ft coverage, not sq yd × coverage 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0365_flooring_knowledge_po_print_carton_cost.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0365_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile PO print carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(billedRateToCartonCost(1.93, "sqft", 23.64)).toBe(
      Math.round((1.93 / 1) * 23.64 * 100) / 100,
    );
    expect(billedRateToCartonCost(17.37, "sqyd", 23.64)).toBe(
      Math.round((17.37 / 9) * 23.64 * 100) / 100,
    );
    expect(billedRateToCartonCost(45.62, "box", 23.64)).toBeNull();
    expect(billedRateToCartonCost(17.37, "sqyd", 0)).toBeNull();
    expect(billedQtyToSqft(1, "sqyd")).toBe(9);

    const poPrint = readFileSync(
      join(root, "src/app/(app)/purchase-orders/[id]/po-print.tsx"),
      "utf8",
    );
    expect(poPrint).toMatch(/billedRateToCartonCost/);
    expect(poPrint).toMatch(/lineUnitKey\(it\)/);
    expect(poPrint).toMatch(
      /Exclusive carpet-tile PO print carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );
    expect(poPrint).toMatch(/formatMoney\(cartonCost\)/);
    expect(poPrint).not.toMatch(
      /\(Number\(it\.unit_cost\) \|\| 0\) \* Number\(it\.sqft_per_box\)/,
    );
    expect(poPrint).not.toMatch(/catalogRateToBillingUnit/);
    expect(poPrint).not.toMatch(/category === ["']carpet_tile["']/);

    const units = readFileSync(join(root, "src/lib/units.ts"), "utf8");
    expect(units).toMatch(/export function billedRateToCartonCost/);
    expect(units).toMatch(
      /Exclusive carpet-tile PO print carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile PO print carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile catalog picker clearance badge boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );

    const help =
      /Exclusive carpet-tile PO print carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Exclusive carpet-tile PO print carton helper boxed rate/,
    );
  });

  it("0366 Exclusive carpet-tile estimate order boxed rate onto sq yd is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0366_flooring_knowledge_order_plan_boxed.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0366_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate order boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface estimate order boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const boxedHs = {
      unit: "box" as const,
      category: "lvp" as const,
      sqft_per_box: 23.64,
    };
    expect(
      poCostOfEstimateLine(
        { unit: "sqft", category: "lvp", material_cost: 0, quantity: 300 },
        45.62,
        boxedHs,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      poCostOfEstimateLine(
        { unit: "sqft", category: "lvp", material_cost: 1.93, quantity: 300 },
        45.62,
        boxedHs,
      ),
    ).toBe(1.93);
    expect(
      poCostOfEstimateLine(
        { unit: "sqft", category: "lvp", material_cost: 0, quantity: 300 },
        45.62,
        { ...boxedHs, sqft_per_box: null },
      ),
    ).toBe(45.62);
    expect(
      poCostOfEstimateLine(
        {
          unit: "box",
          category: "lvp",
          material_cost: 0,
          quantity: 8,
          description: "Stair wrap — wrap qty TBD",
        },
        45.62,
        boxedHs,
      ),
    ).toBe(45.62);

    const boxedTile = {
      unit: "box" as const,
      category: "carpet" as const,
      sqft_per_box: 23.64,
    };
    expect(
      poCostOfEstimateLine(
        {
          unit: "sqyd",
          category: "carpet",
          material_cost: 0,
          quantity: 50,
          sqft_per_box: 23.64,
        },
        45.62,
        boxedTile,
      ),
    ).toBe(Math.round(((45.62 * 9) / 23.64) * 100) / 100);
    expect(
      poCostOfEstimateLine(
        {
          unit: "sqyd",
          category: "carpet",
          material_cost: 0,
          quantity: 50,
          roll_width_ft: 12,
          sqft_per_box: 23.64,
        },
        45.62,
        { ...boxedTile, roll_width_ft: 12 },
      ),
    ).toBe(45.62);
    expect(
      poCostOfEstimateLine(
        { unit: "sqyd", category: "carpet", material_cost: 0, quantity: 50 },
        45.62,
        { unit: "box", category: "carpet" },
      ),
    ).toBe(45.62);
    expect(
      poCostOfEstimateLine(
        { unit: "box", category: "carpet", material_cost: 0, quantity: 8 },
        45.62,
        boxedTile,
      ),
    ).toBe(45.62);

    const poBuild = readFileSync(join(root, "src/lib/po-build.ts"), "utf8");
    expect(poBuild).toMatch(/export function poCostOfEstimateLine/);
    expect(poBuild).toMatch(/catalogRateInLineUnit/);
    expect(poBuild).toMatch(/poCarpetInstallSystemsForBoxedRate/);
    expect(poBuild).toMatch(/lineSkipsAreaCartonMath/);
    expect(poBuild).toMatch(
      /Exclusive carpet-tile estimate order boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(poBuild).toMatch(
      /Hard-surface estimate order boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(poBuild).not.toMatch(/catalogRateToBillingUnit/);
    expect(poBuild).not.toMatch(/category === ["']carpet_tile["']/);

    const poPlan = readFileSync(join(root, "src/lib/data/po-plan.ts"), "utf8");
    expect(poPlan).toMatch(/poCostOfEstimateLine/);
    expect(poPlan).toMatch(
      /Exclusive carpet-tile estimate order boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );

    const poActions = readFileSync(
      join(root, "src/app/(app)/purchase-orders/actions.ts"),
      "utf8",
    );
    expect(poActions).toMatch(/poCostOfEstimateLine/);
    expect(poActions).not.toMatch(/catalogRateToBillingUnit/);
    expect(poActions).not.toMatch(/category === ["']carpet_tile["']/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(orderUi).not.toMatch(/catalogRateToBillingUnit/);
    expect(orderUi).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile PO print carton helper boxed rate onto \$\/carton is sq ft coverage, not sq yd × coverage 1:1/,
    );

    const help =
      /Exclusive carpet-tile estimate order boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface estimate order boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate order boxed rate/,
    );
  });

  it("0367 Catalog picker cost line boxed rate onto sq ft / exclusive-tile sq yd shows /sq ft and /sq yd, not native /box 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0367_flooring_knowledge_picker_cost_unit.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0367_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft shows \/sq ft, not native \/box 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd shows \/sq yd, not native \/box 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
      }),
    ).toEqual(["carpet_tile"]);
    expect(
      catalogCarpetInstallSystemsForBoxedRate({ category: "carpet" }),
    ).toBeUndefined();
    expect(
      catalogCarpetInstallSystemsForBoxedRate({
        category: "carpet",
        sqft_per_box: 23.64,
        roll_width_ft: 12,
      }),
    ).toBeUndefined();

    const boxedHs = {
      category: "lvp" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxedHs, false)).toBe(
      Math.round((45.62 / 23.64) * 100) / 100,
    );
    expect(catalogRateInLineUnit(45.62, { ...boxedHs, sqft_per_box: null }, false)).toBe(45.62);

    const boxed = {
      category: "carpet" as const,
      unit: "box",
      sqft_per_box: 23.64,
    };
    expect(catalogRateInLineUnit(45.62, boxed, true)).toBe(45.62);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate(boxed),
        },
        true,
      ),
    ).toBe(Math.round(((45.62 * 9) / 23.64) * 100) / 100);
    expect(
      catalogRateInLineUnit(
        45.62,
        {
          ...boxed,
          sqft_per_box: null,
          carpetInstallSystems: catalogCarpetInstallSystemsForBoxedRate({
            category: "carpet",
            sqft_per_box: null,
          }),
        },
        true,
      ),
    ).toBe(45.62);

    const picker = readFileSync(
      join(root, "src/app/(app)/estimates/product-picker.tsx"),
      "utf8",
    );
    expect(picker).toMatch(/const costUnit =/);
    expect(picker).toMatch(/isCarpet && boxedArea && costAmount != null/);
    expect(picker).toMatch(/!isCarpet && boxedArea && costAmount != null/);
    expect(picker).toMatch(/formatMoney\(costLine \?\? 0\)/);
    expect(picker).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(picker).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft shows \/sq ft, not native \/box 1:1/,
    );
    expect(picker).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd shows \/sq yd, not native \/box 1:1/,
    );
    expect(picker).not.toMatch(/catalogRateToBillingUnit/);
    expect(picker).not.toMatch(/category === ["']carpet_tile["']/);
    expect(picker).not.toMatch(/formatMoney\(money\.cost\.amount \?\? 0\)/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft shows \/sq ft, not native \/box 1:1/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd shows \/sq yd, not native \/box 1:1/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );

    const help =
      /Exclusive carpet-tile catalog picker cost line boxed rate onto sq yd shows \/sq yd, not native \/box 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft shows \/sq ft, not native \/box 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /cost line boxed rate onto sq (ft|yd) shows/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /shows \/sq (ft|yd), not native \/box/,
    );
  });

  it("0368 Exclusive carpet-tile job purchasing boxed rate onto sq yd is $/coverage, not 1:1", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0368_flooring_knowledge_job_purchasing_boxed.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0368_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job purchasing boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job purchasing boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);

    const boxedHs = {
      unit: "box" as const,
      category: "lvp" as const,
      sqft_per_box: 23.64,
    };
    expect(
      poCostOfEstimateLine(
        { unit: "sqft", category: "lvp", material_cost: 0, quantity: 300 },
        45.62,
        boxedHs,
      ),
    ).toBe(Math.round((45.62 / 23.64) * 100) / 100);
    expect(
      poCostOfEstimateLine(
        { unit: "sqft", category: "lvp", material_cost: 1.93, quantity: 300 },
        45.62,
        boxedHs,
      ),
    ).toBe(1.93);
    expect(
      poCostOfEstimateLine(
        { unit: "sqft", category: "lvp", material_cost: 0, quantity: 300 },
        45.62,
        { ...boxedHs, sqft_per_box: null },
      ),
    ).toBe(45.62);
    expect(
      poCostOfEstimateLine(
        {
          unit: "box",
          category: "lvp",
          material_cost: 0,
          quantity: 8,
          description: "Stair wrap — wrap qty TBD",
        },
        45.62,
        boxedHs,
      ),
    ).toBe(45.62);

    const boxedTile = {
      unit: "box" as const,
      category: "carpet" as const,
      sqft_per_box: 23.64,
    };
    expect(
      poCostOfEstimateLine(
        {
          unit: "sqyd",
          category: "carpet",
          material_cost: 0,
          quantity: 50,
          sqft_per_box: 23.64,
        },
        45.62,
        boxedTile,
      ),
    ).toBe(Math.round(((45.62 * 9) / 23.64) * 100) / 100);
    expect(
      poCostOfEstimateLine(
        {
          unit: "sqyd",
          category: "carpet",
          material_cost: 0,
          quantity: 50,
          roll_width_ft: 12,
          sqft_per_box: 23.64,
        },
        45.62,
        { ...boxedTile, roll_width_ft: 12 },
      ),
    ).toBe(45.62);
    expect(
      poCostOfEstimateLine(
        { unit: "sqyd", category: "carpet", material_cost: 0, quantity: 50 },
        45.62,
        { unit: "box", category: "carpet" },
      ),
    ).toBe(45.62);
    expect(
      poCostOfEstimateLine(
        { unit: "box", category: "carpet", material_cost: 0, quantity: 8 },
        45.62,
        boxedTile,
      ),
    ).toBe(45.62);

    const purchasing = readFileSync(
      join(root, "src/lib/data/job-purchasing.ts"),
      "utf8",
    );
    expect(purchasing).toMatch(/poCostOfEstimateLine/);
    expect(purchasing).toMatch(/productCatalog/);
    expect(purchasing).toMatch(/sqft_per_box, roll_width_ft/);
    expect(purchasing).toMatch(
      /Exclusive carpet-tile job purchasing boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(purchasing).toMatch(
      /Hard-surface job purchasing boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(purchasing).not.toMatch(/unitsAgree/);
    expect(purchasing).not.toMatch(/catalogRateToBillingUnit/);
    expect(purchasing).not.toMatch(/category === ["']carpet_tile["']/);

    const poBuild = readFileSync(join(root, "src/lib/po-build.ts"), "utf8");
    expect(poBuild).toMatch(/export function poCostOfEstimateLine/);
    expect(poBuild).toMatch(
      /Exclusive carpet-tile job purchasing boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(poBuild).toMatch(
      /Hard-surface job purchasing boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(poBuild).not.toMatch(/category === ["']carpet_tile["']/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job purchasing boxed rate onto sq yd is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(
      /Hard-surface job purchasing boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );
    expect(pricing).toMatch(
      /Hard-surface catalog picker cost line boxed rate onto sq ft shows \/sq ft, not native \/box 1:1/,
    );

    const help =
      /Exclusive carpet-tile job purchasing boxed rate onto sq yd is \$\/coverage, not 1:1 — mixed stretch-in \+ tile and unanswered carpet stay 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job purchasing boxed rate onto sq ft is \$\/coverage, not 1:1. Wrap \/ count How many stays 1:1. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job purchasing boxed rate/,
    );
  });

  it("0369 Exclusive carpet-tile warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0369_flooring_knowledge_warehouse_queue_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0369_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount/);
    expect(warehouse).toMatch(/billedQtyToSqft/);
    expect(warehouse).toMatch(/lineUsesAreaCartonMath/);
    expect(warehouse).toMatch(/lineSkipsAreaCartonMath/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(warehouse).not.toMatch(/billedRateToCartonCost/);
    expect(warehouse).not.toMatch(/formatMoney/);
    expect(warehouse).not.toMatch(/catalogRateToBillingUnit/);
    expect(warehouse).not.toMatch(/category === ["']carpet_tile["']/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/hardSurfaceAreaCartonCount/);
    expect(staging).toMatch(/billedQtyToSqft/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job purchasing boxed rate onto sq ft is \$\/coverage, not 1:1/,
    );

    const help =
      /Exclusive carpet-tile warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /warehouse queue carton/,
    );
  });

  it("0370 Exclusive carpet-tile job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0370_flooring_knowledge_job_scope_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0370_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    expect(
      lineSpec({
        quantity: 300,
        unit: "sq ft",
        measure_unit: "sqft",
        sqft: 300,
        length_in: null,
        width_in: null,
        category: "lvp",
        sqft_per_box: 23.64,
      }).cartons,
    ).toBe(13);
    expect(
      lineSpec({
        quantity: 22.22,
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 200,
        length_in: null,
        width_in: null,
        category: "carpet",
        sqft_per_box: 23.64,
        order_as_roll: false,
      }).cartons,
    ).toBe(9);
    expect(
      lineSpec({
        quantity: 8,
        unit: "box",
        measure_unit: "sqft",
        sqft: 300,
        length_in: null,
        width_in: null,
        category: "lvp",
        description: "Lifeproof Oak — wrap qty TBD",
        sqft_per_box: 23.64,
      }).cartons,
    ).toBe(0);
    expect(
      lineSpec({
        quantity: 50,
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 450,
        length_in: null,
        width_in: null,
        category: "carpet",
        sqft_per_box: 23.64,
        roll_width_ft: 12,
      }).cartons,
    ).toBe(0);

    const scopeView = readFileSync(join(root, "src/components/job-scope-view.tsx"), "utf8");
    expect(scopeView).toMatch(/spec\.cartons/);
    expect(scopeView).toMatch(
      /Exclusive carpet-tile job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(scopeView).toMatch(
      /Hard-surface job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(scopeView).not.toMatch(/billedRateToCartonCost/);
    expect(scopeView).not.toMatch(/category === ["']carpet_tile["']/);

    const jobPage = readFileSync(join(root, "src/app/(app)/jobs/[id]/page.tsx"), "utf8");
    expect(jobPage).toMatch(/spec\.cartons/);
    expect(jobPage).toMatch(
      /Exclusive carpet-tile job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(jobPage).toMatch(
      /Hard-surface job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(jobPage).not.toMatch(/billedRateToCartonCost/);
    expect(jobPage).not.toMatch(/category === ["']carpet_tile["']/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/hardSurfaceAreaCartonCount/);
    expect(jobScope).toMatch(
      /Exclusive carpet-tile job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(jobScope).toMatch(
      /Hard-surface job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job scope carton/,
    );
  });

  it("0371 Exclusive carpet-tile estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0371_flooring_knowledge_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0371_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const plan = readFileSync(join(root, "src/lib/data/po-plan.ts"), "utf8");
    expect(plan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(plan).toMatch(
      /Exclusive carpet-tile estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(plan).toMatch(
      /Hard-surface estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(plan).not.toMatch(/billedRateToCartonCost/);
    expect(plan).not.toMatch(/category === ["']carpet_tile["']/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(orderUi).not.toMatch(/billedRateToCartonCost/);
    expect(orderUi).not.toMatch(/category === ["']carpet_tile["']/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job scope carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate order carton/,
    );
  });

  it("0372 Exclusive carpet-tile job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0372_flooring_knowledge_job_materials_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0372_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/hardSurfaceAreaCartonCount/);
    expect(card).toMatch(/cartonCountFor/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/formatMoney/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials carton/,
    );
  });

  it("0373 Exclusive carpet-tile work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0373_flooring_knowledge_wo_editor_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0373_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/hardSurfaceAreaCartonCount/);
    expect(editor).toMatch(/cartonCountFor/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(editor).not.toMatch(/billedRateToCartonCost/);
    expect(editor).not.toMatch(/category === ["']carpet_tile["']/);
    expect(editor).not.toMatch(/formatMoney/);

    const jobPage = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/page.tsx"),
      "utf8",
    );
    expect(jobPage).toMatch(
      /Exclusive carpet-tile work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(jobPage).toMatch(
      /Hard-surface work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(jobPage).toMatch(/sqft_per_box: l\.sqft_per_box/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /work-order editor carton/,
    );
  });

  it("0374 Exclusive carpet-tile job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0374_flooring_knowledge_job_materials_gap_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0374_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        118.2,
      ),
    ).toBe(5);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 13.32,
        },
        13.32,
      ),
    ).toBe(6);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/hardSurfaceAreaCartonCount/);
    expect(card).toMatch(/cartonCountForQty/);
    expect(card).toMatch(/gapCartons/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/formatMoney/);
    expect(card).toMatch(/l\.purchasingGap/);
    expect(card).toMatch(/l\.arrivedQty/);
    expect(card).toMatch(/l\.excessIssued/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /purchasing-gap carton/,
    );
  });

  it("0375 Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0375_flooring_knowledge_incoming_delivery_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0375_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/hardSurfaceAreaCartonCount/);
    expect(incoming).toMatch(/cartonCountFor/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/formatMoney/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).toMatch(
      /Hard-surface warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/billedRateToCartonCost/);
    expect(receiving).not.toMatch(/category === ["']carpet_tile["']/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery carton/,
    );
  });

  it("0376 Exclusive carpet-tile estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0376_flooring_knowledge_estimate_office_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0376_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/hardSurfaceAreaCartonCount/);
    expect(office).toMatch(/lineMath/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(office).toMatch(/carton\(s\)/);
    expect(office).not.toMatch(/billedRateToCartonCost/);
    expect(office).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/estimate office carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/estimate office carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate office carton/,
    );
  });

  it("0377 Exclusive carpet-tile Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0377_flooring_knowledge_builder_collapsed_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0377_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount/);
    expect(builder).toMatch(/sCartons/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(builder).toMatch(/carton\(s\)/);
    expect(builder).not.toMatch(/billedRateToCartonCost/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder collapsed carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder collapsed carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder collapsed carton/,
    );
  });

  it("0378 Exclusive carpet-tile Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0378_flooring_knowledge_guided_review_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0378_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/hardSurfaceAreaCartonCount/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(q).toMatch(/carton\(s\)/);
    expect(q).not.toMatch(/billedRateToCartonCost/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate Review carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate Review carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate Review carton/,
    );
  });

  it("0379 Exclusive carpet-tile approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0379_flooring_knowledge_snapshot_seed_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0379_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const option: EstimateOption = {
      id: "opt-1",
      estimate_id: "est-1",
      name: "Option A",
      position: 0,
      notes: null,
      created_at: "",
    };
    const line: EstimateLineItem = {
      id: "l1",
      option_id: "opt-1",
      position: 0,
      room: "Living",
      description: "Lifeproof Oak",
      note: null,
      line_type: "mat_labor",
      sqft: 300,
      length_in: null,
      width_in: null,
      measure_unit: "sqft",
      material_rate: 4.5,
      labor_rate: 2,
      installed_rate: null,
      flat_amount: null,
      waste_pct: 10,
      product_id: "p1",
      manufacturer: null,
      style: null,
      color: null,
      item_no: null,
      material_cost: null,
      labor_cost: null,
      quantity: 300,
      unit: "sq ft",
      category: "lvp",
      sqft_per_box: 23.64,
      roll_width_ft: null,
      order_as_roll: false,
    };
    const payload = buildApprovalSnapshotPayload({
      estimate: {
        id: "est-1",
        customer_id: "cust-1",
        title: "Living",
        presentation: "detailed",
        show_project_details: true,
        job_description: "Install",
        notes: null,
        tax_rate: 0,
        discount_kind: "amount",
        discount_value: 0,
      },
      option,
      lines: [line],
    });
    expect(payload.option.lines[0].sqft_per_box).toBe(23.64);
    expect(payload.option.lines[0].roll_width_ft).toBeNull();
    expect(payload.option.lines[0].order_as_roll).toBe(false);
    expect(CUSTOMER_SAFE_SNAPSHOT_LINE_KEYS).not.toContain("sqft_per_box");
    expect(CUSTOMER_SAFE_SNAPSHOT_LINE_KEYS).not.toContain("roll_width_ft");
    expect(CUSTOMER_SAFE_SNAPSHOT_LINE_KEYS).not.toContain("order_as_roll");
    const safe = sanitizeApprovalPayloadForCustomer(payload);
    const safeLine = safe.option.lines[0] as unknown as Record<string, unknown>;
    expect(safeLine.sqft_per_box).toBeUndefined();
    expect(safeLine.roll_width_ft).toBeUndefined();
    expect(safeLine.order_as_roll).toBeUndefined();
    const viewed = snapshotLinesAsEstimateLines(payload);
    expect(viewed[0].sqft_per_box).toBe(23.64);
    expect(viewed[0].roll_width_ft).toBeNull();
    expect(viewed[0].order_as_roll).toBe(false);
    expect(hardSurfaceAreaCartonCount(viewed[0], Number(viewed[0].quantity) || 0)).toBe(13);
    const fpSrc = commercialLineFingerprint.toString();
    expect(fpSrc).not.toMatch(/sqft_per_box/);
    expect(fpSrc).not.toMatch(/roll_width_ft/);
    expect(fpSrc).not.toMatch(/order_as_roll/);
    const withCoverage = {
      ...line,
      sqft_per_box: 99,
      roll_width_ft: 12,
      order_as_roll: true,
    };
    expect(commercialLineFingerprint(line)).toBe(commercialLineFingerprint(withCoverage));

    const approval = readFileSync(join(root, "src/lib/estimate-approval.ts"), "utf8");
    expect(approval).toMatch(
      /Exclusive carpet-tile approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(approval).toMatch(
      /Hard-surface approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(approval).toMatch(/sqft_per_box: l\.sqft_per_box \?\? null/);
    expect(approval).toMatch(/roll_width_ft: l\.roll_width_ft \?\? null/);
    expect(approval).toMatch(/order_as_roll: l\.order_as_roll/);

    const ops = readFileSync(join(root, "src/lib/data/job-operational-lines.ts"), "utf8");
    expect(ops).toMatch(
      /Exclusive carpet-tile approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(ops).toMatch(
      /Hard-surface approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const view = readFileSync(join(root, "src/lib/approval-snapshot-view.ts"), "utf8");
    expect(view).toMatch(/sqft_per_box: l\.sqft_per_box \?\? null/);
    expect(view).toMatch(/roll_width_ft: l\.roll_width_ft \?\? null/);
    expect(view).toMatch(/order_as_roll: l\.order_as_roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/approval-snapshot job-seed carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/approval-snapshot job-seed carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate Review carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /approval-snapshot job-seed carton/,
    );
  });

  it("0380 Exclusive carpet-tile Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0380_flooring_knowledge_line_measurements_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0380_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Lifeproof Oak",
          category: "lvp",
          unit: "sq ft",
          sqft_per_box: 23.64,
        },
        300,
      ),
    ).toBe(13);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Living room — Interface carpet tile",
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          order_as_roll: false,
          quantity: 22.22,
        },
        22.22,
      ),
    ).toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        8,
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        50,
      ),
    ).toBe(0);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/hardSurfaceAreaCartonCount/);
    expect(meas).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(meas).toMatch(
      /Hard-surface Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(meas).toMatch(/carton\{cartons === 1 \? "" : "s"\}/);
    expect(meas).not.toMatch(/Math\.ceil\(total \/ perBox\)/);
    expect(meas).not.toMatch(/billedRateToCartonCost/);
    expect(meas).not.toMatch(/category === ["']carpet_tile["']/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount/);
    expect(builder).toMatch(/cartonLine=\{summ\}/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(builder).not.toMatch(/billedRateToCartonCost/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder expanded LineMeasurements carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder expanded LineMeasurements carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface approval-snapshot job-seed carton coverage from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder expanded LineMeasurements carton/,
    );
  });

  it("0381 Exclusive carpet-tile Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0381_flooring_knowledge_guided_review_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0381_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/hardSurfaceAreaCartonCount/);
    expect(q).toMatch(/lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(/carton\(s\)/);
    expect(q).not.toMatch(/billedRateToCartonCost/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate Review order carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate Review order carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded LineMeasurements carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate Review order carton/,
    );
  });

  it("0382 Exclusive carpet-tile Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0382_flooring_knowledge_builder_collapsed_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0382_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount/);
    expect(builder).toMatch(/sCartons/);
    expect(builder).toMatch(/lineOrderQty\(summ\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(/carton\(s\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).not.toMatch(/billedRateToCartonCost/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder collapsed order carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder collapsed order carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder collapsed order carton/,
    );
  });

  it("0383 Exclusive carpet-tile Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0383_flooring_knowledge_linemeasurements_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0383_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/hardSurfaceAreaCartonCount/);
    expect(meas).toMatch(/orderQty \?\? billedQty/);
    expect(meas).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(meas).toMatch(
      /Hard-surface Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(meas).toMatch(/carton\{cartons === 1 \? "" : "s"\}/);
    expect(meas).not.toMatch(/Math\.ceil\(total \/ perBox\)/);
    expect(meas).not.toMatch(/billedRateToCartonCost/);
    expect(meas).not.toMatch(/category === ["']carpet_tile["']/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).not.toMatch(/billedRateToCartonCost/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder expanded LineMeasurements order carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder expanded LineMeasurements order carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder expanded LineMeasurements order carton/,
    );
  });

  it("0384 Exclusive carpet-tile estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0384_flooring_knowledge_estimate_office_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0384_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/hardSurfaceAreaCartonCount/);
    expect(office).toMatch(/lineMath/);
    expect(office).toMatch(/lineOrderQty\(l\)/);
    expect(office).toMatch(/const qty = lineQty\(l\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(/carton\(s\)/);
    expect(office).not.toMatch(/billedRateToCartonCost/);
    expect(office).not.toMatch(/category === ["']carpet_tile["']/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/estimate office order carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/estimate office order carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate office order carton/,
    );
  });

  it("0385 Exclusive carpet-tile job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0385_flooring_knowledge_job_scope_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0385_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    expect(
      lineSpec({
        quantity: 300,
        unit: "sq ft",
        measure_unit: "sqft",
        sqft: 300,
        length_in: null,
        width_in: null,
        category: "lvp",
        sqft_per_box: 23.64,
        waste_pct: 10,
        line_type: "mat_labor",
      }).cartons,
    ).toBe(14);
    expect(
      lineSpec({
        quantity: 300,
        unit: "sq ft",
        measure_unit: "sqft",
        sqft: 300,
        length_in: null,
        width_in: null,
        category: "lvp",
        sqft_per_box: 23.64,
        waste_pct: 10,
        line_type: "mat_labor",
      }).qtyNum,
    ).toBe(300);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      lineSpec({
        quantity: 22.22,
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 200,
        length_in: null,
        width_in: null,
        category: "carpet",
        sqft_per_box: 23.64,
        order_as_roll: false,
        waste_pct: 10,
        line_type: "mat_labor",
      }).cartons,
    ).toBe(10);
    expect(
      lineSpec({
        quantity: 8,
        unit: "box",
        measure_unit: "sqft",
        sqft: 300,
        length_in: null,
        width_in: null,
        category: "lvp",
        description: "Lifeproof Oak — wrap qty TBD",
        sqft_per_box: 23.64,
        waste_pct: 10,
        line_type: "mat_labor",
      }).cartons,
    ).toBe(0);
    expect(
      lineSpec({
        quantity: 50,
        unit: "sq yd",
        measure_unit: "sqyd",
        sqft: 450,
        length_in: null,
        width_in: null,
        category: "carpet",
        sqft_per_box: 23.64,
        roll_width_ft: 12,
        waste_pct: 10,
        line_type: "mat_labor",
      }).cartons,
    ).toBe(0);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/hardSurfaceAreaCartonCount/);
    expect(jobScope).toMatch(/lineOrderQty/);
    expect(jobScope).toMatch(
      /Exclusive carpet-tile job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(jobScope).toMatch(
      /Hard-surface job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(jobScope).not.toMatch(/billedRateToCartonCost/);
    expect(jobScope).not.toMatch(/category === ["']carpet_tile["']/);

    const scopeView = readFileSync(join(root, "src/components/job-scope-view.tsx"), "utf8");
    expect(scopeView).toMatch(/spec\.cartons/);
    expect(scopeView).toMatch(
      /Exclusive carpet-tile job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(scopeView).toMatch(
      /Hard-surface job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(scopeView).not.toMatch(/billedRateToCartonCost/);
    expect(scopeView).not.toMatch(/category === ["']carpet_tile["']/);

    const jobPage = readFileSync(join(root, "src/app/(app)/jobs/[id]/page.tsx"), "utf8");
    expect(jobPage).toMatch(/spec\.cartons/);
    expect(jobPage).toMatch(
      /Exclusive carpet-tile job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(jobPage).toMatch(
      /Hard-surface job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(jobPage).not.toMatch(/billedRateToCartonCost/);
    expect(jobPage).not.toMatch(/category === ["']carpet_tile["']/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount/);
    expect(wo).not.toMatch(/job scope order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job scope order carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job scope order carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job scope order carton/,
    );
  });

  it("0386 Exclusive carpet-tile installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0386_flooring_knowledge_installation_wo_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0386_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Lifeproof Oak — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Lifeproof Oak — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount/);
    expect(wo).toMatch(/lineOrderQty/);
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/Tile bills per sq yd/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/billedRateToCartonCost/);
    expect(wo).not.toMatch(/category === ["']carpet_tile["']/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/lineOrderQty/);
    expect(jobScope).not.toMatch(/installation-wo order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/installation-wo order carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/installation-wo order carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job scope order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /installation-wo order carton/,
    );
  });

  it("0387 Exclusive carpet-tile work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0387_flooring_knowledge_wo_editor_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0387_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Lifeproof Oak — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Lifeproof Oak — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/hardSurfaceAreaCartonCount/);
    expect(editor).toMatch(/cartonCountFor/);
    expect(editor).toMatch(/lineOrderQty/);
    expect(editor).toMatch(/lineOrderQty\(l as unknown as CalcLine\)/);
    expect(editor).toMatch(/waste_pct: number \| null/);
    expect(editor).toMatch(/line_type: string \| null/);
    expect(editor).toMatch(/name="quantity"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(editor).not.toMatch(/billedRateToCartonCost/);
    expect(editor).not.toMatch(/category === ["']carpet_tile["']/);
    expect(editor).not.toMatch(/formatMoney/);

    const jobPage = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/page.tsx"),
      "utf8",
    );
    expect(jobPage).toMatch(
      /Exclusive carpet-tile work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(jobPage).toMatch(
      /Hard-surface work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(jobPage).toMatch(/waste_pct: l\.waste_pct/);
    expect(jobPage).toMatch(/line_type: l\.line_type/);
    expect(jobPage).toMatch(/sqft_per_box: l\.sqft_per_box/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/Tile bills per sq yd/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/work-order editor order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor order carton/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor order carton/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /work-order editor order carton/,
    );
  });

  it("0388 Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0388_flooring_knowledge_job_scope_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0388_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    const padSpec = lineSpec({
      line_type: "mat_labor",
      description: padCalc.description,
      category: "underlayment",
      unit: "sq yd",
      measure_unit: "sqyd",
      quantity: 60,
      waste_pct: 10,
      sqft: 540,
      length_in: null,
      width_in: null,
    });
    expect(padSpec.qtyNum).toBe(60);
    expect(padSpec.rolls).toBe(3);
    expect(padSpec.cartons).toBe(0);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).toMatch(/const q = lineQty/);
    expect(jobScope).toMatch(
      /Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math/,
    );
    expect(jobScope).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(jobScope).toMatch(/hardSurfaceAreaCartonCount\(l, orderQ/);
    expect(jobScope).not.toMatch(/category === ["']carpet_tile["']/);

    const view = readFileSync(join(root, "src/components/job-scope-view.tsx"), "utf8");
    expect(view).toMatch(
      /Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math/,
    );
    expect(view).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const jobPage = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/page.tsx"),
      "utf8",
    );
    expect(jobPage).toMatch(
      /Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math/,
    );
    expect(jobPage).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(
      /Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math/,
    );
    expect(wo).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/job scope order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job scope order pad-roll/);
    expect(print).not.toMatch(/hardSurfaceAreaCartonCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job scope order pad-roll/);
    expect(portal).not.toMatch(/hardSurfaceAreaCartonCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile job scope order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job scope order pad-roll/,
    );
  });

  it("0389 Exclusive carpet-tile warehouse queue order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0389_flooring_knowledge_warehouse_queue_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0389_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse queue order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment warehouse queue order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty\)/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue order pad-roll count stays off 30-yard roll math/,
    );
    expect(warehouse).toMatch(
      /Underlayment warehouse queue order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(warehouse).toMatch(/PAD_ROLL_SQYD/);
    expect(warehouse).not.toMatch(/category === ["']carpet_tile["']/);
    expect(warehouse).not.toMatch(/billedRateToCartonCost/);
    expect(warehouse).not.toMatch(/formatMoney/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/warehouse queue order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/warehouse queue order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/warehouse queue order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/warehouse queue order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/warehouse queue order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment warehouse queue order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment job scope order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile warehouse queue order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment warehouse queue order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /warehouse queue order pad-roll/,
    );
  });

  it("0390 Exclusive carpet-tile job materials order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0390_flooring_knowledge_job_materials_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0390_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment job materials order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).toMatch(/cartonCountFor/);
    expect(card).toMatch(/hardSurfaceAreaCartonCount\(cartonLineOf\(l\), l\.qty\)/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).toMatch(
      /Underlayment job materials order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(card).not.toMatch(/lineOrderQty/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/formatMoney/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty\)/);
    expect(warehouse).not.toMatch(/job materials order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/job materials order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/job materials order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/job materials order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job materials order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job materials order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment warehouse queue order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile job materials order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment job materials order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials order pad-roll/,
    );
  });

  it("0391 Exclusive carpet-tile estimate order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0391_flooring_knowledge_estimate_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0391_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment estimate order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad-roll count stays off 30-yard roll math/,
    );
    expect(orderUi).toMatch(
      /Underlayment estimate order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(orderUi).toMatch(/formatMoney/);
    expect(orderUi).not.toMatch(/lineOrderQty/);
    expect(orderUi).not.toMatch(/category === ["']carpet_tile["']/);
    expect(orderUi).not.toMatch(/billedRateToCartonCost/);

    const plan = readFileSync(join(root, "src/lib/data/po-plan.ts"), "utf8");
    expect(plan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(plan).toMatch(/const qty = Math\.round\(lineOrderQty\(l\)/);
    expect(plan).not.toMatch(/estimate order pad-roll/);
    expect(plan).not.toMatch(/padRollCount/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/estimate order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/estimate order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/estimate order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/estimate order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/estimate order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/estimate order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/estimate order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment estimate order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile estimate order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment estimate order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate order pad-roll/,
    );
  });

  it("0392 Exclusive carpet-tile incoming-delivery pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0392_flooring_knowledge_incoming_delivery_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0392_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile incoming-delivery pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment incoming-delivery pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/hardSurfaceAreaCartonCount/);
    expect(incoming).toMatch(/cartonCountFor/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery pad-roll count stays off 30-yard roll math/,
    );
    expect(incoming).toMatch(
      /Underlayment incoming-delivery pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(incoming).not.toMatch(/lineOrderQty/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/formatMoney/);
    expect(incoming).not.toMatch(/PAD_ROLL_SQYD/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/incoming-delivery pad-roll/);
    expect(receiving).not.toMatch(/padRollCount/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/incoming-delivery pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/incoming-delivery pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/incoming-delivery pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/incoming-delivery pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/incoming-delivery pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/incoming-delivery pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/incoming-delivery pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/incoming-delivery pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment incoming-delivery pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment estimate order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile incoming-delivery pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment incoming-delivery pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery pad-roll/,
    );
  });

  it("0393 Exclusive carpet-tile work-order editor order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0393_flooring_knowledge_wo_editor_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0393_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile work-order editor order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment work-order editor order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/hardSurfaceAreaCartonCount/);
    expect(editor).toMatch(/cartonCountFor/);
    expect(editor).toMatch(/lineOrderQty\(l as unknown as CalcLine\)/);
    expect(editor).toMatch(/waste_pct: number \| null/);
    expect(editor).toMatch(/line_type: string \| null/);
    expect(editor).toMatch(/name="quantity"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor order pad-roll count stays off 30-yard roll math/,
    );
    expect(editor).toMatch(
      /Underlayment work-order editor order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/category === ["']carpet_tile["']/);
    expect(editor).not.toMatch(/billedRateToCartonCost/);
    expect(editor).not.toMatch(/formatMoney/);
    expect(editor).not.toMatch(/PAD_ROLL_SQYD/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/work-order editor order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/work-order editor order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/work-order editor order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/work-order editor order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/work-order editor order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/work-order editor order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment work-order editor order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment incoming-delivery pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile work-order editor order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment work-order editor order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /work-order editor order pad-roll/,
    );
  });

  it("0394 Exclusive carpet-tile estimate office order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0394_flooring_knowledge_estimate_office_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0394_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate office order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment estimate office order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/const qty = lineQty\(l\)/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(/carton\(s\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office order pad-roll count stays off 30-yard roll math/,
    );
    expect(office).toMatch(
      /Underlayment estimate office order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/category === ["']carpet_tile["']/);
    expect(office).not.toMatch(/billedRateToCartonCost/);
    expect(office).not.toMatch(/PAD_ROLL_SQYD/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/estimate office order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/estimate office order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/estimate office order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/estimate office order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/estimate office order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/estimate office order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/estimate office order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/estimate office order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/estimate office order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/estimate office order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment estimate office order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment work-order editor order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile estimate office order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment estimate office order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate office order pad-roll/,
    );
  });

  it("0395 Exclusive carpet-tile Builder collapsed order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0395_flooring_knowledge_builder_collapsed_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0395_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder collapsed order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Builder collapsed order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/carton\(s\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).toMatch(
      /Underlayment Builder collapsed order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);
    expect(builder).not.toMatch(/billedRateToCartonCost/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Builder collapsed order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Builder collapsed order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Builder collapsed order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Builder collapsed order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Builder collapsed order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Builder collapsed order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Builder collapsed order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Builder collapsed order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Builder collapsed order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder collapsed order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder collapsed order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Builder collapsed order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment estimate office order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Builder collapsed order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Builder collapsed order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder collapsed order pad-roll/,
    );
  });

  it("0396 Exclusive carpet-tile Guided Estimate Review order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0396_flooring_knowledge_guided_review_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0396_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Guided Estimate Review order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(/hardSurfaceAreaCartonCount/);
    expect(q).toMatch(/lineOrderQty\(smartLineToCalcLine\(l\)/);
    expect(q).toMatch(/\$\{l\.quantity\} \$\{lineDisplayUnit\(l\)\}/);
    expect(q).toMatch(/formatMoney\(lineTotal\(smartLineToCalcLine\(l\)\)\)/);
    expect(q).toMatch(/carton\(s\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate Review order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).not.toMatch(/Guided Estimate Review order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate Review order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate Review order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Guided Estimate Review order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate Review order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Guided Estimate Review order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate Review order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate Review order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate Review order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate Review order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate Review order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate Review order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate Review order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment Builder collapsed order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate Review order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Guided Estimate Review order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate Review order pad-roll/,
    );
  });

  it("0397 Exclusive carpet-tile Builder expanded LineMeasurements order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0397_flooring_knowledge_linemeasurements_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0397_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Builder expanded LineMeasurements order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).toMatch(/hardSurfaceAreaCartonCount/);
    expect(meas).toMatch(/orderQty \?\? billedQty/);
    expect(meas).toMatch(/carton\{cartons === 1 \? "" : "s"\}/);
    expect(meas).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order pad-roll count stays off 30-yard roll math/,
    );
    expect(meas).toMatch(
      /Underlayment Builder expanded LineMeasurements order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(meas).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(meas).not.toMatch(/Math\.ceil\(total \/ perBox\)/);
    expect(meas).not.toMatch(/category === ["']carpet_tile["']/);
    expect(meas).not.toMatch(/billedRateToCartonCost/);
    expect(meas).not.toMatch(/PAD_ROLL_SQYD/);
    expect(meas).not.toMatch(/companionQty/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).toMatch(
      /Underlayment Builder expanded LineMeasurements order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);
    expect(builder).not.toMatch(/billedRateToCartonCost/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder expanded LineMeasurements order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Builder expanded LineMeasurements order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate Review order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Builder expanded LineMeasurements order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Builder expanded LineMeasurements order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder expanded LineMeasurements order pad-roll/,
    );
  });

  it("0398 Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0398_flooring_knowledge_builder_expanded_pad_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0398_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Builder expanded pad order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/label="Sq ft"/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).toMatch(
      /Underlayment Builder expanded pad order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded LineMeasurements order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);
    expect(builder).not.toMatch(/billedRateToCartonCost/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Builder expanded pad order pad-roll/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).not.toMatch(/Builder expanded pad order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Builder expanded pad order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Builder expanded pad order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Builder expanded pad order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Builder expanded pad order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Builder expanded pad order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Builder expanded pad order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Builder expanded pad order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Builder expanded pad order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Builder expanded pad order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder expanded pad order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder expanded pad order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Builder expanded pad order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment Builder expanded LineMeasurements order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Builder expanded pad order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder expanded pad order pad-roll/,
    );
  });

  it("0399 Exclusive carpet-tile Builder expanded tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0399_flooring_knowledge_builder_expanded_tile_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0399_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder expanded tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Builder expanded tile takeoff order carton count stays off this modular strip. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Shaw roll",
          category: "carpet",
          unit: "sq yd",
          roll_width_ft: 12,
          sqft_per_box: 23.64,
          quantity: 22.22,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Shaw roll",
          category: "carpet",
          unit: "sq yd",
          quantity: 22.22,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded tile takeoff order carton count stays off this modular strip/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);
    expect(builder).not.toMatch(/billedRateToCartonCost/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Builder expanded tile takeoff order carton/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).not.toMatch(/Builder expanded tile takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Builder expanded tile takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Builder expanded tile takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Builder expanded tile takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Builder expanded tile takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Builder expanded tile takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Builder expanded tile takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Builder expanded tile takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Builder expanded tile takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Builder expanded tile takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder expanded tile takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder expanded tile takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded tile takeoff order carton count stays off this modular strip/,
    );
    expect(pricing).toMatch(
      /Underlayment Builder expanded pad order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Builder expanded tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Builder expanded tile takeoff order carton count stays off this modular strip. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder expanded tile takeoff order carton/,
    );
  });

  it("0400 Exclusive carpet-tile Guided Estimate tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0400_flooring_knowledge_guided_estimate_tile_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0400_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Guided Estimate tile takeoff order carton count stays off this modular strip. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Shaw roll",
          category: "carpet",
          unit: "sq yd",
          roll_width_ft: 12,
          sqft_per_box: 23.64,
          quantity: 22.22,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Shaw roll",
          category: "carpet",
          unit: "sq yd",
          quantity: 22.22,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(takeoff\)/);
    expect(q).toMatch(/hardSurfaceAreaCartonCount\(\s*\{\s*description: p\.label/);
    expect(q).toMatch(/lineOrderQty\(\{\s*line_type: "mat_labor"/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate tile takeoff order carton count stays off this modular strip/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate Review order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).not.toMatch(/Guided Estimate tile takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate tile takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate tile takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate tile takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate tile takeoff order carton count stays off this modular strip/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded tile takeoff order carton count stays off this modular strip/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Guided Estimate tile takeoff order carton count stays off this modular strip. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate tile takeoff order carton/,
    );
  });

  it("0401 Exclusive carpet-tile Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0401_flooring_knowledge_guided_running_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0401_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Shaw roll",
          category: "carpet",
          unit: "sq yd",
          roll_width_ft: 12,
          sqft_per_box: 23.64,
          quantity: 22.22,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Shaw roll",
          category: "carpet",
          unit: "sq yd",
          quantity: 22.22,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(takeoff\)/);
    expect(q).toMatch(/hardSurfaceAreaCartonCount\(\s*\{\s*description: p\.label/);
    expect(q).toMatch(/lineOrderQty\(\{\s*line_type: "mat_labor"/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate tile takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate tile takeoff order carton count stays off this modular strip/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).not.toMatch(/Guided Estimate running takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate running takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate running takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate running takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Guided Estimate running takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate running takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Guided Estimate running takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate running takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate running takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate running takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate running takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate running takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate running takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate tile takeoff order carton count stays off this modular strip/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate running takeoff order carton/,
    );
  });

  it("0402 Exclusive carpet-tile Guided Estimate running takeoff order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0402_flooring_knowledge_guided_running_takeoff_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0402_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Guided Estimate running takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/t\.billingQty/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(takeoff\)/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate running takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/Guided Estimate running takeoff order pad-roll/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate running takeoff order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate running takeoff order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate running takeoff order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate running takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate running takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Guided Estimate running takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate running takeoff order pad-roll/,
    );
  });

  it("0403 Exclusive carpet-tile Guided Estimate pad takeoff order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0403_flooring_knowledge_guided_pad_takeoff_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0403_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Guided Estimate pad takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(
      padRollCount("underlayment", computeMaterialTakeoff({
        family: "other",
        measuredSqft: 540,
        wastePct: 0,
        billingUnit: "sqyd",
        takeoffLabel: "Carpet pad",
      }).billingQty, "sqyd"),
    ).toBe(2);
    expect(
      padRollCount("underlayment", computeMaterialTakeoff({
        family: "other",
        measuredSqft: 450,
        wastePct: 10,
        billingUnit: "sqft",
        takeoffLabel: "Underlayment",
      }).billingQty, "sqft"),
    ).toBe(0);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/t\.billingQty/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(takeoff\)/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate pad takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate running takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate pad takeoff order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate pad takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate running takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate pad takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Guided Estimate pad takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate pad takeoff order pad-roll/,
    );
  });

  it("0404 Exclusive carpet-tile Guided Estimate extra takeoff order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0404_flooring_knowledge_guided_extra_takeoff_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0404_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Guided Estimate extra takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    const extraTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padRollCount("underlayment", extraTakeoff.billingQty, extraTakeoff.billingUnit)).toBe(3);
    expect(
      padRollCount("underlayment", computeMaterialTakeoff({
        family: "other",
        measuredSqft: 540,
        wastePct: 0,
        billingUnit: "sqyd",
        takeoffLabel: "Carpet pad",
      }).billingQty, "sqyd"),
    ).toBe(2);
    expect(
      padRollCount("underlayment", computeMaterialTakeoff({
        family: "other",
        measuredSqft: 450,
        wastePct: 10,
        billingUnit: "sqft",
        takeoffLabel: "Underlayment",
      }).billingQty, "sqft"),
    ).toBe(0);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/extraTakeoff\.billingQty/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/t\.billingQty/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(takeoff\)/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate extra takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate pad takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate extra takeoff order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate extra takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate pad takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate extra takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Guided Estimate extra takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate extra takeoff order pad-roll/,
    );
  });

  it("0405 Exclusive carpet-tile job materials purchasing-gap order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0405_flooring_knowledge_job_materials_gap_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0405_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment job materials purchasing-gap order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 66, "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.purchasingGap/);
    expect(card).toMatch(/\$\{gapPadRolls\} roll/);
    expect(card).toMatch(/gapCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).toMatch(/hardSurfaceAreaCartonCount\(cartonLineOf\(l\), l\.qty\)/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).toMatch(
      /Underlayment job materials purchasing-gap order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(card).not.toMatch(/lineOrderQty/);
    expect(card).not.toMatch(/PAD_ROLL_SQYD/);
    expect(card).not.toMatch(/companionQty/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/formatMoney/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/job materials purchasing-gap order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job materials purchasing-gap order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job materials purchasing-gap order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials purchasing-gap order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate extra takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile job materials purchasing-gap order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment job materials purchasing-gap order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials purchasing-gap order pad-roll/,
    );
  });

  it("0406 Exclusive carpet-tile job materials excess order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0406_flooring_knowledge_job_materials_excess_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0406_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials excess order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment job materials excess order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 66, "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.excessIssued/);
    expect(card).toMatch(/\$\{excessPadRolls\} roll/);
    expect(card).toMatch(/excessCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.purchasingGap/);
    expect(card).toMatch(/\$\{gapPadRolls\} roll/);
    expect(card).toMatch(/gapCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).toMatch(/hardSurfaceAreaCartonCount\(cartonLineOf\(l\), l\.qty\)/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials excess order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).toMatch(
      /Underlayment job materials excess order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).not.toMatch(/lineOrderQty/);
    expect(card).not.toMatch(/PAD_ROLL_SQYD/);
    expect(card).not.toMatch(/companionQty/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/formatMoney/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/job materials excess order pad-roll/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/job materials excess order pad-roll/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/job materials excess order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/job materials excess order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/job materials excess order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/job materials excess order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/job materials excess order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/job materials excess order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/job materials excess order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/job materials excess order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/job materials excess order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job materials excess order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job materials excess order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials excess order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials excess order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials purchasing-gap order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile job materials excess order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment job materials excess order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials excess order pad-roll/,
    );
  });

  it("0407 Exclusive carpet-tile job materials arrived order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0407_flooring_knowledge_job_materials_arrived_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0407_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials arrived order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment job materials arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 66, "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.arrivedQty/);
    expect(card).toMatch(/\$\{arrivedPadRolls\} roll/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.excessIssued/);
    expect(card).toMatch(/\$\{excessPadRolls\} roll/);
    expect(card).toMatch(/excessCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.purchasingGap/);
    expect(card).toMatch(/\$\{gapPadRolls\} roll/);
    expect(card).toMatch(/gapCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).toMatch(/hardSurfaceAreaCartonCount\(cartonLineOf\(l\), l\.qty\)/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).toMatch(
      /Underlayment job materials arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials excess order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).not.toMatch(/lineOrderQty/);
    expect(card).not.toMatch(/PAD_ROLL_SQYD/);
    expect(card).not.toMatch(/companionQty/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/formatMoney/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/job materials arrived order pad-roll/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/job materials arrived order pad-roll/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/job materials arrived order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/job materials arrived order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/job materials arrived order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).not.toMatch(/job materials arrived order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/job materials arrived order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/job materials arrived order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/job materials arrived order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/job materials arrived order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/job materials arrived order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job materials arrived order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job materials arrived order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials excess order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile job materials arrived order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment job materials arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials arrived order pad-roll/,
    );
  });

  it("0408 Exclusive carpet-tile warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0408_flooring_knowledge_incoming_arrived_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0408_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/cartonCountFor\(i, entered/);
    expect(incoming).toMatch(/\{enteredCartons\} carton/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(/cartonCountFor\(i, ordered/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/Quantity received/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(incoming).not.toMatch(/lineOrderQty/);
    expect(incoming).not.toMatch(/PAD_ROLL_SQYD/);
    expect(incoming).not.toMatch(/companionQty/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/formatMoney/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/warehouse incoming-delivery arrived order carton/);
    expect(receiving).not.toMatch(/padRollCount/);
    expect(receiving).not.toMatch(/enteredCartons/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.arrivedQty/);
    expect(card).toMatch(/\$\{arrivedPadRolls\} roll/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/warehouse incoming-delivery arrived order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/warehouse incoming-delivery arrived order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/warehouse incoming-delivery arrived order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /warehouse incoming-delivery arrived order carton/,
    );
  });


  it("0409 Exclusive carpet-tile warehouse incoming-delivery arrived order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0409_flooring_knowledge_incoming_arrived_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0409_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment warehouse incoming-delivery arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 66, "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/\{enteredPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, entered/);
    expect(incoming).toMatch(/\{enteredCartons\} carton/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(/cartonCountFor\(i, ordered/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/Quantity received/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(incoming).toMatch(
      /Underlayment warehouse incoming-delivery arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).not.toMatch(/lineOrderQty/);
    expect(incoming).not.toMatch(/PAD_ROLL_SQYD/);
    expect(incoming).not.toMatch(/companionQty/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/formatMoney/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);
    expect(receiving).not.toMatch(/padRollCount/);
    expect(receiving).not.toMatch(/enteredCartons/);
    expect(receiving).not.toMatch(/enteredPadRolls/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.arrivedQty/);
    expect(card).toMatch(/\$\{arrivedPadRolls\} roll/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/warehouse incoming-delivery arrived order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment warehouse incoming-delivery arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile warehouse incoming-delivery arrived order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment warehouse incoming-delivery arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /warehouse incoming-delivery arrived order pad-roll/,
    );
  });

  it("0410 Exclusive carpet-tile warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0410_flooring_knowledge_incoming_outstanding_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0410_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    expect(hardSurfaceAreaCartonCount(lvpCarton, 30)).toBe(2);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/\{outstandingCartons\} carton/);
    expect(incoming).toMatch(/cartonCountFor\(i, entered/);
    expect(incoming).toMatch(/\{enteredCartons\} carton/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/\{enteredPadRolls\} roll/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(/cartonCountFor\(i, ordered/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/Quantity received/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(incoming).not.toMatch(/lineOrderQty/);
    expect(incoming).not.toMatch(/PAD_ROLL_SQYD/);
    expect(incoming).not.toMatch(/companionQty/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/formatMoney/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/warehouse incoming-delivery outstanding order carton/);
    expect(receiving).not.toMatch(/padRollCount/);
    expect(receiving).not.toMatch(/enteredCartons/);
    expect(receiving).not.toMatch(/enteredPadRolls/);
    expect(receiving).not.toMatch(/outstandingCartons/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.arrivedQty/);
    expect(card).toMatch(/\$\{arrivedPadRolls\} roll/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/warehouse incoming-delivery outstanding order carton/);
    expect(builder).not.toMatch(/incoming-delivery outstanding order carton/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/warehouse incoming-delivery outstanding order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/warehouse incoming-delivery outstanding order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/warehouse incoming-delivery outstanding order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Underlayment warehouse incoming-delivery arrived order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /warehouse incoming-delivery outstanding order carton/,
    );
  });

  it("0411 Exclusive carpet-tile warehouse incoming-delivery outstanding order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0411_flooring_knowledge_incoming_outstanding_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0411_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment warehouse incoming-delivery outstanding order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 66, "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/\{outstandingCartons\} carton/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/\{enteredPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, entered/);
    expect(incoming).toMatch(/\{enteredCartons\} carton/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(/cartonCountFor\(i, ordered/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/Quantity received/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order pad-roll count stays off 30-yard roll math/,
    );
    expect(incoming).toMatch(
      /Underlayment warehouse incoming-delivery outstanding order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(incoming).not.toMatch(/lineOrderQty/);
    expect(incoming).not.toMatch(/PAD_ROLL_SQYD/);
    expect(incoming).not.toMatch(/companionQty/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/formatMoney/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);
    expect(receiving).not.toMatch(/padRollCount/);
    expect(receiving).not.toMatch(/enteredCartons/);
    expect(receiving).not.toMatch(/enteredPadRolls/);
    expect(receiving).not.toMatch(/outstandingCartons/);
    expect(receiving).not.toMatch(/outstandingPadRolls/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, l\.arrivedQty/);
    expect(card).toMatch(/\$\{arrivedPadRolls\} roll/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);
    expect(builder).not.toMatch(/incoming-delivery outstanding order pad-roll/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/warehouse incoming-delivery outstanding order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment warehouse incoming-delivery outstanding order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse incoming-delivery outstanding order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile warehouse incoming-delivery outstanding order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment warehouse incoming-delivery outstanding order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /warehouse incoming-delivery outstanding order pad-roll/,
    );
  });

  it("0412 Exclusive carpet-tile job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0412_flooring_knowledge_job_materials_outstanding_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0412_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    expect(hardSurfaceAreaCartonCount(lvpCarton, 30)).toBe(2);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          category: "carpet",
          unit: "sq yd",
          sqft_per_box: 23.64,
          roll_width_ft: 12,
          quantity: 50,
        },
        lineOrderQty({
          line_type: "mat_labor",
          category: "carpet",
          unit: "sq yd",
          quantity: 50,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/cartonCountForQty\(l, l\.arrivedQty/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.arrivedQty/);
    expect(card).toMatch(/\$\{arrivedPadRolls\} roll/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.purchasingGap/);
    expect(card).toMatch(/\$\{gapPadRolls\} roll/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.excessIssued/);
    expect(card).toMatch(/\$\{excessPadRolls\} roll/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).toMatch(/cartonCountForQty\(l, l\.purchasingGap/);
    expect(card).toMatch(/cartonCountForQty\(l, l\.excessIssued/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).not.toMatch(/lineOrderQty/);
    expect(card).not.toMatch(/PAD_ROLL_SQYD/);
    expect(card).not.toMatch(/companionQty/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/formatMoney/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/\{outstandingCartons\} carton/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/\{enteredPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, entered/);
    expect(incoming).toMatch(/\{enteredCartons\} carton/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(/cartonCountFor\(i, ordered/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/Quantity received/);
    expect(incoming).not.toMatch(/job materials outstanding order carton/);
    expect(incoming).not.toMatch(/lineOrderQty/);
    expect(incoming).not.toMatch(/PAD_ROLL_SQYD/);
    expect(incoming).not.toMatch(/companionQty/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/formatMoney/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/job materials outstanding order carton/);
    expect(receiving).not.toMatch(/padRollCount/);
    expect(receiving).not.toMatch(/enteredCartons/);
    expect(receiving).not.toMatch(/enteredPadRolls/);
    expect(receiving).not.toMatch(/outstandingCartons/);
    expect(receiving).not.toMatch(/outstandingPadRolls/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/job materials outstanding order carton/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/job materials outstanding order carton/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/job materials outstanding order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/job materials outstanding order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/job materials outstanding order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/job materials outstanding order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/job materials outstanding order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/job materials outstanding order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/job materials outstanding order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/job materials outstanding order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job materials outstanding order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job materials outstanding order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Underlayment warehouse incoming-delivery outstanding order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials outstanding order carton/,
    );
  });

  it("0413 Exclusive carpet-tile job materials outstanding order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0413_flooring_knowledge_job_materials_outstanding_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0413_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials outstanding order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment job materials outstanding order pad-roll count from remaining order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    expect(padRollCount("underlayment", 50, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 450, "sqft")).toBe(0);
    const padCalc = {
      line_type: "mat_labor" as const,
      description: "Rebond pad",
      category: "underlayment",
      unit: "sq yd",
      quantity: 60,
      waste_pct: 10,
    };
    expect(padRollCount("underlayment", lineQty(padCalc), "sqyd")).toBe(2);
    expect(padRollCount("underlayment", lineOrderQty(padCalc), "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 66, "sqyd")).toBe(3);
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    expect(
      padRollCount("lvp", lineOrderQty({
        line_type: "mat_labor",
        category: "lvp",
        unit: "sq ft",
        quantity: 300,
        waste_pct: 10,
      }), "sqft"),
    ).toBe(0);
    expect(
      padRollCount("carpet", lineOrderQty({
        line_type: "mat_labor",
        category: "carpet",
        unit: "sq yd",
        quantity: 50,
        waste_pct: 10,
      }), "sqyd"),
    ).toBe(0);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.arrivedQty/);
    expect(card).toMatch(/\$\{arrivedPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, l\.arrivedQty/);
    expect(card).toMatch(/arrivedCartons/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.purchasingGap/);
    expect(card).toMatch(/\$\{gapPadRolls\} roll/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.excessIssued/);
    expect(card).toMatch(/\$\{excessPadRolls\} roll/);
    expect(card).toMatch(/padRollCount\(l\.category, l\.qty/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials outstanding order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).toMatch(
      /Underlayment job materials outstanding order pad-roll count from remaining order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Exclusive carpet-tile job materials arrived order pad-roll count stays off 30-yard roll math/,
    );
    expect(card).not.toMatch(/lineOrderQty/);
    expect(card).not.toMatch(/PAD_ROLL_SQYD/);
    expect(card).not.toMatch(/companionQty/);
    expect(card).not.toMatch(/category === ["']carpet_tile["']/);
    expect(card).not.toMatch(/billedRateToCartonCost/);
    expect(card).not.toMatch(/formatMoney/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/\{outstandingCartons\} carton/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/\{enteredPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, entered/);
    expect(incoming).toMatch(/\{enteredCartons\} carton/);
    expect(incoming).toMatch(/orderedCartons/);
    expect(incoming).toMatch(/cartonCountFor\(i, ordered/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/Quantity received/);
    expect(incoming).not.toMatch(/job materials outstanding order pad-roll/);
    expect(incoming).not.toMatch(/lineOrderQty/);
    expect(incoming).not.toMatch(/PAD_ROLL_SQYD/);
    expect(incoming).not.toMatch(/companionQty/);
    expect(incoming).not.toMatch(/category === ["']carpet_tile["']/);
    expect(incoming).not.toMatch(/billedRateToCartonCost/);
    expect(incoming).not.toMatch(/formatMoney/);

    const receiving = readFileSync(
      join(root, "src/app/(app)/warehouse/receiving-actions.ts"),
      "utf8",
    );
    expect(receiving).toMatch(/sqft_per_box/);
    expect(receiving).toMatch(/roll_width_ft/);
    expect(receiving).toMatch(
      /Exclusive carpet-tile warehouse incoming-delivery carton count from sq ft ÷ coverage is the pull, not leftover taped sq ft/,
    );
    expect(receiving).not.toMatch(/job materials outstanding order pad-roll/);
    expect(receiving).not.toMatch(/padRollCount/);
    expect(receiving).not.toMatch(/enteredCartons/);
    expect(receiving).not.toMatch(/enteredPadRolls/);
    expect(receiving).not.toMatch(/outstandingCartons/);
    expect(receiving).not.toMatch(/outstandingPadRolls/);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).not.toMatch(/job materials outstanding order pad-roll/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).not.toMatch(/job materials outstanding order pad-roll/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/job materials outstanding order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/job materials outstanding order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/job materials outstanding order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/job materials outstanding order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/job materials outstanding order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/job materials outstanding order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/job materials outstanding order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/job materials outstanding order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/job materials outstanding order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/job materials outstanding order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials outstanding order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials outstanding order pad-roll count from remaining order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials outstanding order carton count from remaining order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile job materials outstanding order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment job materials outstanding order pad-roll count from remaining order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials outstanding order pad-roll/,
    );
  });

  it("0414 Exclusive carpet-tile Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0414_flooring_knowledge_guided_extra_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0414_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const lvpCarton = {
      description: "Living room — Lifeproof Oak",
      category: "lvp",
      unit: "sq ft",
      sqft: 300,
      quantity: 300,
      sqft_per_box: 23.64,
    };
    const lvpCalc = {
      line_type: "mat_labor" as const,
      description: lvpCarton.description,
      category: lvpCarton.category,
      unit: lvpCarton.unit,
      sqft: lvpCarton.sqft,
      quantity: lvpCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineQty(lvpCalc))).toBe(13);
    expect(hardSurfaceAreaCartonCount(lvpCarton, lineOrderQty(lvpCalc))).toBe(14);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const tileCarton = {
      description: "Living room — Interface carpet tile",
      category: "carpet",
      unit: "sq yd",
      sqft_per_box: 23.64,
      order_as_roll: false,
      quantity: 22.22,
    };
    const tileCalc = {
      line_type: "mat_labor" as const,
      description: tileCarton.description,
      category: tileCarton.category,
      unit: tileCarton.unit,
      quantity: tileCarton.quantity,
      waste_pct: 10,
    };
    expect(hardSurfaceAreaCartonCount(tileCarton, 22.22)).toBe(9);
    expect(hardSurfaceAreaCartonCount(tileCarton, lineOrderQty(tileCalc))).toBe(10);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);
    expect(
      computeMaterialTakeoff({
        family: "other",
        measuredSqft: 540,
        wastePct: 10,
        billingUnit: "sqyd",
        takeoffLabel: "Carpet pad",
      }).cartons,
    ).toBeNull();

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.billingQty/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(takeoff\)/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate extra takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).not.toMatch(/Guided Estimate extra takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate extra takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate extra takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate extra takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Underlayment job materials outstanding order pad-roll count from remaining order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate extra takeoff order carton/,
    );
  });

  it("0415 Exclusive carpet-tile Guided Estimate Review takeoff order pad-roll count stays off 30-yard roll math", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0415_flooring_knowledge_guided_review_takeoff_order_pad_roll.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0415_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate Review takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Underlayment Guided Estimate Review takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(
      padRollCount("lvp", 8, "box"),
    ).toBe(0);
    expect(
      padRollCount("underlayment", 540, "sqft"),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate Review takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Underlayment Guided Estimate Review takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate running takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate Review takeoff order pad-roll/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate Review takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate Review takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate Review takeoff order pad-roll count stays off 30-yard roll math — mixed stretch-in \+ tile and unanswered carpet stay open. Sq-ft underlayment stays off 30-yard roll math. Do not invent a 30-yard roll. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Underlayment Guided Estimate Review takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd. Wrap \/ count How many stays off 30-yard roll math. Do not invent a 30-yard roll/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate Review takeoff order pad-roll/,
    );
  });

  it("0416 Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0416_flooring_knowledge_guided_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0416_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate Review takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate extra takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order pad-roll count stays off 30-yard roll math/,
    );
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Guided Estimate pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Underlayment Guided Estimate Review takeoff order pad-roll count from order qty ÷ 30-yard roll is the pull, not leftover measured sq yd/,
    );

    const help =
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Guided Estimate pad takeoff order carton/,
    );
  });

  it("0417 Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0417_flooring_knowledge_builder_expanded_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0417_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Builder expanded pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Builder expanded pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/Builder expanded pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Builder expanded pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/Builder expanded pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Builder expanded pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Builder expanded pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Builder expanded pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Builder expanded pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder expanded pad takeoff order carton/,
    );
  });

  it("0418 Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0418_flooring_knowledge_builder_collapsed_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0418_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/Builder collapsed pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /Builder collapsed pad takeoff order carton/,
    );
  });

  it("0419 Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0419_flooring_knowledge_estimate_office_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0419_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/estimate office pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/estimate office pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/estimate office pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/estimate office pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/estimate office pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/estimate office pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/estimate office pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/estimate office pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/estimate office pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate office pad takeoff order carton/,
    );
  });

  it("0420 Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0420_flooring_knowledge_wo_editor_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0420_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /work-order editor pad takeoff order carton/,
    );
  });

  it("0421 Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0421_flooring_knowledge_installation_wo_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0421_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/installation-wo pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/installation-wo pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/installation-wo pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/installation-wo pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/installation-wo pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/installation-wo pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/installation-wo pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/installation-wo pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /installation-wo pad takeoff order carton/,
    );
  });

  it("0422 Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0422_flooring_knowledge_estimate_order_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0422_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/estimate order pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate order pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/estimate order pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/estimate order pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/estimate order pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/estimate order pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/estimate order pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/estimate order pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/estimate order pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/estimate order pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate order pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/estimate order pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/estimate order pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /estimate order pad takeoff order carton/,
    );
  });

  it("0423 Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0423_flooring_knowledge_job_materials_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0423_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials pad takeoff order carton/,
    );
  });

  it("0424 Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0424_flooring_knowledge_warehouse_queue_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0424_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);
    expect(office).not.toMatch(/warehouse queue pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);
    expect(editor).not.toMatch(/warehouse queue pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);
    expect(incoming).not.toMatch(/warehouse queue pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);
    expect(orderUi).not.toMatch(/warehouse queue pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(poPlan).not.toMatch(/warehouseQueuePadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouse queue pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);
    expect(card).not.toMatch(/warehouse queue pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty/);
    expect(warehouse).toMatch(/warehouseQueuePadTakeoff\.cartons\.cartonCount/);
    expect(warehouse).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);
    expect(warehouse).not.toMatch(/staging pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);
    expect(staging).not.toMatch(/warehouse queue pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobScope).not.toMatch(/warehouse queue pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);
    expect(wo).not.toMatch(/warehouse queue pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /warehouse queue pad takeoff order carton/,
    );
  });

  it("0425 Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0425_flooring_knowledge_staging_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0425_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(q).not.toMatch(/staging pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(builder).not.toMatch(/staging pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);
    expect(office).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(office).not.toMatch(/staging pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);
    expect(editor).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(editor).not.toMatch(/staging pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);
    expect(incoming).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(incoming).not.toMatch(/staging pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);
    expect(orderUi).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(orderUi).not.toMatch(/staging pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(poPlan).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(poPlan).not.toMatch(/stagingPadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(jobMaterials).not.toMatch(/stagingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/staging pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);
    expect(card).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(card).not.toMatch(/staging pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty/);
    expect(warehouse).toMatch(/warehouseQueuePadTakeoff\.cartons\.cartonCount/);
    expect(warehouse).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);
    expect(warehouse).not.toMatch(/staging pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, g\.qty/);
    expect(staging).toMatch(/stagingPadTakeoff\.cartons\.cartonCount/);
    expect(staging).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(staging).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);
    expect(staging).not.toMatch(/warehouse queue pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobScope).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobScope).not.toMatch(/staging pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);
    expect(wo).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(wo).not.toMatch(/staging pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(print).not.toMatch(/staging pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(portal).not.toMatch(/staging pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /staging pad takeoff order carton/,
    );
  });

  it("0426 Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0426_flooring_knowledge_incoming_delivery_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0426_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(q).not.toMatch(/staging pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(builder).not.toMatch(/staging pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);
    expect(office).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(office).not.toMatch(/staging pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);
    expect(editor).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(editor).not.toMatch(/staging pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/incomingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);
    expect(incoming).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(incoming).not.toMatch(/staging pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);
    expect(orderUi).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(orderUi).not.toMatch(/staging pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(poPlan).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(poPlan).not.toMatch(/stagingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingPadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(jobMaterials).not.toMatch(/stagingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/staging pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);
    expect(card).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(card).not.toMatch(/staging pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty/);
    expect(warehouse).toMatch(/warehouseQueuePadTakeoff\.cartons\.cartonCount/);
    expect(warehouse).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);
    expect(warehouse).not.toMatch(/staging pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, g\.qty/);
    expect(staging).toMatch(/stagingPadTakeoff\.cartons\.cartonCount/);
    expect(staging).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(staging).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);
    expect(staging).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobScope).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobScope).not.toMatch(/staging pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);
    expect(wo).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(wo).not.toMatch(/staging pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(print).not.toMatch(/staging pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(portal).not.toMatch(/staging pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery pad takeoff order carton/,
    );
  });

  it("0427 Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0427_flooring_knowledge_incoming_delivery_arrived_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0427_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(q).not.toMatch(/staging pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(builder).not.toMatch(/staging pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);
    expect(office).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(office).not.toMatch(/staging pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);
    expect(editor).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(editor).not.toMatch(/staging pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/incomingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/incomingArrivedPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/billedQtyToSqft\(entered/);
    expect(incoming).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);
    expect(incoming).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(incoming).not.toMatch(/staging pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);
    expect(orderUi).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(orderUi).not.toMatch(/staging pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(poPlan).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(poPlan).not.toMatch(/stagingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingArrivedPadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(jobMaterials).not.toMatch(/stagingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingArrivedPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/staging pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);
    expect(card).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(card).not.toMatch(/staging pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty/);
    expect(warehouse).toMatch(/warehouseQueuePadTakeoff\.cartons\.cartonCount/);
    expect(warehouse).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);
    expect(warehouse).not.toMatch(/staging pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, g\.qty/);
    expect(staging).toMatch(/stagingPadTakeoff\.cartons\.cartonCount/);
    expect(staging).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(staging).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);
    expect(staging).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobScope).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobScope).not.toMatch(/staging pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);
    expect(wo).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(wo).not.toMatch(/staging pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(print).not.toMatch(/staging pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(portal).not.toMatch(/staging pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery arrived pad takeoff order carton/,
    );
  });

  it("0428 Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0428_flooring_knowledge_incoming_delivery_outstanding_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0428_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(q).not.toMatch(/staging pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(builder).not.toMatch(/staging pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);
    expect(office).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(office).not.toMatch(/staging pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);
    expect(editor).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(editor).not.toMatch(/staging pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/incomingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/incomingArrivedPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/incomingOutstandingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/billedQtyToSqft\(entered/);
    expect(incoming).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(/incomingOutstandingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/billedQtyToSqft\(outstanding/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);
    expect(incoming).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(incoming).not.toMatch(/staging pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);
    expect(orderUi).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(orderUi).not.toMatch(/staging pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(poPlan).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(poPlan).not.toMatch(/stagingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingArrivedPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingOutstandingPadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(jobMaterials).not.toMatch(/stagingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingArrivedPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingOutstandingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/staging pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);
    expect(card).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(card).not.toMatch(/staging pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty/);
    expect(warehouse).toMatch(/warehouseQueuePadTakeoff\.cartons\.cartonCount/);
    expect(warehouse).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);
    expect(warehouse).not.toMatch(/staging pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, g\.qty/);
    expect(staging).toMatch(/stagingPadTakeoff\.cartons\.cartonCount/);
    expect(staging).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(staging).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);
    expect(staging).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobScope).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobScope).not.toMatch(/staging pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);
    expect(wo).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(wo).not.toMatch(/staging pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(print).not.toMatch(/staging pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(portal).not.toMatch(/staging pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery arrived pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery outstanding pad takeoff order carton/,
    );
  });

  it("0429 Exclusive carpet-tile job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0429_flooring_knowledge_job_materials_gap_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0429_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(q).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(q).not.toMatch(/staging pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(builder).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(builder).not.toMatch(/staging pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);
    expect(office).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(office).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(office).not.toMatch(/staging pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(editor).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(editor).not.toMatch(/staging pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/incomingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/incomingArrivedPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/incomingOutstandingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/billedQtyToSqft\(entered/);
    expect(incoming).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(/incomingOutstandingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/billedQtyToSqft\(outstanding/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(incoming).not.toMatch(/jobMaterialsGapPadTakeoff/);
    expect(incoming).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(incoming).not.toMatch(/staging pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(orderUi).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(orderUi).not.toMatch(/staging pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(poPlan).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(poPlan).not.toMatch(/stagingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingArrivedPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingOutstandingPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsGapPadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(jobMaterials).not.toMatch(/stagingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingArrivedPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingOutstandingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/jobMaterialsGapPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/staging pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/jobMaterialsGapPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(/jobMaterialsGapPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/billedQtyToSqft\(l\.purchasingGap/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);
    expect(card).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(card).not.toMatch(/staging pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty/);
    expect(warehouse).toMatch(/warehouseQueuePadTakeoff\.cartons\.cartonCount/);
    expect(warehouse).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(warehouse).not.toMatch(/staging pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, g\.qty/);
    expect(staging).toMatch(/stagingPadTakeoff\.cartons\.cartonCount/);
    expect(staging).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(staging).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(staging).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(jobScope).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobScope).not.toMatch(/staging pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(wo).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(wo).not.toMatch(/staging pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(print).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(print).not.toMatch(/staging pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(portal).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(portal).not.toMatch(/staging pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery arrived pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery outstanding pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials purchasing-gap pad takeoff order carton/,
    );
  });

  it("0430 Exclusive carpet-tile job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0430_flooring_knowledge_job_materials_excess_pad_takeoff_order_carton.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0430_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(
      /Exclusive carpet-tile job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/,
    );
    expect(sql).toMatch(
      /Hard-surface job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(sql).toMatch(/Do NOT SQL-gate floor_map on surface_type/);
    expect(sql).toMatch(/Do NOT SQL-gate carpet_cuts on carpet_install/);
    expect(sql).toMatch(/Do NOT SQL-gate hs_plank_stairs on tile_application/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).not.toMatch(/show_if.*surface_type.*floor_map|floor_map.*show_if.*surface_type/);
    expect(sql).not.toMatch(/key = 'tile_setting'/);

    expect(extraAsksCountQty({ family: "lvp", productUnit: "box" })).toBe(true);
    const padTakeoff = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 540,
      wastePct: 10,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(padTakeoff.cartons).toBeNull();
    expect(padRollCount("underlayment", 60, "sqyd")).toBe(2);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).toBe(3);
    expect(padRollCount("underlayment", padTakeoff.billingQty, padTakeoff.billingUnit)).not.toBe(2);
    expect(padRollCount("underlayment", 6, "sqyd")).toBe(1);
    const boxedPad = computeMaterialTakeoff({
      family: "other",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
      billingUnit: "sqyd",
      takeoffLabel: "Carpet pad",
    });
    expect(boxedPad.cartons?.cartonCount).toBe(14);
    expect(boxedPad.cartons?.cartonCount).not.toBe(13);
    const extraLvp = computeMaterialTakeoff({
      family: "lvp",
      measuredSqft: 300,
      wastePct: 10,
      sqftPerBox: 23.64,
    });
    expect(extraLvp.cartons?.cartonCount).toBe(14);
    expect(extraLvp.cartons?.cartonCount).not.toBe(13);
    const extraTile = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 200,
      wastePct: 10,
      sqftPerBox: 23.64,
      carpetSystems: ["carpet_tile"],
    });
    expect(extraTile.cartons?.cartonCount).toBe(10);
    expect(extraTile.cartons?.cartonCount).not.toBe(9);
    expect(
      padRollCount(
        "carpet",
        computeMaterialTakeoff({
          family: "carpet",
          measuredSqft: 200,
          wastePct: 10,
          sqftPerBox: 23.64,
          carpetSystems: ["carpet_tile"],
        }).billingQty,
        "sqyd",
      ),
    ).toBe(0);
    expect(padRollCount("lvp", 8, "box")).toBe(0);
    expect(padRollCount("underlayment", 540, "sqft")).toBe(0);
    expect(
      hardSurfaceAreaCartonCount(
        {
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          sqft_per_box: 23.64,
          quantity: 8,
        },
        lineOrderQty({
          line_type: "mat_labor",
          description: "Stair wrap — wrap qty TBD",
          category: "lvp",
          unit: "box",
          quantity: 8,
          waste_pct: 10,
        }),
      ),
    ).toBe(0);

    const q = readFileSync(
      join(root, "src/app/(app)/estimates/questionnaire.tsx"),
      "utf8",
    );
    expect(q).toMatch(/padTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{padTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(padTakeoff\)/);
    expect(q).toMatch(/padRollCount\("underlayment", padTakeoff\.billingQty/);
    expect(q).toMatch(/= \{padTakeoffRolls\} roll/);
    expect(q).toMatch(/extraTakeoff\.cartons\.cartonCount/);
    expect(q).toMatch(/= \{extraTakeoff.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(extraTakeoff\)/);
    expect(q).toMatch(/padRollCount\(extraTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{extraPadRolls\} roll/);
    expect(q).toMatch(/const reviewTakeoffPadRolls = reviewTakeoff/);
    expect(q).toMatch(/\{reviewTakeoffPadRolls\} roll/);
    expect(q).toMatch(/Required rolls/);
    expect(q).toMatch(/reviewTakeoff\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/reviewTakeoff\.billingQty/);
    expect(q).toMatch(/EXTRA_AREA_COUNT_TBD_HINT/);
    expect(q).toMatch(/padRollCount\(t\.takeoffLabel \? "underlayment"/);
    expect(q).toMatch(/= \{runningPadRolls\} roll/);
    expect(q).toMatch(/= \{t.cartons.cartonCount\} carton/);
    expect(q).toMatch(/formatTakeoffStrip\(t\)/);
    expect(q).toMatch(/const geCartons = p/);
    expect(q).toMatch(/= \{geCartons\} carton/);
    expect(q).toMatch(/padRollCount\(l\.category, lineOrderQty\(smartLineToCalcLine\(l\)\)/);
    expect(q).toMatch(
      /Exclusive carpet-tile Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(q).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(q).not.toMatch(/job materials pad takeoff order carton/);
    expect(q).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(q).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(q).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(q).not.toMatch(/staging pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(q).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(q).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(q).not.toMatch(/estimate office pad takeoff order carton/);
    expect(q).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(q).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(q).not.toMatch(/PAD_ROLL_SQYD/);
    expect(q).not.toMatch(/companionQty/);
    expect(q).not.toMatch(/category === ["']carpet_tile["']/);
    expect(q).not.toMatch(/billedRateToCartonCost/);

    const takeoffLib = readFileSync(
      join(root, "src/lib/flooring-knowledge/quantities.ts"),
      "utf8",
    );
    expect(takeoffLib).toMatch(/row.label === "Required cartons"/);

    const builder = readFileSync(
      join(root, "src/app/(app)/estimates/estimate-builder.tsx"),
      "utf8",
    );
    expect(builder).toMatch(/builderPadTakeoff\.cartons\.cartonCount/);
    expect(builder).toMatch(/= \{builderPadTakeoff.cartons.cartonCount\} carton/);
    expect(builder).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(builder).toMatch(/hardSurfaceAreaCartonCount\(summ, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/= \{sCartons\} carton/);
    expect(builder).toMatch(/formatTakeoffStrip\(tileTakeoff\)/);
    expect(builder).toMatch(/const sQty = lineQty\(summ\)/);
    expect(builder).toMatch(/billedQty=\{sQty\}/);
    expect(builder).toMatch(/orderQty=\{lineOrderQty\(summ\)\}/);
    expect(builder).toMatch(/formatMoney\(sSell\)/);
    expect(builder).toMatch(/padRollCount\(summ\.category, lineOrderQty\(summ\)/);
    expect(builder).toMatch(/· 📦 \$\{builderPadTakeoff.cartons.cartonCount\} carton\(s\)/);
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(builder).toMatch(
      /Exclusive carpet-tile Builder expanded pad order pad-roll count stays off 30-yard roll math/,
    );
    expect(builder).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(builder).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(builder).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(builder).not.toMatch(/staging pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(builder).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(builder).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(builder).not.toMatch(/estimate office pad takeoff order carton/);
    expect(builder).not.toMatch(/Guided Estimate pad takeoff order carton/);
    expect(builder).not.toMatch(/PAD_ROLL_SQYD/);
    expect(builder).not.toMatch(/companionQty/);
    expect(builder).not.toMatch(/category === ["']carpet_tile["']/);

    const meas = readFileSync(
      join(root, "src/app/(app)/estimates/line-measurements.tsx"),
      "utf8",
    );
    expect(meas).toMatch(/padRollCount\(category, orderQty \?\? billedQty \?\? 0/);
    expect(meas).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(meas).not.toMatch(/job materials excess pad takeoff order carton/);

    const office = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(office).toMatch(/padRollCount\(l\.category, lineOrderQty\(l\)/);
    expect(office).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(office).toMatch(/officePadTakeoff\.cartons\.cartonCount/);
    expect(office).toMatch(/formatMoney\(rate\)/);
    expect(office).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(office).not.toMatch(/Builder collapsed pad takeoff order carton/);
    expect(office).not.toMatch(/Builder expanded pad takeoff order carton/);
    expect(office).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(office).not.toMatch(/job materials pad takeoff order carton/);
    expect(office).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(office).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(office).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(office).not.toMatch(/staging pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(office).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const editor = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/edit-scope.tsx"),
      "utf8",
    );
    expect(editor).toMatch(/padRollCount\(l\.category, lineOrderQty/);
    expect(editor).toMatch(/cartonCountFor\(l\)/);
    expect(editor).toMatch(/woEditorPadTakeoff\.cartons\.cartonCount/);
    expect(editor).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(editor).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(editor).not.toMatch(/estimate office pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(editor).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(editor).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(editor).not.toMatch(/staging pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(editor).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const incoming = readFileSync(
      join(root, "src/app/(app)/warehouse/incoming-deliveries.tsx"),
      "utf8",
    );
    expect(incoming).toMatch(/padRollCount\(i\.category, outstanding/);
    expect(incoming).toMatch(/\{outstandingPadRolls\} roll/);
    expect(incoming).toMatch(/cartonCountFor\(i, outstanding/);
    expect(incoming).toMatch(/padRollCount\(i\.category, ordered/);
    expect(incoming).toMatch(/incomingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/incomingArrivedPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/incomingOutstandingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/padRollCount\(i\.category, entered/);
    expect(incoming).toMatch(/billedQtyToSqft\(entered/);
    expect(incoming).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(/incomingOutstandingPadTakeoff\.cartons\.cartonCount/);
    expect(incoming).toMatch(/billedQtyToSqft\(outstanding/);
    expect(incoming).toMatch(
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(incoming).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(incoming).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(incoming).not.toMatch(/jobMaterialsGapPadTakeoff/);
    expect(incoming).not.toMatch(/jobMaterialsExcessPadTakeoff/);
    expect(incoming).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(incoming).not.toMatch(/staging pad takeoff order carton/);

    const orderUi = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/order/order-materials.tsx"),
      "utf8",
    );
    expect(orderUi).toMatch(/padRollCount\(line\.category, line\.qty/);
    expect(orderUi).toMatch(/line\.cartons/);
    expect(orderUi).toMatch(/formatMoney\(line\.unitCost\)/);
    expect(orderUi).toMatch(/orderPadTakeoff\.cartons\.cartonCount/);
    expect(orderUi).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(orderUi).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(orderUi).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(orderUi).not.toMatch(/installation-wo pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(orderUi).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(orderUi).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(orderUi).not.toMatch(/staging pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(orderUi).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const poPlan = readFileSync(
      join(root, "src/lib/data/po-plan.ts"),
      "utf8",
    );
    expect(poPlan).toMatch(/hardSurfaceAreaCartonCount/);
    expect(poPlan).toMatch(/sqft_per_box: Number\(sqft_per_box\) > 0/);
    expect(poPlan).not.toMatch(/job materials pad takeoff order carton/);
    expect(poPlan).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(poPlan).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(poPlan).not.toMatch(/orderPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(poPlan).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(poPlan).not.toMatch(/stagingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingArrivedPadTakeoff/);
    expect(poPlan).not.toMatch(/incomingOutstandingPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsGapPadTakeoff/);
    expect(poPlan).not.toMatch(/jobMaterialsExcessPadTakeoff/);

    const jobMaterials = readFileSync(
      join(root, "src/lib/data/job-materials.ts"),
      "utf8",
    );
    expect(jobMaterials).toMatch(/wastePct/);
    expect(jobMaterials).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/jobMaterialsPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouseQueuePadTakeoff/);
    expect(jobMaterials).not.toMatch(/stagingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingArrivedPadTakeoff/);
    expect(jobMaterials).not.toMatch(/incomingOutstandingPadTakeoff/);
    expect(jobMaterials).not.toMatch(/jobMaterialsGapPadTakeoff/);
    expect(jobMaterials).not.toMatch(/jobMaterialsExcessPadTakeoff/);
    expect(jobMaterials).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/staging pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(jobMaterials).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const card = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/job-materials-card.tsx"),
      "utf8",
    );
    expect(card).toMatch(/padRollCount\(l\.category, remaining/);
    expect(card).toMatch(/\{outstandingPadRolls\} roll/);
    expect(card).toMatch(/cartonCountForQty\(l, remaining/);
    expect(card).toMatch(/\{outstandingCartons\} carton/);
    expect(card).toMatch(/jobMaterialsPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/jobMaterialsGapPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(/jobMaterialsGapPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/billedQtyToSqft\(l\.purchasingGap/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(/jobMaterialsExcessPadTakeoff\.cartons\.cartonCount/);
    expect(card).toMatch(/billedQtyToSqft\(l\.excessIssued/);
    expect(card).toMatch(
      /Exclusive carpet-tile job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).toMatch(
      /Hard-surface job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(card).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(card).not.toMatch(/estimate order pad takeoff order carton/);
    expect(card).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(card).not.toMatch(/staging pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(card).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const warehouse = readFileSync(
      join(root, "src/app/(app)/warehouse/page.tsx"),
      "utf8",
    );
    expect(warehouse).toMatch(/padRollCount\(m\.category, m\.qty/);
    expect(warehouse).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, m\.qty/);
    expect(warehouse).toMatch(/warehouseQueuePadTakeoff\.cartons\.cartonCount/);
    expect(warehouse).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(warehouse).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(warehouse).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(warehouse).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(warehouse).not.toMatch(/staging pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(warehouse).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const staging = readFileSync(
      join(root, "src/app/(app)/warehouse/staging-sheet-doc.tsx"),
      "utf8",
    );
    expect(staging).toMatch(/padRollCount\(g\.category, g\.qty/);
    expect(staging).toMatch(/hardSurfaceAreaCartonCount\(cartonLine, g\.qty/);
    expect(staging).toMatch(/stagingPadTakeoff\.cartons\.cartonCount/);
    expect(staging).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(staging).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(staging).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(staging).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(staging).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(staging).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const jobScope = readFileSync(join(root, "src/lib/job-scope.ts"), "utf8");
    expect(jobScope).toMatch(/padRollCount\(l\.category, orderQ \|\| q/);
    expect(jobScope).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(jobScope).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(jobScope).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(jobScope).not.toMatch(/staging pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(jobScope).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const wo = readFileSync(
      join(root, "src/app/(app)/jobs/[id]/installation-wo.tsx"),
      "utf8",
    );
    expect(wo).toMatch(/hardSurfaceAreaCartonCount\(l, lineOrderQty\(l\)/);
    expect(wo).toMatch(/spec\.rolls/);
    expect(wo).not.toMatch(/spec\.cartons/);
    expect(wo).toMatch(/installWoPadTakeoff\.cartons\.cartonCount/);
    expect(wo).toMatch(/takeoffLabel: "Carpet pad"/);
    expect(wo).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(wo).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(wo).not.toMatch(/estimate office pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(wo).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(wo).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(wo).not.toMatch(/staging pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(wo).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);

    const print = readFileSync(
      join(root, "src/app/(app)/estimates/[id]/estimate-print.tsx"),
      "utf8",
    );
    expect(print).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(print).not.toMatch(/job materials pad takeoff order carton/);
    expect(print).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(print).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(print).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(print).not.toMatch(/staging pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(print).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(print).not.toMatch(/padRollCount/);

    const portal = readFileSync(
      join(root, "src/app/portal/estimates/[id]/page.tsx"),
      "utf8",
    );
    expect(portal).not.toMatch(/work-order editor pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials purchasing-gap pad takeoff order carton/);
    expect(portal).not.toMatch(/job materials excess pad takeoff order carton/);
    expect(portal).not.toMatch(/warehouse queue pad takeoff order carton/);
    expect(portal).not.toMatch(/staging pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery arrived pad takeoff order carton/);
    expect(portal).not.toMatch(/incoming-delivery outstanding pad takeoff order carton/);
    expect(portal).not.toMatch(/padRollCount/);

    const catalogForm = readFileSync(
      join(root, "src/app/(app)/catalog/product-form.tsx"),
      "utf8",
    );
    expect(catalogForm).toMatch(/\$ \/ unit/);

    const pricing = readFileSync(join(root, "src/lib/catalog-pricing.ts"), "utf8");
    expect(pricing).toMatch(
      /Exclusive carpet-tile warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface warehouse queue pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface staging pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery arrived pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface incoming-delivery outstanding pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials purchasing-gap pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface job materials pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate order pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface installation-wo pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface work-order editor pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface estimate office pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder collapsed pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Exclusive carpet-tile Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Builder expanded pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );
    expect(pricing).toMatch(
      /Hard-surface Guided Estimate pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft/,
    );

    const help =
      /Exclusive carpet-tile job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft — mixed stretch-in \+ tile and unanswered carpet stay cuts. Wrap \/ count How many stays off carton math. Do not invent coverage. Do not invent a carpet-tile category. Do not infer exclusive tile from unit=box/;
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "hs_plank_stairs" }, emptyInstallContext())).toMatch(help);
    expect(knowledgeHelpFor({ key: "work_type" }, emptyInstallContext())).toMatch(help);
    const tileCuts = knowledgeHelpFor(
      { kind: "cuts" },
      { ...emptyInstallContext(), answeredCarpetInstall: ["Carpet tile"] },
    );
    expect(tileCuts).toMatch(help);
    expect(knowledgeHelpFor({ kind: "floor_map" }, emptyInstallContext())).toMatch(
      /Hard-surface job materials excess pad takeoff order carton count from order qty ÷ coverage is the pull, not leftover measured sq ft. Wrap \/ count How many stays off carton math. Do not invent coverage/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).toMatch(
      /Exclusive tile hides the 6-mil/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery arrived pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /incoming-delivery outstanding pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials purchasing-gap pad takeoff order carton/,
    );
    expect(knowledgeHelpFor({ key: "tile_setting" }, emptyInstallContext())).not.toMatch(
      /job materials excess pad takeoff order carton/,
    );
  });

  it("pattern repeat only after pattern match is required", () => {
    const without = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      pattern_match: ["No pattern / no match"],
    });
    expect(without).not.toContain("pattern_repeat");
    const withMatch = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      pattern_match: ["Pattern match required"],
    });
    expect(withMatch).toContain("pattern_repeat");
    expect(withMatch.indexOf("pattern_match")).toBeLessThan(withMatch.indexOf("pattern_repeat")!);
  });

  it("stair landings appear after a carpet step count, not on the dead stairs yes/no", () => {
    const before = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(before).toContain("carpet_stairs");
    expect(before).not.toContain("stairs");
    expect(before).not.toContain("stair_landings");
    const after = walk({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
      carpet_stairs: ["Yes"],
    });
    expect(after).toContain("stair_landings");
    expect(after).toContain("stair_open_sides");
    expect(after.indexOf("carpet_stairs")).toBeLessThan(after.indexOf("stair_landings")!);
  });

  it("unanswered attached pad still shows underlayment on a floating job", () => {
    const keys = walk({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(keys).toContain("hs_underlayment");
  });
});

describe("0194 keys live questions without inventing prices", () => {
  it("keys pad/toilets/appliances and adds tile vs wall + vinyl skim", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0194_flooring_knowledge_job_conditions.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0194_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/carpet_pad/);
    expect(sql).toMatch(/tile_application/);
    expect(sql).toMatch(/vinyl_skim/);
    expect(sql).toMatch(/furniture_level/);
    expect(sql).toMatch(/hs_plank_stairs/);
    expect(sql).toMatch(/Count in EACH/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
  });

  it("wall tile is a warning, not invented labor", () => {
    const ctx = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Tile"],
      install_method: ["Thinset / mortar"],
    });
    const w = knowledgeWarnings(ctx, { pickedLabels: ["Wall"] });
    expect(w.some((x) => x.id === "tile-wall")).toBe(true);
  });
});

describe("0195 captures pattern repeat and delivery without inventing a cut plan", () => {
  it("adds pattern_repeat and delivery_scope as notes", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0195_flooring_knowledge_pattern_delivery.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0195_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/pattern_repeat/);
    expect(sql).toMatch(/delivery_scope/);
    expect(sql).toMatch(/does not generate a cut plan|not a cut plan/i);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(knowledgeQuestionByKey("pattern_repeat")?.purpose).toBe("WAREHOUSE");
    expect(knowledgeQuestionByKey("delivery_scope")?.purpose).toBe("PURCHASING");
  });
});

describe("stair extras and mixed-job measured area", () => {
  it("a stair step count is Yes for landings/open-sides; empty count is not", () => {
    expect(answerGateValues({ kind: "stairs", groups: [{ type: "Waterfall", count: "12" }] })).toEqual([
      "Yes",
      "Waterfall",
    ]);
    expect(answerGateValues({ kind: "stairs", groups: [{ type: "Waterfall", count: "" }] })).toEqual([]);
    expect(answerGateValues({ kind: "hs_stairs", steps: "8" })).toEqual(["Yes"]);
    expect(answerGateValues({ kind: "hs_stairs", steps: "0" })).toEqual([]);
    expect(answerGateValues({ kind: "number", value: "2" })).toEqual(["2"]);
    expect(coerceYesNoChoiceAnswer("choice", { kind: "yesno", yes: true })).toEqual({
      kind: "choice",
      selected: ["Yes"],
    });
    expect(coerceYesNoChoiceAnswer("choice", { kind: "yesno", yes: false })).toEqual({
      kind: "choice",
      selected: ["No"],
    });
    expect(
      coerceYesNoChoiceAnswer("yesno", { kind: "choice", selected: ["Field verify / TBD"] }),
    ).toEqual({ kind: "choice", selected: ["Field verify / TBD"] });
    expect(coerceYesNoChoiceAnswer("yesno", { kind: "choice", selected: ["Yes"] })).toEqual({
      kind: "yesno",
      yes: true,
    });
  });

  it("copies carpet_stairs Yes onto the stairs key the extras still read", () => {
    expect(synthesizeStairGate({ carpet_stairs: ["Yes"] }).stairs).toEqual(["Yes"]);
    expect(synthesizeStairGate({ carpet_install: ["Stretch-in"] }).stairs).toBeUndefined();
  });

  it("mixed floor-map rooms keep their own measured sq ft per product", () => {
    const by = groupMeasuredSqftByLabel([
      { label: "Mohawk carpet", measuredSqft: 300 },
      { label: "LVP click", measuredSqft: 200 },
      { label: "Mohawk carpet", measuredSqft: 50 },
    ]);
    expect(by["Mohawk carpet"]).toBe(350);
    expect(by["LVP click"]).toBe(200);
    const carpet = computeMaterialTakeoff({ family: "carpet", measuredSqft: by["Mohawk carpet"], cutsSqft: 432 });
    const lvp = computeMaterialTakeoff({ family: "lvp", measuredSqft: by["LVP click"], wastePct: 10 });
    expect(carpet.measured.sqft).toBe(350);
    expect(lvp.measured.sqft).toBe(200);
    expect(lvp.orderSqft).toBe(220);
    expect(carpet.measured.sqft).not.toBe(lvp.measured.sqft);
    const byFam = groupMeasuredSqftByFamily([
      { category: "carpet", measuredSqft: 300 },
      { category: "lvp", measuredSqft: 200 },
      { category: "carpet", measuredSqft: 50 },
    ]);
    expect(byFam.carpet).toBe(350);
    expect(byFam.lvp).toBe(200);
    expect(
      measuredSqftForFamilyTakeoff({
        family: "carpet",
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(350);
    expect(
      measuredSqftForFamilyTakeoff({
        family: "lvp",
        totalSqft: 550,
        byFamily: {},
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(0);
    expect(
      measuredSqftForFamilyTakeoff({
        family: "lvp",
        totalSqft: 500,
        byFamily: {},
        jobFamilies: ["lvp"],
      }),
    ).toBe(500);
    expect(
      measuredSqftForQuestionCover({
        key: "carpet_pad",
        kind: "product",
        category: "underlayment",
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(350);
    expect(
      measuredSqftForQuestionCover({
        key: "hs_underlayment",
        kind: "product",
        category: "underlayment",
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(200);
    expect(
      measuredSqftForQuestionCover({
        kind: "product",
        category: "underlayment",
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(0);
    expect(
      measuredSqftForQuestionCover({
        kind: "product",
        category: "underlayment",
        totalSqft: 400,
        byFamily: {},
        jobFamilies: ["carpet"],
      }),
    ).toBe(400);
    expect(
      measuredSqftForQuestionCover({
        kind: "product",
        category: "underlayment",
        totalSqft: 500,
        byFamily: {},
        jobFamilies: ["lvp"],
      }),
    ).toBe(500);
    expect(
      measuredSqftForQuestionCover({
        kind: "product",
        category: "lvp",
        totalSqft: 550,
        byFamily: {},
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(0);
    expect(
      measuredSqftForPrepTakeoff({
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(200);
    expect(
      measuredSqftForPrepTakeoff({
        totalSqft: 550,
        byFamily: {},
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(0);
    expect(
      measuredSqftForPrepTakeoff({
        totalSqft: 400,
        byFamily: {},
        jobFamilies: ["carpet"],
      }),
    ).toBe(400);
    expect(
      measuredSqftForFamiliesTakeoff({
        families: ["lvp", "hardwood"],
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(200);
    const living = { name: "Living", sqft: 350 };
    const kitchen = { name: "Kitchen", sqft: 200 };
    expect(
      roomsForPrepTakeoff({
        rooms: [
          { room: living, family: "carpet" },
          { room: kitchen, family: "lvp" },
        ],
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toEqual([kitchen]);
    expect(
      roomsAssignedToFamilies({
        rooms: [
          { room: living, family: "carpet" },
          { room: kitchen, family: "lvp" },
        ],
        families: ["carpet"],
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toEqual([living]);
    expect(
      emitAreaSqftForQuestion({
        key: "hs_prep",
        purpose: "PREP",
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(200);
    expect(
      emitAreaSqftForQuestion({
        key: "hs_demo",
        purpose: "LABOR",
        totalSqft: 550,
        byFamily: byFam,
        jobFamilies: ["carpet", "lvp"],
      }),
    ).toBe(550);
    expect(
      emitAreaSqftForQuestion({
        key: "vinyl_skim",
        purpose: "PREP",
        totalSqft: 550,
        byFamily: { vinyl: 180, carpet: 370 },
        jobFamilies: ["carpet", "vinyl"],
      }),
    ).toBe(180);
  });

  it("0242 mixed unassigned jobs warn instead of cloning whole-job sq ft", () => {
    const mixed = installContextFromValByKey({
      project_type: ["Carpet", "Hard surface"],
      surface_type: ["LVP / LVT"],
    });
    expect(
      mixedJobAssignmentGaps({
        jobFamilies: mixed.families,
        measuredSqft: 550,
        byFamily: {},
      }).unassignedFamilies.sort(),
    ).toEqual(["carpet", "lvp"]);
    expect(
      mixedJobAssignmentGaps({
        jobFamilies: mixed.families,
        measuredSqft: 550,
        byFamily: { carpet: 350, lvp: 200 },
      }).unassignedFamilies,
    ).toEqual([]);
    expect(
      mixedJobAssignmentGaps({
        jobFamilies: mixed.families,
        measuredSqft: 550,
        byFamily: { carpet: 350 },
      }).unassignedFamilies,
    ).toEqual(["lvp"]);
    expect(
      mixedJobAssignmentGaps({
        jobFamilies: ["lvp"],
        measuredSqft: 500,
        byFamily: {},
      }).unassignedFamilies,
    ).toEqual([]);
    expect(
      mixedJobAssignmentGaps({
        jobFamilies: ["carpet", "lvp"],
        measuredSqft: 0,
        byFamily: {},
      }).unassignedFamilies,
    ).toEqual([]);

    const none = knowledgeWarnings(mixed, { measuredSqft: 550, byFamily: {} });
    expect(none.some((w) => w.id === "mixed-unassigned")).toBe(true);
    expect(none.find((w) => w.id === "mixed-unassigned")?.text).toMatch(/do not clone whole-job sq ft/);
    expect(none.find((w) => w.id === "mixed-unassigned")?.text).toMatch(/Assign each room/);

    const assigned = knowledgeWarnings(mixed, {
      measuredSqft: 550,
      byFamily: { carpet: 350, lvp: 200 },
    });
    expect(assigned.some((w) => w.id === "mixed-unassigned")).toBe(false);

    const partial = knowledgeWarnings(mixed, {
      measuredSqft: 550,
      byFamily: { carpet: 350 },
    });
    expect(partial.some((w) => w.id === "mixed-unassigned")).toBe(true);
    expect(partial.find((w) => w.id === "mixed-unassigned")?.text).toMatch(/LVP \/ LVT has no rooms/);

    const blankRooms = knowledgeWarnings(mixed, {
      measuredSqft: 550,
      byFamily: { carpet: 350, lvp: 150 },
      unassignedRoomSqft: 50,
    });
    expect(blankRooms.some((w) => w.id === "mixed-unassigned")).toBe(true);
    expect(blankRooms.find((w) => w.id === "mixed-unassigned")?.text).toMatch(/Some rooms have no product/);

    const carpetOnly = knowledgeWarnings(
      installContextFromValByKey({ project_type: ["Carpet"] }),
      { measuredSqft: 450, byFamily: {}, hasCuts: false },
    );
    expect(carpetOnly.some((w) => w.id === "mixed-unassigned")).toBe(false);

    expect(knowledgeHelpFor({ kind: "floor_map" }, mixed)).toMatch(/Unassigned rooms stay off the takeoff/);
  });

  it("0196 re-gates stair extras and floating underlayment without inventing prices", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0196_flooring_knowledge_stair_gates.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0196_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/carpet_stairs/);
    expect(sql).toMatch(/hs_plank_stairs/);
    expect(sql).toMatch(/hs_underlayment/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(knowledgeQuestionByKey("stair_landings")?.quantityUnit).toBe("each");
    expect(knowledgeQuestionByKey("stair_landings")?.require).toEqual({ key: "stairs", in: ["Yes"] });
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/answerGateValues/);
    expect(q).toMatch(/groupMeasuredSqftByLabel/);
  });

  it("reads hard-surface step count separately from carpet waterfall", () => {
    const answers = {
      carpet: { kind: "stairs", groups: [{ type: "Waterfall", count: "12" }] },
      hs: { kind: "hs_stairs", steps: "8" },
      rooms: { kind: "number", value: "3" },
    };
    expect(stairStepCountFromAnswers(answers)).toBe(20);
    expect(stairStepCountFromAnswers(answers, ["hs_stairs"])).toBe(8);
    expect(stairStepCountFromAnswers(answers, ["stairs"])).toBe(12);
    expect(stairStepCountFromAnswers({ empty: { kind: "hs_stairs", steps: "" } })).toBe(0);
  });

  it("does not show HS stair-nose fill on carpet-only jobs", () => {
    expect(jobNeedsHardSurfaceStairTrim(["carpet"])).toBe(false);
    expect(jobNeedsHardSurfaceStairTrim(["lvp"])).toBe(true);
    expect(jobNeedsHardSurfaceStairTrim(["hardwood"])).toBe(true);
    expect(jobNeedsHardSurfaceStairTrim(["laminate"])).toBe(true);
    expect(jobNeedsHardSurfaceStairTrim(["vinyl"])).toBe(true);
    expect(jobNeedsHardSurfaceStairTrim(["tile"])).toBe(true);
    expect(jobNeedsHardSurfaceStairTrim(["carpet", "hardwood"])).toBe(true);
    expect(jobNeedsHardSurfaceStairTrim([])).toBe(false);
  });

  it("fills existing TRIM_TYPES treads, risers, and noses in EACH — no invented SKU", () => {
    expect([...HARD_SURFACE_STAIR_TRIM_LABELS]).toEqual(["Stair tread", "Stair riser", "Stair nose"]);
    const filled = applyHardSurfaceStairTrimFill(
      [{ type: "Stair tread", qty: "1" }],
      13,
      (label) => ({ type: label, qty: "" }),
    );
    expect(filled).toEqual([
      { type: "Stair tread", qty: "13" },
      { type: "Stair riser", qty: "13" },
      { type: "Stair nose", qty: "13" },
    ]);
    expect(applyHardSurfaceStairTrimFill([], 0, (label) => ({ type: label, qty: "" }))).toEqual([]);
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/applyHardSurfaceStairTrimFill/);
    expect(q).toMatch(/jobNeedsHardSurfaceStairTrim/);
    expect(q).toMatch(/stairStepCountFromAnswers\(jobAnswers, \["hs_stairs"\]\)/);
    expect(q).not.toMatch(/ensure\(\/tread\/i, "Stair tread"\)/);
  });

  it("warns when hard-surface stairs have no stair-nose trim, and stays quiet once one exists", () => {
    const ctx = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(
      knowledgeWarnings(ctx, { hsStairSteps: 13, hasStairNose: false }).some((x) => x.id === "hs-stair-nose"),
    ).toBe(true);
    expect(
      knowledgeWarnings(ctx, { hsStairSteps: 13, hasStairNose: true }).some((x) => x.id === "hs-stair-nose"),
    ).toBe(false);
    expect(
      knowledgeWarnings(ctx, { hsStairSteps: 0, hasStairNose: false }).some((x) => x.id === "hs-stair-nose"),
    ).toBe(false);
    const carpet = installContextFromValByKey({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(
      knowledgeWarnings(carpet, { hsStairSteps: 12, hasStairNose: false }).some((x) => x.id === "hs-stair-nose"),
    ).toBe(false);
    expect(
      answersHaveTrimType(
        { t: { kind: "trims", rows: [{ type: "Stair nose", qty: "13" }] } },
        /stair\s*nose/i,
      ),
    ).toBe(true);
    expect(answersHaveTrimType({ t: { kind: "trims", rows: [{ type: "Quarter round", qty: "40" }] } }, /stair\s*nose/i)).toBe(
      false,
    );
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/answersHaveTrimType\(answers, \/stair\\s\*nose\/i\)/);
  });
});

describe("0197 scope notes and delivery charge", () => {
  it("adds asbestos and plank direction without inventing prices", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0197_flooring_knowledge_scope_notes.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0197_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/asbestos_risk/);
    expect(sql).toMatch(/hs_direction/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/create table public\.products/);
    expect(knowledgeQuestionByKey("hs_direction")?.families).toEqual(["lvp", "laminate", "hardwood"]);
    expect(knowledgeQuestionByKey("asbestos_risk")?.purpose).toBe("WARNING");
  });

  it("emits Delivery only when Settings already has a positive cost", () => {
    expect(deliveryAddonCost(["Include delivery"], 75)).toBe(75);
    expect(deliveryAddonCost(["Include delivery"], 0)).toBeNull();
    expect(deliveryAddonCost(["Include delivery"], null)).toBeNull();
    expect(deliveryAddonCost(["Customer pickup / will call"], 75)).toBeNull();
    expect(deliveryAddonCost(["Field verify / TBD"], 75)).toBeNull();
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/deliveryAddonCost/);
  });

  it("warns on possible asbestos without inventing abatement dollars", () => {
    const ctx = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["Sheet vinyl"],
      hs_demo: ["Sheet vinyl"],
    });
    const w = knowledgeWarnings(ctx, { pickedLabels: ["Possible — test before removal"] });
    expect(w.some((x) => x.id === "asbestos")).toBe(true);
  });
});

describe("vapor_barrier overlay matches SQL any (floating/glue OR concrete)", () => {
  it("nail-down hardwood over concrete still asks; plywood does not", () => {
    const nailConcrete = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Concrete"],
    });
    expect(nailConcrete).toContain("vapor_barrier");
    expect(nailConcrete).toContain("hardwood_fasteners");

    const nailPlywood = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Hardwood"],
      install_method: ["Nail-down"],
      substrate: ["Plywood / OSB"],
    });
    expect(nailPlywood).not.toContain("vapor_barrier");

    const floating = visibleKnowledgeKeys({
      project_type: ["Hard surface"],
      surface_type: ["Laminate"],
      install_method: ["Floating / click"],
    });
    expect(floating).toContain("vapor_barrier");

    const stretch = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Stretch-in"],
    });
    expect(stretch).not.toContain("vapor_barrier");

    const carpetGlue = visibleKnowledgeKeys({
      project_type: ["Carpet"],
      carpet_install: ["Glue-down"],
    });
    expect(carpetGlue).toContain("vapor_barrier");
  });

  it("SQL walk: nail + concrete shows vapor_barrier; nail + plywood hides it", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0198_flooring_knowledge_vapor_barrier.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0198_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/vapor_barrier/);
    expect(sql).toMatch(/substrate/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(knowledgeQuestionByKey("vapor_barrier")?.any).toEqual([
      { systems: ["floating", "glue"] },
      { substrate: ["Concrete"] },
    ]);
  });
});

describe("salesperson review buckets by purpose, not a level regex", () => {
  it("furniture_level is Specials, never Prep", () => {
    expect(reviewBucketForQuestion({ key: "furniture_level", label: "Furniture moving" })).toBe(
      "specials",
    );
    expect(reviewBucketForQuestion({ key: "selflevel_needed", label: "Self-leveler" })).toBe("prep");
    expect(reviewBucketForQuestion({ key: "vapor_barrier", label: "Moisture barrier" })).toBe("prep");
  });

  it("layout notes sit under Installation, not Special conditions", () => {
    expect(reviewBucketForQuestion({ key: "tile_layout", label: "Tile layout" })).toBe("installation");
    expect(reviewBucketForQuestion({ key: "pattern_match", label: "Pattern match" })).toBe(
      "installation",
    );
    expect(reviewBucketForQuestion({ key: "hs_direction", label: "Plank run direction" })).toBe(
      "installation",
    );
    expect(reviewBucketForQuestion({ key: "vinyl_layout", label: "Sheet vinyl layout" })).toBe(
      "installation",
    );
  });

  it("removal / accessories / stairs land in the right columns", () => {
    expect(reviewBucketForQuestion({ key: "hs_demo", label: "Existing flooring" })).toBe("removal");
    expect(reviewBucketForQuestion({ key: "existing_tack", label: "Existing tack strip?" })).toBe("removal");
    expect(reviewBucketForQuestion({ key: "work_type", label: "New construction or replacement?" })).toBe(
      "removal",
    );
    expect(reviewBucketForQuestion({ key: "asbestos_risk", label: "Asbestos" })).toBe("removal");
    expect(reviewBucketForQuestion({ key: "vinyl_skim", label: "Existing vinyl skim" })).toBe("removal");
    expect(reviewBucketForQuestion({ key: "tack_strip", label: "Tack strip" })).toBe("accessories");
    expect(reviewBucketForQuestion({ key: "hs_transitions", label: "Doorway transitions" })).toBe(
      "accessories",
    );
    expect(reviewBucketForQuestion({ key: "hs_base_trim", label: "Base / QR" })).toBe("accessories");
    expect(reviewBucketForQuestion({ key: "carpet_stairs", label: "Carpet stairs" })).toBe(
      "installation",
    );
    expect(reviewBucketForQuestion({ key: "wet_area", label: "Any wet areas?" })).toBe("specials");
  });

  it("extra-pad area uses measured-vs-equivalent wording, not a bare sqyd conversion", () => {
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).not.toMatch(/wantYd \? numv\(ex\.sqft\) \/ 9/);
    expect(q).toMatch(/formatMeasuredLabel/);
    expect(
      formatMeasuredLabel({ sqft: 270, sqydEquivalent: 30 }, { showEquivalentYd: true }),
    ).toMatch(/equivalent area — not an order quantity/);
  });

  it("Guided Estimate converts catalog SY rates without a 9× surprise", () => {
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/catalogRateToBillingUnit/);
    expect(q).not.toMatch(/productUnit \|\| ""\)\.toLowerCase\(\)\.includes\(["']yd["']\)/);
    const ai = readFileSync(join(root, "src/app/(app)/estimates/ai-actions.ts"), "utf8");
    expect(ai).toMatch(/catalogRateToBillingUnit/);
    expect(ai).not.toMatch(/productUnit \|\| ""\)\.toLowerCase\(\)\.includes\(["']yd["']\)/);
  });
});

describe("0199 hard-surface transitions and base trim without invented SKUs", () => {
  it("adds notes-only hs_transitions and hs_base_trim with correct units", () => {
    const sql = readFileSync(
      join(root, "supabase/migrations/0199_flooring_knowledge_hs_trim.sql"),
      "utf8",
    );
    expect(sql).toMatch(/P0_0199_FLOORING_KNOWLEDGE/);
    expect(sql).toMatch(/hs_transitions/);
    expect(sql).toMatch(/hs_base_trim/);
    expect(sql).toMatch(/Does NOT invent carton coverage/);
    expect(sql).toMatch(/Does NOT enable accounting/);
    expect(sql).not.toMatch(/"emit"/);
    expect(knowledgeQuestionByKey("hs_transitions")?.quantityUnit).toBe("each");
    expect(knowledgeQuestionByKey("hs_base_trim")?.quantityUnit).toBe("lnft");
    expect(knowledgeQuestionByKey("hs_transitions")?.families).toEqual([
      "lvp",
      "hardwood",
      "laminate",
      "vinyl",
      "tile",
    ]);
  });

  it("maps picks onto existing TRIM_TYPES and skips None / TBD", () => {
    expect(
      trimLabelsFromPicks(["T-mold", "Reducer", "Field verify / TBD"], HS_TRANSITION_OPTION_TO_TRIM),
    ).toEqual(["T-mold", "Reducer"]);
    expect(
      trimLabelsFromPicks(["None — keep existing / no new transitions", "Metal"], HS_TRANSITION_OPTION_TO_TRIM),
    ).toEqual(["Metal transition"]);
    expect(trimLabelsFromPicks(["Keep existing base", "Quarter round"], HS_BASE_OPTION_TO_TRIM)).toEqual([
      "Quarter round",
    ]);
    expect(accessoryUnitForType("T-mold")).toBe("each");
    expect(accessoryUnitForType("Quarter round")).toBe("lnft");
    const seeded = applyTrimTypeSeed([{ type: "T-mold", qty: "2" }], ["T-mold", "Reducer"], (label) => ({
      type: label,
      qty: "",
    }));
    expect(seeded).toEqual([
      { type: "T-mold", qty: "2" },
      { type: "Reducer", qty: "" },
    ]);
  });

  it("warns when picked transitions are missing from Trims, and stays quiet once added", () => {
    const ctx = installContextFromValByKey({
      project_type: ["Hard surface"],
      surface_type: ["LVP / LVT"],
      install_method: ["Floating / click"],
    });
    expect(
      knowledgeWarnings(ctx, {
        neededTransitionTrims: ["T-mold"],
        neededBaseTrims: ["Quarter round"],
        presentTrimTypes: [],
      })
        .map((w) => w.id)
        .sort(),
    ).toEqual(["hs-base-trim", "hs-transitions"]);
    expect(
      knowledgeWarnings(ctx, {
        neededTransitionTrims: ["T-mold"],
        neededBaseTrims: ["Quarter round"],
        presentTrimTypes: ["T-mold", "Quarter round"],
      }).some((w) => w.id === "hs-transitions" || w.id === "hs-base-trim"),
    ).toBe(false);
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/applyTrimTypeSeed/);
    expect(q).toMatch(/hsTransitionTrims/);
    expect(q).toMatch(/hsBaseTrims/);
  });
});

describe("removal descriptions distinguish glued vs floating without a second rate", () => {
  it("annotates LVP/laminate/vinyl tear-out with bond, carpet with pad, and leaves other lines alone", () => {
    expect(
      annotateRemovalDescription("Tear-out — LVP", { bond: ["Glued down"] }),
    ).toBe("Tear-out — LVP (glued down — not floating)");
    expect(
      annotateRemovalDescription("Tear-out — laminate", { bond: ["Floating / click — not glued"] }),
    ).toBe("Tear-out — laminate (floating / click — not glued)");
    expect(
      annotateRemovalDescription("Tear-out — carpet", { pad: ["Reuse (explicitly allowed)"] }),
    ).toBe("Tear-out — carpet (reuse existing pad (explicit))");
    expect(
      annotateRemovalDescription("Tear-out — carpet", { pad: ["Remove with old carpet"] }),
    ).toBe("Tear-out — carpet (pad removed with carpet)");
    expect(
      annotateRemovalDescription("Tear-out — carpet", { tack: ["Remove with old carpet"] }),
    ).toBe("Tear-out — carpet (tack strip removed with carpet)");
    expect(
      annotateRemovalDescription("Tear-out — carpet", { tack: ["Keep (unusual)"] }),
    ).toBe("Tear-out — carpet (keep existing tack strip (unusual))");
    expect(annotateRemovalDescription("Furniture moving (light)", { bond: ["Glued down"] })).toBe(
      "Furniture moving (light)",
    );
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/annotateRemovalDescription/);
  });
});

describe("salesperson review warnings are one source of truth for Builder notes", () => {
  it("reviewToJobNotes includes extraWarnings and does not invent a second Flags block", () => {
    const takeoff = computeMaterialTakeoff({
      family: "carpet",
      measuredSqft: 450,
    });
    const review = buildSalespersonReview({
      rooms: [],
      products: ["Berber sample — catalog name only"],
      takeoffs: [takeoff],
      ctx: emptyInstallContext(),
      removal: [],
      installation: [],
      prep: [],
      accessories: [],
      specials: ["Occupancy: Occupied"],
      extraWarnings: [
        {
          id: "carpet-no-cuts",
          text: "Carpet measured by area only — converting sq ft ÷ 9 is equivalent area, not a cut plan. Enter cuts (roll width × length) before ordering.",
        },
        { id: "radiant", text: "Radiant heat present — confirm the selected flooring is rated for radiant heat before ordering." },
      ],
    });
    expect(review.warnings.map((w) => w.id).sort()).toEqual(["carpet-no-cuts", "radiant"]);
    const notes = reviewToJobNotes(review);
    expect(notes).toMatch(/Warnings:/);
    expect(notes).toMatch(/Carpet measured by area only/);
    expect(notes).toMatch(/Radiant heat present/);
    expect(notes).toMatch(/Order quantity: TBD — enter cuts/);
    expect(notes).toMatch(/Billing quantity: TBD — not 50 sq yd from taped area/);
    expect(notes).toMatch(/Unit of measure: sq yd/);
    expect(notes).toMatch(/Waste:/);
    expect(notes).not.toMatch(/Flags to confirm/);
    // Takeoff's similar "no cut list" message must not duplicate the knowledge flag.
    expect(notes.match(/sq ft ÷ 9/g)?.length).toBeGreaterThanOrEqual(1);
    const warningBlock = notes.split("Warnings:")[1] ?? "";
    expect(warningBlock.match(/cut plan/gi)?.length).toBe(1);
    expect(notes).toMatch(/Occupancy: Occupied/);
    expect(notes.match(/Occupancy:/g)?.length).toBe(1);
    expect(notes).not.toMatch(/Conditions:/);
  });

  it("does not reprint occupancy or grade in Conditions when Review already listed them", () => {
    const review = buildSalespersonReview({
      rooms: [],
      products: [],
      takeoffs: [],
      ctx: {
        ...emptyInstallContext(),
        occupancy: ["Occupied"],
        grade: ["Above grade"],
      },
      removal: [],
      installation: ["Construction grade?: Above grade"],
      prep: [],
      accessories: [],
      specials: ["Occupancy: Occupied"],
    });
    expect(review.notes).toEqual([]);
    const notes = reviewToJobNotes(review);
    expect(notes.match(/Occupancy:/g)?.length).toBe(1);
    expect(notes).toMatch(/Construction grade\?: Above grade/);
    expect(notes).not.toMatch(/^Conditions:/m);
    expect(notes).not.toMatch(/• Grade:/);
  });

  it("keeps the takeoff order warning when the questionnaire did not already raise it", () => {
    const takeoff = computeMaterialTakeoff({ family: "vinyl", measuredSqft: 180 });
    const merged = mergeReviewWarnings([], [takeoff]);
    expect(merged.some((w) => w.id === "vinyl-no-layout")).toBe(true);
    expect(merged).toHaveLength(1);
  });

  it("does not resurrect a dismissed carpet-no-cuts flag from takeoff text", () => {
    const takeoff = computeMaterialTakeoff({ family: "carpet", measuredSqft: 450 });
    const merged = mergeReviewWarnings([], [takeoff], { suppressIds: ["carpet-no-cuts"] });
    expect(merged.some((w) => w.id === "carpet-no-cuts")).toBe(false);
    expect(merged).toEqual([]);
  });

  it("questionnaire feeds active flags into the review and drops the duplicate flagText join", () => {
    const q = readFileSync(join(root, "src/app/(app)/estimates/questionnaire.tsx"), "utf8");
    expect(q).toMatch(/extraWarnings:\s*warnings\.filter/);
    expect(q).toMatch(/suppressedWarningIds:\s*dismissed/);
    expect(q).not.toMatch(/Flags to confirm:/);
    expect(q).not.toMatch(/flagText/);
    expect(q).toMatch(/salespersonReview\.warnings\.map/);
  });
});
